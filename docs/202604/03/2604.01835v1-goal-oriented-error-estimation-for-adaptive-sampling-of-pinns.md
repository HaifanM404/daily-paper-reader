---
title: Goal oriented error estimation for adaptive sampling of PINNS
title_zh: 面向目标的自适应采样：物理信息神经网络的误差估计
authors: "Medard Govoeyi, Thomas Richter"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01835v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于偏微分方程的物理信息神经网络(PINNs)自适应采样
tldr: 本研究针对物理信息神经网络及其变分形式Deep Ritz方法求解偏微分方程时，传统采样策略导致目标泛函估计收敛慢的问题，提出了一种面向目标泛函的自适应重要性采样策略。该方法基于对偶加权残差框架构建神经网络内部的误差估计器，并据此指导训练点的自适应采样，将计算资源集中于对泛函值影响最大的区域。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01835v1/fig-001.webp\", \"caption\": \"Figure 5: Case 2: Absolute value of the functional error |E(u)| = |J(u) − J(uN )| during the training. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 15, \"index\": 1, \"width\": 852, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-002.webp\", \"caption\": \"Figure 4: Case 1: Absolute value of the functional error |E(u)| = |J(u) − J(uθ)| during the training. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 14, \"index\": 2, \"width\": 852, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-003.webp\", \"caption\": \"Figure 3: Case 1: Comparison of uniform sampling (left) vs. sampling using the DWR measure pµ (right).\", \"page\": 14, \"index\": 3, \"width\": 760, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-004.webp\", \"caption\": \"Figure 8: Case 4: Absolute value of the functional error |E(u)| = |J(u) − J(uN )| during the training. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 18, \"index\": 4, \"width\": 712, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-005.webp\", \"caption\": \"Figure 2: (a) Pointwise functional error, (b) corresponding pointwise error estimator (b), (c) corresponding pointwise improved error estimator with localization.\", \"page\": 13, \"index\": 5, \"width\": 950, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-006.webp\", \"caption\": \"Figure 9: Case 5: Absolute value of the functional error |E(u)| = |J(u) − J(uN )| during the training. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 19, \"index\": 6, \"width\": 712, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-007.webp\", \"caption\": \"Figure 7: Case 3: Absolute value of the functional error |E(u)| = |J(u) − J(uN )| during the training. The plotted values correspond to the error averaged over every 10 epochs. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 17, \"index\": 7, \"width\": 858, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-008.webp\", \"caption\": \"Figure 1: Case 1: Evolution of the functional error and its estimators during training. The vertical dashed red line indicates the iteration at which resampling is performed.\", \"page\": 12, \"index\": 8, \"width\": 819, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2604.01835v1/fig-009.webp\", \"caption\": \"Figure 6: Case 2: Comparison of the standard method with successive adaptive sampling after 100 epochs each. Absolute value of the functional error |E(u)| = |J(u) − J(uN )| during the training.\", \"page\": 16, \"index\": 9, \"width\": 866, \"height\": 453}]"
motivation: 当使用均匀或基于损失的采样等标准策略时，PINN和Deep Ritz方法在训练中目标泛函误差的收敛速度可能很慢。
method: 提出了一种面向目标泛函的重要性采样策略，其核心是基于对偶加权残差框架，在神经网络内部构建一个可靠且准确的感兴趣量误差估计器。
result: 数值实验表明，所提出的自适应采样策略显著加速了两种方法（PINN和Deep Ritz）在训练中目标泛函误差的收敛。
conclusion: 该自适应采样策略显著加速了目标泛函误差的收敛，并提升了训练中对目标泛函的最小化效果。
---

## 摘要
物理信息神经网络（PINNs）是用于偏微分方程数值近似的无网格方法，其通过最小化由控制方程和边界条件导出的损失函数来训练神经网络。深度Ritz方法可被解释为PINN的一种特殊变分形式，其损失对应于与对称正定问题相关的能量泛函的最小化。

在本工作中，我们使用经典的PINN公式及其变分对应物——深度Ritz方法——来研究拉普拉斯方程的近似求解，目标是精确估计指定的目标泛函。当在训练过程中采用标准采样策略（如均匀采样或基于损失的采样）时，泛函误差的收敛以及所达到的最小泛函值可能非常缓慢。

为了解决这个问题，我们引入了一种面向泛函的重要性采样策略，该策略可同时应用于PINNs和深度Ritz方法。其关键要素是为给定感兴趣量的误差构建一个可靠且准确的估计器。该估计器是利用对偶加权残差（DWR）框架中的概念推导出来的，并完全在神经网络设置中实现。随后，它被用来自适应地指导计算域中训练点的采样，将计算资源集中在那些对泛函值影响最强的区域上。

数值实验表明，对于PINN和深度Ritz两种公式，所提出的自适应采样策略均能显著加速训练过程中泛函误差的收敛并改善目标泛函的最小化效果。

## Abstract
Physics-Informed Neural Networks (PINNs) are mesh-free approaches for the numerical approximation of partial differential equations, where a neural network is trained by minimizing a loss function derived from the governing equations and boundary conditions. The Deep Ritz method can be interpreted as a particular variational form of a PINN, where the loss corresponds to the minimization of an energy functional associated with a symmetric positive definite problem.   In this work, we study the approximation of the Laplace equation using both the classical PINN formulation and its variational counterpart, the Deep Ritz method, with the objective of accurately estimating prescribed goal functionals. When standard sampling strategies, such as uniform or loss-based sampling, are employed during training, the convergence of the functional error and the attained minimal functional value can be slow.   To address this issue, we introduce a functional-oriented importance sampling strategy that can be applied to both PINNs and the Deep Ritz method. The key ingredient is the construction of a reliable and accurate estimator for the error in a given quantity of interest. This estimator is derived using concepts from the Dual Weighted Residual (DWR) framework and is implemented entirely within the neural network setting. It is then used to adaptively guide the sampling of training points in the computational domain, focusing computational effort on regions that have the strongest influence on the functional value.   Numerical experiments demonstrate that the proposed adaptive sampling strategy significantly accelerates the convergence of the functional error and improves the minimization of the target functional during training for both PINN and Deep Ritz formulations.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Goal oriented error estimation for adaptive sampling of PINNS》进行结构化、深入且客观的总结。

---

### **论文总结：面向目标的自适应采样——物理信息神经网络的误差估计**

#### **1. 核心问题与整体含义**
*   **研究背景**：物理信息神经网络（PINNs）及其变体深度Ritz方法（DRM）是求解偏微分方程（PDEs）的无网格方法。然而，其训练过程是一个高度非凸的优化问题，效率低下。
*   **核心问题**：当使用均匀或基于残差的传统采样策略时，PINNs/DRM在估计特定**目标泛函**（如区域平均值、点值等）时收敛缓慢。这是因为这些策略未能将计算资源集中在对目标泛函误差影响最大的区域。
*   **研究动机**：借鉴经典有限元方法中成熟的自适应误差估计与网格细化思想，将其迁移至PINNs框架，以加速目标泛函的收敛并提升求解精度。

#### **2. 方法论**
*   **核心思想**：提出一种**面向目标泛函的自适应重要性采样策略**。其核心是利用对偶加权残差（DWR）方法构建一个完全基于神经网络的、可计算的误差估计器，并利用该估计器的局部化信息来指导训练点的采样。
*   **关键技术细节**：
    1.  **DWR误差估计器构建**：
        *   对于给定的线性目标泛函 `J(u)`，引入伴随方程。
        *   分别使用神经网络近似原问题解 `u_θ` 和伴随问题解 `z_θ`。
        *   推导出启发式误差估计器：`η = ⟨f, z_θ⟩ - ⟨∇u_θ, ∇z_θ⟩ - λ⟨u_θ, z_θ⟩`，该式近似于真实泛函误差 `J(u) - J(u_θ)`。
    2.  **误差局部化与改进**：
        *   为了获得可用于指导采样的空间分布信息，论文进一步改进了估计器。通过利用神经网络参数空间的线性结构，定义了一个依赖于当前解的“导数网络”集合 `V‘_N(u_θ)`。
        *   将伴随解 `z_θ` 替换为在该导数集合中的一个近似 `z‘_ω`，得到局部化的误差指示函数 `μ(x)`。该指示函数能更准确地反映不同空间位置对总泛函误差的“敏感度”。
    3.  **自适应采样流程 (Algorithm 1)**：
        *   **预训练阶段**：使用均匀采样点初步训练原问题和伴随问题的神经网络。
        *   **估计与重采样**：在训练过程中的某个时刻（如第200个epoch），计算局部化误差指示函数 `μ(x)`。
        *   **定义概率密度函数 (PDF)**：基于 `|μ(x)|` 构建一个概率分布 `p_μ(x) ∝ |μ(x)|`。该分布在高误差敏感区域赋予更高的采样概率。
        *   **自适应训练**：后续的训练点不再均匀采样，而是根据 `p_μ(x)` 进行重要性采样。也可以采用“细化”策略，即在原有均匀点基础上添加按 `p_μ` 采样的新点。

#### **3. 实验设计**
*   **基准场景与方法对比**：
    *   所有实验均围绕求解带有狄利克雷边界条件的泊松方程展开。
    *   **主要对比对象是标准方法（Standard method）**，即在整个训练过程中使用均匀分布的固定或随机采样的配置点。
    *   提出的方法被称为自适应方法（Adaptive method）。
*   **测试案例覆盖范围 (共5个案例)**：
    1.  **(PINN) Case 1**: 二维光滑解问题 + 全域平均泛函。用于验证估计器精度和展示局部化效果。
    2.  **(PINN) Case 2**: 二维光滑解问题 + **非光滑泛函**(在子域C上的积分)。测试方法对具有奇异性的伴随问题的适应性及细化策略效果。
    3.  **(PINN) Case 3**: **五维环形域上的高维问题**(d=5)。验证方法在高维场景下的有效性。
    4.  **(DRM) Case 4**: DRM框架下的二维非光滑泛函问题。验证方法在DRM中的适用性。
    5.  **(DRM) Case5**: DRM框架下的二维光滑解+全域平均泛函。

#### **4. &5.资源、算力与实验充分性**
*   **资源与算力说明不足**: 
    > “论文未明确说明所使用的具体硬件配置（如GPU型号、数量）、单次实验的训练时长以及总计算开销。”
    
*   **实验数量与充分性评估**: 
    > “论文共设计了5个数值案例进行验证。这些案例从低维到高维、从光滑到非光滑的目标泛函、覆盖了PINN和Deep Ritz两种主要框架。虽然案例数量不算庞大，但针对所提出的核心思想——‘面向目标的DWR自适应采样’——进行了较为全面的测试。”
    
    > “实验设计基本客观公平：对比基线清晰（标准均匀采样），评价指标统一为‘目标泛函绝对误差随训练epoch的变化’。所有对比均在相同网络架构和超参数设置下进行。”
    
    > “不足之处在于缺乏与其他先进自适应PINN采样方法（如RAR, RAD等）的直接横向比较。”

#### **6.主要结论与发现**
1.  所提出的完全基于神经网络的DWR误差估计器在训练的早期阶段就能相当准确地预测真实的目标泛函误差及其符号变化趋势。
2.  通过引入“导数网络”概念实现的局部化改进版本 (`μ(x)`)，相比简单版本 (`μ_simple(x)`) ，能提供更平滑、更可靠的局部误差敏感度分布图 (`p_μ`) 。
3.  基于该局部化指示函数的自适应重要性采样策略，在所有测试案例中均显著加速了目标泛函误差的收敛速度，并且最终达到的精度也优于标准的均匀采样方法。

#### **7.优点**
1.  ****理论迁移创新****:成功地将经典有限元分析中强大的DWR后验误差估计与自适应框架无缝迁移至无网格的PINNs领域，为解决其效率瓶颈提供了新思路。
2. ****完全无网格实现****:整个流程（包括原问题、伴随问题的求解以及最终的误差估计和重采样）完全在神经网络框架内完成，不依赖任何外部网格结构或有限元求解器，保持了PINNs处理高维问题的潜力优势。
3.**实用性强**:算法流程清晰(Algorithm1)，易于实现和复现。数值结果直观地展示了其在加速收敛方面的显著效果。

####8.**不足与局限**

> “尽管作者指出该方法适用于高维场景(Case3)，但最高仅测试了五维(d=5)的问题。对于真正意义上的超高维度(例如d>100)，该方法中涉及的梯度计算和蒙特卡洛积分是否依然高效且稳定有待验证。”

> “方法的有效性依赖于能够成功训练出原问题和伴随问题的神经网络近似解。对于病态或复杂的PDEs/PINNs本身固有的优化困难可能会影响本方法的稳定性。”

> “目前仅在线性椭圆型模型问题上进行了验证(Poisson方程)。对于非线性PDEs、时间依赖性问题(抛物型/双曲型)、以及更复杂的边界条件处理方式(如诺伊曼边界条件)，方法的普适性和性能尚未得到检验。”

> “缺乏与其他主流自适应方法的系统性比较(RAR,RAD等)，无法量化其相对优势的大小。”

---

**(完)**

好的，我将从“不足与局限”部分继续，并完成总结。

---

#### **8. 不足与局限**
1.  **高维扩展性验证有限**：尽管作者指出该方法适用于高维场景(Case3)，但最高仅测试了五维(d=5)的问题。对于真正意义上的超高维度(例如d>100)，该方法中涉及的梯度计算和蒙特卡洛积分是否依然高效且稳定有待验证。
2.  **对PINNs训练稳定性的依赖**：方法的有效性依赖于能够成功训练出原问题和伴随问题的神经网络近似解。对于病态或复杂的PDEs/PINNs本身固有的优化困难可能会影响本方法的稳定性。
3.  **问题类型普适性待验证**：目前仅在线性椭圆型模型问题上进行了验证(Poisson方程)。对于非线性PDEs、时间依赖性问题(抛物型/双曲型)、以及更复杂的边界条件处理方式(如诺伊曼边界条件)，方法的普适性和性能尚未得到检验。
4.  **缺乏横向比较**：缺乏与其他主流自适应PINN采样方法（如基于残差的RAR、RAD等）的直接系统性比较，无法量化其相对于现有自适应方法的优势大小。
5.  **计算开销分析缺失**：论文未讨论引入伴随问题求解和误差估计器计算所带来的额外计算成本。虽然自适应采样可能减少达到相同精度所需的epoch数，但每个epoch的计算量增加了（需要同时训练两个网络并计算局部化指示函数）。这种“总计算时间 vs. 最终精度”的权衡关系未被分析。

#### **9. 潜在改进方向与未来工作**
1.  **扩展到更广泛的PDE类型**：最直接的方向是将该方法应用于非线性稳态问题、时域问题以及具有不同边界条件的PDEs，检验其鲁棒性并可能发展相应的变体。
2.  **与其他自适应策略结合或对比**：将所提出的面向目标的DWR策略与基于残差的自适应策略（如RAR）进行结合或系统性的性能对比研究，探索在不同场景下的最优选择或混合策略。
3.  **优化实现效率**：研究如何高效地求解伴随问题（例如，与原问题共享部分网络参数），以及如何更廉价地更新误差指示函数，以降低每个epoch的额外开销。
4.  **理论分析的深化**：为所提出的启发式误差估计器提供更严格的理论保证，例如在特定假设下证明其作为真实误差上/下界的可靠性。

#### **10. 总结评价**
本文是一篇具有显著创新性和实用价值的研究工作。它成功地将经典数值分析中的成熟技术——对偶加权残差（DWR）误差估计与自适应网格细化——创造性地迁移至物理信息神经网络（PINNs）这一新兴领域。论文的核心贡献在于提出了一套完全无网格的、面向目标泛函的自适应重要性采样框架。

该框架通过构建一个完全基于神经网络的、可计算的DWR误差估计器及其局部化版本，能够有效识别对特定输出量（目标泛函）误差贡献最大的空间区域，并据此指导训练点的重新分布。数值实验在多个维度、不同目标泛函以及PINN/Deep Ritz两种框架下一致表明，该方法能显著加速目标泛函的收敛并提升最终精度。

尽管论文在问题普适性、高维验证和横向对比方面存在局限，但其提出的方法论清晰、可复现，为解决PINNs在工程应用中关心的特定输出量精度低、收敛慢的问题提供了一个强有力的新工具。它为后续研究开辟了一个富有潜力的方向，即如何将更多经典的数值分析思想与深度学习范式相结合，以克服纯数据驱动或物理驱动方法的内在缺陷。

（完）
