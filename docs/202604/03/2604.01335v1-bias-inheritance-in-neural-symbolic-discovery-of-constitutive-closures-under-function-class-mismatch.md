---
title: Bias Inheritance in Neural-Symbolic Discovery of Constitutive Closures Under Function-Class Mismatch
title_zh: 函数类不匹配下本构闭合神经符号发现中的偏差继承
authors: "Hanbing Liang, Ze Tao, Fujun Liu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01335v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 反应扩散偏微分方程规律的神经符号发现
tldr: 本研究针对已知PDE结构的非线性反应-扩散系统，探究数据驱动的本构关系发现。为避免将低残差预测误认为物理规律恢复，提出了一个三阶段神经符号框架：先学习受物理约束的数值代理模型，再将其压缩为受限的可解释符号族（如多项式、有理式），最后通过前向重模拟验证。研究发现存在“偏差继承”机制——符号压缩无法自动修正本构偏差，其误差紧密跟踪神经代理模型的误差。这表明神经符号建模的主要瓶颈在于初始的数值反问题而非后续的符号压缩。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01335v1/fig-001.webp\", \"caption\": \"Table 3: Representative symbolic compression summary. The symbolic stage stays close to the neural surrogate and the bias inheritance ratios remain near one, especially on the mismatch stress test.\", \"page\": 15, \"index\": 1, \"width\": 810, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2604.01335v1/fig-002.webp\", \"caption\": \"Figure 1: Stage 1 noise-sensitivity sweep across the three 1D benchmark cases. Mean±standard deviation over three seeds for the neural Stage 1 closure errors. In all cases, both diffusion and reaction recovery deteriorate sharply between 1% and 3% observation noise and then flatten toward saturated high-error regimes by 5%.\", \"page\": 14, \"index\": 2, \"width\": 814, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2604.01335v1/fig-003.webp\", \"caption\": \"Figure 3: Rollout preservation and bias inheritance. Top: symbolic unseen rollout closely follows neural unseen rollout. Bottom: the bias inheritance ratios remain near one, showing that symbolic compression preserves rather than repairs Stage 1 constitutive bias.\", \"page\": 18, \"index\": 3, \"width\": 814, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.01335v1/fig-004.webp\", \"caption\": \"Figure A.4: Case Exp observation-degradation breakdown. Left: representative clean, 1%, and 5% noise checkpoints for the neural closure errors, together with the symbolic unseen rollout error. Right: temporal stride 2 alone leaves the mismatch benchmark almost unchanged, while spatial stride 2 accounts for nearly all of the sparse-setting degradation.\", \"page\": 28, \"index\": 4, \"width\": 814, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.01335v1/fig-005.webp\", \"caption\": \"Table 1: Representative baseline comparison. In matched-library settings, polynomial baselines are exceptionally strong. In mismatch settings, the neural surrogate serves as a flexible numerical constitutive approximator, while the symbolic stage largely preserves its behavior.\", \"page\": 13, \"index\": 5, \"width\": 810, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2604.01335v1/fig-006.webp\", \"caption\": \"Figure 2: Neural-to-symbolic error propagation. Stage 2 compression error remains small, while Stage 3 true symbolic error tracks the Stage 1 neural true error. This is the empirical signature of bias preservation.\", \"page\": 16, \"index\": 6, \"width\": 814, \"height\": 469}]"
motivation: 旨在从时空观测数据中稳健地恢复非线性反应-扩散系统的扩散和反应定律，避免将低残差或短时预测错误等同于物理规律的恢复。
method: 提出一个三阶段神经符号框架：在物理约束下学习数值代理模型，将其压缩为受限的可解释符号族，并通过前向重模拟进行验证。
result: 数值实验揭示了两种不同机制：在函数类匹配时，弱多项式基线表现良好；在不匹配时，神经代理模型提供必要灵活性并可被压缩为紧凑符号定律，但存在关键的“偏差继承”现象。
conclusion: 研究强调，本构关系的最终确立必须基于前向验证，而非仅依赖残差最小化。
---

## 摘要
我们研究了在已知偏微分方程（PDE）控制结构的非线性反应-扩散系统中，本构闭合的数据驱动发现。我们的目标是从时空观测中稳健地恢复扩散和反应规律，同时避免将低残差或短时预测与物理规律恢复混为一谈的常见陷阱。我们提出了一个三阶段的神经符号框架：（1）使用噪声鲁棒的弱形式驱动目标，在物理约束下学习数值代理模型；（2）将这些代理模型压缩到受限的可解释符号族（例如多项式、有理式和饱和形式）中；（3）通过在未见初始条件下进行显式正向重模拟来验证符号闭合。广泛的数值实验揭示了两种不同的机制。在匹配库设置下，弱多项式基线表现为正确指定的参考估计器，表明神经代理模型并未普遍优于经典基函数。相反，在函数类不匹配的情况下，神经代理模型提供了必要的灵活性，并能被压缩成紧凑的符号规律，且滚动预测性能下降最小。然而，我们发现了一种关键的“偏差继承”机制：符号压缩不会自动修正本构偏差。在各种观测机制下，符号闭合的真实误差紧密跟踪神经代理模型的误差，导致偏差继承比率接近1。这些发现表明，神经符号建模的主要瓶颈在于初始的数值反问题，而非后续的符号压缩。我们强调，对本构规律的断言必须通过正向验证严格支持，而不能仅依赖于残差最小化。

## Abstract
We investigate the data-driven discovery of constitutive closures in nonlinear reaction-diffusion systems with known governing PDE structures. Our objective is to robustly recover diffusion and reaction laws from spatiotemporal observations while avoiding the common pitfall where low residuals or short-horizon predictions are conflated with physical recovery. We propose a three-stage neural-symbolic framework: (1) learning numerical surrogates under physical constraints using a noise-robust weak-form-driven objective; (2) compressing these surrogates into restricted interpretable symbolic families (e.g., polynomial, rational, and saturation forms); and (3) validating the symbolic closures through explicit forward re-simulation on unseen initial conditions. Extensive numerical experiments reveal two distinct regimes. Under matched-library settings, weak polynomial baselines behave as correctly specified reference estimators, showing that neural surrogates do not uniformly outperform classical bases. Conversely, under function-class mismatch, neural surrogates provide necessary flexibility and can be compressed into compact symbolic laws with minimal rollout degradation. However, we identify a critical "bias inheritance" mechanism where symbolic compression does not automatically repair constitutive bias. Across various observation regimes, the true error of the symbolic closure closely tracks that of the neural surrogate, yielding a bias inheritance ratio near one. These findings demonstrate that the primary bottleneck in neural-symbolic modeling lies in the initial numerical inverse problem rather than the subsequent symbolic compression. We underscore that constitutive claims must be rigorously supported by forward validation rather than residual minimization alone.

---

## 论文详细总结（自动生成）

# 论文总结：函数类不匹配下本构闭合神经符号发现中的偏差继承

## 1. 核心问题与整体含义
*   **研究背景**：许多物理和工程系统由偏微分方程（PDE）控制，其宏观守恒定律已知，但具体的本构关系（如扩散系数、反应速率）未知。数据驱动的科学机器学习旨在从观测数据中发现这些基本物理规律，而不仅仅是构建预测模型。
*   **核心问题**：在已知PDE结构（如反应-扩散方程）但未知具体本构关系的情况下，如何从时空观测数据中**稳健且可验证地恢复出真实的物理定律**。
*   **关键挑战**：这是一个不适定的反问题。仅最小化观测轨迹的残差或实现短时预测误差小，并不能保证正确识别底层物理规律。不同的扩散和反应项组合可能在有限的动态激励下相互补偿，产生相似的轨迹预测，导致严重的本构错误。这造成了“拟合数据”与“识别真实物理定律”之间的根本差距。

## 2. 方法论
论文提出了一个解耦的三阶段神经符号框架：

*   **第一阶段：弱形式驱动的数值本构恢复**
    *   **核心思想**：避免对噪声和稀疏数据敏感的直接强形式微分，采用空间弱形式来训练数值代理模型。
    *   **关键技术**：
        1.  使用空间测试函数对PDE进行积分，得到弱形式残差作为主要训练信号。
        2.  构建一个紧凑的神经网络（两个分支分别对应扩散D(u)和反应R(u)），并施加物理约束（如扩散项为正、平滑性）。
        3.  训练目标是一个混合损失函数，结合了弱形式一致性、辅助滚动预测、质量守恒重加权、锚点约束和正则化项。
    *   **目标**：学习一个稳定、灵活的数值本构代理模型。

*   **第二阶段：受限符号压缩**
    *   **核心思想**：将第一阶段学习到的数值代理模型（曲线）压缩到预定义的、可解释的符号族中，而不是直接从原始轨迹进行无限制的符号回归。
    *   **关键技术**：
        1.  在状态变量u的密集网格上评估学习到的数值闭合物理量。
        2.  使用受限的符号库进行拟合（例如，对于扩散项：低阶多项式、有理式、指数衰减形式；对于反应项：低阶多项式、有理式、u*exp(·)、u/(1+·)等形式）。
        3.  选择在给定容差范围内复杂度最低的候选表达式。
    *   **目标**：获得一个紧凑、可解释的符号表达式对 (D_sym, R_sym)，并忠实于神经代理模型的行为。

*   **第三阶段：前向重模拟验证**
    *   **核心思想**：将压缩得到的符号闭合重新代入原始PDE中，在未见过的初始条件下进行显式正向模拟。
    *   **关键技术**：
        1.  分别使用神经代理模型和符号闭合进行滚动预测。
        2.  比较两者的滚动预测误差以及它们与真实轨迹的误差。
    *   **目标**：这是最终的验证步骤。只有当压缩后的符号定律能够成功驱动系统动力学时，该发现过程才算完成。

## 3. 实验设计
*   **研究场景**：一维周期边界条件下的非线性反应-扩散系统 `u_t = ∂_x (D(u) u_x) + R(u)`。
*   **数据集/案例**：
    1.  **Case A & Case B (匹配库机制)**：真实本构关系位于受限的多项式库内。用于验证方法在正确指定模型下的表现。
    2.  **Case Exp (函数类不匹配机制)**：真实本构关系包含指数项，位于多项式库之外。这是对神经符号管道的主要压力测试。
*   **数据生成与扰动**：
    *   使用随机傅里叶初始条件生成多个轨迹。
    *   考察不同观测条件的影响：“干净”、“5%高斯噪声”、“时空稀疏采样”（空间/时间步长跨度为2）。
*   **对比方法 (Benchmark)**：
    1.  Strong Polynomial Baseline: 基于强形式残差的多项式拟合。
    2.  Weak Polynomial Baseline: 基于弱形式残差的多项式拟合（主要基准）。
    3.  Neural Numerical Surrogate: 论文提出的第一阶段神经网络模型。
    4.  Neural + Symbolic Compression: 完整的论文三阶段框架（第二阶段使用受限符号库）。
    5.  （补充实验）Neural + PySR: 用更强大的符号回归引擎PySR替换第二阶段，以检验偏差继承是否源于受限库。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量及总训练时长等硬件细节。实验部分提到了训练周期数（如150 epochs）、优化器（Adam）和学习率等超参数设置。代码已开源。

## 5. 实验数量与充分性
实验设计较为系统和全面：
*   **主实验对比**：在三种不同案例（A, B, Exp）、三种观测条件（干净、噪声5%、稀疏）下，系统比较了四种主要方法家族的性能指标（闭合物理量误差ErrD/ErrR、未见滚动误差）。
*   **关键机制验证实验组**：
    1.  **激励覆盖度实验**：对比“低激励”和“高激励”训练集对恢复效果的影响，验证了低残差不等于正确识别的理论命题。
    2.  **噪声敏感性扫描**：对三个案例进行了0%到5%共6个噪声水平的系统性测试，揭示了性能下降的关键区间。
    3.  **偏差继承量化实验**：计算并报告了偏差继承比率 `BIR = Err_sym_true / Err_neural_true` ，并在所有设置下展示其接近1的现象。
    4.  **消融实验 (Ablation)**：
        *   对第一阶段混合损失函数进行了消融研究（纯弱形式 vs.弱+辅助 vs完整混合），分析了各组件的作用。
        *   用PySR替换第二阶段的受限库进行对比实验，检验偏差继承的根本原因是否在于压缩阶段的表达能力不足。
        *   分析了时空稀疏采样各自的影响贡献度分离实验。
    5.  **鲁棒性检查**: “反逆向犯罪”交叉分辨率检查——使用与数据生成不同的网格和时间步长进行验证模拟；以及一个初步的二维周期性域扩展检查。

总体而言，实验围绕核心论点展开层层递进的验证和支持性分析是充分的。通过设置匹配库作为参考基准并与更强大的外部工具(PySR)对比增强了客观性和说服力。

##6 .主要结论与发现
1 .在函数类匹配的情况下 ，经典弱多项式基线是强大且正确的参考估计器 ，不应被简单地视为朴素比较对象 。神经代理模型在此情况下并无普遍优势 。
2 .在函数类不匹配的情况下 ，神经代理模型作为灵活的数值近似器变得必要 ，并可被成功压缩为紧凑的符号定律 ，且附加的代理误差或滚动退化很小 。
3 .发现了关键的“偏差继承”机制 ：符号压缩阶段本质上是一个保持行为而非修正错误的蒸馏过程 。一旦压缩误差很小 ，最终符号闭合物理量的真实误差将紧密跟踪初始神经代理模型的误差 （BIR ≈1）。这意味着 ，如果第一阶段的数值反问题未能准确恢复本构关系 ，后续再强大的符号回归也无法自动修复这一根本性偏差 。
4 .因此 ，整个神经 -符號建模流程的主要瓶颈在于第一阶段的数值反问题求解 （特别是在有限动态激励 、噪声和模型失配条件下 ）而非第二阶段的符號壓縮 。这强调了任何关于本构关系的声称都必须通过严格的前向重模拟驗證来支持 ，而不能仅仅依赖于訓練殘差的最小化 。

##7 .优点
-提出了清晰解耦的三阶段框架 （數值恢復 -符號壓縮 -前向驗證 ）使得誤差來源可以被隔離和分析 。
-明確區分了“函數類匹配 ”與“不匹配 ”兩種機制並進行對比研究具有重要理論與實踐意義 。
-通過理論命題 （非唯一性 、近似偏置分解 、偏置保持不等式 ）為實驗觀察提供了紮實的概念解釋 。
-實驗設計系統全面不僅比較最終性能還深入剖析了激勵覆蓋度噪聲敏感性等影響識別性的關鍵因素 。
-引入了“偏置繼承比率 ”這一量化指標來刻畫核心現象 。
-進行了PySR替換等對照實驗有力地排除了其他潛在解釋增強了結論的可信度 。

##8 .不足與局限
-研究範圍較窄 ：主要聚焦於一維週期性反應 -擴散系統雖然附錄進行了初步二維檢查但尚未擴展到更複雜的高維多變量各向異性或具有歷史依賴性的閉合關係場景 。
-噪聲魯棒性有限 ：實驗表明即使採用弱形式驅動在第一階段當觀測噪聲達到一定水平時恢復性能會急劇下降這限制了方法在實際高噪數據中的應用 。論文指出未來工作需要探索更魯棒的數值恢復方案例如全時空弱表述 。
-符號庫受限 ：第二階段的符號庫是預先定義且有限的這雖然符合其“受控壓縮 ”的研究目標但也意味著如果真實物理定律完全不在該庫內則無法被發現 。未來可探索如何可控地擴展庫空間 。
-計算成本未詳細討論 ：雖然框架清晰但完整的二階段訓練加壓縮加驗證流程總體計算成本可能高於直接的多項式擬合尤其是在需要大量超參調優時 。

(完)

好的，我们继续从“（完）”之后开始。由于您提供的总结已非常完整，我将补充一些可能的扩展性讨论或未来展望部分，以使总结更加全面。

---

## 9. 扩展性讨论与未来展望
*   **框架的通用性与可迁移性**：
    *   论文提出的三阶段框架（数值恢复-符号压缩-前向验证）具有清晰的模块化结构。理论上，第一阶段可以替换为其他更鲁棒的数值反演方法（如基于PINNs的变体、全时空弱形式等），第二阶段可以接入更强大的符号回归工具或知识库，第三阶段的验证标准也可以根据具体问题调整。这为方法的扩展和改进提供了清晰的路径。
    *   然而，其在一维反应-扩散方程上的成功，尚需在更广泛的PDE类型（如Navier-Stokes方程、波动方程）、边界条件（如Dirichlet/Neumann边界）、以及多物理场耦合问题上进行验证。

*   **对“偏差继承”机制的深入理解**：
    *   论文揭示了“偏差继承”是神经符号发现中的一个根本性限制，而非特定方法缺陷。这为整个领域提供了一个重要的警示：**数据拟合的保真度不等于物理定律识别的正确性**。
    *   未来的研究可以进一步探索在何种条件下（例如，通过设计特定的激励、引入额外的物理约束或先验知识）可以打破或减轻这种偏差继承，从而在第一阶段获得更准确的本构关系估计。

*   **迈向实际应用的挑战**：
    1.  **数据需求与质量**：方法对观测数据的覆盖范围（激励充分性）和噪声水平较为敏感。在实际工程或科学观测中，获取高分辨率、低噪声、动态范围充分的数据往往成本高昂或不可行。
    2.  **高维与复杂本构关系**：现实世界的本构关系可能依赖于多个状态变量、梯度甚至历史路径（非局部/记忆效应）。将当前方法扩展到此类场景需要设计新的神经网络架构和符号表示形式。
    3.  **计算效率与自动化**：虽然框架清晰，但实现一个完整的、自动化的发现流程仍需解决超参数选择、符号库设计、压缩容差设定等工程问题，以平衡准确性、可解释性和计算成本。

## 10. 总体评价
该论文是一项扎实且见解深刻的研究工作。它没有停留在提出一个性能更好的新模型上，而是深入剖析了数据驱动本构关系发现这一反问题中的核心困境——拟合误差最小化与物理真实性之间的鸿沟。通过严谨的理论分析和系统的实验验证，论文明确指出了现有神经符号方法中普遍存在的“偏差继承”现象，并将性能瓶颈定位在第一阶段的数值反问题求解上。这一结论具有重要的方法论意义，提醒研究者需要更加关注反问题的适定性和数据的充分性，而非仅仅追求复杂的模型架构。论文提出的三阶段框架和量化指标（BIR）为后续研究提供了有价值的分析工具和比较基准。

（完）
