---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩PINNs：偏微分方程与Landau-de Gennes模型的多解学习
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 将紧缩损失与PINNs和DeepONets结合用于偏微分方程
tldr: 非线性偏微分方程在数学物理和工程中普遍存在。物理信息神经网络是求解PDE的有力工具，但通常难以识别多个不同的解。为此，本文提出了Deflation-PINNs框架，它通过将通缩损失与基于PINNs和深度算子网络的架构相结合，系统地迫使模型寻找并收敛到多个不同的解分支。研究提供了模型收敛的理论证据，并通过液晶Landau-de Gennes模型的数值实验证明了该方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 解决PINNs难以识别PDE多个不同解的问题。
method: 引入基于PINNs和DeepONets的架构，并在损失函数中加入通缩项。
result: 数值实验表明该方法能有效找到多个解分支。
conclusion: Deflation-PINNs能成功识别和表征多个不同的晶体结构。
---

## 摘要
非线性偏微分方程在数学物理与工程领域普遍存在。虽然物理信息神经网络已成为求解偏微分方程问题的有力工具，但由于其设计为每次仅寻找一个解，通常难以识别多个不同的解。为突破这一局限，我们提出了Deflation-PINNs——一种将紧缩损失项与基于PINNs及深度算子网络的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法能系统性地迫使Deflation-PINN探索并收敛于有限多个不同的解分支。我们提供了模型收敛的理论证明，并通过液晶Landau-de Gennes模型的数值实验展示了Deflation-PINNs的有效性——该体系以其复杂的能量景观和多重平衡态而著称。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》进行结构化、深入且客观的总结。

---

### **1. 核心问题与整体含义**
*   **研究动机**：传统的物理信息神经网络旨在通过最小化物理残差损失来求解偏微分方程，但其优化过程通常收敛于单一解。对于许多非线性PDE（如Landau-de Gennes模型），存在多个稳定或亚稳态解，这些解对应不同的物理状态。传统PINN框架无法系统地发现所有这些解。
*   **研究目标**：提出一种新的、基于神经网络的框架，能够**一次性、系统地发现并逼近PDE的多个（有限个）不同解**，克服传统PINN和随机初始化方法的局限性。

### **2. 方法论：Deflation-PINNs**
*   **核心思想**：结合PINNs的物理约束和DeepONets的多函数逼近能力，并引入一个“紧缩损失”来强制模型学习彼此不同的解。
*   **关键技术细节**：
    1.  **架构设计（简化DeepONet）**：
        *   将DeepONet中用于编码输入函数的“分支网络”替换为可学习的“分支权重”向量 `β^k ∈ R^p`，每个向量对应一个待求的解 `u_k`。
        *   保留用于编码空间坐标的“主干网络” `τ(x)`。
        *   最终模型输出为：`G̃(k, x) = Σ_{i=1}^p τ_i(x) · β_i^k ≈ u_k(x)`。该架构专为学习有限个函数而设计，参数更高效。
    2.  **硬边界约束处理**：
        *   采用 `u_θ(x) = ω(x) · NN_θ(x) + b(x)` 的形式构建网络，其中 `ω(x)` 在边界上为零，`b(x)` 精确满足边界条件。这消除了边界损失项，简化了优化。
        *   论文提出了在星形域上使用径向边界距离函数进行边界延拓的具体方法。
    3.  **紧缩损失**：
        *   定义损失项 `L_Def(G̃) = (2/(K(K-1))) Σ_{i<j} max(1 - (||G̃(i,·)-G̃(j,·)||_{L2})/d_min, 0)`。
        *   该损失惩罚那些在L2范数下距离小于预设阈值 `d_min` 的解对，从而迫使网络输出的K个解彼此分离。
    4.  **总损失函数**：
        *   `L_total(G̃) = α · Σ_{i=1}^K E_G(G̃(i,·)) + β · L_Def(G̃)`。
        *   其中 `E_G` 是标准的PINN物理残差损失（内部PDE残差），`α` 和 `β` 是超参数。

### **3. 实验设计**
*   **基准场景**：二维简化Landau-de Gennes模型。这是一个描述向列相液晶的变分问题，其Euler-Lagrange方程是一个非线性PDE系统。在正方形区域上，已知存在六个稳定的平衡解（两个“对角态”D1、D2和四个“旋转态”R1-R4）。
*   **对比方法**：
    *   **有限元方法 (FEM)**：作为基准真值。引用自Maity等人的工作 (2021a,b)。
    *   **其他神经网络方法**：论文在引言中讨论了相关但不同的方法作为背景对比（如HomPINNs、随机初始化多个PINNs），但并未在实验中直接进行数值比较。

### **4. 资源与算力**
文中明确说明：实验在一张 **NVIDIA RTX 3060 Ti GPU (8GB VRAM)** 上进行。训练了10,000个epoch。

### **5. 实验数量与充分性**
*   **实验设置**：主要进行了单组核心实验——使用提出的Deflation-PINN框架求解前述LdG模型的一次性训练。
*   **充分性与客观性评估**：
    *   **充分性方面存在局限**：实验仅在一个特定PDE问题（LdG模型）上验证了方法。虽然该问题具有代表性（多解、非线性），但缺乏在其他经典多解PDE问题（如非线性薛定谔方程、多稳态ODE等）上的测试，以证明方法的普适性。
    *   **公平性方面尚可但可加强**：与FEM结果的对比是定性的（可视化比较场图），显示了结构一致性。然而，缺乏定量的误差指标比较（如相对L2误差）。同时，没有与引言中提到的其他寻找多解的神经网络方法进行直接、定量的性能对比（如收敛成功率、计算成本等），这使得对新方法优势的评估不够全面。

### **6. 主要结论与发现**
*   Deflation-PINNs能够成功识别出Landau-de Gennes模型中所有六个已知的稳定解（D1, D2, R1-R4）。
*   通过结合简化的DeepONet架构和紧缩损失项，该方法能够在一个训练过程中系统地找到多个彼此不同的解分支，而无需依赖多次随机初始化。
*   理论分析表明，所提出的架构在通用近似意义下是完备的。

### **7. 优点**
*   **创新性融合**：巧妙地将PINNs的物理驱动特性、DeepONets的多函数逼近思想以及数值分析中的“紧缩”概念相结合。
*   **系统性与效率**：提供了一种系统性寻找多解的框架，相比训练大量独立PINN可能更高效（避免冗余计算）。
*   **理论支撑**：不仅提供了数值结果，还给出了关于硬约束模型近似能力和架构通用性的理论证明附录。
*   **工程细节扎实**：详细描述了硬边界条件的实现方式（径向延拓），并公开了代码。

### **8. 不足与局限**
*   ***应用范围限制*** ：目前仅在单一且特定的物理模型上得到验证。其在更广泛类型的多解PDE问题上的有效性和鲁棒性有待检验。
*   ***超参数敏感性*** ：紧缩损失中的关键超参数 `d_min` （解间最小距离）需要凭经验选择或通过理论估计获得。文中提到是通过试错法确定的 (`d_min=0.4`) ，这在实际未知解的应用中可能是一个挑战。
*   ***实验对比不全面*** ：如前所述，缺乏与同类先进方法的定量比较和消融实验（例如分析紧缩损失项的必要性、分支权重维度p的影响等）。
*   ***计算复杂度*** ：虽然架构针对有限个解进行了简化，但当需要寻找的解的数量K很大时，“分支权重”的参数量和成对比较的紧缩损失计算量仍会增长。

---
(完)

### **9. 潜在影响与未来方向**
*   **对科学计算的影响**：该方法为使用神经网络求解具有多稳态、多分支解的非线性PDE问题提供了一种新的范式。它弥合了传统数值方法（如紧缩牛顿法）与数据驱动方法之间的鸿沟，展示了神经网络在探索解空间结构方面的潜力。
*   **对物理建模的启示**：对于Landau-de Gennes这类复杂物理模型，Deflation-PINNs提供了一种高效发现所有可能稳定构型（如液晶缺陷态）的工具，有助于更全面地理解系统的相图和能量景观。
*   **未来研究方向**：
    1.  **普适性验证**：在更多类型的多解PDE（如带非线性源项的椭圆方程、非线性波动方程）上测试该框架。
    2.  **自适应参数选择**：研究如何自适应地确定关键超参数 `d_min` 和所需解的数量 `K`，例如结合渐进误差估计或启发式搜索。
    3.  **扩展到无限维或连续谱解**：当前方法针对有限个离散解。如何将其推广到处理依赖于连续参数的解族（如特征值问题中的特征函数族）是一个有挑战性的方向。
    4.  **与不确定性量化结合**：将解的多重性与贝叶斯神经网络或集合方法结合，量化不同解被发现的可能性或其稳定性。
    5.  **性能优化与理论深化**：进一步分析损失景观，改进优化策略以避免陷入平庸解；深化关于紧缩损失如何影响收敛性和解的质量的理论理解。

### **10. 总结评价**
本文提出了一种新颖且构思巧妙的神经网络框架——Deflation-PINNs，用于系统性求解具有多个稳定解的偏微分方程。其核心贡献在于将物理信息约束、参数化函数逼近和数值紧缩思想有机融合，在一个统一的训练过程中同时逼近多个不同的解分支。

论文的方法论部分设计严谨，不仅给出了清晰的架构和损失函数定义，还深入探讨了硬边界条件的精确实施这一工程细节，并辅以理论近似能力的证明，体现了工作的完整性。在特定案例（Landau-de Gennes模型）上的实验成功验证了其核心设想的有效性。

然而，该工作的主要局限性在于其验证范围较为单一。实验部分未能充分证明方法的普适性和相对于其他竞争方法的显著优势。缺乏在不同基准问题上的测试、定量误差分析以及与同类神经求解器的直接对比，使得对其性能、鲁棒性和实用性的全面评估受到限制。

总体而言，《Deflation-PINNs》是一篇具有高创新性和启发性的研究论文。它成功地开辟了一个利用深度学习探索PDE多解空间的新方向，并为后续研究奠定了坚实的基础。未来的工作若能解决其在普适性验证和对比实验方面的不足，将极大提升该方法的认可度和应用价值。

（完）
