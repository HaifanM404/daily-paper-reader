---
title: Physics-Informed Neural Networks for Solving Derivative-Constrained PDEs
title_zh: 求解导数约束偏微分方程的物理信息神经网络
authors: "Kentaro Hoshisashi, Carolyn E Phelan, Paolo Barucca"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13723v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于求解导数约束PDE的物理信息神经网络
tldr: 针对物理信息神经网络（PINN）在处理导数约束（如单调性、凸性、不可压缩性等）方面的不足，本文提出DC-PINNs框架。该方法将偏微分方程求解转化为基于最小目标函数准则的优化问题，通过自动微分嵌入非线性导数约束，并结合自适应损失平衡技术。实验证明，DC-PINNs能显著降低约束违背，提高物理保真度，在热扩散、金融波动率及流体流动等任务中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13723v1/fig-001.webp\", \"caption\": \"FIG. 2: Second-derivative profiles with respect to x at multiple time snapshots. Dashed: analytical solution.\", \"page\": 6, \"index\": 1, \"width\": 514, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.13723v1/fig-002.webp\", \"caption\": \"FIG. 4: Spatial derivative ∂u/∂x at multiple time snapshots. Dashed: numerical reference.\", \"page\": 7, \"index\": 2, \"width\": 514, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.13723v1/fig-003.webp\", \"caption\": \"TABLE IX: Sensitivity of DC-PINNs on the Navier–Stokes equation. Reported values are RMSE in logarithmic scale, evaluated on dense grids and expressed as (L0,Lf ,Lh, µ1/µ2), where L0 = max{L0u ,L0v ,L0p},\", \"page\": 14, \"index\": 3, \"width\": 1002, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.13723v1/fig-004.webp\", \"caption\": \"TABLE VII: Sensitivity of DC-PINNs on the Heat problem. Reported values are RMSE in log-scale, given as (L0,Lf ,Lb,L(h1+h2)) in dense evaluation grids. Bold entries indicate the overall top-4 choices (by the sum of the four metrics; more negative is better).\", \"page\": 13, \"index\": 4, \"width\": 723, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.13723v1/fig-005.webp\", \"caption\": \"TABLE VIII: Sensitivity of DC-PINNs on the volatility surface calibration. The reported values are RMSE on the logarithmic scale, given as (L0,Lb,Lf,Lh(x+ xx+ t)) on dense evaluation grids. The bold entries indicate the overall top-4 configurations, and the lower values correspond to better performance.\", \"page\": 13, \"index\": 5, \"width\": 723, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.13723v1/fig-006.webp\", \"caption\": \"TABLE III: Validation (RMSE: bold is best) for the volatility surface calibration.\", \"page\": 8, \"index\": 6, \"width\": 690, \"height\": 200}]"
motivation: 传统的PINN难以直接处理物理规律中同样关键的导数约束，导致求解结果可能违反基本的物理一致性。
method: 提出DC-PINNs框架，利用自动微分显式嵌入非线性导数约束，并采用自适应损失平衡机制动态调整各项权重。
result: 在热扩散、金融套利及流体动力学等基准测试中，DC-PINNs显著减少了约束违背，提升了物理一致性和数值精度。
conclusion: 通过显式编码导数约束，DC-PINNs能有效引导优化过程走向物理上合理的极小值，为受限偏微分方程提供了更可靠的求解方案。
---

## 摘要
物理信息神经网络（PINNs）通过最小化基于残差的目标函数，将偏微分方程（PDE）的求解重构为函数空间中的优化问题。然而，许多应用场景需要额外的基于导数的关系，这些关系与控制方程同样具有基础性。在本文中，我们提出了导数约束物理信息神经网络（DC-PINNs），这是一个通用框架，将受约束的 PDE 求解视为由最小目标函数准则引导的优化过程，其中物理规律蕴含在最小化原理中。DC-PINNs 嵌入了针对状态和导数的通用非线性约束（如边界、单调性、凸性、不可压缩性），并利用自动微分进行高效计算；同时，它们采用自适应损失平衡来调节各目标项的影响，减少了对手动超参数和特定问题架构的依赖。在包括带边界的热扩散、无套利金融波动率以及带涡旋脱落的流体流动等基准测试中，DC-PINNs 相比于基准 PINN 变体和代表性的硬约束方案，一致地减少了约束违反并提升了物理保真度。显式编码导数约束稳定了训练过程，并引导优化向物理上可容许的极小值收敛，即使在 PDE 残差本身较小时也是如此，从而为基于能量最小化原理的受约束 PDE 提供了可靠的解。

## Abstract
Physics-Informed Neural Networks (PINNs) recast PDE solving as an optimisation problem in function space by minimising a residual-based objective, yet many applications require additional derivative-based relations that are just as fundamental as the governing equations. In this paper, we present Derivative-Constrained PINNs (DC-PINNs), a general framework that treats constrained PDE solving as an optimisation guided by a minimum objective function criterion where the physics resides in the minimum principle. DC-PINNs embed general nonlinear constraints on states and derivatives, e.g., bounds, monotonicity, convexity, incompressibility, computed efficiently via automatic differentiation, and they employ self-adaptive loss balancing to tune the influence of each objective, reducing reliance on manual hyperparameters and problem-specific architectures. DC-PINNs consistently reduce constraint violations and improve physical fidelity versus baseline PINN variants, representative hard-constraint formulations on benchmarks, including heat diffusion with bounds, financial volatilities with arbitrage-free, and fluid flow with vortices shed. Explicitly encoding derivative constraints stabilises training and steers optimisation toward physically admissible minima even when the PDE residual alone is small, providing reliable solutions of constrained PDEs grounded in energy minimum principles.

---

## 论文详细总结（自动生成）

这是一份关于论文《Physics-Informed Neural Networks for Solving Derivative-Constrained PDEs》（求解导数约束偏微分方程的物理信息神经网络）的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
传统的物理信息神经网络（PINNs）通过将 PDE 残差嵌入损失函数，将方程求解转化为优化问题。然而，在实际物理和金融应用中，解往往需要满足特定的**导数约束**（如单调性、凸性、物理量上下界、不可压缩性等）。
*   **研究动机**：仅靠 PDE 残差项不足以保证解的物理合理性。忽略这些导数约束会导致模型在数据稀疏或噪声干扰下产生数学上可行但物理上荒谬的解（例如，热传导中出现温度自发上升，或金融模型中出现套利机会）。
*   **核心问题**：如何构建一个通用的框架，在不依赖复杂手动调参的情况下，显式且高效地将非线性导数不等式约束集成到 PINNs 的优化过程中。

### 2. 论文提出的方法论：DC-PINNs
论文提出了 **DC-PINNs（Derivative-Constrained PINNs）** 框架，其核心思想是将受限 PDE 求解视为一个多目标优化问题。
*   **核心思想**：利用自动微分（AD）计算高阶导数，通过**单边惩罚函数（Hinge Loss）**处理不等式约束，并引入**双层自适应损失平衡机制**。
*   **关键技术细节**：
    *   **约束编码**：对于不等式约束 $h(x, Du) \leq 0$，定义违背损失 $L_h = \frac{1}{N_h} \sum [h]_+^2$，其中 $[ \cdot ]_+$ 是 Hinge 算子，仅对违反约束的部分产生梯度。
    *   **自适应平衡（核心创新）**：
        1.  **样本级缩放（Individual Balancing）**：为每个配置点分配可学习权重 $m$，通过梯度上升更新，强化对严重违背约束区域的关注。
        2.  **类别级加权（Categorised Balancing）**：动态调整 PDE 残差、边界条件和导数约束之间的权重 $\lambda$，基于各梯度项的平均绝对值进行平衡，减少对手动超参数的依赖。
*   **算法流程**：在每次迭代中，首先计算各损失项梯度，随后按预设频率更新自适应权重 $m$ 和 $\lambda$，最后更新网络参数 $\theta$。

### 3. 实验设计
论文在三个具有代表性的基准场景上验证了 DC-PINNs：
*   **场景 1：一维热传导方程**。约束：二阶空间导数非正（凸性）和时间导数非正（单调衰减）。
*   **场景 2：金融波动率曲面校准（Dupire 方程）**。约束：无套利条件（对行权价的单调性和凸性，以及对到期时间的单调性）。
*   **场景 3：不可压缩纳维-斯托克斯（Navier-Stokes）方程**。场景为绕流圆柱（Re=100）。约束：质量守恒（散度为零）和基于伯努利原理的压力梯度界限。
*   **对比方法（Benchmark）**：
    *   标准 PINNs。
    *   PINNs+Ineq（固定权重的惩罚项）。
    *   硬约束基准：hPINN（架构强制）、惩罚同伦法（Penalty Method）、增广拉格朗日法（AL-PINNs）。

### 4. 资源与算力
*   **硬件环境**：使用 Google Colab 平台，配置为 **Google Cloud TPU v6e-1**，拥有 173 GB 高带宽内存（HBM）。
*   **软件框架**：基于 JAX 和 Flax 实现，利用 XLA 编译加速。
*   **训练时长**：
    *   热传导方程：约 5 秒。
    *   波动率校准：约 6 秒。
    *   纳维-斯托克斯方程：约 100 秒。
    *   DC-PINNs 的训练耗时约为标准 PINNs 的 **1.5 至 2 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括精度（RMSE）、稳定性（误差轨迹波动）、效率（收敛速度）和违背率（约束满足程度）。
*   **消融实验**：专门设计了消融实验来验证自适应权重 $\lambda$ 和 $m$ 的各自贡献，证明了双层平衡机制对降低误差的必要性。
*   **敏感性分析**：针对自适应学习率和更新频率进行了超参数搜索（如 $\eta_m, p_m, p_\lambda$），验证了算法在不同设置下的鲁棒性。
*   **公平性**：所有对比方法均在相同的网络架构和数据点下运行，实验设计较为客观、公平。

### 6. 论文的主要结论与发现
*   **物理一致性显著提升**：DC-PINNs 在所有任务中均能显著降低导数约束的违背率，生成的解在物理上更具保真度。
*   **训练稳定性增强**：自适应平衡机制有效缓解了多目标优化中的梯度不平衡问题，减少了训练过程中的震荡。
*   **精度与约束的权衡**：在简单问题（热传导、金融）中，DC-PINNs 精度最高；但在复杂耦合系统（如 NS 方程）中，DC-PINNs 主要提升的是物理一致性，而绝对精度（数据拟合）可能略逊于专门调优的硬约束方法。
*   **高维扩展性**：在高达 128 维的热传导方程测试中，DC-PINNs 表现出良好的数值稳定性，主要瓶颈在于 GPU 内存而非算法本身。

### 7. 优点（亮点）
*   **通用性强**：不依赖于特定方程的解析性质，可处理各种非线性导数不等式。
*   **自动化程度高**：通过自适应加权减少了繁琐的损失项权重调优工作。
*   **理论支撑扎实**：将 PINNs 重新解释为基于能量最小化原理的变分优化过程，具有较强的物理直觉。

### 8. 不足与局限
*   **计算开销**：由于需要频繁计算高阶导数和更新自适应权重，计算成本比标准 PINN 高出近一倍。
*   **复杂系统中的精度瓶颈**：在纳维-斯托克斯方程等强耦合系统中，DC-PINNs 虽然保证了约束，但在有限的计算预算下，其数据拟合精度（RMSE）并未全面超越增广拉格朗日法。
*   **高维实验单一**：高维扩展性测试仅基于线性热传导方程，尚未在复杂非线性高维 PDE 上验证其有效性。

（完）
