---
title: Learning Parameterized Nonlinear Elasticity on Curved Surfaces
title_zh: 学习曲面上的参数化非线性弹性
authors: "Yankang Liu, Ke Zhang, Maziar Raissi, Roya Zandi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12170v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于控制方程的物理信息神经网络
tldr: 本研究利用物理信息神经网络（PINN）学习曲面上的参数化非线性弹性力学。针对晶体壳层、弹性膜和病毒衣壳等受曲率和拓扑缺陷影响的复杂力学系统，该方法通过将控制方程和边界条件直接引入损失函数，使单个模型能表示跨几何与材料参数的连续弹性平衡族。相比传统有限元方法，该框架在处理对称性破缺或参数变化时更具扩展性，并在球体表面单旋错基准问题上验证了其准确性与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12170v1/fig-001.webp\", \"caption\": \"Table 3: Snapshot of stage 1 training log (every 500 epochs). Number of training points / N : 2167/4000. The neural network has 4 hidden layers and 200 neurons/layer. Time is per 500 epochs.\", \"page\": 10, \"index\": 1, \"width\": 564, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2604.12170v1/fig-002.webp\", \"caption\": \"Figure 2: Comparison of neural network outputs (dashed lines) and ground truth solutions (light solid lines). Subplots show (q, νp) ∈ {(0, 0.3), (1, 0.8)}. Colors red, blue, and green denote β = 1, 2, 3, respectively. Setup: 4 layers, 200 neurons/layer, N = 4000.\", \"page\": 3, \"index\": 2, \"width\": 680, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.12170v1/fig-003.webp\", \"caption\": \"Figure 4: Comparison of neural network outputs (dashed lines) and ground truth solutions (light solid lines). Subplots show (q, νp) ∈ {(1, 0.3), (0, 0.8)}. Colors red, blue, and green denote β = 1, 2, 3, respectively. Setup: 4 layers, 200 neurons/layer and N = 4000.\", \"page\": 9, \"index\": 3, \"width\": 680, \"height\": 248}]"
motivation: 传统数值求解器在处理曲面弹性力学时，需针对每组参数重新初始化且难以应对对称性破缺，限制了其在大规模参数探索中的效率。
method: 提出一种物理信息神经网络框架，通过在损失函数中强制执行控制方程和边界条件，实现对曲面非线性弹性平衡态的参数化学习。
result: 在类球面上的单旋错基准测试中，该网络准确复现了精确解，并展现出对训练集外几何与材料参数的良好泛化性能。
conclusion: 该研究为学习曲面流形上的非线性弹性系统建立了可扩展框架，为进一步研究蛋白质衣壳等复杂多缺陷弹性网络奠定了基础。
---

## 摘要
我们利用物理信息神经网络学习曲面上的参数化非线性弹性，该网络通过损失函数直接施加控制方程和边界条件，使得单个训练好的模型能够表示跨几何和材料参数的连续弹性平衡族。曲面流形上的非线性弹性是晶体壳层、弹性薄膜和病毒衣壳力学的基础，其中曲率和拓扑缺陷决定了平衡结构和稳定性。传统的精确解法和有限元求解器依赖于对称性约化，且必须针对每个参数选择进行重新初始化，这在对称性破缺或参数变化时限制了其可扩展性。我们在曲面弹性的一个基准问题上验证了所提出的基于学习的求解器，即具有已知精确解和数值解的类球面上一维单旋错问题。该网络准确地复现了这些解，包括训练中未包含的参数组合，展示了在几何和材料制度下的泛化能力。本研究建立了一个用于学习曲面流形上非线性弹性系统的可扩展框架，并为扩展到与蛋白质壳层和其他曲面弹性网络相关的全二维及多缺陷构型奠定了基础。

## Abstract
We learn parameterized nonlinear elasticity on curved surfaces using a physics-informed neural network that enforces governing equations and boundary conditions directly through the loss function, enabling a single trained model to represent a continuous family of elastic equilibria across geometric and material parameters. Nonlinear elasticity on curved manifolds underlies the mechanics of crystalline shells, elastic membranes, and viral capsids, where curvature and topological defects determine equilibrium structure and stability. Traditional exact and finite element solvers rely on symmetry reduction and must be reinitialized for each parameter choice, limiting scalability when symmetry is broken or parameters vary. We validate the proposed learning-based solver on a benchmark problem from curved elasticity, namely the one-dimensional single disclination on a spheroidal surface with known exact and numerical solutions. The network accurately reproduces these solutions, including parameter combinations excluded from training, demonstrating generalization across geometry and material regimes. This study establishes a scalable framework for learning nonlinear elastic systems on curved manifolds and lays the groundwork for extensions to fully two-dimensional and multi-defect configurations relevant to protein shells and other curved elastic networks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Parameterized Nonlinear Elasticity on Curved Surfaces》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在曲面流形（如病毒衣壳、晶体壳层）上高效求解非线性弹性力学方程，并实现跨几何和材料参数的泛化。
*   **研究背景**：曲面上的弹性力学受曲率诱导的“几何挫折”影响，通常需要引入拓扑缺陷（如旋错）来达到能量平衡。
*   **研究动机**：
    *   **传统方法局限**：有限元法（FEM）或精确解析解通常依赖于高度对称性。一旦对称性破缺或参数（如形状、材料属性）发生变化，传统求解器必须重新初始化和计算，在大规模参数空间探索中效率极低。
    *   **机器学习潜力**：物理信息神经网络（PINN）能够将物理定律嵌入损失函数，有望构建一个“参数化求解器”，即单个模型即可表示一整族不同参数下的物理平衡态。

### 2. 论文提出的方法论
*   **核心思想**：利用 **参数化物理信息神经网络（Parameterized PINN）**。将空间坐标与物理参数（几何形状、缺陷电荷、泊松比）共同作为网络输入，通过最小化物理残差来学习解函数。
*   **关键技术细节**：
    *   **控制方程**：基于协变力平衡方程（Covariant force balance equation），考虑了度规张量、克里斯托费尔符号（Christoffel symbols）以及应力张量。
    *   **网络输入**：输入向量为 $\tilde{r} = (r, \beta, q, \nu_p)$，其中 $r$ 是径向坐标，$\beta$ 控制类球面的曲率，$q$ 是缺陷电荷，$\nu_p$ 是泊松比。
    *   **损失函数设计**：
        1.  **$L_{phys}$**：控制方程的物理残差（通过自动微分计算）。
        2.  **$L_{bc}$**：边界条件残差（如边缘应力为零）。
        3.  **$L_0$**：原点处的正则化约束。
        4.  **$L_{data}$**：引入极少量地面真值（Ground Truth）数据辅助收敛。
*   **算法流程**：使用带有双曲正切（tanh）激活函数的全连接网络，采用两阶段训练计划（先高学习率后低学习率），通过 Adam 等优化器最小化综合损失。

### 3. 实验设计
*   **实验场景**：类球面（Spheroidal surface）上的单中心旋错（Single centered disclination）问题。这是一个典型的一维对称约化基准问题。
*   **Benchmark（基准）**：使用有限元方法（FEM）生成的数值解作为地面真值。
*   **对比与验证**：
    *   **插值泛化测试**：在训练集中剔除特定的参数组合（如特定的 $\beta, q, \nu_p$ 值），测试模型对未见参数的预测精度。
    *   **消融实验**：改变配置点（Collocation points）的数量 $N$，以及改变神经网络的深度（层数）和宽度（每层神经元数），观察模型稳定性和收敛性。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA RTX A6000 GPU**。
*   **训练时长**：
    *   第一阶段：200k 步（学习率 $10^{-4}$）。
    *   第二阶段：100k 步（学习率 $10^{-5}$）。
    *   **计算效率**：在 A6000 上，每 500 步优化大约耗时 **6.2 秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   数据集包含约 $2 \times 10^5$ 个解样本。
    *   训练集使用了约 57% 的总样本量，其余用于验证泛化性。
    *   进行了多组针对 $N$（1000 到 7000）和网络架构（层数 2-6，神经元 160-240）的对比实验。
*   **充分性评价**：实验设计较为充分，涵盖了参数空间内的插值验证和架构敏感性分析。通过剔除特定参数点进行测试，客观地证明了模型的泛化能力而非简单的记忆。

### 6. 论文的主要结论与发现
*   **高精度复现**：单个 PINN 模型能够准确复现不同几何和材料制度下的弹性变形剖面，预测结果与 FEM 地面真值高度吻合。
*   **参数空间泛化**：模型在处理训练集中未出现的参数组合时表现出极强的插值能力，证明了其捕捉底层物理流形的能力。
*   **高效性**：相比于为每个参数点单独运行 FEM，训练好的 PINN 可以瞬时推理出新参数下的解。
*   **稳定性**：实验发现，虽然大多数架构能收敛，但某些特定架构（如过浅或过深且宽度不足）可能导致不收敛（残差极大），强调了架构选择的重要性。

### 7. 优点
*   **参数化建模**：打破了传统数值方法“一参一算”的限制，实现了连续参数空间的统一表示。
*   **物理约束**：通过将复杂的协变微分方程嵌入损失函数，保证了结果的物理一致性。
*   **数据效率**：实验表明，即使配置点数量较少（如 $N=1000$），物理约束仍能引导模型达到高精度。

### 8. 不足与局限
*   **维度局限**：目前仅验证了一维对称约化后的模型。虽然论文声称可扩展，但尚未在全二维（2D）非对称曲面或多缺陷相互作用的复杂场景下进行验证。
*   **架构敏感性**：实验显示模型对网络深度和宽度的选择较为敏感，存在不收敛的风险，缺乏自动化的架构调优机制。
*   **计算开销**：虽然推理快，但 PINN 的训练过程（300k 步）相对于简单的 FEM 求解单次任务来说，前期计算成本依然较高。

（完）
