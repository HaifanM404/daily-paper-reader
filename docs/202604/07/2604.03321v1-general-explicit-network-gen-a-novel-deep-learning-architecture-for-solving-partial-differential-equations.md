---
title: "General Explicit Network (GEN): A novel deep learning architecture for solving partial differential equations"
title_zh: 通用显式网络 (GEN)：一种用于求解偏微分方程的新型深度学习架构
authors: "Genwei Ma, Ting Luo, Ping Yang, Xing Zhao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.03321v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于求解偏微分方程的深度学习架构
tldr: 针对物理信息神经网络（PINN）在处理偏微分方程（PDE）时存在的点对点拟合局限性及鲁棒性不足等问题，本文提出了一种通用显式网络（GEN）。该架构通过引入基于先验知识的基函数，实现了从点到函数的求解模式。实验证明，GEN在求解PDE时具有极强的鲁棒性和扩展性，为复杂物理问题的深度学习求解提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03321v1/fig-001.webp\", \"caption\": \"Figure 1: This comparative study demonstrates two approaches for approximating y = sin(x). (a): Explicit polynomial fitting reveals a critical trade-off: low-degree polynomials (e.g., 3rd-degree, red) fail to capture the curvature of the function within the sampled region (x ∈ [−1, 1]), whereas higher-degree fitting (5th-7th degrees, orange/green) provides improved in-sample accuracy at the cost of severe overfitting and unstable extrapolation (wild oscillations beyond |x| > 1). (b): Taylor series expansion exhibits progressive refinement: lower-order approximations (1- 2 terms, red/brown) approximate the core trend, whereas higher-order expansions (3-4 terms, green/blue) systematically enhance both the local accuracy and global extrapolation stability of the system by aligning derivative constraints with the intrinsic physics of sin(x). These divergent extrapolation behaviours underscore the superiority of series expansions over purely empirical polynomial regression schemes.\", \"page\": 3, \"index\": 1, \"width\": 612, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.03321v1/fig-002.webp\", \"caption\": \"Figure 2: Comparison among solutions for the heat equation: (a) The PINN. (b-c) The SineGEN and GaussGEN developed under the proposed GEN framework. (d-e) The temporal profiles produced at x = 0.5 (left) and x = 1.0 (right), demonstrating increasing extrapolation errors beyond the training domain (t > 2.0), which is particularly evident in the PINN predictions. (f-g) The 25 basis functions learned by the two GEN methods.\", \"page\": 7, \"index\": 2, \"width\": 892, \"height\": 782}, {\"url\": \"assets/figures/arxiv/2604.03321v1/fig-003.webp\", \"caption\": \"Figure 3: Wave equation: (a) Heatmap comparison among the numerical solution, the PINN and the two GEN results, with the colour intensities representing the solution values. (b) and (c) Temporal snapshots of the solutions produced at spatial locations x = 0.5 and x = 3.0, respectively. The green shaded area represents the fitting region (training domain), and the red shaded area represents the extrapolation region.\", \"page\": 8, \"index\": 3, \"width\": 891, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.03321v1/fig-004.webp\", \"caption\": \"Figure 4: Burgers’ equation: (a)-(c) Heatmap comparisons between exact solutions and GENs with various numbers of basis functions. (d)-(f) Spatial snapshots of the solutions produced at temporal positions t = 0.25, 0.5 and 0.75, respectively. Magnified views near the maxima and minima are shown in the upper-right and lower-left corners, respectively. Marked fitting accuracy discrepancies are observed when a smaller number of basis functions is employed.\", \"page\": 9, \"index\": 4, \"width\": 897, \"height\": 475}]"
motivation: 传统的PINN方法主要采用点对点拟合，且因连续激活函数的局限导致模型在处理PDE时鲁棒性和可扩展性较差。
method: 提出通用显式网络（GEN），利用基于PDE先验知识构建的基函数，将求解模式从点对点拟合转变为点对函数拟合。
result: 实验结果表明，GEN架构在求解偏微分方程时表现出显著的鲁棒性提升和更强的模型扩展能力。
conclusion: GEN通过结合先验知识与显式网络结构，有效克服了现有深度学习PDE求解器的局限，具有广泛的应用潜力。
---

## 摘要
机器学习，特别是物理信息神经网络 (PINN) 及其神经网络变体，已被广泛用于求解涉及偏微分方程 (PDE) 的问题。然而，此类方法在学术研究之外的成功部署仍然有限。例如，PINN 方法主要考虑离散的点对点拟合，未能考虑到真实解的潜在特性。在这些方法中采用连续激活函数会导致局部特征虽然与方程解一致，但却导致可扩展性和鲁棒性较差。本文提出了一种实现“点对函数”PDE 求解的通用显式网络 (GEN)。“函数”部分可以根据我们对原始 PDE 的先验知识，通过相应的基函数进行拟合构建。实验结果表明，该方法能够获得具有高鲁棒性和强可扩展性的解。

## Abstract
Machine learning, especially physics-informed neural networks (PINNs) and their neural network variants, has been widely used to solve problems involving partial differential equations (PDEs). The successful deployment of such methods beyond academic research remains limited. For example, PINN methods primarily consider discrete point-to-point fitting and fail to account for the potential properties of real solutions. The adoption of continuous activation functions in these approaches leads to local characteristics that align with the equation solutions while resulting in poor extensibility and robustness. A general explicit network (GEN) that implements point-to-function PDE solving is proposed in this paper. The "function" component can be constructed based on our prior knowledge of the original PDEs through corresponding basis functions for fitting. The experimental results demonstrate that this approach enables solutions with high robustness and strong extensibility to be obtained.

---

## 论文详细总结（自动生成）

这是一份关于论文《General Explicit Network (GEN): A novel deep learning architecture for solving partial differential equations》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前以**物理信息神经网络（PINN）**为代表的深度学习 PDE 求解器存在的局限性进行了探讨。
*   **核心问题**：传统的 PINN 采用“点对点”的拟合模式，将神经网络视为黑盒来逼近解。这种方式忽略了真实物理解的全局拓扑性质，导致模型在训练区域内虽然精度尚可，但在**外推（Extrapolation）**区域表现极差，且缺乏鲁棒性和可解释性。
*   **研究动机**：受泰勒级数和傅里叶级数展开的启发，作者希望构建一种“点对函数”的架构，通过显式引入符合物理先验的基函数，使网络能够学习到更具一般性和物理一致性的解结构。

### 2. 方法论：核心思想与技术细节
论文提出了**通用显式网络（GEN）**架构，其核心思想是将解表示为基函数的非线性合成。
*   **核心公式**：解 $u(x, t)$ 被表示为：
    $$u = K_{\theta}(\{f_1(x), \dots, f_m(x)\}, \{g_1(t), \dots, g_n(t)\})$$
    其中，$f_i(x)$ 和 $g_j(t)$ 是预定义的**基函数**（如三角函数、高斯函数），而 $K_{\theta}$ 是一个参数化的**合成网络**（通常是浅层 MLP）。
*   **关键技术细节**：
    *   **基函数参数化**：基函数内部包含可学习参数（如三角函数的频率 $\omega$、振幅 $a$；高斯函数的均值 $\mu$ 和方差 $\sigma$），这些参数随网络一同优化。
    *   **合成算子**：利用神经网络的非线性激活函数（如 tanh）实现基函数之间的复杂耦合，超越了传统级数展开的线性叠加。
    *   **物理约束**：沿用了 PINN 的损失函数框架，包括 PDE 残差损失、边界条件（BC）损失和初始条件（IC）损失。

### 3. 实验设计
作者在三个经典的 PDE 场景上验证了 GEN 的性能：
*   **实验场景**：
    1.  **热传导方程（Heat Equation）**：测试平滑扩散过程及时间外推能力。
    2.  **波动方程（Wave Equation）**：利用特征线理论设计基函数，测试周期性保持和波形传播。
    3.  **Burgers 方程**：测试非线性对流扩散问题及不同基函数数量对精度的影响。
*   **Benchmark（基准）**：解析解（Analytical Solution）和高精度数值解（Numerical Solution）。
*   **对比方法**：标准物理信息神经网络（PINN）。

### 4. 资源与算力
*   **硬件环境**：单张 **Nvidia RTX 4090 GPU**。
*   **训练细节**：
    *   迭代次数：100,000 次（10万次迭代）。
    *   优化器：Adam 优化器（学习率 1e-3）。
    *   网络结构：合成网络包含隐藏层（20个神经元），使用 tanh 激活函数。

### 5. 实验数量与充分性
*   **实验规模**：论文针对三个代表性方程进行了深入对比。
    *   在热传导方程中对比了 Sine 映射和 Gauss 映射两种基函数。
    *   在波动方程中展示了在训练域外（$t > 2.0$）的长效预测表现。
    *   在 Burgers 方程中进行了**消融研究**，对比了 25 个基函数与 100 个基函数的精度差异。
*   **充分性评价**：实验设计较为客观，特别强调了 PINN 容易被忽视的“外推失败”问题。虽然测试的方程数量不算多，但涵盖了抛物型、双曲型和非线性方程，具有较强的代表性。

### 6. 主要结论与发现
*   **外推能力显著增强**：在热传导和波动方程中，当时间 $t$ 超出训练范围时，PINN 的误差迅速发散，而 GEN（尤其是 SineGEN）能够完美保持物理特性，实现长效预测。
*   **先验知识的重要性**：基函数的选择直接决定了求解质量。例如，利用波动方程的特征线原理设计的基函数，其表现远优于通用型网络。
*   **精度与复杂度的权衡**：增加基函数数量（如从 25 增至 100）可以显著提升对非线性细节（如 Burgers 方程中的激波前缘）的捕捉精度。

### 7. 优点（亮点）
*   **架构创新**：打破了 PINN 的黑盒模式，将传统的级数展开思想与现代深度学习结合，实现了“显式”求解。
*   **高鲁棒性**：通过基函数的全局解析特性，天然地引入了空间连续性和时间稳定性。
*   **可解释性**：学习到的基函数可以进行可视化分析（如频率分布），有助于理解物理过程。

### 8. 不足与局限
*   **基函数依赖性**：GEN 的成功高度依赖于人工选择合适的基函数。如果缺乏对 PDE 物理特性的预先了解，选错基函数可能导致结果不如 PINN。
*   **收敛速度慢**：文中提到 GEN 需要 10 万次迭代，计算开销显著高于标准 PINN，存在训练效率瓶颈。
*   **参数调优**：基函数的数量（基数）目前需要手动设定，缺乏自动调整基函数规模的智能机制。
*   **应用范围**：目前仅在 1D/2D 简单几何域上验证，对于复杂边界或极高维问题的适用性尚待证明。

（完）
