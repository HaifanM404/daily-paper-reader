---
title: Task Scarcity and Label Leakage in Relational Transfer Learning
title_zh: 关系迁移学习中的任务稀缺性与标签泄漏
authors: "Francisco Galuppo Azevedo, Clarissa Lima Loures, Denis Oliveira Correa"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29914v1"
tags: ["query:ddi"]
score: 6.0
evidence: 数据库中预测目标的关联迁移学习
tldr: 本研究探讨了关系型基础模型在迁移学习中的“任务稀缺”和“标签泄漏”问题。由于数据库中预测目标有限，模型易学习任务特定的捷径，导致迁移性能下降。作者提出了K-Space架构，结合预训练表格编码器与轻量级消息传递核心，并引入梯度投影方法以消除表示更新中的标签预测方向。实验表明，该方法在RelBench上显著提升了迁移效果，证明了任务多样性是构建关系型基础模型的关键约束。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29914v1/fig-001.webp\", \"caption\": \"Figure 1: Schematic of the proposed architecture. A TabICL encoder (column embedder and row interactor) encodes table features; its output is concatenated with RWPE and time features and passed through an input projection and per-table-type RoPE into a stack of N Hetero (SMPNN) blocks with reversible (REV) and forward (FWD) paths. Zoomed views show the SMPNN block and its GCN and pointwise feedforward sub-blocks. An output projection feeds two heads: an adversarial MLP head and a TabICL predictor head.\", \"page\": 3, \"index\": 1, \"width\": 684, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2603.29914v1/fig-002.webp\", \"caption\": \"Figure 2: Geometry of per-sample gradients in the representation space of h, the shared representation just before the two heads. For each sample xi, the light green arrow is the in-context learning gradient ∇hℓICL(xi), the red arrow is the adversary gradient ∇hℓadv(xi), and the dark green arrow is the refined gradient ∇̃hℓICL(xi) after subtracting its projection onto ∇hℓadv(xi) (dashed gray segment).\", \"page\": 4, \"index\": 2, \"width\": 681, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2603.29914v1/fig-003.webp\", \"caption\": \"Table 1: RelBench AUROC. ST, WD, CD, ALL are the supervision regimes. Within each regime, the left column is the base model, and the right column (Adv.) uses the adversarial head with sample-wise gradient projection. RelGT is the baseline.\", \"page\": 5, \"index\": 3, \"width\": 829, \"height\": 511}]"
motivation: 关系型数据库中可用的监督任务有限，导致模型在学习时容易产生标签泄漏，从而损害跨任务的迁移能力。
method: 提出K-Space模块化架构，并结合一种梯度投影技术，通过移除表示更新中与标签相关的方向来抑制泄漏。
result: 在RelBench基准测试中，该方法使数据集内迁移的平均AUROC提升了0.145，接近单任务训练的性能。
conclusion: 研究表明，限制关系型基础模型发展的不仅是数据量，任务多样性的匮乏也是核心瓶颈。
---

## 摘要
训练关系基础模型需要学习能够跨任务迁移的表示，然而可用的监督信号通常局限于每个数据库中少量的预测目标。这种任务稀缺性导致学习到的表示编码了特定于任务的捷径，从而降低了即使在同一模式（schema）内的迁移性能，我们将这一问题称为标签泄漏。我们利用 K-Space 研究了这一现象，这是一种将冻结的预训练表格编码器与轻量级消息传递核心相结合的模块化架构。为了抑制泄漏，我们引入了一种梯度投影方法，从表示更新中移除具有标签预测性的方向。在 RelBench 基准测试中，该方法将数据集内迁移的平均 AUROC 提升了 0.145，通常能恢复到接近单任务训练的性能。我们的结果表明，限制关系基础模型发展的不仅是有限的数据，还有有限的任务多样性。

## Abstract
Training relational foundation models requires learning representations that transfer across tasks, yet available supervision is typically limited to a small number of prediction targets per database. This task scarcity causes learned representations to encode task-specific shortcuts that degrade transfer even within the same schema, a problem we call label leakage. We study this using K-Space, a modular architecture combining frozen pretrained tabular encoders with a lightweight message-passing core. To suppress leakage, we introduce a gradient projection method that removes label-predictive directions from representation updates. On RelBench, this improves within-dataset transfer by +0.145 AUROC on average, often recovering near single-task performance. Our results suggest that limited task diversity, not just limited data, constrains relational foundation models.