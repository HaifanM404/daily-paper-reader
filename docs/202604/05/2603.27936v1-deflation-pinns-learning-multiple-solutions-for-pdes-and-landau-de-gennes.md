---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: Deflation-PINNs：学习偏微分方程与 Landau-de Gennes 模型的多解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于学习PDE多解的Deflation-PINNs
tldr: 针对物理信息神经网络（PINNs）难以求解非线性偏微分方程（PDEs）多解的问题，本文提出Deflation-PINNs框架。该方法结合了紧缩（deflation）损失函数与DeepONets架构，通过在损失函数中引入紧缩项，强制模型系统性地寻找并收敛到不同的解分支。在具有复杂能量景观的Landau-de Gennes液晶模型上的实验证明，该方法能有效识别并表征多种不同的晶体结构，并具有理论收敛性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统的PINNs设计初衷是寻找单一解，难以处理具有多个不同解分支的非线性PDE问题。
method: 提出Deflation-PINNs，通过在PINNs和DeepONets架构中引入紧缩损失项，系统性地排斥已发现的解以寻找新解。
result: 在Landau-de Gennes模型中成功识别并表征了多种不同的液晶平衡态结构。
conclusion: Deflation-PINNs为求解具有多解特性的复杂物理系统提供了一种有效且具有理论支撑的新工具。
---

## 摘要
非线性偏微分方程（PDEs）在数学物理和工程领域无处不在。尽管物理信息神经网络（PINNs）已成为求解 PDE 问题的强大工具，但由于其设计初衷是单次寻找单个解，因此通常难以识别多个不同的解。为了解决这一局限性，我们引入了 Deflation-PINNs，这是一种将紧缩损失（deflation loss）与基于 PINNs 和深度算子网络（DeepONets）的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法能够系统地强制 Deflation-PINN 寻找并收敛至多个不同的有限解分支。我们提供了模型收敛性的理论证明，并通过对液晶 Landau-de Gennes 模型的数值实验验证了 Deflation-PINNs 的有效性，该系统以其复杂的能量景观和多种平衡态而著称。实验结果表明，Deflation-PINNs 能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》的结构化深入分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：非线性偏微分方程（PDEs）在物理和工程中经常存在多个不同的解（多解性），例如液晶的多种平衡态。
*   **研究动机**：传统的物理信息神经网络（PINNs）设计目标是寻找单一解，通常只能收敛到损失函数景观中的一个局部最小值。虽然可以通过多次随机初始化来尝试寻找不同解，但这种方法缺乏系统性且存在大量冗余。本文旨在开发一种能够一次性、系统性地发现并表征所有（有限个）解分支的新框架。

### 2. 方法论：核心思想与技术细节
Deflation-PINNs 结合了算子学习架构与一种特殊的“排斥”损失机制：
*   **简化架构（Modified DeepONet）**：
    *   借鉴了 DeepONet 的思想，但将其简化以适应有限个解。
    *   模型由一个 **Trunk Net**（处理坐标输入 $x$）和一组可训练的 **Branch Weights**（分支权重 $\beta_k$）组成。
    *   每个解 $u_k(x)$ 被表示为 Trunk Net 输出与对应分支权重的内积。这种设计比标准 DeepONet 更轻量，且能直接编码解的结构。
*   **紧缩损失（Deflation Loss）**：
    *   这是该方法的核心。在总损失中引入一个惩罚项，计算不同解分支之间的 $L^2$ 距离。
    *   公式逻辑：如果两个分支之间的距离小于预设的超参数 $d_{min}$，则产生惩罚。这强制模型在优化过程中将不同的解“推开”，从而收敛到不同的物理分支。
*   **硬约束边界条件**：
    *   引入了基于**径向边界距离函数（RBD function）**的构造方法，确保神经网络输出在数学结构上严格满足 Dirichlet 边界条件，从而简化了损失函数的优化难度。
*   **理论支撑**：
    *   论文提供了关于硬约束模型近似能力的通用近似定理证明，确保了该架构在理论上可以逼近任何连续函数解。

### 3. 实验设计与 Benchmark
*   **实验场景**：二维 **Landau-de Gennes (LdG)** 液晶模型。这是一个极具挑战性的基准，因为该模型具有复杂的能量景观，在特定边界条件下存在 6 个稳定的平衡态（2 个对角态 D1/D2，4 个旋转态 R1-R4）。
*   **Benchmark（基准）**：对比了传统的**有限元方法（FEM）**计算出的参考解。
*   **对比逻辑**：验证 Deflation-PINNs 是否能同时识别出 FEM 所能发现的所有 6 种稳定结构，并对比矢量场分布的准确性。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **NVIDIA RTX 3060-Ti GPU (8GB VRAM)** 的设备上运行。
*   **训练细节**：
    *   使用 Adam 优化器。
    *   训练 10,000 个 Epoch。
    *   Trunk Net 宽度为 4000，包含 1 个隐藏层，激活函数为 tanh。
    *   配置点（Collocation points）采用 33×33 的网格。

### 5. 实验数量与充分性
*   **实验规模**：论文重点针对 LdG 模型这一典型多解问题进行了深入实验。
*   **充分性评价**：
    *   **覆盖面**：成功找齐了理论上的全部 6 个解分支，证明了方法的有效性。
    *   **客观性**：通过与成熟的 FEM 结果进行可视化对比（矢量场图），直观展示了神经网络解的物理正确性。
    *   **局限性**：实验主要集中在 LdG 这一特定物理模型上，尚未在更多种类的非线性 PDE（如多解的流体力学问题）上进行大规模消融实验或泛化性测试。

### 6. 主要结论与发现
*   **多解发现能力**：Deflation-PINNs 能够通过单一训练过程同时捕获多个不同的 PDE 解分支，无需多次手动重启或依赖随机初始化的运气。
*   **精度与效率**：该方法生成的解可以作为传统数值解法（如 Newton 法）的高质量初始值，显著提升寻找高精度物理特解的效率。
*   **硬约束优势**：通过 RBD 函数引入硬约束，避免了在损失函数中平衡边界条件权重的繁琐调参。

### 7. 优点与亮点
*   **系统性强**：相比于随机初始化的“撞大运”方式，Deflation Loss 提供了一种确定性的机制来探索解空间。
*   **架构优雅**：将 DeepONet 简化为分支权重形式，既保留了算子学习的表达能力，又极大地减少了参数量。
*   **理论完备**：不仅有数值实验，还提供了关于硬约束架构的收敛性和通用近似性的数学证明。

### 8. 不足与局限
*   **超参数敏感性**：$d_{min}$（最小解距离）是一个关键超参数。如果设置过大，模型可能无法收敛；设置过小，则可能无法有效区分不同解。目前该参数仍需根据经验或先验知识调整。
*   **解的数量预设**：模型需要预先设定解的数量 $K$。在实际应用中，如果 PDE 的解分支数量未知，可能需要多次尝试不同的 $K$ 值。
*   **计算开销**：随着解数量 $K$ 的增加，计算所有分支两两之间距离的 Deflation Loss 的计算复杂度会呈平方级增长。

（完）
