---
title: "PINNs in PDE Constrained Optimal Control Problems: Direct vs Indirect Methods"
title_zh: 偏微分方程约束最优控制问题中的物理信息神经网络：直接法与间接法
authors: "Zhen Zhang, Shanqing Liu, Alessandro Alla, Jerome Darbon, George Em Karniadakis"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04920v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于偏微分方程最优控制的物理信息神经网络
tldr: 本研究探讨了物理信息神经网络（PINNs）在半线性偏微分方程约束最优控制问题中的应用，对比了基于目标函数最小化的直接法和基于一阶最优性系统的间接法。通过Allen-Cahn控制问题的数值实验，研究发现PINN参数化具有隐式正则化效应，且间接PINN在保持物理约束和最优性结构方面表现更优，提供了比直接PINN更精确的数值近似。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04920v1/fig-001.webp\", \"caption\": \"Fig. 2: Control profiles, shown from top to bottom: adjoint starting from scratch, direct PINN, indirect PINN, and adjoint initialized from the direct PINN solution, which is treated as the reference value for this optimal control problem. The relative L2 errors of control are shown in titles. Note that according to the optimality condition, the terminal solution is y(T ) = −βQu(T ), βQ = 1× 10−3.\", \"page\": 6, \"index\": 1, \"width\": 362, \"height\": 1143}, {\"url\": \"assets/figures/arxiv/2604.04920v1/fig-002.webp\", \"caption\": \"Fig. 1: Loss histories for the three approaches: adjoint optimization starting from scratch (top), direct PINN (middle), and indirect PINN (bottom). In the adjoint panel, the legend shows the total objective J , the terminal tracking term JT , the control regularization term Ju, and the relative L2 error of the control, relL2(u). In the direct PINN panel, the legend additionally separates the PDE, boundary-condition (BC), and initial-condition (IC) residual losses, together with JT , Ju, and relL2(u). In the indirect PINN panel, the legend further includes the residual losses associated with the adjoint equation (PDE λ), adjoint boundary condition (BC λ), and terminal optimality condition (T (λ)), in addition to the state PDE/BC/IC residuals, the total loss, and relL2(u). For the PINN cases, only the SSBroyden phase is shown after the short Adam warm-up, and the total wall-clock time of each method is indicated in the panel title.\", \"page\": 6, \"index\": 2, \"width\": 362, \"height\": 854}, {\"url\": \"assets/figures/arxiv/2604.04920v1/fig-003.webp\", \"caption\": \"Fig. 3: State trajectories corresponding to the converged controls, shown from top to bottom: adjoint starting from scratch, direct PINN, indirect PINN, and adjoint initialized from the direct PINN solution. For the PINN-based methods, each panel also compares the state predicted directly by the neural network with the state recomputed by the numerical solver using the converged PINN control. The relative L2 error of each time snapshot is marked in the legend.\", \"page\": 7, \"index\": 3, \"width\": 362, \"height\": 1070}]"
motivation: 旨在评估并对比PINNs在求解受半线性偏微分方程约束的最优控制问题时，直接法与间接法两种不同架构的性能差异。
method: 提出了直接PINN和间接PINN两种公式，并将其与传统的先离散后优化伴随法在Allen-Cahn方程控制问题上进行对比实验。
result: 实验表明PINN具有产生平滑控制曲线的隐式正则化作用，且间接PINN在满足PDE约束和最优性条件方面比直接PINN更准确。
conclusion: 间接PINN是处理PDE约束最优控制问题更有效的方法，能够更忠实地保留数学最优性结构并提高神经近似的精度。
---

## 摘要
我们研究了物理信息神经网络（PINNs）作为半线性偏微分方程（PDE）最优控制数值工具的应用。我们首先回顾了 PDE 最优控制中经典的直接和间接观点，随后提出了两种 PINN 建模方式：一种是基于在状态约束下最小化目标函数的直接法，另一种是基于一阶最优性系统的间接法。针对一类半线性抛物方程，我们推导了状态方程、伴随方程和驻定条件，其形式与连续时间庞特里亚金型（Pontryagin-type）最优性条件保持一致。我们将该框架应用于 Allen-Cahn 控制问题，并对比了三种数值方法：(i) 先离散后优化的伴随法，(ii) 直接 PINN，以及 (iii) 间接 PINN。数值结果表明，PINN 参数化具有隐式正则化效应，倾向于产生更平滑的控制剖面。此外，结果还显示，与直接 PINN 相比，间接 PINN 能更忠实地保持 PDE 约束和最优性结构，并提供更精确的神经网络近似。

## Abstract
We study physics-informed neural networks (PINNs) as numerical tools for the optimal control of semilinear partial differential equations. We first recall the classical direct and indirect viewpoints for optimal control of PDEs, and then present two PINN formulations: a direct formulation based on minimizing the objective under the state constraint, and an indirect formulation based on the first-order optimality system. For a class of semilinear parabolic equations, we derive the state equation, the adjoint equation, and the stationarity condition in a form consistent with continuous-time Pontryagin-type optimality conditions. We then specialize the framework to an Allen-Cahn control problem and compare three numerical approaches: (i) a discretize-then-optimize adjoint method, (ii) a direct PINN, and (iii) an indirect PINN. Numerical results show that the PINN parameterization has an implicit regularizing effect, in the sense that it tends to produce smoother control profiles. They also indicate that the indirect PINN more faithfully preserves the PDE contraint and optimality structure and yields a more accurate neural approximation than the direct PINN.

---

## 论文详细总结（自动生成）

这是一份关于论文《PINNs in PDE Constrained Optimal Control Problems: Direct vs Indirect Methods》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：探讨物理信息神经网络（PINNs）在解决受半线性偏微分方程（PDE）约束的最优控制问题时的有效性，并重点对比“直接法”与“间接法”两种架构的性能差异。
*   **研究背景**：PDE约束的最优控制问题在流体力学、热传导等领域至关重要，但传统数值方法面临计算复杂、非线性强、时空离散规模大等挑战。PINNs作为一种无网格、参数化的方法，为这类问题提供了新的解决思路，但目前缺乏关于直接最小化目标函数（直接法）与求解一阶最优性系统（间接法）在PINN框架下的系统性对比。

### 2. 论文提出的方法论
论文针对半线性抛物型方程的最优控制问题，提出了两种PINN构建方式：
*   **直接 PINN (Direct PINN)**：
    *   **核心思想**：将控制问题视为受约束的优化问题。
    *   **技术细节**：使用两个神经网络分别参数化状态变量 $y$ 和控制变量 $u$。损失函数由 PDE 残差、初始/边界条件残差以及控制问题的目标函数（Cost Functional）加权组成。
*   **间接 PINN (Indirect PINN)**：
    *   **核心思想**：基于庞特里亚金极大值原理（KKT条件），求解一阶必要最优性系统。
    *   **技术细节**：除了状态网络和控制网络，还引入（或通过构造满足）伴随变量（Adjoint Variable）$\lambda$。损失函数强制执行状态方程、伴随方程、边界/初始/终端条件以及驻定条件（Stationarity Condition）。
*   **关键技术优化**：
    *   使用 **SSBroyden**（一种二阶拟牛顿优化器）进行精细训练。
    *   采用 **float64（双精度）** 运算以确保在小残差阶段的收敛稳定性。

### 3. 实验设计
*   **实验场景**：一维 **Allen-Cahn 方程** 的分布式最优控制问题。该方程具有强非线性，常用于描述相场动力学。
*   **Benchmark（基准）**：传统的“先离散后优化”伴随法（Discretize-then-optimize Adjoint Method），使用高精度 RK4 时间步进和中心差分离散。
*   **对比方法**：
    1.  从零开始训练的传统伴随法。
    2.  直接 PINN。
    3.  间接 PINN。
    4.  以直接 PINN 的结果作为初始值的伴随法（混合法）。

### 4. 资源与算力
*   **算力说明**：论文展示了各方法的“墙钟时间”（Wall-clock time）。
    *   伴随法约耗时 104 秒。
    *   直接 PINN 约耗时 214 秒。
    *   间接 PINN 约耗时 143 秒。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号或 CPU 配置，但强调了双精度浮点数对收敛至关重要。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 Allen-Cahn 这一典型非线性案例进行了深入的消融对比。
*   **充分性评价**：
    *   **客观性**：通过将 PINN 预测的控制量重新代入高精度数值求解器（RK4）来验证状态轨迹，避免了神经网络自验证的偏差。
    *   **公平性**：所有方法在相同的时空网格和评估基准下进行对比。
    *   **局限性**：实验仅限于一维空间，尚未扩展到更高维度或具有复杂几何边界的场景。

### 6. 论文的主要结论与发现
*   **间接法优于直接法**：间接 PINN 比直接 PINN 更能准确地捕捉最优控制剖面，因为它直接强化了最优性系统的数学结构，而直接法在目标函数项和 PDE 残差项之间存在权重平衡难题。
*   **隐式正则化效应**：PINN 的神经网络参数化具有天然的平滑作用，能够产生比传统伴随法（从零开始时）更平滑、物理上更合理的控制曲线。
*   **优化器与精度的重要性**：对于控制问题，仅靠 Adam 优化器是不够的，必须结合二阶优化器（如 SSBroyden）和双精度计算才能达到定量准确的解。
*   **PINN 作为初始化工具**：直接 PINN 虽然精度稍逊，但非常适合作为传统伴随法的“热启动”工具，能显著改善后者的收敛质量。

### 7. 优点
*   **理论与实践结合**：将经典的控制理论（庞特里亚金原理）与现代深度学习框架深度融合。
*   **验证严谨**：不依赖神经网络自身的预测值，而是通过外部数值求解器重构状态，确保了结果的可信度。
*   **实用建议**：为从业者提供了明确的训练指南（二阶优化器 + 双精度 + 间接法优先）。

### 8. 不足与局限
*   **维度限制**：目前仅验证了一维简单域问题，高维问题的扩展性（如训练时间是否指数增长）尚待观察。
*   **超参数敏感性**：尽管间接法表现更好，但损失函数中多项残差的权重平衡（Loss Weighting）依然是一个需要经验调优的挑战。
*   **应用范围**：主要针对无约束控制问题，对于带有控制约束（如 $u_{min} \le u \le u_{max}$）或状态约束的情况讨论较少。

（完）
