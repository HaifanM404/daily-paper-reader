---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩PINN：学习偏微分方程与Landau-de Gennes模型的多重解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v2"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于学习PDE多解的Deflation-PINNs
tldr: 非线性偏微分方程在物理和工程中普遍存在，但传统物理信息神经网络（PINNs）一次只能找到一个解。为解决这一限制，本研究提出了紧缩-PINNs（Deflation-PINNs）新框架，它将紧缩损失项与基于PINNs和深度算子网络的架构相结合。通过在损失函数中加入紧缩项，该方法能系统地迫使模型寻找并收敛于有限个不同的解分支。研究提供了理论收敛证据，并以具有复杂能量景观和多平衡态的液晶Landau-de Gennes模型为例进行数值实验，证明了该方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v2/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统PINNs难以识别非线性偏微分方程的多重解，需要新方法解决此限制。
method: 提出Deflation-PINNs框架，在损失函数中引入紧缩项，结合PINNs和DeepONets架构来寻找不同解分支。
result: 数值实验表明，该方法能有效找到液晶Landau-de Gennes模型中多个不同的平衡态解。
conclusion: 该方法成功识别并表征了多个不同的晶体结构，为解决复杂PDE多解问题提供了新工具。
---

## 摘要
非线性偏微分方程在数学物理与工程领域无处不在。尽管物理信息神经网络已成为求解偏微分方程问题的有力工具，但由于其设计为一次仅寻找一个解，通常难以识别多个不同的解。为解决这一局限，我们提出了Deflation-PINNs——一种将紧缩损失与基于PINNs和深度算子网络的架构相结合的新颖框架。通过在损失函数中加入紧缩项，我们的方法能系统地迫使Deflation-PINN寻找并收敛于有限多个不同的解分支。我们提供了模型收敛的理论依据，并通过在液晶Landau-de Gennes模型上的数值实验证明了Deflation-PINNs的有效性；该系统以其复杂的能量景观和多重平衡态而闻名。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》进行结构化、深入且客观的总结。

---

### **1. 核心问题与整体含义**
*   **研究动机**：非线性偏微分方程（PDE）通常存在多个解（如多稳态系统），这些解对应不同的物理状态。传统的物理信息神经网络（PINNs）在训练时倾向于收敛到单一解，难以系统地发现和区分多个解。现有方法（如多次随机初始化训练多个PINN）存在冗余和资源浪费的问题。
*   **研究目标**：提出一个能够**在一次训练中同时、系统地发现并逼近PDE的有限个不同解**的神经网络框架。

### **2. 方法论：Deflation-PINNs**
*   **核心思想**：将“紧缩”（Deflation）思想引入PINN框架，通过修改损失函数和网络架构，迫使模型同时学习多个彼此“远离”的解。
*   **关键技术细节**：
    1.  **架构设计（基于DeepONet简化）**：
        *   摒弃了DeepONet中用于处理输入函数的“分支网络”（branch net），改为直接学习K个固定的“分支权重”向量 `β^k ∈ R^p`，每个向量对应一个待求的解。
        *   保留用于处理空间坐标的“主干网络”（trunk net）`τ(x)`。
        *   最终输出为K个函数：`ũ_k(x) = Σ_i τ_i(x) * β^k_i`。该架构是通用逼近器的一个特例。
    2.  **硬约束边界条件**：
        *   采用 `u_θ(x) = ω(x) * NN_θ(x) + b(x)` 的形式构造网络，其中 `ω(x)` 在边界上为零，`b(x)`精确满足边界条件。这消除了边界损失项，简化了优化问题。
        *   论文提出了一种基于径向边界距离函数（RBD）的方法来构造`b(x)`的连续延拓。
    3.  **紧缩损失函数**：
        *   核心创新。定义了一个损失项 `L_Def`，旨在惩罚模型学到的不同解之间的相似性。
        *   公式：`L_Def = (2/(K(K-1))) Σ_{i<j} max(1 - (||ũ_i - ũ_j||_{L2} / d_min), 0)`。
        *   当任意两个近似解的L2距离小于预设阈值 `d_min` 时，该损失项为正，驱动优化过程使它们分离；当所有解对距离都大于 `d_min` 时，该项为零。
    4.  **总损失函数**：
        *   `L_total = α * Σ_k E_G(ũ_k) + β * L_Def`。
        *   其中 `E_G` 是标准的PINN残差损失（仅包含PDE残差，因边界条件已硬约束）。α和β为超参数。

### **3. 实验设计**
*   **基准场景**：二维简化Landau-de Gennes（LdG）液晶模型。这是一个著名的具有多个稳定平衡态（解）的系统。在正方形域上已知存在6个稳定解：2个对角态（D1, D2）和4个旋转态（R1-R4）。
*   **对比基准**：有限元方法（FEM）。将Deflation-PINNs找到的解与文献中通过FEM计算得到的已知精确解进行定性（矢量场图）和定量比较。
*   **对比方法**：论文主要与以下方法进行概念性对比：
    *   **HomPINNs**: 半监督方法，需要观测数据。
    *   **多次随机初始化的PINNs**: 效率低且无法保证解的多样性。

### **4. 资源与算力**
*   论文明确说明实验在一张RTX 3060-Ti GPU (8GB VRAM)上进行。
*   训练进行了10,000个epoch。

### **5. 实验数量与充分性**
*   **主要实验数量有限但聚焦**：论文在一个核心的、公认具有挑战性的多解PDE问题（LdG模型）上进行了验证。成功复现了全部6个已知稳定态。
*   **消融实验与分析不足**：
    1.  未系统研究超参数（如紧缩距离 `d_min`, α/β权重比、网络深度/宽度p）对性能的影响及其选择依据。文中提到 `d_min=0.4`是通过经验测试找到的。
    2.  未与其他旨在寻找多解的机器学习方法进行直接的、定量的性能比较（如收敛速度、精度、计算成本）。
    3.  未在更广泛或更复杂的PDE问题上测试方法的普适性。
*   **客观性与公平性**：
    *   与FEM结果的对比是客观的，展示了该方法能找到物理上正确的解结构。
    *   对自身方法的局限性有提及。

### **6. 主要结论与发现**
*   Deflation-PINNs框架能够成功地一次性学习到LdG模型的全部6个稳定解分支。
*   学到的解的矢量场结构与FEM计算结果高度一致，验证了方法的有效性。
*   理论分析表明，所提出的简化架构具有通用逼近性质。

### **7. 优点**
1.  **创新性思路明确**: “紧缩”思想的引入为解决PINNs的多解发现问题提供了一个清晰且有效的新范式。
2.  **端到端无监督**:无需任何先验数据或观测值即可发现所有可能的多稳态结构.
3.  **(相对)高效**:一次训练即可获得多个解,避免了重复训练多个独立网络的巨大开销.
4.  **(相对)系统化**:通过预设的距离阈值,能更可靠地确保找到的解彼此不同,减少了随机初始化方法的冗余风险.
5.**理论支撑**:提供了关于架构通用逼近能力和硬约束模型的理论证明.

###8.**不足与局限**
1.**应用范围限制**:目前仅在单个特定问题上得到验证其普适性有待在其他具有多解的PDE上进行检验.
2.**超参数敏感性未充分探索**:紧缩距离 d_min是一个关键但需要手动调整的超参数对于未知问题如何合理设置缺乏指导.
3.**计算成本仍存疑**:虽然比训练多个独立网络高效但单次训练的复杂度高于标准PINN因为要同时优化K组分支权重并计算O(K^2)对的紧缩损失对于非常大的K可能成为瓶颈.
4.**对初始化的依赖**:尽管引入了系统性约束但神经网络的非凸优化本质意味着最终收敛到的具体K个解集可能仍受初始化影响未能完全证明其总能找到全局最优的"最分散"的解集.
5.**仅限于Dirichlet边界条件**:文中采用的硬约束方法主要针对Dirichlet边界条件对于其他类型的边界条件需要重新设计.

(完)

好的，我将从您提供的中断处继续，对论文的不足与局限、潜在影响与未来方向进行补充总结。

---

### **8. 不足与局限（续）**
6.  **解的数量K需预先指定**：该方法要求用户预先设定希望寻找的解的数量K。对于未知其确切解数量的PDE问题，这构成了一个先验知识要求。虽然可以通过设置较大的K并观察结果来探索，但缺乏自动确定最优K的机制。
7.  **对“距离”定义的依赖**：紧缩损失的核心是解之间的L2距离。对于某些问题，不同的解可能在L2范数下非常接近（例如相位不同的波），但在其他度量（如能量、拓扑不变量）下差异显著。单一的L2距离可能不足以有效区分所有类型的多解。
8.  **缺乏收敛性保证**：论文提供了架构的表达能力证明，但未对优化过程的收敛性（即总能找到K个满足PDE且彼此远离的解）提供理论保证。这仍然是基于梯度下降的非凸优化中的一个普遍挑战。

### **9. 潜在影响与未来方向**
1.  **多稳态物理系统的探索**：该方法为计算物理和材料科学中研究多稳态、亚稳态和相变问题提供了一个强大的无监督计算工具，可用于自动探索复杂能量景观。
2.  **与其他机器学习范式结合**：未来工作可将紧缩思想与更先进的神经网络架构（如傅里叶特征网络、自适应激活函数）或优化策略结合，以提升精度和效率。
3.  **自适应机制开发**：
    *   **自适应K**：研究如何动态调整K值，例如在训练过程中合并过于相似的解分支或分裂未能充分满足PDE的分支。
    *   **自适应d_min**：开发基于解分布或残差动态调整距离阈值d_min的策略。
4.  **扩展到更复杂的问题**：
    *   **时变PDEs**：将框架扩展到时间依赖的PDEs，以发现动力系统中的多个吸引子或周期轨道。
    *   **参数化PDEs**：研究在参数空间（如不同边界条件、系数）中连续追踪多个解分支的能力。
    *   **其他边界条件**：推广硬约束方法以处理Neumann、Robin等混合边界条件。
5.  **与不确定性量化结合**：将多解发现框架与贝叶斯神经网络或集成学习结合，不仅可以找到多个点估计解，还能评估每个解的不确定性。

### **10. 总体评价**
本文提出的Deflation-PINNs是一个构思巧妙、具有显著实用价值的方法学贡献。它精准地抓住了传统PINNs在多解发现问题上的核心痛点，并通过引入“紧缩”这一来自计算数学的经典思想，提供了一个简洁而有效的解决方案。尽管其在单一基准问题上进行了验证，且存在对超参数和预设解数量的依赖等局限性，但其核心范式——通过修改损失函数来系统性驱散解——为PINNs社区开辟了一条富有前景的新研究方向。论文的实验结果扎实，理论分析到位，清晰地展示了该方法的有效性和潜力。未来的研究有望在此基础上进行扩展和完善，使其成为一个更鲁棒、更通用的多解PDE发现工具。

（完）
