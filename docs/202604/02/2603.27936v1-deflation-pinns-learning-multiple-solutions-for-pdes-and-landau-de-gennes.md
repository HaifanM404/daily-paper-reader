---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩-物理信息神经网络：学习偏微分方程与Landau-de Gennes模型的多重解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于学习PDE多解的Deflation-PINNs
tldr: 非线性偏微分方程在物理和工程中普遍存在，但传统物理信息神经网络一次只能找到一个解。针对此局限，本研究提出了紧缩PINNs新框架，它将紧缩损失项与基于PINN和深度算子网络的架构相结合，通过修改损失函数来系统性地寻找并收敛于有限个不同的解分支。论文提供了理论收敛性证明，并以具有复杂能量景观和多平衡态的液晶Landau-de Gennes模型为例进行数值验证，成功识别出多种不同的晶体结构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统PINNs难以发现PDE的多个不同解，而许多物理系统（如液晶模型）恰恰存在多个平衡态。
method: 提出Deflation-PINNs框架，通过在损失函数中引入紧缩项，并结合PINNs与DeepONets架构来系统性地寻找不同解分支。
result: 数值实验表明，该方法能成功识别并表征液晶Landau-de Gennes模型中多个不同的晶体结构。
conclusion: 该框架为求解具有多解特性的复杂PDE系统提供了一种有效的新途径。
---

## 摘要
非线性偏微分方程在数学物理和工程领域无处不在。尽管物理信息神经网络已成为解决偏微分方程问题的有力工具，但由于其设计为每次仅寻找一个解，通常难以识别多个不同的解。为应对这一局限，我们提出了Deflation-PINNs——一个将紧缩损失与基于PINNs和深度算子网络架构相结合的新颖框架。通过在损失函数中加入紧缩项，我们的方法系统地迫使Deflation-PINN寻找并收敛于有限多个不同的解分支。我们提供了模型收敛的理论依据，并通过液晶的Landau-de Gennes模型（一个以其复杂能量景观和多重平衡态而闻名的系统）的数值实验证明了Deflation-PINNs的有效性。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

# Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes 论文总结

## 1. 核心问题与整体含义
*   **研究动机**：许多非线性偏微分方程（PDE）存在多个解，这些解对应着物理系统中的不同稳定状态（如液晶的不同分子排列构型）。传统的物理信息神经网络（PINNs）旨在寻找单个解，难以系统性地发现和区分多个不同的解分支。
*   **研究背景**：在物理信息机器学习领域，现有方法如随机初始化多个PINN或使用同伦PINN等方法，要么效率低下、存在冗余，要么依赖于观测数据。本文旨在开发一种能够同时、系统地学习PDE有限个不同解的完全无监督框架。

## 2. 方法论
*   **核心思想**：结合PINNs的物理约束和DeepONets的算子学习架构，并引入一个“紧缩损失”项，迫使模型同时学习多个彼此“远离”的解。
*   **关键技术细节**：
    1.  **架构设计**：对标准DeepONet架构进行简化。不再将输入函数编码为分支网络的输入，而是为每个待求解（共K个）分配一个可学习的特征向量（称为“分支权重” `β^k`）。模型输出为 `G̃(k, x) = Σ_i τ_i(x) * β^k_i`，其中 `τ` 是主干网络。这相当于一个能同时输出K个近似解的神经网络。
    2.  **紧缩损失**：为确保学到的K个解彼此不同，定义紧缩损失 `L_Def`。该损失惩罚那些在L²范数下距离小于预设阈值 `d_min` 的解对。具体形式为基于距离的铰链损失函数。
    3.  **硬约束边界条件**：采用径向边界距离函数等方法构造辅助函数 `ω(x)` 和 `b̃(x)`，将Dirichlet边界条件硬编码到网络架构中（`u_θ(x) = ω(x) * NN_θ(x) + b̃(x)`），从而简化损失函数（无需边界项），并避免超参数调优问题。
    4.  **总损失函数**：总损失是物理信息损失（PIML loss）和紧缩损失的加权和：`L_total = α * Σ_i E_G(G(i,·)) + β * L_Def(G)`。

## 3. 实验设计
*   **基准场景**：二维简化Landau-de Gennes模型。这是一个描述液晶的变分问题，其Euler-Lagrange方程是一个非线性PDE系统。在单位正方形区域上施加特定的梯形Dirichlet边界条件时，已知存在六个稳定的平衡解（两个对角态D1、D2和四个旋转态R1-R4）。
*   **对比方法/基准**：
    *   主要与使用有限元方法计算出的已知精确解进行定性比较（通过向量场图对比）。
    *   在相关工作中提及了其他方法作为背景对比：
        *   HomPINNs (Zheng et al., 2024)：半监督方法。
        *   随机初始化多个PINN (Zou et al., 2025)：可能产生冗余解。

## 4. 资源与算力
*   文中明确说明实验在一张RTX-3060 Ti GPU (8GB VRAM)上进行。
*   训练了10,000个epochs。
*   初始学习率为1e-3，使用Adam优化器。

## 5. 实验数量与充分性
*   **主要实验数量**：在一个核心的Landau-de Gennes问题上进行了验证。
*   **实验充分性与客观性评估**：
    *   **优点**：
        1.  成功复现了该问题所有六个已知的稳定解（D1, D2, R1-R4），并与FEM结果进行了直观的矢量场可视化对比（图2、图4），结果吻合良好。
        2.  提供了理论收敛性证明（定理1及推论），从逼近论角度支持了方法的可行性。
        3.  对关键超参数 `d_min` （解间最小距离）的选择过程进行了说明（通过经验测试调整）。
    *   **局限性/不足**：
        1.  实验场景较为单一。仅在二维正方形域上的一个特定LdG模型上进行了验证。缺乏在其他具有多解的PDE问题或更复杂几何域上的测试。
        2.  未与其他基于神经网络的寻多解方法进行定量的误差或效率比较。
        3.  虽然证明了理论收敛性，但数值实验部分未提供定量的误差指标来精确衡量解的精度。

## 6. 主要结论与发现
*   提出的Deflation-PINNs框架能够成功识别并表征Landau-de Gennes模型中所有六个已知的稳定液晶结构。
*   该方法提供了一种系统性的、不依赖于随机初始化的方式来寻找PDE的多个不同解。
*   通过将DeepONet架构简化为仅学习有限个解，并结合紧缩损失和硬边界约束，构建了一个高效且理论上可靠的求解框架。

## 7. 优点
1.  **创新性融合**：创造性地将PINNs、DeepONets以及数值分析中的“紧缩”思想相结合，解决了传统PINNs难以寻多解的痛点。
2.  **系统性寻优**：通过引入紧缩损失项，主动引导模型探索不同的解空间区域，避免了随机初始化方法的盲目性和冗余计算。
3.  **理论支撑**：不仅提出了新方法，还提供了相应的逼近定理证明其可行性。
4.  **无监督与高效架构**：完全无监督学习；简化的DeepONet架构参数更少（仅需K*p个分支权重参数），针对有限多解问题比通用DeepONet更高效。

##8 .不足与局限
1 .应用范围待验证 ：目前仅在单一且相对经典的液晶模型问题上得到验证 。方法的普适性有待在其他具有多解的复杂PDE系统上进行更多测试 。
2 .超参数依赖 ：紧缩效果依赖于超参数 d_min （最小距离阈值 ）的选择 ，文中提到该值通过经验测试获得 ，缺乏自动确定该值的理论指导或鲁棒策略 。
3 .计算成本 ：虽然比训练大量独立 PINN可能更高效 ，但单次训练仍需优化包含 K组分支权重的较大网络 ，且需计算所有解对之间的 L²距离 ，当 K较大时开销仍会增加 。
4 .局部极小风险 ：如同其他非凸优化问题 ，训练可能陷入局部极小 ，导致无法找到全部真实解或找到的解质量不佳 。尽管有紧缩项引导 ，但并未完全消除此风险 。
5 .定量评估缺失 ：实验部分侧重于定性展示找到的解并与已知 FEM结果对比 ，缺乏如能量值 、相对误差等定量指标的详细报告 。

(完 )

## 9. 潜在影响与未来方向
*   **对物理信息机器学习的影响**：为求解具有多稳态的复杂物理系统提供了一种新的、系统的无监督范式。它弥合了传统数值分析中“紧缩法”思想与深度学习框架之间的鸿沟，展示了将经典数值思想融入神经网络的潜力。
*   **对计算物理与材料科学的影响**：为像Landau-de Gennes理论这类描述相变和缺陷的复杂模型提供了一种高效的、基于神经网络的多解探索工具，有助于加速新材料构型或物理状态（如液晶显示模式、超导涡旋态）的发现与研究。
*   **未来研究方向**：
    1.  **扩展应用场景**：将方法应用于其他具有多解的非线性PDE，如Navier-Stokes方程（多流态）、非线性薛定谔方程（孤子解）、或更复杂的相场模型。
    2.  **处理无限多解问题**：当前框架针对有限个（K个）解。如何扩展以处理连续谱的解或未知数量的解是一个重要挑战。
    3.  **自适应机制**：开发自适应策略来自动确定最优的`d_min`阈值，甚至动态调整待求解的数量K。
    4.  **与不确定性量化结合**：将多解发现框架与贝叶斯神经网络或集成学习结合，以评估每个解的稳定性或置信度。
    5.  **提升优化鲁棒性**：研究更先进的优化策略或损失函数设计，以进一步避免局部极小值，确保更可靠地收敛到所有全局解。

## 10. 总结评价
本文提出的Deflation-PINNs是一项构思巧妙且执行扎实的工作。它精准地识别了PINNs在求解多解PDE时的核心局限，并通过融合DeepONet架构和紧缩思想给出了一个优雅的解决方案。理论上的收敛性证明增强了方法的可信度，而在经典LdG模型上的成功演示则提供了有力的实证支持。

尽管在实验广度、定量评估和超参数自动化方面存在局限，但这篇论文为“基于神经网络的PDE多解发现”这一新兴方向树立了一个重要的基准。其核心价值在于提供了一个清晰、可扩展的框架原型，为后续研究开辟了富有潜力的道路。未来的工作可以在此基础上进行拓展、改进和更广泛的验证。

（完）
