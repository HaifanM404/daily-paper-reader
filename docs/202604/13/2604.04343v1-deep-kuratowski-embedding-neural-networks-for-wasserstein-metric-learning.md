---
title: Deep Kuratowski Embedding Neural Networks for Wasserstein Metric Learning
title_zh: 用于 Wasserstein 度量学习的深度 Kuratowski 嵌入神经网络
authors: Andrew Qing He
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04343v1"
tags: ["query:pde-llm"]
score: 6.0
evidence: 用于嵌入和隐式正则化的神经常微分方程
tldr: 针对成对 Wasserstein 距离计算开销巨大的瓶颈问题，本文受 Kuratowski 嵌入定理启发，提出了 DeepKENN 和 ODE-KENN 两种神经网络架构。DeepKENN 通过聚合 CNN 特征图距离进行学习，而 ODE-KENN 利用 Neural ODE 将输入嵌入无限维 Banach 空间以实现隐式正则化。实验表明，该方法能显著提升 $W_2$ 距离的近似精度和泛化能力，为大规模数据分析提供了高效的代理计算方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04343v1/fig-001.webp\", \"caption\": \"Figure 1: Experimental results for all three models trained for 2,000 epochs on MNIST W2 distance learning. Top left: Training (dashed) and validation (solid) MSE loss curves on a log scale. ODE-KENN (green) converges fastest and maintains the smallest train/val gap throughout training. Top right: Predicted vs. true W2 distances on the test set (2,750 pairs). ODE-KENN produces the tightest scatter around the ideal diagonal, particularly at small W2 values. Bottom left: Learned layer weights λk for DeepKENN. The first two convolutional layers are nearly fully suppressed (λ ≈ 0), while FC1 dominates (λ ≈ 1.62), indicating that abstract compressed representations are most informative for W2. Bottom right: Learned time weights λt for ODE-KENN over t ∈ [0, 1]. The bell-shaped profile peaks near t ≈ 0.35, downweighting the raw feature (t = 0) and the late trajectory, and assigning greatest importance to the early-to-mid ODE evolution.\", \"page\": 7, \"index\": 1, \"width\": 976, \"height\": 763}, {\"url\": \"assets/figures/arxiv/2604.04343v1/fig-002.webp\", \"caption\": \"Table 2: CNN encoder architecture shared by all three models.\", \"page\": 11, \"index\": 2, \"width\": 898, \"height\": 211}]"
motivation: 旨在解决数据分析中成对 Wasserstein 距离计算复杂度高、速度慢的瓶颈问题。
method: 提出了基于 CNN 特征聚合的 DeepKENN 和基于 Neural ODE 连续嵌入的 ODE-KENN 两种架构来近似 $W_2$ 距离。
result: "在 MNIST 数据集上，ODE-KENN 的测试均方误差比单层基准降低了 28%，比 DeepKENN 降低了 18%，且泛化误差更小。"
conclusion: 该研究证明了通过深度嵌入网络可以有效且快速地近似 Wasserstein 距离，为下游任务提供了高性能的替代模型。
---

## 摘要
计算成对的 Wasserstein 距离是数据分析流程中的一个基本瓶颈。受经典 Kuratowski 嵌入定理的启发，我们提出了两种神经网络架构，用于从数据中学习近似 Wasserstein-2 距离 ($W_2$)。第一种架构 DeepKENN 使用可学习的正权重聚合 CNN 所有中间特征图之间的距离。第二种架构 ODE-KENN 用神经常微分方程 (Neural ODE) 取代了离散层堆叠，将每个输入嵌入到无限维 Banach 空间 $C^1([0,1], \mathbb{R}^d)$ 中，并通过轨迹平滑性提供隐式正则化。在 MNIST 数据集上使用预先计算的精确 $W_2$ 距离进行的实验表明，在参数量匹配的情况下，ODE-KENN 的测试均方误差 (MSE) 比单层基准模型低 28%，比 DeepKENN 低 18%，同时表现出更小的泛化差距。由此产生的快速代理模型可以在下游成对距离计算中取代昂贵的 $W_2$ 精确计算。

## Abstract
Computing pairwise Wasserstein distances is a fundamental bottleneck in data analysis pipelines. Motivated by the classical Kuratowski embedding theorem, we propose two neural architectures for learning to approximate the Wasserstein-2 distance ($W_2$) from data. The first, DeepKENN, aggregates distances across all intermediate feature maps of a CNN using learnable positive weights. The second, ODE-KENN, replaces the discrete layer stack with a Neural ODE, embedding each input into the infinite-dimensional Banach space $C^1([0,1], \mathbb{R}^d)$ and providing implicit regularization via trajectory smoothness. Experiments on MNIST with exact precomputed $W_2$ distances show that ODE-KENN achieves a 28% lower test MSE than the single-layer baseline and 18% lower than DeepKENN under matched parameter counts, while exhibiting a smaller generalization gap. The resulting fast surrogate can replace the expensive $W_2$ oracle in downstream pairwise distance computations.