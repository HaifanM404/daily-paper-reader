---
title: "FNO$^{\\angle θ}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems"
title_zh: "FNO$^{\\angle θ}$：用于学习分布参数系统状态与最优控制的扩展傅里叶神经算子"
authors: "Zhexian Li, Ketan Savla"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05187v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于学习PDE状态和控制的扩展傅里叶神经算子
tldr: "本研究提出了一种扩展的傅里叶神经算子架构FNO$^{\\angle θ}$，旨在解决分布式参数系统的状态学习与最优控制问题。通过引入Ehrenpreis-Palamodov基本原理，将FNO的频率变量从实数域扩展至复数域，使其能够捕捉更复杂的积分表示。实验证明，该方法在处理非线性Burgers方程时，不仅显著降低了训练误差，还大幅提升了对非周期边界条件的预测精度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05187v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of relative mean squared errors (MSE) over all training data during the training phase of FNO and FNO∠θ for the Burgers’ equation (16). FNO∠θ achieves order of magnitude improvement over FNO.\", \"page\": 5, \"index\": 1, \"width\": 1052, \"height\": 314}]"
motivation: 传统的傅里叶神经算子在处理具有非周期边界条件或复杂控制问题的分布式参数系统时存在局限性。
method: "基于Ehrenpreis-Palamodov基本原理，将FNO层的逆傅里叶变换频率变量从实域扩展到复域，构建了FNO$^{\\angle θ}$架构。"
result: 在非线性Burgers方程的任务中，该方法相比传统FNO在训练误差上实现了一个数量级的提升，且对非周期边界值的预测更准确。
conclusion: 扩展复数域频率的FNO架构能更有效地表征偏微分方程的状态与控制，显著增强了模型处理非周期性及复杂动力学系统的能力。
---

## 摘要
我们提出了一种扩展傅里叶神经算子（FNO）架构，用于学习由偏微分方程支配的系统的状态和线性二次加性最优控制。利用 Ehrenpreis-Palamodov 基本原理，我们证明了常系数线性偏微分方程的任何状态和最优控制都可以表示为复域中的积分。该表示形式的被积函数包含与傅里叶逆变换中相同的指数项，而后者在 FNO 层中用于表示卷积算子。受此观察启发，我们通过将傅里叶逆变换中的频率变量从实域扩展到复域来修改 FNO 层，以捕捉基本原理中的积分表示。我们展示了该方法在学习非线性 Burgers 方程的状态和最优控制方面的性能，结果表明，与原始 FNO 相比，其训练误差有了数量级的改进，并且对非周期边界值的预测更加准确。

## Abstract
We propose an extended Fourier neural operator (FNO) architecture for learning state and linear quadratic additive optimal control of systems governed by partial differential equations. Using the Ehrenpreis-Palamodov fundamental principle, we show that any state and optimal control of linear PDEs with constant coefficients can be represented as an integral in the complex domain. The integrand of this representation involves the same exponential term as in the inverse Fourier transform, where the latter is used to represent the convolution operator in FNO layer. Motivated by this observation, we modify the FNO layer by extending the frequency variable in the inverse Fourier transform from the real to complex domain to capture the integral representation from the fundamental principle. We illustrate the performance of FNO in learning state and optimal control for the nonlinear Burgers' equation, showing order of magnitude improvements in training errors and more accurate predictions of non-periodic boundary values over FNO.

---

## 论文详细总结（自动生成）

以下是对论文 **《FNO$^{\angle \theta}$: Extended Fourier neural operator for learning state and optimal control of distributed parameter systems》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的傅里叶神经算子（FNO）在处理具有**非周期边界条件**或**有界区域**的偏微分方程（PDE）系统时，由于其核心依赖于实数域的傅里叶级数（本质是周期的），会导致边界预测精度下降。
*   **研究动机**：分布式参数系统（由 PDE 支配）的状态预测和最优控制计算量巨大。作者希望利用数学中的 **Ehrenpreis-Palamodov 基本原理**，通过将频率变量从实域扩展到复域，设计一种能更自然地表征 PDE 解和最优控制律的神经算子架构。

### 2. 方法论
*   **核心思想**：将 FNO 层中的逆傅里叶变换频率变量从实数扩展为复数，引入可学习的相位参数 $\theta$，从而捕捉解在复域中的积分表示。
*   **关键技术细节**：
    *   **理论证明**：论文首先证明了对于常系数线性 PDE，其线性二次（LQ）最优控制问题的状态和控制律均可以表示为复域中的指数积分形式。
    *   **架构改进**：在 FNO 的积分算子层中，将原始频率 $k$ 替换为复频率 $z = k e^{i\theta}$。
    *   **公式表达**：算子层 $K_{\theta}$ 定义为：
        $$(K_{\theta} v)(\xi) = \frac{1}{(2\pi)^n} \int_{\mathbb{R}^n} e^{i z(k, \theta) \cdot \xi} \hat{K}(k) \hat{v}(k) dk$$
        其中 $\theta$ 是新增的可学习参数。当 $\theta=0$ 时，该架构完全退化为标准 FNO。
    *   **参数化**：$\theta$ 的维度仅取决于选取的频率数量，与隐藏层维度（width）无关，因此增加的计算开销极小。

### 3. 实验设计
*   **实验场景**：非线性 **Burgers 方程**（粘性系数 $\nu=0.05$，具有类似冲击波的非平滑行为）。
*   **任务类型**：
    1.  **状态算子学习**：输入边界条件，预测全域状态 $\phi(x, t)$。
    2.  **最优控制算子学习**：输入边界条件，预测最优控制输入 $u(x, t)$。
*   **Benchmark**：标准傅里叶神经算子（FNO）。
*   **数据集**：通过有限差分法和伴随法生成 50 组轨迹数据，边界条件通过高斯随机场采样。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或具体的训练时长。
*   **超参数设置**：使用了 $L=4$ 个傅里叶层，频率截断 $M=4$，激活函数为 GELU，并采用了批归一化（Batch Normalization）。

### 5. 实验数量与充分性
*   **实验规模**：针对状态学习和控制学习两个任务分别进行了对比实验。
*   **充分性评价**：
    *   **客观性**：实验在相同的网络深度和宽度（$n_v = n_a$）下进行对比，证明了在低模型容量下 $FNO^{\angle \theta}$ 的优越性。
    *   **局限性**：实验仅限于一维空间+时间维度（1D+1T），且样本量较小（50个样本），尚未在大规模工业级数据集或更高维（如 3D 流体）场景下验证。

### 6. 主要结论与发现
*   **精度提升**：在非线性 Burgers 方程的任务中，$FNO^{\angle \theta}$ 的训练误差比标准 FNO 降低了**一个数量级**（例如状态学习 MSE 从 0.039 降至 0.0089）。
*   **边界效应消除**：标准 FNO 的误差高度集中在边界处（由于周期性假设），而 $FNO^{\angle \theta}$ 能够准确预测非周期边界值，显著提高了边界一致性。
*   **控制学习能力**：在复杂的最优控制律学习任务中，新架构表现出更强的捕捉输入输出间复杂映射的能力。

### 7. 优点
*   **数学支撑深厚**：并非盲目增加网络深度，而是基于 PDE 的基本解理论（Ehrenpreis-Palamodov 原理）进行架构创新。
*   **参数效率高**：通过引入极少量的相位参数 $\theta$，实现了模型表达能力的质变。
*   **通用性**：继承了 FNO 的通用逼近性质，且对非周期性问题的处理更加鲁棒。

### 8. 不足与局限
*   **实验覆盖不足**：目前仅验证了一类非线性 PDE，对于更复杂的 Navier-Stokes 方程或具有极不规则几何边界的问题尚待测试。
*   **理论跨度**：核心理论推导基于线性 PDE，虽然实验证明其对非线性 PDE 有效，但缺乏非线性情况下的严格数学收敛性证明。
*   **应用限制**：目前主要针对连续域的算子学习，对于离散观测数据极其稀疏的情况，其表现尚未可知。

（完）
