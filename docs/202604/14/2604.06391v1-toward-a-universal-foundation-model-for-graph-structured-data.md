---
title: Toward a universal foundation model for graph-structured data
title_zh: 迈向图结构数据的通用基础模型
authors: "Sakib Mostafa, Lei Xing, Md. Tauhidul Islam"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06391v1"
tags: ["query:ddi"]
score: 9.0
evidence: 生物医学研究和分子相互作用中图结构数据的基座模型
tldr: 本研究针对生物医学领域图数据缺乏通用基础模型的问题，提出一种特征无关的图基础模型。该模型通过将度统计、中心性和社区结构等图属性编码为结构提示，结合消息传递机制，实现了跨领域的通用表征学习。实验证明，该模型在零样本和少样本场景下表现优异，显著提升了图数据的泛化能力和预测精度，为图结构数据分析提供了可复用的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06391v1/fig-001.webp\", \"caption\": \"Figure 5. The pretrained GFM generalizes to unseen protein structural fold classes across 144 tissue-specific interaction networks. a, Classification performance at K = 20 labeled examples per fold class. Mean ROC-AUC, accuracy, and specificity are shown for all six models, evaluated on test fold classes that were entirely absent during training. Error bars denote one standard deviation across five evaluation seeds. b, Macro-averaged ROC curves across all test fold classes. Solid lines denote GFM variants; dashed lines denote supervised baselines. The dotted diagonal denotes random performance. c, Few-shot generalization curves on unseen fold classes. Mean accuracy is shown as a function of labeled examples per fold class (K ∈ {3,5,7,10,15,20}), evaluated on test fold classes using a logistic probe trained on K support examples per class. Solid lines denote GFM variants; dashed lines denote supervised baselines.\", \"page\": 11, \"index\": 1, \"width\": 1044, \"height\": 1096}, {\"url\": \"assets/figures/arxiv/2604.06391v1/fig-002.webp\", \"caption\": \"Figure 1. Architecture of the Graph Foundation Model. a, Structural prompt construction. For each node, local context descriptors—degree, clustering coefficient, k-core number, ego-network statistics, and PageRank—are combined with global context indicators derived from two community detection algorithms, Label Propagation and SCoDA, each contributing community identity, community size, and intra-community density. These descriptors are assembled into a natural-language node profile and tokenized by a miniature language model (MiniLM) to produce a context embedding. Node features are projected into a matching space and concatenated with this embedding to form the Graph Feature Embedding (GFE), a representation that encodes both attribute content and structural role without relying on dataset-specific feature semantics. b, Multi-graph pretraining. A GraphSAGE backbone ingests multiple heterogeneous graphs simultaneously, producing a Graph Structure Embedding (GSE) for every node via neighborhood aggregation. Contrastive alignment is applied between the GFE and the GSE: the text embedding of node i is matched against the graph embeddings of structurally related nodes and repelled from structurally dissimilar nodes, training the backbone to map diverse graphs into a shared topological representation space. c, Downstream deployment. The frozen or fine-tuned GFM generates GFE and GSE representations for nodes in an unseen graph. These are concatenated and passed to a lightweight task-specific head for node-level classification or regression, requiring minimal labeled data on the target graph. 2/31\", \"page\": 2, \"index\": 2, \"width\": 836, \"height\": 1104}, {\"url\": \"assets/figures/arxiv/2604.06391v1/fig-003.webp\", \"caption\": \"Figure 3. The pretrained GFM generalizes across the ogbn-proteins benchmark and captures GO hierarchy structure. a, Macro-averaged ROC curves across all GO term labels for GFM zero-shot, GFM Trained, and four supervised GNN baselines (GCN, GraphSAGE, GAT, GIN). Solid lines denote GFM variants; dashed lines denote supervised baselines. The dotted diagonal denotes random performance. b, Summary classification performance. Mean ROC-AUC and accuracy are shown for all six models. The dashed vertical line separates the two reported metrics. All models use the standard ogbn-proteins train/validation/test split. c, Multifunctionality–embedding density correspondence. Bars show the Spearman rank correlation between the number of GO annotations per protein and local embedding density computed from the 15 nearest neighbors. The dashed vertical line separates the pretrained GFM zero-shot model from supervised baselines. Positive values indicate that highly annotated proteins occupy denser embedding neighborhoods. d, Few-shot generalization curves. Mean ROC-AUC is shown as a function of the number of labeled proteins per GO process (K ∈ {1,5,10,20}), evaluated on held-out test proteins using a logistic probe trained on K positive and K negative examples per label. Solid lines denote GFM variants; dashed lines denote supervised baselines. e, GO hierarchy depth stratification. Mean ROC-AUC is shown separately for GO terms at shallow, medium, and deep levels of the GO biological process hierarchy. GFM Trained performance increases with hierarchy depth while baseline performance remains flat or declines. 7/31\", \"page\": 7, \"index\": 3, \"width\": 886, \"height\": 1131}, {\"url\": \"assets/figures/arxiv/2604.06391v1/fig-004.webp\", \"caption\": \"Figure 4. The pretrained GFM generalizes across GO ontologies in the STRING interaction network. a, Mean ROC-AUC and accuracy for Molecular Function (MF) GO term prediction. All six models are compared under the same train/validation/test split on the StringGO network. b, Mean ROC-AUC and accuracy for Cellular Component (CC) GO term prediction under identical conditions. c, Macro-averaged ROC curves for MF. Solid lines denote GFM variants; dashed lines denote supervised baselines. The dotted diagonal denotes random performance. d, Macro-averaged ROC curves for CC. Same protocol as c. e, Cross-functional co-enrichment in GFM Trained embeddings for MF. Each cell reports the mean fraction of GO-term annotated neighbors that a protein from one anchor MF term accumulates within a 25-nearest-neighbor embedding neighborhood annotated by another term. Rows and columns are ordered by hierarchical clustering of the symmetrized co-enrichment matrix. f, Cross-functional co-enrichment for CC. Same protocol as e. Three distinct spatial modules are visible: a mitochondrial module, a nuclear and cytoplasmic module, and a membrane module. 8/31\", \"page\": 8, \"index\": 4, \"width\": 990, \"height\": 1208}, {\"url\": \"assets/figures/arxiv/2604.06391v1/fig-005.webp\", \"caption\": \"Figure 2. The pretrained GFM encodes protein functional organization in SagePPI and generalizes with minimal supervision. a, Few-shot generalization curves. Mean ROC-AUC is shown as a function of the number of labeled proteins per GO process, evaluated on held-out test proteins using a logistic probe trained on K positive and K negative examples per label (K ∈ {1, 5, 10, 20}). Solid lines denote GFM variants; dashed lines denote supervised baselines. b, Multifunctionality–embedding density correspondence. Bars show the Spearman rank correlation between the number of GO process annotations per protein and the local embedding density computed from the 15 nearest neighbors in each model’s embedding space. The dashed vertical line separates the pretrained GFM zero-shot model (left) from supervised GNN baselines (right). Positive values indicate that highly annotated proteins occupy denser embedding neighborhoods. c, Cross-functional co-enrichment in GFM Trained embeddings. Each cell reports the mean fraction of GO-annotated neighbors that a protein from one anchor process accumulates within a 25-nearest-neighbor embedding neighborhood annotated by another process. Rows and columns are ordered by hierarchical clustering of the symmetrized co-enrichment matrix. Darker entries indicate higher cross-process neighborhood sharing. d, Macro-averaged ROC curves across all 121 GO process labels. Each curve corresponds to the mean interpolated true positive rate at fixed false positive rate thresholds, averaged across labels with at least two represented classes. The dotted diagonal denotes random performance. e, Summary classification performance. Mean ROC-AUC and accuracy are shown for GFM zero-shot, GFM Trained, and four supervised GNN baselines (GCN, GraphSAGE, GAT, GIN). The dashed vertical line separates the two performance metrics. All models use the same train/validation/test split. 4/31\", \"page\": 4, \"index\": 5, \"width\": 836, \"height\": 1070}]"
motivation: 传统图神经网络因绑定特定节点特征和拓扑而难以跨领域迁移，限制了其在多变生物医学网络中的泛化应用。
method: 采用特征无关的图属性（如中心性、社区结构和扩散特征）作为结构提示，构建可在大规模异构图上预训练的通用骨干网络。
result: "预训练模型在多个基准上超越了强监督基准，特别是在SagePPI任务中通过微调实现了95.5%的ROC-AUC，比传统模型提升21.8%。"
conclusion: 该方法成功构建了适用于图结构数据的通用基础模型，为生物医学和网络科学研究提供了强大的可复用分析工具。
---

## 摘要
图是生物医学研究中的核心表示形式，涵盖了分子相互作用网络、基因调节回路、细胞间通信图谱以及知识图谱。尽管其重要性不言而喻，但目前尚无能够与改变语言和视觉领域的模型相媲美的、广泛可重用的图分析基础模型。现有的图神经网络通常在单一数据集上进行训练，且仅学习特定于该图节点特征、拓扑结构和标签空间的表示，从而限制了其跨领域迁移的能力。这种泛化能力的缺失在生物学和医学领域尤为突出，因为不同队列、实验方法和机构之间的网络存在显著差异。在本文中，我们介绍了一种图基础模型，旨在学习不特定于具体节点身份或特征方案的可迁移结构表示。我们的方法利用了与特征无关的图属性，包括度统计、中心性度量、社区结构指标以及基于扩散的特征，并将其编码为结构提示（structural prompts）。这些提示与消息传递骨干网络相结合，将多样的图嵌入到一个共享的表示空间中。该模型在异构图上进行一次预训练，随后只需极少的调整即可重用于未见的数据集。在多个基准测试中，我们的预训练模型达到或超过了强大的监督基线，同时在留出图上展示了卓越的零样本（zero-shot）和少样本（few-shot）泛化能力。在 SagePPI 基准测试中，对预训练骨干网络进行监督微调达到了 95.5% 的平均 ROC-AUC，比最佳监督消息传递基线提高了 21.8%。因此，所提出的技术为生物医学和网络科学应用中图结构数据的可重用基础规模模型提供了一种独特的方法。

## Abstract
Graphs are a central representation in biomedical research, capturing molecular interaction networks, gene regulatory circuits, cell--cell communication maps, and knowledge graphs. Despite their importance, currently there is not a broadly reusable foundation model available for graph analysis comparable to those that have transformed language and vision. Existing graph neural networks are typically trained on a single dataset and learn representations specific only to that graph's node features, topology, and label space, limiting their ability to transfer across domains. This lack of generalization is particularly problematic in biology and medicine, where networks vary substantially across cohorts, assays, and institutions. Here we introduce a graph foundation model designed to learn transferable structural representations that are not specific to specific node identities or feature schemes. Our approach leverages feature-agnostic graph properties, including degree statistics, centrality measures, community structure indicators, and diffusion-based signatures, and encodes them as structural prompts. These prompts are integrated with a message-passing backbone to embed diverse graphs into a shared representation space. The model is pretrained once on heterogeneous graphs and subsequently reused on unseen datasets with minimal adaptation. Across multiple benchmarks, our pretrained model matches or exceeds strong supervised baselines while demonstrating superior zero-shot and few-shot generalization on held-out graphs. On the SagePPI benchmark, supervised fine-tuning of the pretrained backbone achieves a mean ROC-AUC of 95.5%, a gain of 21.8% over the best supervised message-passing baseline. The proposed technique thus provides a unique approach toward reusable, foundation-scale models for graph-structured data in biomedical and network science applications.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种旨在解决图神经网络（GNN）泛化性难题的**图基础模型（Graph Foundation Model, GFM）**。以下是对该论文的结构化深入分析：

### 1. 核心问题与研究动机
*   **研究背景**：图结构在生物医学（如蛋白质相互作用、基因调控）中至关重要。然而，与自然语言处理（NLP）和计算机视觉（CV）不同，图领域缺乏通用的基础模型。
*   **核心痛点**：
    *   **特征不兼容**：不同图的节点特征（如分子指纹 vs. 文本向量）维度和语义完全不同。
    *   **拓扑特异性**：现有模型通常绑定在单一数据集的拓扑和标签空间上，跨领域迁移能力极差。
    *   **长程依赖缺失**：传统消息传递机制难以捕捉全局结构信号。
*   **研究动机**：开发一种不依赖特定节点身份或特征方案，仅通过图的**内在拓扑属性**来学习通用表征的基础模型。

### 2. 方法论：核心思想与技术细节
GFM 的核心在于将图的拓扑结构转化为“语言提示”，并利用对比学习对齐结构与特征。

*   **结构提示构建（Structural Prompts）**：
    *   **局部上下文**：提取度（Degree）、聚类系数、k-core 数、1-2 层自我网络（Ego-network）统计量、PageRank。
    *   **全局上下文**：利用 Label Propagation 和 SCoDA 两种社区检测算法提取社区身份、大小和密度。
    *   **文本化编码**：将上述数值指标组装成自然语言描述，通过小型语言模型（MiniLM）转换为 384 维的上下文嵌入（Context Embedding）。
*   **图特征嵌入（GFE）与图结构嵌入（GSE）**：
    *   **GFE**：将原始节点特征与结构提示嵌入拼接，通过一个可学习的适配器（Adapter）投影到共享空间。
    *   **GSE**：使用两层 GraphSAGE 骨干网络进行邻域聚合，生成结构表征。
*   **对比预训练（Contrastive Pre-training）**：
    *   **目标**：在 9 个异构非生物图（如引用网络、社交网络、电商网络）上同时训练。
    *   **对齐机制**：使用 InfoNCE 损失函数，将节点的文本描述嵌入与通过个性化 PageRank（PPR）筛选出的结构相关节点的图嵌入进行对齐。
*   **下游适配**：在未见过的图上，冻结骨干网络，仅通过无监督对比学习微调一个轻量级适配器。

### 3. 实验设计
*   **数据集与场景**：
    1.  **SagePPI**：人类蛋白质相互作用网络（121 个 GO 标签）。
    2.  **ogbn-proteins**：大规模 STRING 数据库网络，采用物种分层划分（测试集物种未见）。
    3.  **StringGO**：使用氨基酸组成作为特征，测试跨本体（MF, BP, CC）泛化。
    4.  **Fold-PPI**：144 个组织特异性网络，测试完全未见的蛋白质折叠类别（Disjoint Class Split）。
*   **基准（Benchmark）与对比方法**：
    *   对比了四种经典的监督学习 GNN：**GCN, GraphSAGE, GAT, GIN**。
    *   评估模式包括：**Zero-shot**（零样本，仅线性探测）、**Few-shot**（少样本，K=1~20）和 **Full Supervision**（全监督微调）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了预训练进行了 250 个 epoch，每 epoch 128 步，batch size 为 1024。使用了混合精度训练（Mixed-precision training）以提高效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   预训练使用了 9 个不同领域的异构图。
    *   下游验证涵盖了 4 个大型生物医学基准，涉及数千个子任务（如 Fold-PPI 包含 144 个网络）。
    *   进行了少样本梯度实验、噪声鲁棒性实验、GO 层次深度分析以及 t-SNE 可视化。
*   **充分性评价**：实验设计非常充分且严谨。特别是采用了“物种分层”和“类别不相交”的划分方式，极大地增强了结论的客观性，证明了模型不是靠“背诵”数据，而是真正理解了结构规律。

### 6. 主要结论与发现
*   **跨领域迁移成功**：仅在非生物图（如论文引用网络）上预训练的模型，在生物网络上的表现竟然超过了直接在生物数据上训练的监督模型。
*   **零样本优势**：GFM Zero-shot 在多个基准上优于最强的监督基线（如 GIN）。在 SagePPI 中，GFM 微调后的 ROC-AUC 达到 95.5%，比基线高出 21.8%。
*   **少样本效率**：GFM 仅需 1 个标注样本的效果，往往能达到监督模型需要 20 个样本才能达到的水平。
*   **结构决定功能**：实验发现 GFM 在预测“深层/具体”的 GO 术语时优势更明显，说明其捕捉长程拓扑信号的能力极强。

### 7. 优点与亮点
*   **特征无关性**：通过“结构提示”巧妙解决了不同图数据特征维度不一的难题，实现了真正的“通用”。
*   **生物学一致性**：模型在无监督情况下自动聚类出了线粒体、细胞核等亚细胞结构，并发现多功能蛋白质处于嵌入空间的中心位置，符合生物学直觉。
*   **强大的泛化边界**：在 Fold-PPI 实验中，面对完全未见的折叠类别，Zero-shot 表现甚至优于微调模型，证明了拓扑特征的稳健性。

### 8. 不足与局限
*   **拓扑依赖性**：该模型高度依赖图的拓扑质量。如果输入图的边缘噪声极大或极其稀疏，基于社区检测和扩散的结构提示可能会失效。
*   **计算开销**：在推理前需要计算多种复杂的拓扑指标（如 PageRank, 社区检测），对于超大规模动态图，预处理成本可能较高。
*   **适配器微调**：虽然骨干网络冻结，但每个新图仍需进行无监督的适配器微调，尚未实现完全的“即插即用”。
*   **算力透明度**：缺乏算力消耗的具体数据，难以评估普通研究机构复现该基础模型的门槛。

（完）
