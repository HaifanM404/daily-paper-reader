---
title: "PI-JEPA: Label-Free Surrogate Pretraining for Coupled Multiphysics Simulation via Operator-Split Latent Prediction"
title_zh: PI-JEPA：通过算子分裂潜空间预测实现耦合多物理场模拟的无标签代理模型预训练
authors: "Brandon Yee, Pairie Koh"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01349v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 无需完整PDE求解的物理信息多物理场模拟预训练
tldr: 针对油藏模拟中输入参数易得但模拟标注昂贵的问题，本文提出PI-JEPA预训练框架。该方法无需完整的PDE求解，通过在无标注参数场上进行掩码潜空间预测，并结合基于算子分裂（Lie-Trotter）的物理约束残差正则化，实现了高效的代理模型预训练。在极少标注数据下，其性能显著优于FNO和DeepONet，大幅降低了多物理场模拟的计算成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01349v1/fig-001.webp\", \"caption\": \"Figure 1: PI-JEPA architecture overview. The solution field u(x, t) is partitioned into context and target patch sets. A context encoder fθ and an EMA target encoder fξ map these to latent codes zc and zt, respectively. A bank of K latent predictors {gϕk } predicts the target embeddings ẑ(k)t aligned to each sub-operator in the physical splitting. The total loss L combines a predictive term, a PDE residual physics term, and a covariance regularizer. The EMA update ensures the target encoder provides stable, slowly-evolving learning targets throughout self-supervised pretraining.\", \"page\": 7, \"index\": 1, \"width\": 553, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2604.01349v1/fig-002.webp\", \"caption\": \"Figure 3: Spatiotemporal block masking strategy. Context patches (blue) are selected from a contiguous subregion at time t. Target patches (orange) form a spatially displaced block at the subsequent timestep t+∆t. The predictor must anticipate the latent representation of the target region, implicitly learning the causal dynamics—advection, diffusion, or reaction—linking context to target. Unmasked patches (gray) contribute to the target encoder representation but are not used in the predictive loss.\", \"page\": 10, \"index\": 2, \"width\": 536, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.01349v1/fig-003.webp\", \"caption\": \"Table 4: PI-JEPA architectural and training hyperparameters.\", \"page\": 18, \"index\": 3, \"width\": 660, \"height\": 746}, {\"url\": \"assets/figures/arxiv/2604.01349v1/fig-004.webp\", \"caption\": \"Figure 4: Data efficiency on single-phase Darcy flow (64×64). PI-JEPA (blue) outperforms FNO (red) and DeepONet (orange) for Nℓ ≤ 100. The gap between PI-JEPA and the scratch baseline (gray, dashed) quantifies the benefit of self-supervised pretraining. FNO surpasses PI-JEPA at Nℓ ≥ 250 where its spectral inductive bias is fully exploited. All results averaged over 3 seeds.\", \"page\": 13, \"index\": 4, \"width\": 820, \"height\": 499}, {\"url\": \"assets/figures/arxiv/2604.01349v1/fig-005.webp\", \"caption\": \"Figure 2: Operator splitting correspondence. The numerical Lie–Trotter splitting (top row) decomposes each timestep into sequential physical sub-operators L1, . . . ,LK . PI-JEPA’s latent predictor bank (bottom row) mirrors this structure: predictor gϕk advances the latent state through the k-th sub-step, and a per-sub-operator PDE residual loss L(k) phys regularizes each prediction. The dashed arrows indicate this physics supervision coupling. Illustrated here for K = 2 (pressure + saturation), the scheme extends to K = 3 for reactive transport by adding a reaction predictor.\", \"page\": 8, \"index\": 5, \"width\": 770, \"height\": 232}]"
motivation: 现有的神经算子代理模型高度依赖昂贵的标注模拟数据，无法有效利用易于获取的大规模无标注输入参数场。
method: 提出一种结合掩码潜空间预测与算子分裂物理约束的预训练架构，将复杂的物理过程分解为独立的受限潜空间模块进行学习。
result: "在仅有100个标注样本时，其误差比FNO和DeepONet分别降低了1.9倍和2.4倍，且在500个样本时较纯监督学习提升了24%。"
conclusion: PI-JEPA证明了无标注预训练能显著减少多物理场代理模型部署所需的模拟预算，有效解决了模拟数据获取成本高的难题。
---

## 摘要
油藏模拟工作流面临着根本性的数据不对称性：输入参数场（如地质统计渗透率实现、孔隙率分布）可以任意数量自由生成，但现有的神经算子代理模型需要大量昂贵的有标签模拟轨迹语料库，且无法利用这种无标签结构。我们提出了 PI-JEPA（物理信息联合嵌入预测架构），这是一种代理模型预训练框架，它无需任何完整的 PDE 求解，通过在逐子算子 PDE 残差正则化下对无标签参数场进行掩码潜空间预测来进行训练。预测器库在结构上与控制方程的 Lie-Trotter 算子分裂分解相对齐，为每个子过程（压力、饱和度输运、反应）分配了独立的物理约束潜空间模块，从而实现了仅需 100 次有标签模拟运行即可完成微调。在单相达西流任务中，当 $N_\ell=100$ 时，PI-JEPA 的误差比 FNO 低 1.9 倍，比 DeepONet 低 2.4 倍；在 $N_\ell=500$ 时，其性能比仅监督训练提高了 24%。这表明无标签代理模型预训练显著降低了部署多物理场代理模型所需的模拟预算。

## Abstract
Reservoir simulation workflows face a fundamental data asymmetry: input parameter fields (geostatistical permeability realizations, porosity distributions) are free to generate in arbitrary quantities, yet existing neural operator surrogates require large corpora of expensive labeled simulation trajectories and cannot exploit this unlabeled structure. We introduce \textbf{PI-JEPA} (Physics-Informed Joint Embedding Predictive Architecture), a surrogate pretraining framework that trains \emph{without any completed PDE solves}, using masked latent prediction on unlabeled parameter fields under per-sub-operator PDE residual regularization. The predictor bank is structurally aligned with the Lie--Trotter operator-splitting decomposition of the governing equations, dedicating a separate physics-constrained latent module to each sub-process (pressure, saturation transport, reaction), enabling fine-tuning with as few as 100 labeled simulation runs. On single-phase Darcy flow, PI-JEPA achieves $1.9\times$ lower error than FNO and $2.4\times$ lower error than DeepONet at $N_\ell{=}100$, with 24\% improvement over supervised-only training at $N_\ell{=}500$, demonstrating that label-free surrogate pretraining substantially reduces the simulation budget required for multiphysics surrogate deployment.

---

## 论文详细总结（自动生成）

### PI-JEPA 论文深度总结

#### 1. 核心问题与研究动机
论文针对油藏模拟及多物理场仿真中的**数据不对称性**问题展开研究。在实际工程中，生成模拟所需的输入参数场（如地质统计学渗透率、孔隙率分布）成本极低，但通过数值求解器获得高保真的模拟轨迹（标注数据）却极其昂贵且耗时。现有的神经算子（如 FNO、DeepONet）属于监督学习范式，高度依赖大量标注数据，无法有效利用海量的无标注参数场。因此，研究动机在于开发一种能够利用“廉价”参数场进行预训练，从而在极少量“昂贵”标注数据下实现高精度模拟的代理模型。

#### 2. 方法论：PI-JEPA 架构
PI-JEPA（物理信息联合嵌入预测架构）的核心思想是将自监督学习中的联合嵌入预测（JEPA）与物理算子分裂相结合：
*   **算子分裂潜空间预测**：基于 Lie–Trotter 分裂理论，将复杂的耦合 PDE 分解为多个物理子过程（如压力求解、饱和度输运、化学反应）。架构中设计了一个预测器库（Predictor Bank），每个子过程对应一个独立的 Transformer 模块，依次推进潜空间状态。
*   **掩码预测机制**：采用时空块掩码策略。上下文编码器处理当前时刻的部分空间块，预测器则尝试预测下一时刻被掩码区域在潜空间中的嵌入表示。
*   **物理信息正则化（PI）**：在预训练阶段，通过轻量级解码器将预测的潜空间向量映射回物理空间，并计算逐子算子的 PDE 残差（如达西定律残差、质量守恒残差）。这种方式无需完整求解 PDE，仅需计算残差作为约束。
*   **稳定性机制**：引入 EMA（指数移动平均）目标编码器来提供稳定的预测目标，并使用 VICReg（方差-不变性-协方差正则化）防止潜空间表征坍缩。

#### 3. 实验设计
论文在三个具有代表性的 PDE 系统上进行了验证：
*   **数据集**：
    1.  **单相达西流 (GRF)**：基础基准，使用高斯随机场生成的渗透率。
    2.  **二相 CO2-水流 (SPE10)**：工业级复杂场景，基于 SPE10 模型的非均质渗透率分布。
    3.  **反应输运 (PDEBench ADR)**：涉及平流、扩散和化学反应的耦合系统。
*   **Benchmark 与对比方法**：
    *   对比了 **FNO** (Fourier Neural Operator) 和 **DeepONet** 两种主流神经算子。
    *   设置了 **PI-JEPA (scratch)** 消融实验，即不经过预训练直接进行监督学习，以量化预训练的贡献。
    *   核心评估指标是**数据效率曲线**，即在不同标注样本量 $N_\ell$（10 到 500）下的相对 $\ell_2$ 误差。

#### 4. 资源与算力
论文提到计算支持由 Yee Collins Research Group 提供，并使用了 AdamW 优化器进行训练。
*   **具体算力信息**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总的训练时长（小时/天）。
*   **训练参数**：预训练阶段为 500 个 epoch，微调阶段为 300 个 epoch，批大小为 64。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单单相流到复杂多相流及反应输运的跨度，实验设计较为全面。
*   **充分性与公平性**：
    *   每组实验均运行了 3 个随机种子并取平均值，保证了结果的客观性。
    *   对比方法（FNO, DeepONet）在相同的标注数据量和 epoch 预算下训练，确保了比较的公平性。
    *   通过改变 $N_\ell$ 跨度，清晰地展示了模型在“极低数据”到“中等数据”环境下的表现。
    *   **不足之处**：在反应输运（ADR）实验中，仅针对单一参数机制进行了评估，未完全覆盖所有 Péclet 和 Damköhler 数组合。

#### 6. 主要结论与发现
*   **低数据量优势显著**：在单相达西流任务中，当仅有 100 个标注样本时，PI-JEPA 的误差比 FNO 低 1.9 倍，比 DeepONet 低 2.4 倍。
*   **预训练增益**：在所有基准测试中，预训练版本均优于从头训练版本。在二相流中，预训练带来了 20-25% 的性能提升。
*   **算子分裂的有效性**：通过将预测器与物理子过程对齐，模型能够更好地处理具有不同时间尺度的耦合动力学。
*   **性能拐点**：在标注数据充足（如 $N_\ell \ge 250$）时，具有强谱归纳偏置的 FNO 会追平甚至超越 PI-JEPA，说明该方法的核心价值在于“标签稀缺”场景。

#### 7. 优点与亮点
*   **无需标签的预训练**：首次实现了完全基于无标注参数场的神经算子预训练，极大地降低了代理模型的部署门槛。
*   **物理与架构的深度融合**：将数值计算中的算子分裂思想引入 Transformer 架构，使模型结构具有明确的物理含义。
*   **潜空间预测**：避开了高维像素空间的重建难题，专注于学习物理场的语义特征，提高了训练效率和泛化能力。

#### 8. 不足与局限
*   **领域迁移限制**：在达西流上预训练的模型迁移到反应输运（ADR）时增益较小，说明预训练仍存在一定的领域依赖性。
*   **几何局限性**：目前的实验主要集中在规则的 2D 笛卡尔网格上，对于非规则网格或复杂的 3D 几何结构的适用性尚待验证。
*   **超参数敏感性**：物理残差权重 $\lambda_p$ 和 VICReg 权重等超参数可能需要针对不同物理系统进行精细调优。
*   **算力细节缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估其训练成本。

（完）
