---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: Deflation-PINNs：学习偏微分方程与 Landau-de Gennes 模型的多解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v2"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于寻找非线性PDE多个解的物理信息神经网络
tldr: 针对物理信息神经网络（PINNs）难以求解非线性偏微分方程（PDEs）多解的问题，本文提出Deflation-PINNs框架。该方法结合了紧缩（deflation）损失函数与DeepONets架构，通过在损失函数中引入紧缩项，强制模型系统性地寻找并收敛于不同的解分支。在Landau-de Gennes液晶模型上的实验证明，该方法能有效识别复杂的多种平衡态结构，为探索非线性系统多解问题提供了新工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v2/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统的PINNs设计初衷是寻找单一解，难以捕捉非线性PDE中普遍存在的多个不同解分支。
method: 提出Deflation-PINNs，通过在PINNs和DeepONets架构中引入紧缩损失项，系统性地排斥已发现的解以寻找新解。
result: 在具有复杂能量景观的Landau-de Gennes模型中，该方法成功识别并刻画了多种不同的晶体结构。
conclusion: Deflation-PINNs为求解具有多解特性的非线性物理系统提供了一种理论完备且有效的数值模拟新方案。
---

## 摘要
非线性偏微分方程（PDEs）在数学物理和工程领域无处不在。尽管物理信息神经网络（PINNs）已成为求解 PDE 问题的强大工具，但由于其设计初衷是单次寻找单个解，因此通常难以识别多个不同的解。为了解决这一局限性，我们引入了 Deflation-PINNs，这是一种将紧缩损失（deflation loss）与基于 PINNs 和深度算子网络（DeepONets）的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法能够系统地强制 Deflation-PINN 寻找并收敛至多个不同的有限解分支。我们提供了模型收敛性的理论证明，并通过对液晶 Landau-de Gennes 模型的数值实验验证了 Deflation-PINNs 的有效性，该系统以其复杂的能量景观和多种平衡态而著称。实验结果表明，Deflation-PINNs 能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

以下是对论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：非线性偏微分方程（PDEs）在物理和工程中广泛存在，且往往具有多个不同的解（多解性）。传统的物理信息神经网络（PINNs）虽然强大，但其优化目标通常只收敛于单一解，难以捕捉系统的完整解空间。
*   **背景**：在液晶理论的 Landau-de Gennes (LdG) 模型中，系统存在多个能量极小值点，对应不同的物理稳定态（如对角态和旋转态）。现有的多解寻找方法（如随机初始化集成法）存在冗余且缺乏系统性保证。

### 2. 方法论
*   **核心思想**：提出 **Deflation-PINNs** 框架，通过在损失函数中引入“紧缩项”（Deflation term），并结合简化的 DeepONet 架构，强制模型在单次训练中同时学习并区分有限个（$K$ 个）不同的解分支。
*   **关键技术细节**：
    *   **架构创新**：采用简化版 DeepONet。不同于标准 DeepONet 处理函数映射，该模型将 $K$ 个解表示为 $K$ 个可学习的特征向量（Branch Weights $\beta_k$），通过共享的 Trunk Net $\tau(x)$ 进行解码。这种设计极大地减少了参数量。
    *   **紧缩损失（Deflation Loss）**：定义为 $L_{Def} = \sum \sum \max(1 - \frac{1}{d_{min}} ||u_i - u_j||_{L2}, 0)$。该项确保任意两个解分支之间的 $L2$ 距离不小于超参数 $d_{min}$，从而防止多个分支收敛到同一个解。
    *   **硬约束边界条件**：引入了一种基于径向边界距离函数（RBD function）的自定义扩展方法，确保模型在任何参数下都严格满足 Dirichlet 边界条件，提高了训练效率和物理一致性。
    *   **理论支撑**：论文提供了关于硬约束模型近似能力的数学证明（基于通用近似定理的推论）。

### 3. 实验设计
*   **实验场景**：二维 Landau-de Gennes (LdG) 液晶模型。这是一个典型的多解问题，涉及对称无迹矩阵 $Q$  tensors 的最小化。
*   **Benchmark（基准）**：对比了传统的**有限元方法（FEM）**计算出的已知稳定解。
*   **对比内容**：主要验证模型能否识别出 LdG 模型在正方形区域内的 6 个稳定态：2 个对角态（D1, D2）和 4 个旋转态（R1, R2, R3, R4）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3060-Ti GPU** (8GB VRAM)。
*   **训练细节**：
    *   优化器：Adam。
    *   迭代次数：10,000 个 epoch。
    *   学习率：$1 \times 10^{-3}$。
    *   网络规模：Trunk Net 宽度为 4000，包含 1 个隐藏层，激活函数为 tanh。
    *   配置：$p=16$（特征维度），$K=6$（解的数量）。

### 5. 实验数量与充分性
*   **实验规模**：论文集中展示了针对 LdG 模型的完整解搜索实验。通过调整超参数 $d_{min}$ 和损失权重 $\alpha, \beta$ 进行了参数敏感性测试。
*   **充分性评价**：实验设计较为专注，成功复现了 FEM 发现的所有 6 个物理分支。虽然实验主要集中在一个核心物理模型上，但该模型复杂度高、代表性强，足以证明 Deflation-PINNs 处理复杂能量景观的能力。

### 6. 主要结论与发现
*   **多解识别能力**：Deflation-PINNs 能够一次性成功识别并表征 LdG 模型的所有 6 个稳定晶体结构，且结果与 FEM 高度吻合。
*   **效率提升**：相比于通过多次随机初始化训练多个 PINN 的方法，Deflation-PINNs 通过紧缩损失系统性地排斥重复解，显著降低了计算冗余。
*   **初始化价值**：该方法生成的粗略近似解可以作为传统数值解法（如 Newton 法）的高质量初始值，加速高精度收敛。

### 7. 优点
*   **系统性强**：通过数学手段（Deflation Loss）强制解的分离，而非依赖随机性。
*   **架构优雅**：将 DeepONet 简化为处理有限解分支的形式，既保留了算子学习的通用性，又优化了参数效率。
*   **硬约束集成**：径向外推法处理边界条件比传统的惩罚项法更稳健，减少了超参数调优的难度。

### 8. 不足与局限
*   **超参数依赖**：$d_{min}$（解之间的最小距离）和 $K$（解的数量）需要预验知识或反复试验。如果 $d_{min}$ 设置过大，可能导致物理上接近的解无法被发现；若 $K$ 设置错误，可能遗漏解或导致训练不稳定。
*   **泛化验证有限**：目前仅在 LdG 模型上进行了验证，尚未在具有无限解分支或更高维度的 PDE 系统中测试。
*   **计算开销**：随着 $K$ 的增加，计算两两分支间距离的 Deflation Loss 项会呈 $O(K^2)$ 增长，在大规模多解搜索中可能存在瓶颈。

（完）
