---
title: "Adaptive Randomized Neural Networks with Locally Activation Function: Theory and Algorithm for Solving PDEs"
title_zh: 具有局部激活函数的自适应随机神经网络：求解偏微分方程的理论与算法
authors: "Ran Bi, Weibing Deng"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08869v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于求解偏微分方程的物理信息随机神经网络
tldr: 本研究建立了随机神经网络（RaNNs）的逼近理论，揭示了采样域大小与目标函数光滑度及神经元数量的内在联系。基于此理论，提出了一种结合单位分解（PoU）的自适应物理信息随机神经网络（PIRaNN），通过后验误差指标动态细化局部区域，有效解决了具有局部低正则性的偏微分方程求解问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08869v1/fig-001.webp\", \"caption\": \"Fig. 4. Convergence curve and adaptive PoU of Example 3 based on the adaptive PIRaNNs.\", \"page\": 25, \"index\": 1, \"width\": 748, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.08869v1/fig-002.webp\", \"caption\": \"Fig. 5. Convergence curve and adaptive PoU of Example 4 based on the adaptive PIRaNNs.\", \"page\": 25, \"index\": 2, \"width\": 721, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.08869v1/fig-003.webp\", \"caption\": \"Fig. 1. Error and residual curves as functions of the truncation parameter M for different wavenumbers k and numbers of neurons N , without the use of a PoU. Left column: N = 500; right column: N = 1000. Top row: k = 16; bottom row: k = 32.\", \"page\": 22, \"index\": 3, \"width\": 723, \"height\": 649}, {\"url\": \"assets/figures/arxiv/2604.08869v1/fig-004.webp\", \"caption\": \"Fig. 6. Convergence curve and adaptive PoU of Example 5 based on the adaptive PIRaNNs.\", \"page\": 27, \"index\": 4, \"width\": 713, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.08869v1/fig-005.webp\", \"caption\": \"Fig. 7. Numerical result of Adaptive PIRaNN method for Example 5. The first column shows\", \"page\": 27, \"index\": 5, \"width\": 767, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2604.08869v1/fig-006.webp\", \"caption\": \"Fig. 3. Convergence curves and adaptive PoUs for the adaptive PIRaNNs. (a)(b): θ = 0.6; (c)(d): θ = 0.7; (e)(f): θ = 0.8.\", \"page\": 24, \"index\": 6, \"width\": 719, \"height\": 992}]"
motivation: 旨在解决随机神经网络在处理具有局部复杂特征的偏微分方程时，采样参数范围难以确定且逼近效率不足的问题。
method: 提出一种自适应PIRaNN方法，将单位分解技术与RaNN结合，并利用后验误差指标动态调整局部激活函数的细化程度。
result: 理论上证明了采样域与函数光滑度的关系，数值实验验证了该方法在多个偏微分方程基准问题上具有强大的逼近能力和效率。
conclusion: 该研究为随机神经网络提供了坚实的理论支撑，并为高效求解复杂偏微分方程提供了一种灵活且自适应的数值新方案。
---

## 摘要
本文为随机神经网络 (RaNNs) 建立了一个逼近定理，其隐藏层参数是从预设的有界区域中均匀采样的。我们的分析表明，对于形式为 $\sum_i W_i \sigma(A_i, b_i)$ 的 RaNNs，实现最优逼近所需的采样区域大小与目标函数的平滑度以及神经元数量内在相关。受此理论见解的启发，我们将单位分解 (PoU) 与 RaNNs 相结合，开发了一种自适应物理信息随机神经网络 (PIRaNN) 方法，用于求解具有有限局部正则性的偏微分方程。所提出的自适应策略根据后验误差指示器细化单位分解，使网络能够高效地捕捉解的局部特征。数值实验验证了理论结果，并展示了 RaNNs 强大的逼近能力，证实了自适应 PIRaNN 方法在一系列基准问题上的有效性。

## Abstract
This paper establishes an approximation theorem for randomized neural networks (RaNNs) whose hidden-layer parameters are uniformly sampled from a prescribed bounded domain. Our analysis shows that, for RaNNs of the form $\mathop{\sum}_i W_i σ(A_i, b_i)$, the size of the sampling domain required to achieve optimal approximation is intrinsically linked to the smoothness of the target function and the number of neurons. Motivated by this theoretical insight, we integrate a partition of unity (PoU) with RaNNs to develop an adaptive physics-informed randomized neural network (PIRaNN) method for solving partial differential equations with limited local regularity. The proposed adaptive strategy refines the PoU based on a posteriori error indicators, enabling the network to efficiently capture localized solution features. Numerical experiments validate the theoretical results and demonstrate the strong approximation capabilities of RaNNs, confirming the effectiveness of the adaptive PIRaNN method on a range of benchmark problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Randomized Neural Networks with Locally Activation Function: Theory and Algorithm for Solving PDEs》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然物理信息神经网络（PINNs）在求解偏微分方程（PDEs）方面表现出色，但其非线性、非凸的优化过程（如 Adam 或 L-BFGS 训练）容易陷入局部最优且计算开销大。随机神经网络（RaNNs，如 ELM）通过固定隐藏层随机参数、仅训练输出层权重，将问题简化为线性最小二乘法，极大地提升了效率。
*   **核心问题**：
    1.  **理论层面**：RaNNs 的随机参数采样范围（$M$）如何影响逼近精度？它与目标函数的光滑度有何内在联系？
    2.  **算法层面**：如何处理具有局部奇异性、尖峰或激波等低正则性特征的 PDE 解？全局均匀采样在这些区域往往效率低下。

### 2. 论文提出的方法论
*   **理论构建**：
    *   引入了**广义 Barron 谱空间** $B_s^{k,p}(\Omega)$，利用傅里叶变换的衰减特性刻画函数的光滑度。
    *   证明了 RaNNs 在 Sobolev 范数下的逼近误差界。关键结论是：**最优采样域 $M$ 的大小应随目标函数光滑度的降低和神经元数量 $N$ 的增加而增大**（即 $M = O(N^\alpha)$）。
*   **核心算法：自适应 PIRaNN**：
    *   **单位分解（PoU）结合**：将物理域划分为多个子域，在每个子域上构建局部 RaNN。通过仿射变换将参考单元上的随机基函数映射到物理子域，这在数学上等同于根据子域大小自动缩放采样范围 $M$。
    *   **自适应策略**：借鉴自适应有限元（AFEM）的“**求解(Solve) → 估计(Estimate) → 标记(Mark) → 细化(Refine)**”循环。
    *   **后验误差指示器**：定义了基于 PDE 残差、边界残差以及子域间界面“跳跃项”（Jump terms）的误差指示器 $\eta_j$。
    *   **Dörfler 标记**：根据误差贡献度选择需要加密的子域，实现动态网格细化。

### 3. 实验设计
论文设计了五个具有代表性的数值实验来验证理论与算法：
*   **场景 1：2D Helmholtz 方程**。用于验证理论：观察不同波数 $k$（影响光滑度）下，误差随采样参数 $M$ 变化的曲线。
*   **场景 2 & 3：2D/3D Poisson 方程（高斯尖峰解）**。解在中心具有极大的梯度，用于测试自适应 PoU 捕捉局部特征的能力。
*   **场景 4：L 型区域 Poisson 方程**。在原点处具有奇异性（正则性低），测试算法对非凸区域和奇异点的处理。
*   **场景 5：1D 粘性 Burgers 方程**。非线性时空问题，测试算法捕捉随时间演化的激波（Shock）的能力。
*   **Benchmark 与对比**：主要对比了不同 $M$ 取值下的误差、不同标记参数 $\theta$ 的收敛性，以及与非自适应方法的定性对比。

### 4. 资源与算力
*   **软件环境**：使用 MATLAB 进行仿真，线性最小二乘求解调用 `lsqminnorm`，非线性问题使用 Levenberg-Marquardt 算法。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或 CPU 核心数。由于 RaNNs 核心步骤是求解线性方程组，其对算力的需求远低于深度学习的梯度下降训练。
*   **训练时长**：未给出具体秒数，但强调了其作为随机方法的“快速”特性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了线性与非线性、稳态与瞬态、2D 与 3D、以及奇异性区域等多种工况，实验设计较为全面。
*   **充分性评价**：
    *   **理论验证充分**：通过 Helmholtz 实验清晰地展示了 $M$ 与光滑度的依赖关系，证实了定理 3.7。
    *   **自适应性验证客观**：展示了网格随解的特征自动加密的过程（如 Burgers 方程中网格随激波移动）。
    *   **局限性**：对比实验主要集中在自身参数的变动，较少直接与其他前沿方法（如自适应 PINNs 或传统高阶有限元）在相同精度下的计算耗时进行量化对比。

### 6. 论文的主要结论与发现
1.  **采样域规律**：RaNNs 的性能高度依赖于隐藏层参数的采样范围。对于不光滑的解，必须扩大采样域以包含更高频的基函数。
2.  **PoU 的等效性**：通过 PoU 细化网格在物理上等效于在局部扩大参数采样范围，这为自适应 RaNN 提供了理论支撑。
3.  **收敛特性**：自适应 PIRaNN 在 $L^2$ 和 $H^1$ 范数下表现出一致的收敛阶，且不会像传统有限元那样在求导时出现显著的阶数下降。
4.  **维数灾难缓解**：虽然理论界显示受维度 $d$ 影响，但对于具有足够光滑度的函数，RaNNs 仍能保持较好的逼近效率。

### 7. 优点（亮点）
*   **理论与算法高度统一**：并非盲目引入自适应机制，而是先通过数学证明确定了“采样域-光滑度”的关系，再据此设计 PoU 细化策略。
*   **计算效率高**：避免了 PINNs 繁琐的非凸优化过程，将 PDE 求解转化为高效的线性代数运算。
*   **局部特征捕捉精准**：结合后验误差估计，能够自动识别解的难点区域（如激波、奇异点）并进行资源倾斜。

### 8. 不足与局限
*   **精度瓶颈**：RaNNs 作为一种随机方法，其最终精度往往受限于线性系统的条件数。当神经元数量极多时，矩阵可能变得病态。
*   **参数依赖**：虽然实现了自适应，但仍需预设一些超参数（如初始 $M$、惩罚因子 $\lambda$、标记比例 $\theta$），这些参数对不同问题的敏感度未做深入讨论。
*   **高维扩展性**：虽然做了 3D 实验，但在更高维度（如 10D 以上）下，PoU 的网格划分会面临组合爆炸问题，该方法在超高维领域的适用性有待商榷。

（完）
