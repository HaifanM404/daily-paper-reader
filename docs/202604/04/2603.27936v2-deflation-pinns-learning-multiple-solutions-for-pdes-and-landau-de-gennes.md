---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: Deflation-PINNs：学习偏微分方程与朗道-德热纳模型的多解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v2"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于解决PDE问题的物理信息神经网络 (PINNs)
tldr: 针对物理信息神经网络（PINNs）难以求解非线性偏微分方程（PDEs）多解的问题，本文提出Deflation-PINNs框架。该方法结合了紧缩（deflation）损失函数与DeepONets架构，通过在损失函数中引入紧缩项，强制模型系统性地寻找并收敛到不同的解分支。在具有复杂能量景观的Landau-de Gennes液晶模型上的实验证明，该方法能有效识别并表征多种不同的晶体结构，并具有理论收敛性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v2/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统的PINNs设计初衷是寻找单一解，难以处理具有多个不同解分支的非线性PDE问题。
method: 提出Deflation-PINNs，通过在PINNs和DeepONets架构中引入紧缩损失项，系统性地排斥已发现的解以寻找新解。
result: 在Landau-de Gennes模型中成功识别并表征了多种不同的液晶平衡态结构。
conclusion: Deflation-PINNs为求解具有多解特性的复杂物理系统提供了一种有效且具有理论支撑的新工具。
---

## 摘要
非线性偏微分方程（PDEs）在数学物理和工程领域无处不在。尽管物理信息神经网络（PINNs）已成为求解偏微分方程问题的强大工具，但由于其设计初衷是每次寻找单个解，因此通常难以识别多个不同的解。为了解决这一局限性，我们引入了 Deflation-PINNs，这是一种将紧缩损失（deflation loss）与基于 PINNs 和深度算子网络（DeepONets）的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法系统地迫使 Deflation-PINN 寻找并收敛于多个不同的有限解分支。我们提供了关于模型收敛性的理论证据，并通过对液晶朗道-德热纳模型（该系统以其复杂的能量景观和多个平衡态而闻名）的数值实验证明了 Deflation-PINNs 的有效性。结果表明，Deflation-PINNs 能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

### 论文总结：Deflation-PINNs：学习偏微分方程与朗道-德热纳模型的多解

#### 1. 核心问题与整体含义（研究动机和背景）
非线性偏微分方程（PDEs）在物理和工程中常表现出**多稳态**特征，即在相同的边界条件下存在多个不同的解分支。传统的物理信息神经网络（PINNs）虽然强大，但其损失函数设计通常引导模型收敛至单一解，难以系统性地捕获所有潜在的平衡态。现有的方法（如多次随机初始化）存在冗余且缺乏理论保证。本文的研究动机是开发一种能够**一次性、无监督地学习并区分有限个离散解**的新型框架，特别针对具有复杂能量景观的 Landau-de Gennes (LdG) 液晶模型。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Deflation-PINNs** 框架，其核心由三部分组成：
*   **简化 DeepONet 架构**：
    *   借鉴 DeepONet 的分支网络（Branch Net）和主干网络（Trunk Net）结构。
    *   不同于学习算子映射，该架构将分支网络简化为 $K$ 个可训练的**分支权重向量（Branch Weights）** $\beta_k$，每个向量代表一个潜在的解分支。主干网络 $\tau(x)$ 则负责学习空间基函数。
    *   最终输出形式为：$\tilde{G}(k, x) = \sum_{i=1}^p \tau_i(x) \cdot \beta_{ki}$。
*   **紧缩损失函数（Deflation Loss）**：
    *   为了防止 $K$ 个分支收敛到同一个解，引入了紧缩项 $L_{Def}$。
    *   该项通过惩罚不同分支之间的 $L2$ 距离来强制解的分离。公式逻辑为：若两个分支的距离小于预设阈值 $d_{min}$，则产生高额损失；若距离超过 $d_{min}$，损失为零。
*   **硬约束边界条件（Hard Constraints）**：
    *   利用**径向边界距离函数（RBD function）**构造了一种特殊的架构，使得神经网络输出在结构上强制满足 Dirichlet 边界条件，从而消除了损失函数中边界项的权重调优问题。
*   **理论支撑**：论文证明了该架构在连续函数空间内具有通用近似能力（基于 Chen & Chen 定理的推论）。

#### 3. 实验设计
*   **实验场景**：二维 Landau-de Gennes (LdG) 液晶模型。该模型描述了液晶在正方形区域内的排列，已知在特定参数下存在 6 个稳定的平衡态（2 个对角态 D1/D2，4 个旋转态 R1/R2/R3/R4）。
*   **Benchmark（基准）**：使用传统的**有限元方法（FEM）**计算出的数值解作为金标准。
*   **对比逻辑**：验证 Deflation-PINNs 是否能同时识别出这 6 个不同的物理结构，并对比其向量场分布与 FEM 结果的一致性。

#### 4. 资源与算力
*   **硬件**：使用了一张 **NVIDIA RTX 3060-Ti GPU** (8GB VRAM)。
*   **训练细节**：
    *   使用 Adam 优化器训练 10,000 个 epoch。
    *   初始学习率为 $1 \times 10^{-3}$。
    *   主干网络宽度为 4000，深度为 1 层（带残差连接），激活函数为 tanh。
    *   配置了 $p=16$ 维的表示向量。

#### 5. 实验数量与充分性
*   **实验规模**：论文重点展示了针对 LdG 模型的完整求解过程，成功捕获了全部 6 个稳定解。
*   **充分性评价**：
    *   **客观性**：通过与成熟的 FEM 方法进行可视化对比，证明了结果的物理准确性。
    *   **局限性**：实验主要集中在 LdG 这一特定基准问题上，虽然该问题极具代表性且难度较高，但缺乏在其他多解 PDE（如多稳态反应扩散方程）上的泛化性测试。
    *   **消融/参数敏感性**：文中提到了超参数 $d_{min}$ 的经验选取过程，但未提供详细的参数敏感性分析图表。

#### 6. 主要结论与发现
*   **多解捕获能力**：Deflation-PINNs 能够成功且系统地识别出 LdG 模型的所有 6 个稳定解，无需依赖幸运的随机初始化。
*   **架构优势**：简化的 DeepONet 架构比标准 DeepONet 更高效（参数更少），且比传统的集成 PINNs（Ensemble PINNs）更具确定性。
*   **硬约束有效性**：通过 RBD 函数引入的硬约束显著降低了优化难度，确保了边界处的物理一致性。

#### 7. 优点（亮点）
*   **系统性**：将“寻找新解”这一目标直接编码进损失函数，避免了盲目搜索。
*   **理论与实践结合**：不仅提出了算法，还给出了关于硬约束模型近似能力的数学证明。
*   **高效性**：分支权重向量的设计极大地压缩了表示多个解所需的参数量。

#### 8. 不足与局限
*   **超参数依赖**：核心参数 $d_{min}$（解之间的最小距离）需要预验知识或反复试错，若设置不当可能导致无法找到所有解或模型不收敛。
*   **解的数量预设**：模型需要预先设定解的数量 $K$。在实际应用中，很多 PDE 的解分支数量是未知的。
*   **计算开销**：虽然参数量优化了，但计算 $L_{Def}$ 需要在所有分支对之间进行 $L2$ 范数计算，当 $K$ 较大时，计算复杂度会呈平方级增长。

（完）
