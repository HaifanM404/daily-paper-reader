---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩PINN：学习偏微分方程与Landau-de Gennes模型的多重解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v2"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于学习偏微分方程多解的 Deflation-PINNs
tldr: 本研究针对传统物理信息神经网络（PINNs）难以识别偏微分方程（PDE）多个解的局限性，提出Deflation-PINNs框架。该框架将消去损失项与基于PINNs和深度算子网络（DeepONets）的架构相结合，通过损失函数中的消去项系统性地寻找多个不同解分支。论文以液晶Landau-de Gennes模型为例进行数值实验，成功识别了多种晶体结构，证明了该方法在复杂能量景观中处理多解问题的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v2/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v2/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统PINNs难以识别PDE的多个解，需要新方法处理多解问题。
method: 通过引入消去损失项与PINNs和DeepONets结合的架构，系统性地寻找多个解分支。
result: 数值实验表明Deflation-PINNs能成功识别液晶Landau-de Gennes模型中的多个不同解。
conclusion: 该框架成功识别了液晶模型中的多种晶体结构，验证了其在复杂能量景观中的有效性。
---

## 摘要
非线性偏微分方程在数学物理和工程领域无处不在。尽管物理信息神经网络已成为求解偏微分方程问题的有力工具，但由于其设计为一次仅寻找一个解，通常难以识别多个不同的解。为克服这一局限，我们提出了Deflation-PINNs——一种将紧缩损失与基于PINNs及深度算子网络架构相结合的新框架。通过在损失函数中加入紧缩项，我们的方法能系统地迫使Deflation-PINN寻找并收敛于有限多个不同的解分支。我们提供了模型收敛的理论依据，并通过液晶Landau-de Gennes模型的数值实验验证了Deflation-PINNs的有效性；该模型以其复杂的能量景观和多重平衡态而著称。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》进行结构化、深入且客观的总结。

---

### **论文总结：Deflation-PINNs**

#### **1. 核心问题与整体含义**
*   **研究动机**：传统的物理信息神经网络（PINNs）在求解偏微分方程（PDE）时，通常被优化以收敛到单一解。然而，许多非线性PDE（如描述液晶的Landau-de Gennes模型）存在多个稳定解（即多个能量局部极小点）。传统PINN框架难以系统地发现所有这些不同的解分支。
*   **研究目标**：提出一种新的、无监督的机器学习框架，能够同时、系统地识别和逼近一个PDE问题的**有限个不同解**，克服传统PINN和随机初始化方法的局限性。

#### **2. 方法论：核心思想与关键技术**
*   **核心思想**：将“紧缩”思想引入PINN框架。通过修改损失函数和网络架构，迫使模型在训练过程中避免已找到的解，从而探索并收敛到新的、不同的解。
*   **关键技术细节**：
    1.  **架构设计（简化DeepONet）**：
        *   借鉴深度算子网络（DeepONet）的结构，但进行简化以专门学习有限个解。
        *   模型形式为：`Ũ(k, x) = Σ τ_i(x) · β_k^i`。其中 `τ_i(x)` 是共享的“主干网络”，`β_k` 是第 `k` 个解对应的可训练“分支权重”向量。这种设计用 `K·p` 个参数直接编码 `K` 个解，比标准DeepONet更高效。
    2.  **硬约束处理边界条件**：
        *   使用径向边界距离函数等方法构造辅助函数 `ω(x)` 和 `b(x)`，将Dirichlet边界条件硬编码到网络输出中：`G(x) = ω(x) * NN(x) + b(x)`。这消除了损失函数中的边界项，简化了优化。
    3.  **紧缩损失函数**：
        *   定义关键的超参数 `d_min`（期望的解间最小距离）。
        *   紧缩损失定义为：所有成对解的L2距离低于 `d_min` 时的惩罚项之和。公式为：
          ```
          L_Def(Ũ) = (2/(K*(K-1))) Σ_{i<j} max(1 - ||Ũ(i,·) - Ũ(j,·)||_L2 / d_min, 0)
          ```
        *   当所有近似解的成对距离都大于等于 `d_min` 时，此项为零。
    4.  **总损失函数**：
        *   `L_total = α * Σ EG(Ũ(i,·)) + β * L_Def(Ũ)`。
        *   其中 `EG` 是标准的PIML残差损失（仅包含控制方程残差），确保每个输出都满足PDE；`L_Def` 确保各输出彼此不同。

#### **3. 实验设计**
*   **主要场景/问题**：二维简化Landau-de Gennes模型。这是一个描述液晶取向的变分问题，其Euler-Lagrange方程是一个非线性椭圆型PDE系统。在正方形域上存在6个已知的稳定解（2个对角态D1/D2和4个旋转态R1-R4）。
*   **Benchmark与对比方法**：
    *   **主要对比基准**：有限元方法（FEM）的计算结果（引用自Maity et al., 2021b）。这是该领域公认的高精度数值方法。
    *   **相关方法讨论（未直接对比数值）**：
        *   Homotopy PINNs (HomPINNs)：半监督方法，需要观测数据。
        *   随机初始化多PINNs：并行训练多个独立PINN，依赖随机性发现不同解，存在冗余风险。
*   **评估指标**：通过可视化向量场图定性比较Deflation-PINNs与FEM得到的解的结构一致性。

#### **4. 资源与算力**
文中明确说明：
*   **硬件**：使用单张RTX-3060 Ti GPU (8GB VRAM)。
*   **训练时长/规模**：使用Adam优化器训练了10,000个epoch。

#### **5. 实验数量与充分性**
*   **实验数量较少但聚焦深入**：
    1.  在一个核心的、具有代表性的物理模型（Landau-de Gennes）上进行了验证。
    2.  成功复现了该模型已知的全部6个稳定解结构。
    3.  通过与高精度FEM结果的直接可视化对比，证明了所发现解的物理正确性。
*   **客观性与公平性评估**：
    *   ✅优点：选择了一个公认的、具有复杂多解特性的标准测试问题；与权威的FEM结果进行定性比较是合理的；开源了代码以确保可复现性。
    *   ❌局限性/不足：
        1. 缺乏与其他基于神经网络的多元法（如HomPINNs或随机初始化集成）在收敛效率、成功率或计算成本上的定量比较。
        2. “消融实验”不完整。虽然讨论了硬约束和紧缩损失的作用，但未展示移除其中任一组件后的性能退化情况（例如是否只能找到一个解或解的质量下降）。
        3. `d_min=0.4`这一关键超参数是通过经验试错选择的，缺乏理论指导或鲁棒性分析。

#### **6. 主要结论与发现**
*   提出的Deflation-PINNs框架能够成功、系统地识别出Landau-de Gennes模型中所有6个已知的稳定液晶结构（D1, D2, R1-R4）。
*   该方法不依赖于多次随机初始化或外部数据引导，而是通过精心设计的损失函数和网络架构，“主动”且“同时”地寻找多个不同的解分支。
*   理论分析表明该简化架构仍保持通用逼近性质。

#### **7. 优点**
1. 创新性地将数值分析中的“紧缩”概念引入基于神经网络的PDE求解器设计思路清晰有效
2. “主干-分支”架构设计巧妙结合了DeepONet的思想但针对有限多解的特定问题进行简化提高了参数效率
3. “硬约束”处理边界条件的方法避免了多目标损失中权重调优的难题
4. “无监督”特性完全依赖物理方程无需额外观测数据

#### .8不足与局限**
1.**泛化能力验证不足**:仅在单一且特定的Landau-de Gennes问题上进行了验证未在其他类型的多解PDE问题上测试方法的普适性存疑
2.**超参数敏感性**:紧缩损失中的关键超参数 dmin需要手动调整且文中未提供系统的选取策略这在实际应用中可能带来困难
3.**计算效率未充分论证**:虽然声称比标准DeepONet更高效但未与传统“训练多个独立PINN”的方法进行详细的运行时间内存消耗或成功率方面的定量比较
4.**理论收敛保证有限**:提供的理论主要是关于网络表达能力的通用逼近定理而非优化过程能保证找到所有解的严格理论对于复杂的非凸优化问题训练的鲁棒性和可重复性仍需更多实证研究
5.**仅限于稳态/平衡态问题**:当前框架旨在寻找稳态方程的多个平衡态对于如何应用于时间依赖性问题或分岔问题的连续解分支追踪尚未涉及

完

好的，我将从您提供的上一次输出中断处继续补全。根据您提供的文本，上一次总结在“**8.不足与局限**”部分结束，但缺少了最后的“（完）”标记。我将从该部分之后开始，补充论文中可能涉及但未在您提供的总结中充分展开的其他关键方面，并最终完成总结。

---

#### **9. 潜在影响与未来方向**
*   **对PIML领域的贡献**：
    *   为物理信息机器学习（PIML）领域提供了一个新的、专门用于解决多解问题的工具范式。
    *   展示了将经典数值分析思想（紧缩法）与神经网络架构设计相结合的有效路径，为开发更专业的PDE求解器提供了思路。
*   **对未来工作的启示**：
    1.  **扩展到更复杂问题**：自然的下一步是将该方法应用于其他具有多稳态或分岔行为的物理系统，如流体力学中的涡旋结构、材料科学中的相变问题等。
    2.  **自动化超参数选择**：研究如何自适应地确定 `d_min` 等关键超参数，例如通过分析解空间的先验知识或设计启发式算法。
    3.  **与分岔分析结合**：一个极具潜力的方向是将Deflation-PINNs与延拓法结合，用于自动追踪解分支随参数变化的路径，实现真正的分岔分析。
    4.  **处理更高维或无限维解空间**：当前方法预设了寻找的解的数量 `K`。未来可探索如何将其推广到未知数量解的情况，或处理连续谱的解。

#### **10. 总体评价**
本文提出了一种新颖且构思巧妙的机器学习框架——Deflation-PINNs，旨在解决非线性PDE多解发现这一具有挑战性的问题。其核心贡献在于成功地将数值分析中的“紧缩”思想无缝集成到PINN的损失函数和网络架构设计中，从而能够无监督地、同时逼近多个不同的稳定解。

论文在方法论上的设计（如简化的主干-分支架构、硬边界约束）显示出对问题本质的深刻理解。通过在经典的Landau-de Gennes液晶模型上的成功演示，有力地证明了该方法的可行性和有效性。

然而，该工作目前仍是一项“原理验证”性质的研究。其评估主要依赖于单一物理案例的定性对比，在方法的**普适性、鲁棒性、计算效率以及与现有替代方案的定量比较**方面存在明显空白。这些局限性也为后续研究指明了清晰的方向。

总体而言，Deflation-PINNs是一个有前途的起点，为基于机器学习的多物理场模拟和分岔分析开辟了一条新的途径。其实用价值的最终确立，有待于在更广泛、更复杂的场景中得到进一步验证和完善。

（完）
