---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩-物理信息神经网络：学习偏微分方程与Landau-de Gennes模型的多重解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 将紧缩损失与物理信息神经网络及算子网络结合求解偏微分方程
tldr: 非线性偏微分方程在数学物理和工程中普遍存在，但传统物理信息神经网络通常难以识别多个不同的解。为此，研究提出Deflation-PINNs框架，通过将通缩损失与基于PINNs和深度算子网络的架构相结合，系统性地迫使模型寻找并收敛到多个不同的解分支。理论分析和在液晶Landau-de Gennes模型上的数值实验证明了该方法的有效性，能够成功识别和表征多个不同的晶体结构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统PINNs难以识别PDE的多个不同解。
method: 引入基于PINNs和DeepONets的架构，并在损失函数中加入通缩项。
result: 数值实验表明该方法能成功找到多个解分支。
conclusion: Deflation-PINNs框架能有效识别和表征多个不同的晶体结构。
---

## 摘要
非线性偏微分方程在数学物理和工程领域无处不在。尽管物理信息神经网络已成为求解偏微分方程问题的有力工具，但它们通常难以识别多个不同的解，因为其设计初衷是每次寻找一个解。为克服这一局限，我们提出了Deflation-PINNs——一种将紧缩损失项与基于PINNs及深度算子网络架构相结合的新框架。通过在损失函数中加入紧缩项，我们的方法能系统性地迫使Deflation-PINN寻找并收敛于有限多个不同的解分支。我们提供了模型收敛的理论证明，并通过在液晶Landau-de Gennes模型上的数值实验验证了Deflation-PINNs的有效性；该体系以其复杂的能量景观和多重平衡态而著称。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》的论文进行结构化、深入且客观的总结。

---

### **1. 核心问题与整体含义**
*   **研究动机**：非线性偏微分方程（PDE）通常存在多个解（如多稳态系统），这些解对应不同的物理状态。传统的物理信息神经网络（PINNs）旨在寻找单个解，其优化过程容易收敛到某个局部最小值，而无法系统地探索和识别所有可能的解分支。
*   **研究目标**：提出一种能够**一次性、系统地发现并区分PDE多个不同解**的神经网络框架，以克服传统PINNs在此类问题上的局限性。

### **2. 提出的方法论**
*   **核心思想**：将“紧缩”思想与PINN框架相结合。通过修改网络架构和损失函数，迫使模型同时学习多个互不相同的解。
*   **关键技术细节**：
    1.  **架构设计（基于DeepONet的简化）**：
        *   传统DeepONet学习从输入函数到输出函数的算子，过于通用。本文将其简化为一个能同时输出K个解的模型。
        *   模型形式为：`Ũ(k, x) = Σ τ_i(x) · β_k^i`。其中 `τ_i(x)` 是一个共享的“主干网络”（Trunk Net），`β_k` 是第k个解对应的可训练“分支权重”向量。这种设计用 `K·p` 个参数直接编码K个不同的解。
    2.  **紧缩损失函数**：
        *   在标准PINN损失（保证每个输出满足PDE）基础上，增加一个“紧缩损失”项 `L_Def`。
        *   `L_Def` 惩罚那些在L2范数下过于接近的解对，强制不同输出之间保持至少 `d_min` 的距离。公式为各解对距离不足部分的惩罚之和。
    3.  **硬边界约束处理**：
        *   采用径向边界距离函数等方法构造满足精确狄利克雷边界条件的网络输出形式，从而将边界条件作为硬约束嵌入网络架构，简化了损失函数（无需边界损失项），提高了训练稳定性。
*   **算法流程**：
    1.  构建具有硬边界约束的简化DeepONet架构，设置要寻找的解的数量K。
    2.  定义总损失函数：`L_total = α * Σ EG(Ũ(k,·)) + β * L_Def(Ũ)`，其中EG为PDE残差项。
    3.  使用优化器（如Adam）最小化总损失，同时优化主干网络参数和所有分支权重向量。

### **3. 实验设计**
*   **主要场景/基准问题**：二维简化Landau-de Gennes (LdG)液晶模型。这是一个著名的具有多个稳定平衡态（晶体结构）的非线性PDE系统。在正方形域上已知存在6个稳定解：2个对角态(D1, D2)和4个旋转态(R1-R4)。
*   **Benchmark对比方法**：有限元方法（FEM）。文中引用了Maity等人(2021b)使用FEM计算出的结果作为基准真值进行对比。
*   **实验内容**：展示了Deflation-PINN能够成功找到全部6个稳定解（D1, D2, R1-R4），并将得到的向量场图与FEM结果进行视觉对比（见图2、图4），显示了高度一致性。

### **4. 资源与算力**
论文在“可复现性声明”一节中明确提到：
*   **硬件**：单张 NVIDIA RTX 3060-Ti GPU (8GB VRAM)。
*   **训练时长/规模**：使用Adam优化器训练了10,000个epoch。这表明该方法在消费级显卡上即可实现，算力需求相对亲民。

### **5. 实验数量与充分性**
*   **实验组数**：主要展示了一组核心实验——在特定参数(`ε=0.02`)和边界条件下的LdG模型上寻找6个解。文中提到了通过经验测试调整超参数 `d_min` (最终设为0.4)和损失权重(`α=0.02`, `β=2`)的过程。
*   **充分性与客观性评估**：
    *   **优点**：选择了该领域公认的、具有明确已知多解的经典难题作为验证，并与高精度数值方法(FEM)的结果进行直接对比，说服力强。
    *   **局限/不足**：
        1.  **场景单一**：所有数值实验均围绕同一个LdG模型展开。虽然这是一个有力的证明案例，但缺乏在其他类型多解PDE问题上的广泛测试（如不同的方程类型、域形状、边界条件）。
        2.  **缺乏定量误差对比**：主要提供了解的定性可视化对比，未给出与FEM解的定量误差指标（如L2误差）。
        3.  **消融实验不完整**：虽然提及了调整 `d_min` ，但未系统展示不同架构选择、不同紧缩策略或与传统“随机初始化+多次运行”方法的全面性能比较。

### **6. 主要结论与发现**
1.  Deflation-PINNs框架能够有效、系统地发现非线性PDE的多个不同稳态解。
2.  通过结合简化的DeepONet架构和紧缩损失项，该方法可以一次性学习所有目标解分支，避免了传统PINNs需要多次独立运行且结果不确定的问题。
3.  理论分析证明了该框架在通用近似意义下的收敛性。
4.  在复杂的Landau-de Gennes液晶模型上的实验成功复现了全部6个已知的稳定分子排列结构。

### **7. 优点**
1.  **创新性融合**：创造性地将数值分析中的“紧缩”思想引入基于神经网络的PDE求解器设计之中。
2.  **架构高效性**：对通用DeepONet进行针对性简化，使其专门用于有限多函数逼近，减少了参数量和不必要的复杂性。
3.  **系统性求解**：提供了一种系统性发现多解的范式，相比依赖随机初始化的方法更具确定性和效率潜力。
4.  **理论支撑扎实**:不仅提出方法,还提供了关于硬约束近似能力和模型通用性的理论证明(Corollary)，增强了工作的严谨性。

### .8不足与局限
1.**应用范围待验证**:目前仅在单一类型的物理问题上得到验证其普适性在其他更广泛或更复杂的多解PDE问题中的表现尚属未知
2.**超参数敏感性**:紧缩距离 d_min是一个关键的超参数需要根据先验知识或经验调整论文提到通过试错法确定这在实际应用中可能带来不便
3.**计算成本权衡**:虽然一次性学习多个看似高效但网络规模和总损失函数的复杂性可能随求解数量 K增加而增长对于极多分支的问题其效率优势可能需要重新评估
4.**对初始化的潜在依赖**:尽管引入了紧缩机制但神经网络的训练仍可能受初始化和优化过程影响理论上不能绝对保证找到全部且不重复的解尤其是在复杂的高维能量景观中

完

好的，我将从您提供的“不足与局限”部分继续，并补充论文的其他重要方面。

---

### **8. 不足与局限**
1.  **应用范围待验证**：目前仅在单一类型的物理问题上得到验证。其普适性在其他更广泛或更复杂的多解PDE问题（如流体力学中的多稳态流场、量子力学中的激发态）中的表现尚属未知。
2.  **超参数敏感性**：紧缩距离 `d_min` 是一个关键的超参数，需要根据先验知识或经验调整。论文提到通过试错法确定，这在实际应用中可能带来不便。
3.  **计算成本权衡**：虽然一次性学习多个解看似高效，但网络规模和总损失函数的复杂性可能随求解数量 `K` 增加而增长。对于极多分支的问题，其效率优势可能需要重新评估。
4.  **对初始化的潜在依赖**：尽管引入了紧缩机制，但神经网络的训练仍可能受初始化和优化过程影响。理论上不能绝对保证找到全部且不重复的解，尤其是在复杂的高维能量景观中。

### **9. 潜在影响与未来方向**
*   **对PINN领域的贡献**：为求解具有多稳态、分岔等特性的复杂PDE提供了一种新的、系统性的神经网络范式。它推动了PINNs从“寻找一个解”向“探索解空间”的转变。
*   **对科学计算的影响**：将传统数值分析中的紧缩法与新兴的深度学习相结合，展示了领域知识（如算子结构、边界处理）在提升神经网络方法鲁棒性和效率方面的关键作用。
*   **未来工作展望**：
    1.  **扩展到更广泛的问题**：在更多类型的非线性PDE（如Navier-Stokes方程、Schrödinger方程）上测试该框架的有效性。
    2.  **自适应确定解的数量K**：当前方法需要预先指定要寻找的解的数量 `K`。未来的改进方向是开发能够自动发现和计数稳定解分支的算法。
    3.  **处理连续谱和解流形**：当前方法针对离散的多个稳态解。对于具有连续对称性或无穷维解流形的问题（如行波解），需要新的架构和损失设计。
    4.  **与不确定性量化结合**：探索如何将解的多样性估计与PINNs中的不确定性量化框架相结合。

### **10. 总结评价**
本文提出了一种新颖且构思巧妙的神经网络框架——Deflation-PINNs，成功解决了传统PINNs在求解多解PDE时的一个核心痛点。其核心贡献在于通过一个经过简化的网络架构和一个精心设计的紧缩损失函数，将多个不同的物理状态同时编码在一个模型中并进行协同优化。

该方法在经典的Landau-de Gennes液晶模型上取得了令人信服的成果，成功复现了所有已知稳定态，并与高精度有限元结果高度一致。论文的理论分析为其架构的有效性提供了支撑。

尽管目前的应用验证集中于单一物理模型，且存在对超参数敏感等局限性，但其思想具有启发性。它为利用深度学习系统性地探索复杂物理系统的相空间或能量景观开辟了一条有前景的道路。这项工作代表了科学机器学习领域向解决更具挑战性、更贴近实际物理现象问题迈出的坚实一步。

（完）
