---
title: "HS-FNO: History-Space Fourier Neural Operator for Non-Markovian Partial Differential Equations"
title_zh: HS-FNO：用于非马尔可夫偏微分方程的历史空间傅里叶神经算子
authors: Lennon J. Shikhman
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09523v2"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于非马尔可夫 PDE 的傅里叶神经算子
tldr: 针对非马尔可夫偏微分方程（如时滞和记忆系统），传统神经算子因假设瞬时状态完整而失效。本文提出历史空间傅里叶神经算子（HS-FNO），通过提升状态空间表征历史信息。该方法将状态更新分解为可学习的未来预测和精确的历史平移追加，有效降低了学习维度并引入了归纳偏置。在延迟反应扩散、流行病学等五类基准测试中，HS-FNO在自回归预测精度上显著优于现有基准，且参数量更少。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09523v2/fig-001.webp\", \"caption\": \"Figure 3: Mean rollout error over autoregressive prediction steps. HS-FNO has the lowest error at every rollout step among the main models.\", \"page\": 10, \"index\": 1, \"width\": 1056, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2605.09523v2/fig-002.webp\", \"caption\": \"Figure 2: Aggregate relative errors across benchmark–regime cells. Bars show ten-seed means and error bars show 95% bootstrap confidence intervals. Lower is better.\", \"page\": 9, \"index\": 2, \"width\": 916, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2605.09523v2/fig-003.webp\", \"caption\": \"Figure 1: HS-FNO architecture. The predictor PΘ receives the current history field ut(θ, x) together with conditioning variables (µ, τ,∆t) and predicts the newly exposed future slice û(t + ∆t, ·). The known portion of the history is transported exactly, and ShiftAppend combines this with the predicted slice to form the updated history GΘ(ut).\", \"page\": 4, \"index\": 3, \"width\": 976, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2605.09523v2/fig-004.webp\", \"caption\": \"Figure 4: Real-world traffic sanity check under the standard 12-input/12-output protocol. Bars show denormalized average MAE on METR-LA and PEMS-BAY; lower is better\", \"page\": 11, \"index\": 4, \"width\": 932, \"height\": 307}]"
motivation: 传统的神经算子假设系统具有马尔可夫性，无法处理具有时滞或记忆效应的非马尔可夫偏微分方程。
method: 提出HS-FNO架构，将历史状态更新分解为对未来切片的学习预测和对已知历史窗口的精确平移追加。
result: 在五类非马尔可夫PDE基准测试中，HS-FNO在单步和长期自回归预测误差上均达到最低，显著提升了预测稳定性。
conclusion: 强制执行历史状态演化的离散平移结构是构建非马尔可夫PDE代理模型的有效归纳偏置。
---

## 摘要
神经算子为随时间变化的偏微分方程（PDE）提供了快速的代理模型，但其标准的自回归应用通常假设瞬时场 $u(t,\cdot)$ 是一个完整状态。这一假设在时滞方程、分布式记忆系统和其他非马尔可夫动力学中失效：两条轨迹可能在时间 $t$ 处一致，但由于其历史不同，仍具有不同的未来演化。我们引入了历史空间傅里叶神经算子（HS-FNO），这是一种针对时滞和记忆驱动的 PDE 的神经算子，其构建在提升状态 $u_t(θ,x)=u(t+θ,x)$, $θ\in[-τ,0]$ 之上。关键的计算步骤是将一次历史状态更新分解为：针对新暴露的未来切片的学习预测器，以及针对前一状态已知历史窗口部分的精确移位-追加（shift-append）传输。这避免了学习确定性的历史坐标，减少了学习的输出维度，并强制执行了自然的离散历史更新。我们在五个基准系列上测试了 HS-FNO，涵盖了时滞反应-扩散、空间流行病学、非局部神经场动力学、时滞波和分布式记忆闭合。在十个随机种子中，HS-FNO 在主要基准模型中获得了最低的综合单步、历史空间和外推（rollout）误差。最大的提升出现在自回归预测中，综合外推误差从当前状态算子、滞后堆叠算子和无约束历史到历史算子的 0.241、0.188 和 0.185 分别下降到 0.094。该模型使用的参数也比无约束历史预测更少。这些结果表明，强制执行历史状态演化的离散移位结构是非马尔可夫 PDE 代理建模的一种有效归纳偏置。

## Abstract
Neural operators provide fast surrogate models for time-dependent partial differential equations, but their standard autoregressive use usually assumes that the instantaneous field $u(t,\cdot)$ is a complete state. This assumption fails for delay equations, distributed-memory systems, and other non-Markovian dynamics: two trajectories may agree at time $t$ and nevertheless have different futures because their histories differ. We introduce the History-Space Fourier Neural Operator (HS-FNO), a neural operator for delay and memory-driven PDEs formulated on the lifted state $u_t(θ,x)=u(t+θ,x)$, $θ\in[-τ,0]$. The key computational step is to decompose one history-state update into a learned predictor for the newly exposed future slice and an exact shift-append transport for the portion of the history window already known from the previous state. This avoids learning deterministic history coordinates, reduces the learned output dimension, and enforces the natural discrete history update. We test HS-FNO on five benchmark families covering delayed reaction--diffusion, spatial epidemiology, nonlocal neural-field dynamics, delayed waves, and distributed-memory closures. Across ten random seeds, HS-FNO attains the lowest aggregate one-step, history-space, and rollout errors among the principal baselines. The largest gain occurs in autoregressive prediction, where aggregate rollout error decreases from $0.241$, $0.188$, and $0.185$ for current-state, lag-stack, and unconstrained history-to-history operators, respectively, to $0.094$. The same model uses fewer parameters than unconstrained history prediction. These results indicate that enforcing the discrete shift structure of history-state evolution is an effective inductive bias for non-Markovian PDE surrogate modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《HS-FNO: History-Space Fourier Neural Operator for Non-Markovian Partial Differential Equations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的神经算子（如 FNO）在处理随时间演化的 PDE 时，通常假设系统具有**马尔可夫性**，即未来的状态仅取决于当前的瞬时场 $u(t, \cdot)$。然而，在生物动力学（时滞反应扩散）、化学动力学、控制系统以及粗粒化湍流建模（Mori–Zwanzig 还原）中，系统往往具有**非马尔可夫性**（时滞或记忆效应）。在这种情况下，两个在 $t$ 时刻完全相同的场，可能因为历史轨迹不同而产生截然不同的未来。论文的核心问题是如何为这类非马尔可夫 PDE 构建有效的神经算子代理模型，解决状态表征不完整导致的长程预测失效问题。

### 2. 方法论：HS-FNO 核心思想与技术细节
论文提出了**历史空间傅里叶神经算子（HS-FNO）**，其核心思想是将状态空间从瞬时场提升到“历史段”空间。
*   **提升状态表征**：定义状态为 $u_t(\theta, x) = u(t + \theta, x)$，其中 $\theta \in [-\tau, 0]$。这意味着模型处理的是一个包含时间维度和空间维度的“时空切片”。
*   **算子分解（核心创新）**：HS-FNO 不直接学习“历史到历史”的完整映射，而是将更新过程分解为两部分：
    1.  **学习预测器（Predictor）**：使用 FNO 骨干网络仅预测新暴露的未来切片 $\hat{u}(t + \Delta t, \cdot)$。
    2.  **确定性平移追加（Shift-Append）**：对于历史窗口中已知的旧数据，直接进行精确的离散平移，然后将预测的新切片追加到末尾。
*   **归纳偏置**：这种设计强制执行了物理上的历史演化逻辑，避免了神经网络浪费参数去学习简单的“数据平移”操作，从而降低了输出维度和学习难度。

### 3. 实验设计
*   **五大基准问题**：
    1.  **时滞反应-扩散**：模拟种群动力学中的成熟延迟。
    2.  **空间流行病学**：具有观察滞后或感染潜伏期的传染病模型。
    3.  **非局部神经场**：具有空间依赖性延迟的神经元耦合。
    4.  **时滞波动方程**：测试相位准确性和长程稳定性。
    5.  **分布式记忆动力学**：模拟具有记忆核的非局部时间闭合项。
*   **对比方法**：
    *   **Current-state**：仅输入当前时刻场的标准 FNO。
    *   **Lag-stack**：将过去几个时刻的场作为额外通道输入的 FNO。
    *   **History2History (H2H)**：输入历史并直接预测完整未来历史的无约束模型。
    *   **序列模型**：包括 ConvLSTM、Temporal U-Net 和 Transformer。
*   **评估指标**：单步误差、历史空间误差、多步自回归外推（Rollout）误差。

### 4. 资源与算力
*   **硬件支持**：文中提到获得了 Dell Technologies 提供的 Dell Pro Precision 硬件支持。
*   **具体细节**：论文**未明确列出**具体的 GPU 型号、数量以及总训练时长。但提供了推理时间（约 0.0018s/step）和内存占用（约 522MB）的对比数据，显示其在计算效率上具有竞争力。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 5 类不同的物理方程，每类方程都在 10 个随机种子下运行以确保统计显著性。
*   **充分性**：
    *   **多维度评估**：不仅测试了分布内性能，还测试了**跨延迟范围（Held-out delay）**、**跨参数范围**以及**空间分辨率转移**的泛化能力。
    *   **消融实验**：对 Shift-Append 结构、延迟调节（Conditioning）方式、骨干网络（FNO vs U-Net vs Transformer）进行了详细消融。
    *   **现实数据验证**：在 METR-LA 和 PEMS-BAY 交通流量数据集上进行了“现实世界健全性检查”。
*   **客观性**：使用了统一的训练/验证/测试集划分，并提供了 95% 置信区间，实验设计较为严谨客观。

### 6 主要结论与发现
*   **性能领先**：HS-FNO 在所有基准测试中均获得了最低的综合误差。特别是在自回归外推中，误差从基准模型的 0.185-0.241 大幅下降至 **0.094**。
*   **结构优势**：实验证明，仅仅提供历史信息（如 Lag-stack）是不够的，**强制执行平移追加结构**（Shift-Append）对于保持长程预测的稳定性至关重要。
*   **效率提升**：相比于直接预测整个历史窗口的 H2H 模型，HS-FNO 的参数量更少（约 1/30），且精度更高。

### 7. 优点
*   **物理启发式架构**：将时滞系统的数学本质（算子半群理论）转化为神经网络的归纳偏置，设计优雅且高效。
*   **解决维度灾难**：通过只预测未来切片，显著降低了算子学习的输出空间维度。
*   **鲁棒性强**：在长程自回归预测中表现出极佳的稳定性，有效抑制了误差随时间的指数级累积。

### 8. 不足与局限
*   **分辨率转移挑战**：尽管在同分辨率下表现优异，但在**跨空间分辨率转移**实验中，HS-FNO 的表现仍有待提高，说明历史空间的归纳偏置未能完全解决算子的泛化难题。
*   **超参数依赖**：模型性能依赖于预设的历史窗口长度 $\tau$ 和采样频率，对于具有极长记忆或变时滞的系统，存储开销可能会增加。
*   **复杂时滞处理**：对于状态依赖型时滞（State-dependent delay）或复杂的非线性空间变时滞，简单的平移追加可能需要更复杂的插值机制。

（完）
