---
title: Can We Formally Verify Neural PDE Surrogates? SMT Compilation of Small Fourier Neural Operators
title_zh: 我们能否形式化验证神经 PDE 代理模型？小型傅里叶神经算子的 SMT 编译
authors: "Ali Baheri, David Millard, Ignacio Laguna Peralta"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08938v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 偏微分方程傅里叶神经算子的形式化验证
tldr: 本研究探讨了傅里叶神经算子（FNO）在偏微分方程模拟中的形式化验证问题。针对FNO缺乏物理结构保证的现状，作者提出将固定权重和网格下的谱卷积视为线性映射，从而将整个前向传播过程转化为分段线性模型，并利用Z3求解器进行精确编码。通过对1D平流-扩散-反应方程的小型FNO模型进行测试，成功实现了正值性证明和质量守恒违规的检测，揭示了形式化验证在神经算子安全性评估中的潜力与扩展性挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08938v1/fig-001.webp\", \"caption\": \"Figure 2. Model quality. Left: Test MSE. Right: Severity of Z3’s sound counterexample for mass non-increase, with every counterexample re-evaluated on the original model.\", \"page\": 4, \"index\": 1, \"width\": 461, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2605.08938v1/fig-002.webp\", \"caption\": \"Figure 3. Mass non-increase (ε=0.05): severity of the worst counterexample found by Z3 exact (sound), gradient falsification, and MC(5K). Z3 finds the most severe CE on 7 of 10 models.\", \"page\": 4, \"index\": 2, \"width\": 461, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2605.08938v1/fig-003.webp\", \"caption\": \"Figure 1. The FNO-to-SMT bridge. We compile the spectral convolution into an explicit dense matrix and then query physics-motivated properties with an SMT solver. The exact encoding is sound but dense; the frozen encoding is faster but approximate.\", \"page\": 3, \"index\": 3, \"width\": 956, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2605.08938v1/fig-004.webp\", \"caption\": \"Figure 4. Positivity. Left: Z3 exact results: green = sound proof, red = sound CE, gray = timeout. Right: Minimum output found by Z3, gradient falsification, and MC. Z3 proofs certify positivity for all constrained inputs.\", \"page\": 5, \"index\": 4, \"width\": 461, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.08938v1/fig-005.webp\", \"caption\": \"Figure 5. Z3 solve time with the exact encoding. Left: Mass. Right: Positivity. Linear models solve faster, while ReLU models time out for positivity proofs.\", \"page\": 5, \"index\": 5, \"width\": 462, \"height\": 308}]"
motivation: 傅里叶神经算子在加速PDE模拟时缺乏物理一致性的形式化保证，可能导致其预测结果违反基本的物理规律。
method: 将FNO的谱卷积编译为Z3线性实数算术中的精确矩阵运算，并对比了精确编码与近似的冻结编码两种方案。
result: 在小型1D模型上，精确编码成功提供了正值性证明和比梯度法更优的质量违规反例，而冻结编码显著提升了在大网格下的检测速度。
conclusion: 研究明确了神经算子验证中可靠性与可扩展性之间的权衡，为未来大规模神经算子的形式化验证指明了方向。
---

## 摘要
傅里叶神经算子 (FNOs) 可以极大地加速偏微分方程 (PDE) 模拟，但它们在使用时往往缺乏保持基本物理结构的形式化保证。我们证明，一旦训练权重和网格固定，FNO 中的谱卷积就是一个线性映射。因此，整个前向传播过程是分段线性的，并且可以在 Z3 的线性实数算术中精确表示。我们研究了两种编码方式：精确编码将谱卷积编译为稠密矩阵乘法，这对于证明和反例都是可靠的；更轻量级的冻结编码则用常数替换谱路径，使其速度更快但具有近似性。在针对一维平流-扩散-反应方程的 10 个小型 FNO 代理模型（85 至 117 个参数，网格大小 8 至 32）上，精确编码在模型线性（无 ReLU）的情况下给出了 2 个可靠的正性证明、5 个可靠的正性反例和 10 个可靠的质量违背反例；其余 3 个针对 ReLU 模型的正性查询超时。对于质量非增性，在 10 个模型中的 7 个上，Z3 发现的反例比基于梯度的证伪方法和蒙特卡罗方法发现的违背程度更深。冻结编码可扩展至网格大小 64 并实现亚秒级的正性检查，但它不再为原始 FNO 提供凭证。总的来说，这些结果明确了可靠性与可扩展性之间的权衡，并指出了生产规模神经算子形式化验证的研究方向。

## Abstract
Fourier Neural Operators (FNOs) can greatly accelerate PDE simulation, but they are often used without formal guarantees that they preserve basic physical structure. We show that, once the trained weights and grid are fixed, the spectral convolution in an FNO is a linear map. As a result, the full forward pass is piecewise-linear and can be represented exactly in Z3's linear real arithmetic. We study two encodings. The exact encoding compiles the spectral convolution into a dense matrix multiplication, which is sound for both proofs and counterexamples. The lighter frozen encoding replaces the spectral path with a constant, making it faster but approximate. On 10 small FNO surrogates for 1D advection-diffusion-reaction (85 to 117 parameters, grids 8 to 32), the exact encoding gives 2 sound positivity proofs on linear (ReLU-free) models, 5 sound positivity counterexamples, and 10 sound mass-violation counterexamples; the remaining 3 positivity queries on ReLU models time out. For mass non-increase, Z3 finds worse counterexamples than both gradient-based falsification and Monte Carlo on 7 of 10 models. The frozen encoding scales to grid size 64 with sub-second positivity checks, but it no longer provides certificates for the original FNO. Overall, the results make the soundness--scalability tradeoff explicit and point to what is needed for formal verification of production-scale neural operators.

---

## 论文详细总结（自动生成）

这篇论文探讨了傅里叶神经算子（FNO）在偏微分方程（PDE）代理模型中的形式化验证问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：傅里叶神经算子（FNO）虽然在加速 PDE 模拟方面表现优异，但其预测结果往往缺乏物理一致性保证（如可能违反质量守恒、产生负浓度等非物理现象）。
*   **研究动机**：传统的验证方法（如蒙特卡罗测试）只能检查有限的样本点，无法提供覆盖整个连续输入空间的完备性保证。本文旨在探索是否可以利用形式化验证工具（如 SMT 求解器）来证明 FNO 满足特定的物理属性，或者找到其违反物理规律的确凿反例。

### 2. 方法论
*   **核心思想**：作者发现，在**固定网格**和**固定训练权重**的前提下，FNO 中的谱卷积（包含 DFT、频率缩放、逆 DFT）本质上是一个**线性映射**。
*   **关键技术细节**：
    *   **线性转化**：将谱卷积操作表示为一个显式的稠密矩阵 $W_{spec}$。结合 ReLU 激活函数的分段线性特性，整个 FNO 前向传播过程可以被视为一个分段线性映射。
    *   **精确编码（Exact Encoding）**：将 FNO 编译为 Z3 求解器的线性实数算术（Linear Real Arithmetic）。这种方法是“可靠的”（Sound），即 SMT 返回的 UNSAT 代表属性绝对成立，SAT 则代表找到了真实的物理违规反例。
    *   **冻结编码（Frozen Encoding）**：为了提升扩展性，将谱路径在参考输入处的输出视为常数。这是一种近似方法，虽然计算速度快，但不再提供原始模型的正式凭证。
*   **验证属性**：
    1.  **质量非增性（Mass non-increase）**：验证输出的总质量是否超过输入质量加上一个微小阈值 $\epsilon$。
    2.  **正值性（Positivity）**：验证在正初始条件下，输出是否在所有网格点上保持非负。

### 3. 实验设计
*   **物理场景**：一维平流-扩散-反应方程（1D ADR），具有周期性边界条件。
*   **模型设置**：训练了 10 个小型 FNO 模型，参数量在 85 到 117 之间，网格大小 $N \in \{8, 16, 32\}$，隐藏层宽度 $H=2$，包含线性模型（1层）和 ReLU 模型（2层）。
*   **Benchmark 与对比方法**：
    *   **蒙特卡罗（MC）**：使用 5,000 个随机平滑输入进行测试。
    *   **基于梯度的证伪（Gradient-based falsification）**：利用有限差分上升法寻找违反物理属性的最大值。
    *   **Z3 求解器**：作为核心验证工具。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练总时长。
*   **求解耗时**：文中记录了 Z3 的求解时间。对于小型线性模型，证明或寻找反例通常在 0.1s 到 3s 之间；对于包含 ReLU 的模型，部分正值性查询在规定时间内超时（Timeout）。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个不同配置的模型进行了测试，涵盖了质量守恒和正值性两个关键物理属性。
*   **充分性评价**：作为“概念验证”（Proof of Concept）类研究，实验设计是充分且客观的。它不仅展示了成功证明的案例，也诚实地记录了 SMT 求解器在处理 ReLU 非线性时的超时情况，并对比了传统数值搜索方法的优劣。

### 6. 主要结论与发现
*   **首次实现证明**：在网格大小高达 16 的线性 FNO 模型上，首次获得了正值性的形式化证明凭证。
*   **反例搜索优势**：在质量非增性测试中，Z3 在 10 个模型中的 7 个上找到了比梯度法和 MC 方法更严重的物理违规反例（违规程度高出 1.1 到 2.5 倍）。
*   **可靠性与扩展性权衡**：精确编码在小型模型上表现优异，但随着网格增加和非线性增强，计算复杂度呈指数级增长；冻结编码虽能扩展至 $N=64$，但失去了形式化严谨性。

### 7. 优点
*   **理论洞察**：揭示了固定网格下谱卷积的线性本质，为神经算子的验证开辟了新路径。
*   **严谨性**：提供了“可靠”的证明，这是传统深度学习验证方法难以企及的。
*   **工具链打通**：成功将 FNO 架构映射到成熟的 SMT 求解框架中。

### 8. 不足与局限
*   **模型规模受限**：实验仅限于极小规模的模型（约 100 个参数），远小于生产环境中的 FNO。
*   **维度灾难**：精确编码生成的稠密矩阵规模为 $O(N^2 H^2 L)$，对于高分辨率（如 $N=128$ 或更高）的 FNO，内存和计算开销将不可接受。
*   **非线性挑战**：ReLU 带来的分支爆炸导致 SMT 求解器在处理多层非线性模型时容易超时。
*   **应用限制**：目前仅验证了 1D 简单方程，尚未扩展到 2D/3D 或更复杂的非线性 PDE 系统。

（完）
