---
title: "Metal-Sci: A Scientific Compute Benchmark for Evolutionary LLM Kernel Search on Apple Silicon"
title_zh: Metal-Sci：一个用于 Apple Silicon 上进化 LLM 算子搜索的科学计算基准
authors: Víctor Gallego
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09708v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 大语言模型驱动PDE计算内核的进化循环
tldr: 本研究推出了 Metal-Sci，一个针对 Apple Silicon Metal 科学计算内核的 10 任务基准测试，涵盖模板计算、n-body 等六大优化领域。研究通过轻量级框架结合大语言模型（LLM）与 (1+1) 进化算法进行自动内核搜索，并引入留出集评分机制以验证泛化性。实验表明，该方法能显著提升内核性能，同时有效识别出仅在特定规模下表现优异但在泛化测试中失效的错误优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09708v1/fig-001.webp\", \"caption\": \"Figure 1. The METAL-SCI benchmark. Top: six optimization regimes (R1–R6), each stressing a structurally distinct GPU/memory bottleneck whose canonical recipe does not transfer to its neighbors (Sec. 2). Bottom: the harness loop. A frozen LLM M emits a Metal source κk; the harness runtime-compiles it, dispatches across the in-distribution size configurations ΣT , and scores it against per-size roofline ceilings. The candidate becomes the new incumbent κ⋆ k only when ST strictly improves. Compile diagnostics and per-size (χT , aT ) flow back through a structured feedback packet Fk that primes the next iteration. The held-out evaluation ΦT at σ⋆ T runs once at end-of-run (Sec. 3).\", \"page\": 1, \"index\": 1, \"width\": 517, \"height\": 674}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-002.webp\", \"caption\": \"Figure 3. Paradigmatic candidate evolution: hmc, Opus 4.7, iter 5→ 6. Both Opus and Gemini independently arrive at this structural change. The lever is one declaration: template <uint D>with runtime-dispatched instantiations on d. Iter 5 had a manually-unrolled float4 inner loop against a fixed Dmax=32 layout (over-computing at d=8, plus a 4-way horizontal sum); iter 6 sizes q,p,f exactly to D so both loops fully unroll into scalar FMAs — moving d=8 from 121 to 970 GFLOPS in one iteration after five had stalled at 2–4% of peak. Opus enumerated D∈{8, 16, 32} and broke correctness at the held-out d=24; Gemini kept a runtime-d fallback and generalized cleanly.\", \"page\": 6, \"index\": 2, \"width\": 804, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-003.webp\", \"caption\": \"Figure 4. GPT-5.5 fft3d iter-10 best: hand-coded fft_line_32/64/128 routines (left dispatch) deliver the 2.95× in-distribution self-speedup; for any N outside {32, 64, 128} the kernel falls into a textbook direct O(N2) DFT (right). At held-out N=256 this costs ∼32× more arithmetic per output than the seed’s O(N logN) Stockham FFT, producing the 0.23× held-out slowdown reported in Table 3. The fast paths reuse a 64-entry constant float2 W128[] twiddle table whose stride indexing only covers N≤128, the structural reason the fallback is direct DFT rather than a longer FFT.\", \"page\": 7, \"index\": 3, \"width\": 921, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-004.webp\", \"caption\": \"Table 1. METAL-SCI versus existing LLM kernel-generation benchmarks across the axes that drive the design of our harness. Domain: ML = neural-network operators (GEMM, attention, convolution, normalization, activation); HPC = scientific compute (stencil, N -body, LBM, MD, MCMC, PDE solver, FFT). Score basis: what achieved is normalized against. Multi-size: whether the score requires generalization across problem sizes. Search: the loop structure exposed to the LLM. Within the ML row, all listed benchmarks span a single optimization regime (matmul-style, with norm/activation epilogues); METAL-SCI spans six structurally distinct regimes (R1–R6 of Section 2).\", \"page\": 3, \"index\": 4, \"width\": 1017, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-005.webp\", \"caption\": \"Table 2. The 10 METAL-SCI tasks. “Optimization lever” names the dominant move an LLM must reach for in that regime. Nx×Ny grids written as N2 when square; cube edges as N3. saxpy is a bandwidth smoke-test outside the regime structure, used to validate the harness.\", \"page\": 3, \"index\": 5, \"width\": 1017, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-006.webp\", \"caption\": \"Figure 6. fft3d iter-10 best, Opus (left) vs Gemini iter-10 best (right). Opus implements Stockham radix-4 with threadgroup-memory ping-pong and a barrier per stage; Gemini exploits the 32-wide simdgroup to do the first five stages with simd_shuffle_xor, eliminating five barriers per 1D FFT. In-distribution gmean: Opus 0.167 vs Gemini 0.282.\", \"page\": 13, \"index\": 6, \"width\": 837, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-007.webp\", \"caption\": \"Figure 5. lbm iter-23 best, Opus (left) vs Gemini iter-13 best (right). Opus extracts A once, folds feq k into two FMAs per direction and the relaxation into a third, and pins the threadgroup geometry; Gemini stays with the canonical BGK formula and the default geometry. In-distribution gmean: Opus 0.576 vs Gemini 0.553.\", \"page\": 12, \"index\": 7, \"width\": 837, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-008.webp\", \"caption\": \"Table 3. Evolutionary kernel refinement sweeps on Apple M1 Pro (Opus = claude-opus-4-7, Gemini = gemini-3.1-pro-preview, GPT = gpt-5.5). In-dist. × = best / seed, gmean over three in-distribution size configurations. Held-out frac-of-ceiling = achieved/ceiling at the unseen size, measured in a single fresh session for all three models; held-out × = best / seed at that size config. Bold in the in-distribution and held-out speedup columns marks meaningful improvements (≥ 1.05×).\", \"page\": 5, \"index\": 8, \"width\": 1017, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.09708v1/fig-009.webp\", \"caption\": \"Figure 2. Per-task running self-speedup (best-so-far / seed) versus iteration, Opus 4.7 (orange) vs Gemini 3.1 Pro (purple) vs GPT-5.5 (green). Filled circles mark the iteration that achieved the final best; each x along y=1 is a candidate that compiled or ran wrong (the incumbent is unchanged). The visible counts make the silent-correctness story concrete: Opus emits more failures than Gemini or GPT at every task with non-trivial search (nbody, hmc, ising, lj, wave3d). The plot also justifies the asymmetric per-task budgets: on most tasks (saxpy, heat2d, nbody, gradshaf, hmc, ising, lj) the incumbent stops moving by iter ∼8 for all three models, but lbm’s Opus run plateaus at 1.36× from iter 3 through iter 22 and only breaks through to 1.46× at iter 23 (the BGK fold + pinned threadgroup of App. C); wave3d’s Opus run shows the same shape with the lift at iter 14 (1.26×). A 10-iter budget would have missed both Opus exemplars. GPT’s fft3d climbs monotonically to 2.95×, and is exactly the win that fails to generalize on the held-out gate.\", \"page\": 5, \"index\": 9, \"width\": 1017, \"height\": 435}]"
motivation: 旨在解决在 Apple Silicon 平台上自动搜索和优化高性能科学计算 Metal 内核的挑战。
method: 提出一个包含 10 项任务的基准测试，并利用 LLM 驱动的进化循环结合屋顶线模型适应度函数进行内核自动搜索。
result: 在 M1 Pro 芯片上，不同 LLM 搜索出的内核实现了最高 10.7 倍的加速，并成功通过留出集评分发现了潜在的泛化性错误。
conclusion: 引入留出集评分函数作为机械监督原语，对于确保自动搜索生成的内核在未见配置下的正确性和性能稳定性至关重要。
---

## 摘要
我们提出了 Metal-Sci，这是一个包含 10 个任务的科学 Apple Silicon Metal 计算算子基准，涵盖了六种优化范式（模板计算、n 体问题中的全对计算、多场玻尔兹曼、邻居列表分子动力学、多算子偏微分方程、快速傅里叶变换）。每个任务都提供了一个 CPU 参考实现、一个基于 Roofline 模型的适应度函数以及一个留出的泛化尺寸。我们将该基准与一个轻量级的自动算子搜索框架配对，该框架在运行时编译每个候选算子，根据不同尺寸下的 Roofline 模型对其进行评分，并将结构化的编译信息和各尺寸的正确性诊断反馈给驱动 (1+1) 进化循环的冻结 LLM。我们报告了在 M1 Pro 上对 Claude Opus 4.7、Gemini 3.1 Pro 和 GPT 5.5 进行的匹配单模型扫描结果：分布内自加速比范围在 1.00 倍到 10.7 倍之间。除了原始的加速比之外，我们的核心方法论主张是结构性的：留出的门控评分函数 Φ_T（在运行结束时对智能体在搜索过程中从未见过的配置进行一次评估）在该自动搜索循环中充当了一种廉价的机械监督原语，能够捕捉到例如：Opus 模板 <uint D> HMC 在未见维度上返回错误样本的情况，以及 GPT FFT3D 的最佳结果在分布内实现了 2.95 倍加速，但在 256^3 的留出立方体上性能骤降至 0.23 倍，这种隐性退化是仅凭分布内评分无法察觉的。代码开源于 https://github.com/vicgalle/metal-sci-kernels

## Abstract
We present Metal-Sci, a 10-task benchmark of scientific Apple Silicon Metal compute kernels spanning six optimization regimes (stencils, all-pairs in $n$-body problems, multi-field Boltzmann, neighbor-list molecular dynamics, multi-kernel PDE, FFT). Each task ships a CPU reference, a roofline-anchored fitness function, and a held-out generalization size. We pair the benchmark with a lightweight harness for automatic kernel search that runtime-compiles each candidate, scores it against the roofline across multiple sizes, and feeds structured compile and per-size correctness diagnostics back to a frozen LLM driving a $(1{+}1)$ evolutionary loop. We report matched single-model sweeps of Claude Opus 4.7, Gemini 3.1 Pro, and GPT 5.5 on M1 Pro: in-distribution self-speedups span $1.00\times$ to $10.7\times$. Beyond raw speedup, our central methodological claim is structural: the held-out gate scoring function $Φ_\mathcal{T}$ (evaluated once at end-of-run on a configuration the agent never sees during search) functions as a cheap mechanical oversight primitive on this automatic search loop, catching e.g. an Opus template <uint D> HMC win that returns wrong samples at unseen dimensions, and a GPT FFT3D best that wins in-distribution at $2.95\times$ speedup but collapses to $0.23\times$ on a $256^3$ held-out cube, a silent regression that the in-distribution score alone cannot see. Code at https://github.com/vicgalle/metal-sci-kernels

---

## 论文详细总结（自动生成）

以下是对论文《Metal-Sci: A Scientific Compute Benchmark for Evolutionary LLM Kernel Search on Apple Silicon》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：当前大语言模型（LLM）在代码生成领域表现出色，但现有的算子（Kernel）优化基准测试（如 KernelBench）大多集中在 CUDA 平台上的深度学习算子（如 GEMM、Attention）。
*   **核心问题**：科学计算（HPC）领域的算子优化逻辑与深度学习截然不同，且 Apple Silicon 的 Metal 编程环境在 LLM 预训练数据中代表性不足。
*   **研究动机**：旨在建立一个针对 Apple Silicon Metal 的科学计算基准，探索 LLM 作为进化优化器在不同计算范式（如模板计算、FFT、分子动力学等）下的性能上限，并解决自动搜索过程中可能出现的“过拟合”或“隐性退化”问题。

### 2. 方法论
*   **核心思想**：构建一个闭环的进化搜索框架，利用 LLM 迭代生成并优化 Metal 源码，通过硬件理论性能上限（Roofline Model）进行评分。
*   **关键技术细节**：
    *   **10项任务与6种范式**：涵盖了 Stencils（模板）、All-pairs（全对交互）、Lattice Boltzmann（玻尔兹曼）、Atomics（原子操作）、Multi-kernel PDE（多算子偏微分方程）和 FFT（快速傅里叶变换）。
    *   **(1+1) 进化循环**：采用最简单的进化策略，即一个父代产生一个变异子代。若子代评分更高，则替换父代。
    *   **运行时编译与反馈**：使用 Python 绑定（PyObjC）实现 Metal 源码的即时编译（JIT），将编译错误、运行耗时、正确性诊断（与 CPU 参考实现对比）反馈给 LLM。
    *   **评分函数 $S_T$**：基于分布内（In-distribution）三个不同规模任务的几何平均性能，并以硬件 Roofline（峰值带宽或计算量）的百分比作为得分。
    *   **留出集监督（Held-out Gate）**：引入 $\Phi_T$ 函数，在搜索结束后对智能体从未见过的任务规模进行一次性评估，用于检测代码的泛化能力。

### 3. 实验设计
*   **硬件环境**：Apple M1 Pro 芯片（峰值 4500 GFLOPS, 200 GB/s）。
*   **对比模型**：Claude Opus 4.7、Gemini 3.1 Pro、GPT-5.5。
*   **Benchmark 构成**：10 个科学计算任务（如 heat2d, nbody, fft3d 等），每个任务包含 CPU 参考代码、Roofline 锚点和留出集配置。
*   **对比基准**：以任务自带的种子内核（Seed Kernel）作为基准，计算自加速比（Self-speedup）。

### 4. 资源与算力
*   **算力设备**：实验在单台 Apple M1 Pro 设备上完成。
*   **迭代预算**：每个任务通常分配 10 次迭代（LBM 为 25 次，Wave3D 为 15 次）。
*   **生成耗时**：模型间差异显著。Opus 约 0.6 分钟/迭代，Gemini 约 3.5 分钟/迭代，GPT-5.5 约 6.6 分钟/迭代（因其推理链更长）。

### 5. 实验数量与充分性
*   **实验规模**：对 3 个顶尖模型在 10 个任务上进行了完整的进化扫描，总计进行了数百次内核编译与运行测试。
*   **客观性与公平性**：所有模型使用相同的系统提示词、迭代预算和硬件环境。通过 Roofline 模型而非手动编写的 Baseline 进行评分，确保了评价标准的客观物理意义。
*   **充分性**：实验不仅关注加速比，还深入分析了模型在不同优化范式下的行为差异（如 Opus 擅长微调，Gemini 擅长算法重构），实验设计较为全面。

### 6. 论文的主要结论与发现
*   **性能提升**：LLM 驱动的进化搜索实现了显著加速，分布内自加速比最高达 10.7 倍（HMC 任务）。
*   **泛化性失效（核心发现）**：
    *   **正确性崩溃**：Opus 在 HMC 任务中通过模板特化获得高分，但在留出集维度上返回了错误结果。
    *   **性能退化**：GPT-5.5 在 FFT 任务中实现了 2.95 倍的分布内加速，但在留出集规模下因回退到 O(N²) 算法，性能骤降至原始代码的 0.23 倍。
*   **模型特性**：Gemini 在整个测试中表现最稳健，正确性失败率为零，且在留出集上没有出现严重的性能崩溃。

### 7. 优点
*   **填补空白**：首次针对 Apple Silicon Metal 建立了科学计算领域的 LLM 算子优化基准。
*   **监督原语创新**：提出的“留出集门控”作为一种廉价的机械监督手段，能有效识别自动编程智能体产生的“自信的错误”。
*   **高效反馈**：利用统一内存架构实现了亚秒级的“编译-运行-验证”循环，极大地提升了进化搜索的效率。

### 8. 不足与局限
*   **Roofline 模型简化**：静态的 Roofline 计算未考虑系统级缓存（SLC）在小规模任务中的驻留效应，可能导致评分在某些尺寸下不够精确。
*   **进化策略单一**：仅使用了 (1+1) 策略，未探索更复杂的种群算法（如 FunSearch 中的存档机制）。
*   **任务覆盖面**：虽然涵盖了 6 种范式，但尚未包含稀疏线性代数（如 SpMV）或跨芯片（如 M2 进化、M4 评估）的泛化测试。

（完）
