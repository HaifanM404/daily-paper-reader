---
title: Isotropic Fourier Neural Operators
title_zh: 各向同性傅里叶神经算子
authors: Michael F. Staddon
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02597v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于求解偏微分方程的傅里叶神经算子
tldr: 傅里叶神经算子（FNO）在求解偏微分方程方面表现优异，但其标准线性变换未能充分利用物理系统中的等向性对称性。本文提出等向性傅里叶神经算子（IFNO），通过改进傅里叶层使其遵循空间对称性。该方法在提升模型性能的同时，显著降低了参数量，在2D和3D场景下分别实现了高达16倍和96倍的参数压缩，为高效物理模拟提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02597v1/fig-001.webp\", \"caption\": \"FIG. 1. Isotropic Fourier Neural Operator. (a) The full model architecture. Lift the input to a higher dimensional channel space by neural network P . Apply L Fourier layers. Project down to output channel space by neural network Q. (b) Fourier layer architecture. On top: Fourier transform the input, apply linear transform R on the lowest n modes, apply the inverse Fourier transform. On the bottom: apply linear transform W . Finally, add the top and bottom branches and apply non-linear activation function σ. (c) Isotropic Fourier Kernel. The Fourier kernel R is generated using symmetries of space from an upper right triangular tensor Riso. Starting from the left, the original parameters are used to generate the full parameters through reflections representing transposition, reflection in y, and reflection in x.\", \"page\": 2, \"index\": 1, \"width\": 501, \"height\": 284}]"
motivation: 传统的傅里叶神经算子在处理各向同性的物理系统时，其参数化方式未能显式尊重空间对称性，导致模型冗余且物理一致性不足。
method: 通过对傅里叶模态的线性变换施加对称性约束，确保模型输出不随坐标系的旋转或平移而改变，从而构建了等向性傅里叶层。
result: 实验表明，IFNO在提升预测精度的同时，将2D模型的参数量减少了16倍，3D模型减少了96倍。
conclusion: 在神经算子中引入等向性对称性约束，是实现高效、轻量化且符合物理规律的PDE求解器的有效途径。
---

## 摘要
傅里叶神经算子（Fourier Neural Operators）是学习函数空间之间映射的深度学习模型，可用于学习和求解偏微分方程（PDEs），在某些情况下其速度显著快于传统的 PDE 求解器。模型内部包含傅里叶层，这些层直接对傅里叶模态应用线性变换，其参数取决于波数。然而，大多数物理系统是各向同性的，即结果与所选坐标系无关，但线性变换并不一定遵循这些对称性。我们提出了一种对线性变换的改进，以确保遵循空间对称性，称为各向同性傅里叶神经算子（Isotropic Fourier Neural Operator）。该方法不仅提高了模型性能，还在二维和三维情况下分别将参数量减少了高达 16 倍和 96 倍。

## Abstract
Fourier Neural Operators are deep learning models that learn mappings between function spaces and can be used to learn and solve partial differential equations (PDEs), in some cases significantly faster than traditional PDE solvers. Within the model are Fourier layers, which apply linear transformations directly to the Fourier modes, with parameters depending on the wave numbers. However, most physical systems are isotropic, with the results being independent of the coordinate system chosen, but the linear transformations do not necessarily respect these symmetries. We propose a modification to the linear transformations that ensures spatial symmetries are respected, called the Isotropic Fourier Neural Operator, which both improves model performance and reduces the number of parameters by up to a factor of 16 in 2D and 96 in 3D.

---

## 论文详细总结（自动生成）

这是一份关于论文《Isotropic Fourier Neural Operators》（各向同性傅里叶神经算子）的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：傅里叶神经算子（FNO）在求解偏微分方程（PDE）方面表现卓越，其核心是通过傅里叶变换在频域学习算子映射。
*   **核心问题**：大多数物理系统具有**各向同性（Isotropy）**，即物理规律不随坐标系的旋转或镜像而改变。然而，标准 FNO 的傅里叶核（Kernel）参数是独立学习的，并不显式遵循这些空间对称性。
*   **动机**：通过将空间对称性（如旋转、镜像、转置）硬编码到模型架构中，可以减少模型冗余、降低参数量，并提高模型在物理任务上的泛化能力。

### 2. 方法论：Iso-FNO 的核心思想
*   **核心思想**：强制傅里叶层中的线性变换矩阵 $R$ 遵循二面体群 $D_4$ 的对称性（针对 2D 空间）。
*   **关键技术细节**：
    *   **对称性约束**：要求傅里叶核在 $x$ 方向镜像（$R_{k,l} = R_{-k,l}$）、$y$ 方向镜像（$R_{k,l} = R_{k,-l}$）以及坐标转置（$R_{k,l} = R_{l,k}$）下保持一致。
    *   **实值约束**：为了保证实值输入产生实值输出，要求 $R_{k,l} = R^*_{-k,-l}$，这使得参数从复数变为实数，进一步减少了一半参数。
    *   **参数生成**：Iso-FNO 仅学习一个极小的上三角张量 $R_{iso}$，然后通过镜像和转置操作“展开”生成完整的傅里叶核 $R$。
*   **参数压缩率**：在 2D 空间中，理论参数量减少至 1/16；在 3D 空间中，由于引入 $z$ 轴对称和 3 元素全排列，理论压缩率可达 1/96。

### 3. 实验设计
*   **数据集/场景**：使用了经典的 **2D Darcy Flow（达西流）** 问题，模拟流体在多孔介质中的流动。
*   **Benchmark**：以原始的 **Fourier Neural Operator (FNO)** 作为基准对比对象。
*   **实验设置**：
    *   分辨率：128 x 128。
    *   样本量：1000 个训练样本，200 个测试样本。
    *   超参数：32 个通道，16 个傅里叶模态，3 层傅里叶层。
    *   评估指标：$L_2$ 误差和 $H_2$ 误差。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率提及**：作者指出，虽然参数量大幅减少，但推理时间（Inference Time）与标准 FNO 基本持平，因为最终参与计算的张量大小是一致的，主要的节省体现在内存占用和模型存储上。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 Darcy Flow 这一单一基准任务进行了深入对比。
*   **实验内容**：
    *   标准训练与测试误差对比。
    *   **对称性泛化测试**：将训练数据进行转置、$x$ 轴翻转、$y$ 轴翻转后直接测试模型，验证其等变性。
*   **充分性评价**：实验在验证“对称性约束”这一核心论点上是充分且客观的。但相比于原始 FNO 论文，其实验场景较为单一（仅 Darcy Flow），未涵盖纳维-斯托克斯方程（Navier-Stokes）等更复杂的动态问题。

### 6. 主要结论与发现
*   **参数大幅减少**：在 2D 实验中，Iso-FNO 的参数量从 4.2M 降至 0.57M（约 7.4 倍实际压缩，接近理论值）。
*   **性能提升**：尽管训练误差略高于 FNO（因为约束限制了拟合能力），但 **测试误差降低了约 30%**。这表明 Iso-FNO 有效抑制了过拟合，具有更强的泛化性能。
*   **完美的等变性**：Iso-FNO 在处理翻转或转置的数据时，误差保持恒定，而标准 FNO 在面对变换后的数据时误差会飙升至与随机测试集相当的水平。

### 7. 优点
*   **物理一致性**：通过架构设计直接保证了物理上的各向同性，比单纯的数据增强更可靠。
*   **极高的参数效率**：在保持甚至提升精度的前提下，实现了近百倍（3D 理论值）的参数压缩，极大地降低了内存需求。
*   **通用性**：该对称性约束逻辑可以轻松集成到 F-FNO、D-FNO 等其他 FNO 变体中。

### 8. 不足与局限
*   **任务覆盖面**：实验仅限于标量场（Scalar Field）和静态 PDE，未探讨矢量场（如流体速度向量）或含时（Time-dependent）问题的复杂对称性需求。
*   **灵活性限制**：对于某些非各向同性的特殊物理系统（如受重力影响的有偏系统），强制各向同性可能会导致模型欠拟合。
*   **算力细节缺失**：缺乏训练效率的具体量化数据（如收敛速度对比）。

（完）
