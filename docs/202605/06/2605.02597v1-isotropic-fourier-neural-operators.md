---
title: Isotropic Fourier Neural Operators
title_zh: 各向同性傅里叶神经算子
authors: Michael F. Staddon
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02597v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于学习和求解偏微分方程的傅里叶神经算子
tldr: 傅里叶神经算子（FNO）在求解偏微分方程方面表现出色，但其线性变换往往忽略了物理系统中的各向同性对称性。本文提出了各向同性傅里叶神经算子（IFNO），通过修改傅里叶层中的线性变换以确保空间对称性。该方法不仅提升了模型在物理模拟中的性能，还显著减少了参数量（2D减少16倍，3D减少96倍），实现了更高效且符合物理规律的建模。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02597v1/fig-001.webp\", \"caption\": \"FIG. 1. Isotropic Fourier Neural Operator. (a) The full model architecture. Lift the input to a higher dimensional channel space by neural network P . Apply L Fourier layers. Project down to output channel space by neural network Q. (b) Fourier layer architecture. On top: Fourier transform the input, apply linear transform R on the lowest n modes, apply the inverse Fourier transform. On the bottom: apply linear transform W . Finally, add the top and bottom branches and apply non-linear activation function σ. (c) Isotropic Fourier Kernel. The Fourier kernel R is generated using symmetries of space from an upper right triangular tensor Riso. Starting from the left, the original parameters are used to generate the full parameters through reflections representing transposition, reflection in y, and reflection in x.\", \"page\": 2, \"index\": 1, \"width\": 501, \"height\": 284}]"
motivation: 传统的傅里叶神经算子在处理具有各向同性特征的物理系统时，其线性变换无法保证空间对称性。
method: 提出一种改进的线性变换方式，通过约束参数使其尊重空间对称性，构建出各向同性傅里叶神经算子。
result: 该方法在提升模型预测性能的同时，将2D和3D场景下的参数量分别降低了最高16倍和96倍。
conclusion: 引入各向同性约束能使神经算子更好地捕捉物理对称性，从而在降低计算复杂度的同时提高泛化能力。
---

## 摘要
傅里叶神经算子（Fourier Neural Operators）是学习函数空间之间映射的深度学习模型，可用于学习和求解偏微分方程（PDEs），在某些情况下其速度显著快于传统的 PDE 求解器。该模型内部包含傅里叶层，这些层直接对傅里叶模态应用线性变换，其参数取决于波数。然而，大多数物理系统是各向同性的，其结果与所选坐标系无关，但线性变换并不一定遵循这些对称性。我们提出了一种对线性变换的改进方法，以确保遵循空间对称性，称之为各向同性傅里叶神经算子（Isotropic Fourier Neural Operator）。该方法不仅提高了模型性能，还将 2D 情况下的参数量减少了高达 16 倍，3D 情况下减少了高达 96 倍。

## Abstract
Fourier Neural Operators are deep learning models that learn mappings between function spaces and can be used to learn and solve partial differential equations (PDEs), in some cases significantly faster than traditional PDE solvers. Within the model are Fourier layers, which apply linear transformations directly to the Fourier modes, with parameters depending on the wave numbers. However, most physical systems are isotropic, with the results being independent of the coordinate system chosen, but the linear transformations do not necessarily respect these symmetries. We propose a modification to the linear transformations that ensures spatial symmetries are respected, called the Isotropic Fourier Neural Operator, which both improves model performance and reduces the number of parameters by up to a factor of 16 in 2D and 96 in 3D.

---

## 论文详细总结（自动生成）

以下是对论文《Isotropic Fourier Neural Operators》（各向同性傅里叶神经算子）的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：傅里叶神经算子（FNO）在求解偏微分方程（PDE）方面表现卓越，其核心是通过傅里叶层在频域进行线性变换。
*   **核心问题**：大多数物理系统具有**各向同性（Isotropy）**，即物理规律不随坐标系的旋转或镜像而改变。然而，标准 FNO 的傅里叶核参数是独立学习的，无法保证这种空间对称性，导致模型参数冗余且在处理对称变换后的数据时泛化能力较差。
*   **研究动机**：通过在 FNO 架构中显式引入空间对称性约束，构建一种既能减少参数量又能提升物理一致性的模型。

### 2. 论文提出的方法论
*   **核心思想**：提出 **Iso-FNO**，通过约束傅里叶核 $R$ 的参数，使其遵循 $D_4$ 二面体群（2D 空间中的反射、旋转和转置对称性）。
*   **关键技术细节**：
    *   **反射对称性**：要求 $R_{k,l} = R_{-k,l}$（x轴反射）和 $R_{k,l} = R_{k,-l}$（y轴反射）。
    *   **实值约束**：为了保证实值输入产生实值输出，要求 $R_{k,l} = R^*_{-k,-l}$，这消除了复数参数的需求，将参数量减半。
    *   **转置对称性**：要求 $R_{k,l} = R_{l,k}$（交换 x 和 y 坐标）。
*   **算法流程**：
    1.  定义一个极小的上三角张量 $R_{iso}$ 作为原始参数池。
    2.  通过一系列反射和转置操作，从 $R_{iso}$ 生成完整的傅里叶核 $R$。
    3.  将生成的对称核应用于傅里叶层，确保模型在推理时具有等变性（Equivariance）。

### 3. 实验设计
*   **数据集/场景**：**2D Darcy Flow（达西流）**，模拟流体通过多孔介质的压力分布。
*   **Benchmark（基准）**：原始傅里叶神经算子（FNO）。
*   **对比维度**：
    *   参数量对比。
    *   标准测试集上的 L2 和 H2 误差。
    *   **对称性泛化测试**：将训练数据进行转置（Transpose）、x轴翻转（Flip-x）和 y轴翻转（Flip-y）后，观察模型的预测误差。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练总时长。
*   **训练配置**：使用了 1000 个训练样本和 200 个测试样本，分辨率为 $128 \times 128$。训练采用余弦退火策略（Cosine Annealing），共 100 个 epoch，batch size 为 20，初始学习率为 0.001。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 Darcy Flow 这一经典 PDE 问题进行了深入对比。
*   **充分性评价**：
    *   **优点**：实验设计非常精准地指向了“对称性”。通过对输入数据进行几何变换（翻转、转置）来测试模型，有力地证明了 Iso-FNO 的等变性。
    *   **不足**：实验场景较为单一（仅 Darcy Flow），未在更复杂的流体动力学（如 Navier-Stokes）或 3D 场景下进行大规模验证。

### 6. 论文的主要结论与发现
*   **参数大幅压缩**：在 2D 场景下，Iso-FNO 的参数量比标准 FNO 减少了约 **7.4 倍**（理论极限为 16 倍）；在 3D 场景下理论可减少 **96 倍**。
*   **性能提升**：在测试集上，Iso-FNO 的 L2 误差比标准 FNO **降低了约 30%**。
*   **泛化与鲁棒性**：标准 FNO 在面对翻转或转置的输入时误差剧增（与测试集误差相当），而 Iso-FNO 在这些变换下的误差与原始训练误差完全一致，证明了其完美的空间对称性。

### 7. 优点
*   **物理一致性**：将物理先验（各向同性）直接嵌入网络结构，而非仅仅依赖数据驱动。
*   **极高的参数效率**：在保持甚至提升精度的前提下，极大地降低了模型的内存占用和存储需求。
*   **无额外计算开销**：与需要复杂群卷积的 G-FNO 不同，Iso-FNO 仅通过参数共享实现对称性，推理速度与原版 FNO 持平。

### 8. 不足与局限
*   **实验覆盖面**：缺乏 3D 实验数据支持，虽然理论推导完整，但未展示 96 倍压缩下的实际表现。
*   **应用限制**：该方法假设系统是完全各向同性的。对于某些具有特定方向性（如受重力影响、有固定流向）的物理系统，强制各向同性可能会导致欠拟合。
*   **场类型扩展**：目前主要针对标量场，对于矢量场（如速度矢量）或伪矢量场（如涡度），需要更复杂的对称性处理逻辑。

（完）
