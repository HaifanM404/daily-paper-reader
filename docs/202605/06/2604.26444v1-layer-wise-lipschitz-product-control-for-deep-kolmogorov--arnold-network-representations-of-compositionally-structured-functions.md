---
title: Layer-wise Lipschitz-Product Control for Deep Kolmogorov--Arnold Network Representations of Compositionally Structured Functions
title_zh: 具有复合结构的函数的深度 Kolmogorov-Arnold 网络表示的逐层 Lipschitz 乘积控制
authors: Aleksander Tankman
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26444v1"
tags: ["query:pde-llm"]
score: 6.0
evidence: 结构化函数的Kolmogorov-Arnold网络表示
tldr: 本研究探讨了深度 Kolmogorov-Arnold 网络（KAN）对具有复合稀疏结构的连续函数的表示能力。作者证明了任何由有限计算树表示的函数都可以通过深层 KAN 实现，并提出了一种逐层 Lipschitz 乘积控制方法。该方法使得 Lipschitz 界限与输入维度无关，且在标准算子下乘积可降至 1。研究还给出了层宽和逼近误差的界限，解决了深度 KAN 堆栈中 Lipschitz 控制的理论空白，并通过实验验证了其在复合结构函数上的优越性。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决深度 KAN 网络在表示复合结构函数时缺乏 Lipschitz 连续性控制和理论界限的问题。
method: 通过将计算树的内部节点映射为具有受控深度和 Lipschitz 乘积的原始 KAN 块，构建深层 KAN 表示。
result: 证明了 Lipschitz 乘积与输入维度无关，在标准运算下可达到单位值，并获得了最优的 B-样条逼近速率。
conclusion: 该研究填补了深度 KAN 理论中关于 Lipschitz 控制的空白，为处理高维复合结构函数提供了坚实的数学基础。
---

## 摘要
我们证明，任何可由具有 N 个内部节点且复合稀疏度 s = O(1) 的有限计算树表示的从 [0,1]^n 到 R 的连续函数 f，都存在深度 Kolmogorov-Arnold 网络 (KAN) 表示。每个内部节点由一个具有受控块深度和 Lipschitz 乘积的原始 KAN 块实现。逐层 Lipschitz 乘积满足与输入维度 n 无关的主域敏感界限。该界限可简化为 P(KAN_f) <= max(C*,1)^L_f，其中 L_f <= c_max * N。对于在 [0,1] 有界输入上的标准操作 {+,-,x,sin,cos}（包含乘法节点），我们得到 P(KAN) <= 1。层宽满足 n_l <= n + 2 w_max * N。一致逼近误差由 N * max(C*,1)^d(f) * epsilon_Op 限定（当 C* <= 1 时可简化）。对于 f ∈ C^m，我们获得了最优 B 样条速率。此外，我们还推导了值域界限（对于加法树，B_f <= N+1）。这解决了 Liu 等人 (2024) 指出的深度 KAN 堆栈中 Lipschitz 控制方面的空白。实验证实了对于几种具有复合结构的函数，P(KAN)=1.0。

## Abstract
We prove that any continuous function f from [0,1]^n to R representable by a finite computation tree with N internal nodes and compositional sparsity s = O(1) admits a deep Kolmogorov-Arnold Network (KAN) representation. Each internal node is realised by a primitive KAN block with controlled block depth and Lipschitz product. The layer-wise Lipschitz product satisfies the primary domain-sensitive bound independent of the input dimension n. It simplifies to P(KAN_f) <= max(C*,1)^L_f with L_f <= c_max * N. For the standard operations {+,-,x,sin,cos} with x nodes on [0,1]-bounded inputs we obtain P(KAN) <= 1. Layer widths satisfy n_l <= n + 2 w_max * N. The uniform approximation error is bounded by N * max(C*,1)^d(f) * epsilon_Op (simplifies when C* <=1). For f in C^m we obtain optimal B-spline rates. Range bounds are also derived (B_f <= N+1 for additive trees). This addresses the gap on Lipschitz control in deep KAN stacks noted by Liu et al. (2024). Experiments confirm P(KAN)=1.0 for several compositionally structured functions.