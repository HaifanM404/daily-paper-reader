---
title: Kernel Learning of PDE Solution Operators
title_zh: 偏微分方程解算子的核学习
authors: "Jianyu Hu, Juan-Pablo Ortega"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09643v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: PDE解算子的核学习
tldr: 本文提出了一种基于核方法的偏微分方程（PDE）解算子学习框架。该方法通过将PDE算子作为物理先验融入核岭回归，构建了一个正则化算子学习器，实现了从输入函数到PDE解的直接映射。与传统监督学习不同，该方法无需成对的训练数据，具备强大的外推能力和严谨的误差分析。实验表明，该方法在处理达西流和亥姆霍兹方程时，在精度和计算效率上均优于现有算子学习技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09643v1/fig-001.webp\", \"caption\": \"Fig. 1. Input functions: f(x, y) = { 6, x ≥ 0.5, 4, x < 0.5, g(x, y) = { (x− 0.5)2 + y2, x ≥ 0.5, 2(x− 0.5)2 + y2, x < 0.5. .\", \"page\": 17, \"index\": 1, \"width\": 773, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2605.09643v1/fig-002.webp\", \"caption\": \"Fig. 2. Helmholtz equation. (a,c) True solutions with Kernel Operator (KO) and Green Operator (GO) approximations for ω = 20 and ω = 200, respectively; (b,d) corresponding absolute errors.\", \"page\": 19, \"index\": 2, \"width\": 773, \"height\": 581}]"
motivation: 针对传统深度学习方法依赖大量成对数据且难以整合物理先验的问题，寻求一种更高效、具有理论保障的PDE算子学习手段。
method: 利用核岭回归框架结合正则化理论，将PDE物理约束编码进核函数中，从而推导出不依赖特定输入函数的闭式算子估计器。
result: 数值实验证明该方法在达西流和亥姆霍兹方程等复杂问题上实现了高精度求解，且计算成本相比现有算子学习方法更具优势。
conclusion: 该研究成功将PDE求解转化为算子学习问题，为非齐次PDE的快速求解提供了一种无需配对数据且具备收敛性保证的新方案。
---

## 摘要
本文提出了一种基于核的方法，用于学习一般非齐次偏微分方程（PDEs）的解算子。该方法将通常通过 PDE 算子编码的物理先验融入核岭回归框架，并采用基于正则化的公式构建算子学习器。这产生了一个闭式估计量，该估计量独立于决定底层 PDE 的输入函数。从正则化理论的角度来看，所得估计量诱导了一个定义良好的算子，该算子连接了输入和输出空间，这两个空间分别包含定义 Dirichlet 问题及其解的函数。因此，它有效地从 PDE 求解器转变为基于算子的求解器。与标准的监督学习方法相比，它不依赖于成对的输入-输出训练数据，并能够实现超出观测范围的系统性外推。本文进行了完整的误差分析，提供了在适当选择正则化参数下基于算子的求解器的收敛率。广泛的数值实验（包括 Darcy 流和 Helmholtz 方程）表明，所提方法在各种问题设置中均实现了高精度和高效率，并且在逼近质量和计算成本方面均优于现有的算子学习方法。

## Abstract
A kernel-based approach for the learning of the solution operator of general nonhomogeneous partial differential equations (PDEs) is proposed. The method incorporates physical priors, typically encoded through the PDE operator, into a kernel ridge regression framework, and employs a regularization-based formulation to construct an operator learner. This yields a closed-form estimator that is independent of the input functions that determine the underlying PDE. From the perspective of regularization theory, the resulting estimator induces a well-defined operator that links input and output spaces, which contain the functions that define a Dirichlet problem and its solution, respectively. Consequently, it effectively shifts from a PDE solver to an operator-based solver. In contrast to standard supervised learning methods, it does not rely on paired input--output training data and enables systematic extrapolation beyond observed regimes.   A full error analysis is conducted, providing convergence rates for the operator-based solver under suitable choices of regularization parameters. Extensive numerical experiments, including Darcy flow and Helmholtz equations, demonstrate that the proposed method achieves high accuracy and efficiency across a range of problem settings, and compares favorably with operator learning approaches in both approximation quality and computational cost.

---

## 论文详细总结（自动生成）

### 论文总结：偏微分方程解算子的核学习 (Kernel Learning of PDE Solution Operators)

#### 1. 核心问题与整体含义
论文的研究动机在于解决传统数值方法（如有限元、有限差分）在重复调用 PDE 求解器（如设计优化、不确定性量化）时计算成本过高的问题。同时，现有的科学机器学习方法存在局限：
*   **算子学习（如 DeepONet, FNO）**：通常是“黑盒”模式，需要海量成对的输入-输出数据，且在未见过的输入分布上泛化性差。
*   **物理信息神经网络（PINNs）**：虽然不需要成对数据，但针对每一个新的边界条件或源项都需要重新训练网络，无法作为通用的算子求解器。
**核心问题**：如何构建一个既能利用物理先验（PDE 方程本身），又不需要成对训练数据，且能实现“一次训练，多次快速推理”的线性 PDE 解算子？

#### 2. 方法论
论文提出了一种基于**再生核希尔伯特空间（RKHS）**的物理信息核学习框架：
*   **核心思想**：将 PDE 的微分算子 $L_s$ 和边界算子 $B$ 直接嵌入到核岭回归（Kernel Ridge Regression）的损失函数中。
*   **关键技术细节**：
    *   **统一算子表示**：将 PDE 问题转化为 $P_s u = h$ 的形式，其中 $P_s$ 整合了内部微分和边界条件。
    *   **微分再生性质**：利用核函数的微分再生性质，证明了即使在损失函数中包含导数项，解依然可以表示为核基函数的线性组合。
    *   **闭式估计量**：通过变分推导，得到了解算子的闭式解（Closed-form estimator）。这意味着求解过程转化为一个广义的 Gram 矩阵回归问题。
    *   **算子化转换**：该方法最显著的特点是其估计量与具体的输入函数 $h$（源项和边界值）解耦。一旦选定核函数和采样点，可以预计算一组“核基（Kernel Basis）”，对于任何新的输入 $h$，只需进行简单的向量内积即可获得解。
*   **误差分析**：论文提供了严谨的理论证明，将总误差分解为估计误差和逼近误差，并给出了在正则化参数 $\lambda$ 下的收敛阶。

#### 3. 实验设计
论文通过多个物理场景验证了方法的有效性：
*   **场景 1：Darcy Flow（达西流）**：测试了平滑、异质以及不连续渗透率场下的二维流动问题。
*   **场景 2：Helmholtz Equation（亥姆霍兹方程）**：重点测试了高频（$\omega=200$）振荡情况下的求解精度。
*   **场景 3：Schrödinger & Heat Equations（薛定谔方程与热传导方程）**：在补充材料中验证了含时问题和复杂势能场问题。
*   **Benchmark 与对比方法**：
    *   主要对比了 **Green Operator Learning**（基于格林函数的算子学习方法）。
    *   对比指标包括相对 $L^2$ 误差、相对 $L^\infty$ 误差以及计算耗时。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 或 CPU 型号。
*   **计算效率**：文中强调了该方法的极高效率。例如，在 Darcy Flow 实验中，预计算和推理的总耗时通常在 **1.5 秒左右**；在 Helmholtz 方程中，该方法仅需几秒钟，而对比的格林算子方法则需要约 0.7 小时的训练时间。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从一维到三维（补充材料中含 3D Poisson 问题）的多种线性 PDE 类型。
*   **充分性**：实验设计较为全面，不仅包含了标准平滑算例，还专门针对**不连续输入**和**高频振荡**等数值计算中的难点进行了测试。
*   **客观性**：通过与最新的同类核方法进行对比，并展示了在不同正则化参数下的收敛曲线，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **高精度与高效率**：该方法在保持 $10^{-3}$ 级别误差的同时，计算速度比现有的数据驱动算子学习方法快数个数量级。
*   **无需配对数据**：该方法仅需 PDE 方程本身和域内采样点，完全摆脱了对“输入-输出”标签数据的依赖。
*   **强大的外推能力**：由于内置了物理算子结构，该方法在处理训练分布之外的输入函数时，表现远优于纯数据驱动的方法（如格林算子学习在高频下会失效，而本文方法依然稳健）。
*   **格林函数连接**：理论上证明了所学习的核基实际上是对底层 PDE 格林函数的数值逼近。

#### 7. 优点
*   **理论完备性**：不同于许多深度学习论文，本文提供了完整的 RKHS 框架下的收敛性分析和误差界。
*   **闭式解优势**：避免了神经网络训练中的随机梯度下降（SGD）不稳定性及调参难题。
*   **算子解耦**：实现了真正的“算子学习”，预计算完成后，新输入的推理成本极低。

#### 8. 不足与局限
*   **线性限制**：目前的方法论主要针对**线性 PDE**。虽然文中提到了向非线性扩展的可能性，但闭式解的优越性在线性情形下最为突出，非线性问题可能仍需迭代或线性化处理。
*   **核函数选择**：性能在一定程度上依赖于核函数（如高斯核）带宽参数的选取，对于极度复杂的奇异性问题，可能需要更复杂的自适应核。
*   **维度灾难**：尽管核方法在某种程度上缓解了维度问题，但在极高维空间下的采样效率和 Gram 矩阵的数值稳定性仍是潜在挑战。

（完）
