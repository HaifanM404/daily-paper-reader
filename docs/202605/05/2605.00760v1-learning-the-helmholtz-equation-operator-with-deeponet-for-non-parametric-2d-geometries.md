---
title: Learning the Helmholtz equation operator with DeepONet for non-parametric 2D geometries
title_zh: 使用 DeepONet 学习非参数化二维几何的亥姆霍兹方程算子
authors: "Rodolphe Barlogis, Ferhat Tamssaouet, Quentin Falcoz, Stéphane Grieu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00760v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于亥姆霍兹方程的物理信息神经算子
tldr: 本研究利用基于物理信息的 DeepONet 框架，解决了非参数化二维几何形状下的亥姆霍兹方程求解问题。通过符号距离函数（SDF）编码任意形状的散射体几何信息，模型能够学习从几何结构到散射场的算子映射。该方法无需有限元（FEM）生成的训练数据，避免了重复网格划分，在保持泛化能力的同时，显著提升了计算效率，为复杂波动物理问题的快速仿真提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00760v1/fig-001.webp\", \"caption\": \"Figure 6: Evolution of the training losses over iterations. Individual contributions from the PDE residual, the inner boundary condition (Γ), and the outer absorbing boundary condition (Γout) are shown separately.\", \"page\": 15, \"index\": 1, \"width\": 652, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-002.webp\", \"caption\": \"Figure 9: Normalized pointwise error between the trained model predictions and the FEM reference solutions, as a function of the incidence angle. The vertical dashed lines at ±30◦ delimit the training range.\", \"page\": 22, \"index\": 2, \"width\": 777, \"height\": 1002}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-003.webp\", \"caption\": \"Figure 2: Schematic representation of the physics-informed DeepONet solving an Helmholtz problem. Highlight the decomposition of total wave field into the incident field Winc and the scattered field Ws\", \"page\": 10, \"index\": 3, \"width\": 752, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-004.webp\", \"caption\": \"Figure 5: Symmetrical geometry encoded with ϕ evaluated in ten different points. It serves as input of the branch net.\", \"page\": 14, \"index\": 4, \"width\": 409, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-005.webp\", \"caption\": \"Figure 8: Absolute pointwise error between the trained model predictions and the FEM reference solutions, for three incidence angles. The +60◦ case lies outside the training range, i.e., [−30◦,+30◦].\", \"page\": 17, \"index\": 5, \"width\": 814, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-006.webp\", \"caption\": \"Figure 1: 2D Domain with inclusion and absorbing outer borders. Highlight the incident wave field Winc and the scattered field Ws\", \"page\": 9, \"index\": 6, \"width\": 819, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2605.00760v1/fig-007.webp\", \"caption\": \"Figure 7: FEM solution of the Helmholtz problem (Incidence angle: 0◦)\", \"page\": 16, \"index\": 7, \"width\": 814, \"height\": 257}]"
motivation: 旨在解决传统有限元方法在处理变几何亥姆霍兹方程时计算量大且需频繁重新划分网格的问题。
method: 采用物理信息驱动的 DeepONet 架构，利用符号距离函数编码散射体几何作为分支网络输入，结合局部坐标作为主干网络输入。
result: 该模型在未见过的几何形状上表现出良好的泛化能力，且计算速度远快于传统的有限元方法。
conclusion: 该框架成功实现了对非参数化几何算子的隐式物理建模，提供了一个无需依赖标注数据且计算轻量化的替代仿真方案。
---

## 摘要
本文研究了在非参数化区域上求解二维亥姆霍兹方程的问题，利用了基于 DeepONet 框架的物理信息神经算子网络。我们考虑一个二维正方形区域，其中心包含一个具有任意边界几何形状的内含物。该内含物作为入射谐波的散射体。本研究旨在学习将散射体几何形状与产生的散射场联系起来的算子。通过在区域内若干点评估到内部内含物边界的有向距离函数来编码其几何形状。该函数作为 DeepONet 架构中分支（branch）部分的输入，而局部信息则作为主干（trunk）部分的输入。这种方法能够编码任意几何形状，无论其是否经过参数化。通过将模型在未见几何形状上的评估结果与等效的有限元法（FEM）结果进行比较，测试了其泛化能力。训练好的网络权重隐式地嵌入了局部物理规律及其与区域几何的相互作用。如果训练空间充分覆盖了目标评估空间，模型就能实现相应的泛化。此外，该模型可以经过微调以扩展到另一个感兴趣的区域，而无需从头开始重新训练。该框架还避免了针对每种几何形状重新划分网格的需要。所提出的方法提供了一个比 FEM 替代方案计算量更轻的代理模型，并且避免了对 FEM 生成的训练数据的依赖。

## Abstract
This paper deals with solving the 2D Helmholtz equation on non-parametric domains, leveraging a physics-informed neural operator network based on the DeepONet framework. We consider a 2D square domain with an inclusion of arbitrary boundary geometry at its center. This inclusion acts as a scatterer for an incoming harmonic wave. The aim is to learn the operator linking the geometry of the scatterer to the resulting scattered field. A signed distance function to the boundary of the inner inclusion, evaluated at several points in the domain, is used to encode its geometry. It serves as input for the branch part of the DeepONet architecture, while local information is used as input for the trunk part. This approach enables the encoding of arbitrary geometries, whether they are parameterized or not. The evaluation of the model on unseen geometries is compared with its finite element method (FEM) equivalent to test its generalization capabilities. The trained network weights implicitly embed the local physics and their interaction with the domain geometry. If the training space sufficiently covers the target evaluation space, the model can generalize accordingly. Furthermore, it can be refined to extend to another region of interest without retraining from scratch. This framework also avoids the need to remesh the domain for each geometry. The proposed approach delivers a computationally lighter surrogate model than FEM alternatives and avoids relying on FEM-generated training data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning the Helmholtz equation operator with DeepONet for non-parametric 2D geometries》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在波动物理学中，求解偏微分方程（PDE）如亥姆霍兹方程（Helmholtz equation）通常依赖有限元法（FEM）。然而，当几何形状发生变化时，FEM 需要重新划分网格并重新计算，这在设计优化或逆问题求解中构成了巨大的计算瓶颈。
*   **核心问题**：如何构建一个轻量级的代理模型，能够直接学习从“任意几何形状”到“物理场解”的算子映射，且无需依赖昂贵的模拟数据进行训练。
*   **整体含义**：本文提出了一种结合物理信息（Physics-Informed）与神经算子（DeepONet）的框架，旨在实现非参数化几何下的快速波场仿真。

### 2. 论文提出的方法论
*   **核心思想**：利用 **DeepONet** 架构学习解算子。该架构分为两个子网络：**Branch Net（分支网络）**负责编码全局几何特征，**Trunk Net（主干网络）**负责处理局部空间坐标。
*   **关键技术细节**：
    *   **几何编码（SDF）**：使用符号距离函数（Signed Distance Function, SDF）来表征几何。Branch Net 的输入是在域内固定采样点处评估的 SDF 向量；Trunk Net 的输入则包含查询点坐标 $(x, y)$、该点的局部 SDF 值及其梯度（用于提供边界法向信息）。
    *   **物理驱动训练**：该模型是**无监督**的。损失函数（Loss Function）直接定义为 PDE 的残差，包括：
        1.  **亥姆霍兹方程残差**：$\nabla \cdot (\mu \nabla W_s) + \rho \omega^2 W_s = 0$。
        2.  **内边界条件**：在散射体边缘强制执行零通量（刚性边界）。
        3.  **外边界条件**：在计算域边缘执行吸收边界条件（Sommerfeld 辐射条件）。
    *   **场分解**：网络仅预测散射场 $W_s$，总场通过解析给出的入射场 $W_{inc}$ 与预测的 $W_s$ 叠加得到，这降低了训练难度。

### 3. 实验设计
*   **场景设定**：一个 2D 正方形区域，中心包含一个具有复杂凹陷形状的对称散射体。
*   **数据集与 Benchmark**：
    *   **训练集**：仅包含 5 种特定旋转角度（-30°, -10°, 0°, 10°, 30°）的几何形状。
    *   **Benchmark**：使用开源有限元软件 **FreeFem++** 生成的数值解作为基准参考。
*   **对比与评估**：在 [-60°, 60°] 范围内的所有整数角度上评估模型，对比 DeepONet 预测值与 FEM 参考值之间的点对点绝对误差和相对 $L_2$ 误差。

### 4. 资源与算力
*   **硬件支持**：文中提到受益于法国 LAAS 实验室提供的 GPU 计算资源。
*   **软件框架**：基于 **TensorFlow** 后端的 **DeepXDE** 库实现。
*   **明确性说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 V100）、具体的训练总时长以及显存占用情况。但作者强调了 PINN 训练阶段计算量大，而推理阶段（Inference）极快。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 15,000 个全域配置点，并针对边界区域额外增加了 50% 的采样点。
    *   测试了 121 组几何配置（从 -60° 到 60° 的整数旋转）。
*   **充分性评价**：
    *   **内插验证充分**：在训练角度范围（[-30°, 30°]）内，实验证明了模型具有极高的插值精度。
    *   **外推测试客观**：作者诚实地展示了当角度超过 ±30° 时误差迅速上升的现象，体现了实验的客观性。
    *   **局限性**：实验主要集中在单一基础形状的旋转上，对于拓扑结构完全不同的几何（如孔洞数量变化）未做消融实验或广泛验证。

### 6. 论文的主要结论与发现
*   **算子学习可行性**：证明了 DeepONet 可以通过 SDF 编码成功学习非参数化几何的物理算子。
*   **推理优势**：一旦训练完成，模型推理速度远快于 FEM，且无需重新划分网格。
*   **泛化规律**：模型在训练集覆盖的几何空间内表现良好，但外推能力受限。
*   **物理嵌入**：网络权重隐式地捕获了局部物理规律及其与几何的相互作用。

### 7. 优点（亮点）
*   **非参数化表征**：引入 SDF 采样作为 Branch Net 输入，解决了传统 PINN 难以处理任意形状几何的问题，比 NURBS 参数化更具通用性。
*   **数据效率**：完全基于物理方程驱动，无需预先生成大量的 FEM 标签数据，解决了“数据获取昂贵”的问题。
*   **局部信息增强**：在 Trunk Net 中加入局部 SDF 梯度，显著提升了边界条件的执行精度。

### 8. 不足与局限
*   **频率限制**：实验仅在低频（低波数 $\omega$）下进行。对于高频问题，由于波场的剧烈振荡，PINN 往往难以收敛（光谱偏差问题），论文未对此给出高频下的表现。
*   **外推能力弱**：模型对训练范围外的几何形状泛化能力较差，这限制了其在完全未知环境下的应用。
*   **几何多样性不足**：目前的验证仅限于单一形状的旋转，尚未证明其在处理具有完全不同拓扑特征（如多个散射体、尖锐边缘）时的鲁棒性。

（完）
