---
title: Isotropic Fourier Neural Operators
title_zh: 各向同性傅里叶神经算子
authors: Michael F. Staddon
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02597v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于求解偏微分方程的傅里叶神经算子
tldr: 傅里叶神经算子（FNO）在求解偏微分方程方面表现出色，但其线性变换往往忽略了物理系统中的等向性对称性。本文提出等向性傅里叶神经算子（IFNO），通过修改傅里叶层中的线性变换，使其严格遵循空间对称性。该方法不仅提升了模型在物理模拟中的预测性能，还显著减少了参数量，在2D和3D场景下分别实现了高达16倍和96倍的参数压缩，实现了精度与效率的双重提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02597v1/fig-001.webp\", \"caption\": \"FIG. 1. Isotropic Fourier Neural Operator. (a) The full model architecture. Lift the input to a higher dimensional channel space by neural network P . Apply L Fourier layers. Project down to output channel space by neural network Q. (b) Fourier layer architecture. On top: Fourier transform the input, apply linear transform R on the lowest n modes, apply the inverse Fourier transform. On the bottom: apply linear transform W . Finally, add the top and bottom branches and apply non-linear activation function σ. (c) Isotropic Fourier Kernel. The Fourier kernel R is generated using symmetries of space from an upper right triangular tensor Riso. Starting from the left, the original parameters are used to generate the full parameters through reflections representing transposition, reflection in y, and reflection in x.\", \"page\": 2, \"index\": 1, \"width\": 501, \"height\": 284}]"
motivation: 传统的傅里叶神经算子在处理等向性物理系统时，其线性变换无法保证结果与坐标系选择无关，违背了物理对称性。
method: 通过对傅里叶层中的线性变换进行约束和修改，确保模型能够尊重空间对称性，从而构建出等向性傅里叶神经算子。
result: 实验表明，该方法在提高模型性能的同时，将2D和3D模型的参数量分别降低了16倍和96倍。
conclusion: 等向性傅里叶神经算子通过引入物理对称性约束，成为一种更高效、更符合物理规律的偏微分方程求解工具。
---

## 摘要
傅里叶神经算子（Fourier Neural Operators）是学习函数空间之间映射的深度学习模型，可用于学习和求解偏微分方程（PDEs），在某些情况下其速度显著快于传统的 PDE 求解器。模型内部包含傅里叶层，这些层直接对傅里叶模态应用线性变换，其参数取决于波数。然而，大多数物理系统是各向同性的，即结果与所选坐标系无关，但线性变换并不一定遵循这些对称性。我们提出了一种对线性变换的改进方法，以确保遵循空间对称性，称之为各向同性傅里叶神经算子。该方法不仅提高了模型性能，还将 2D 情况下的参数量减少了多达 16 倍，3D 情况下减少了多达 96 倍。

## Abstract
Fourier Neural Operators are deep learning models that learn mappings between function spaces and can be used to learn and solve partial differential equations (PDEs), in some cases significantly faster than traditional PDE solvers. Within the model are Fourier layers, which apply linear transformations directly to the Fourier modes, with parameters depending on the wave numbers. However, most physical systems are isotropic, with the results being independent of the coordinate system chosen, but the linear transformations do not necessarily respect these symmetries. We propose a modification to the linear transformations that ensures spatial symmetries are respected, called the Isotropic Fourier Neural Operator, which both improves model performance and reduces the number of parameters by up to a factor of 16 in 2D and 96 in 3D.

---

## 论文详细总结（自动生成）

这是一份关于论文《Isotropic Fourier Neural Operators》（各向同性傅里叶神经算子）的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：傅里叶神经算子（FNO）是求解偏微分方程（PDE）的强大工具，通过在频率空间进行线性变换来学习函数映射。
*   **核心问题**：大多数物理系统具有**各向同性（Isotropy）**，即物理规律不随坐标系的旋转或镜像而改变。然而，标准 FNO 的傅里叶层参数是独立学习的，无法自动保证这种空间对称性。
*   **研究动机**：通过将物理对称性（如旋转、镜像、转置）硬编码到模型架构中，既能提高模型对物理规律的表达精度，又能通过参数共享大幅减少模型规模。

### 2. 论文提出的方法论：Iso-FNO
*   **核心思想**：强制傅里叶核 $R$ 遵循二面体群 $D_4$ 的对称性（针对 2D 空间），确保输入在经过旋转或镜像变换后，输出也发生相应的变换。
*   **关键技术细节**：
    *   **实值约束**：为了保证实值输入产生实值输出，要求傅里叶参数满足共轭对称，这消除了虚部参数。
    *   **空间对称约束**：在 2D 情况下，要求傅里叶核 $R_{k,l}$（$k,l$ 为波数）满足：
        *   $R_{k,l} = R_{-k,l}$（x 方向镜像）
        *   $R_{k,l} = R_{k,-l}$（y 方向镜像）
        *   $R_{k,l} = R_{l,k}$（转置/坐标交换）
    *   **参数生成流程**：模型仅学习一个位于上三角区域的极小参数张量 $R_{iso}$，然后通过转置和镜像操作填充生成完整的傅里叶核 $R$。
*   **参数压缩率**：理论上，这种对称性约束在 2D 中可减少 16 倍参数，在 3D 中可减少 96 倍（结合了 3 个维度的镜像和 3 个坐标轴的排列组合）。

### 3. 实验设计
*   **实验场景**：**2D Darcy Flow（达西流）**，这是一个经典的流体力学 PDE 问题，描述流体在多孔介质中的流动。
*   **数据集**：使用 1000 个训练样本和 200 个测试样本，分辨率为 $128 \times 128$。
*   **Benchmark 与对比方法**：
    *   **基准模型**：标准傅里叶神经算子（FNO）。
    *   **评估指标**：$L_2$ 误差和 $H_2$ 误差。
    *   **泛化性测试**：对比了模型在原始测试集、转置测试集、x 轴翻转测试集和 y 轴翻转测试集上的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到使用了 Cosine Annealing 学习率调度，训练 100 个 epoch，batch size 为 20，初始学习率为 0.001。模型配置为 16 个模态（modes）和 32 个通道（channels）。

### 5. 实验数量与充分性
*   **实验规模**：主要集中在 2D Darcy Flow 这一种物理场景。
*   **充分性评价**：
    *   **客观性**：通过对比训练误差和测试误差，清晰地展示了 Iso-FNO 缓解过拟合的能力。
    *   **公平性**：在相同的超参数（模态数、通道数、层数）下对比，确保了性能提升源于架构改进而非资源堆砌。
    *   **局限性**：实验仅限于 2D 标量场，未在 3D 场景或更复杂的矢量场（如 Navier-Stokes 方程）上进行大规模验证。

### 6. 主要结论与发现
*   **性能提升**：在测试集上，Iso-FNO 的 $L_2$ 误差比标准 FNO 降低了约 **30%**。
*   **参数极度压缩**：在 2D 实验中，Iso-FNO 的参数量从 4.2M 减少到 0.57M（约 **7.4 倍**实际压缩，理论上限为 16 倍，差异源于 FFT 实现细节）。
*   **完美的对称泛化**：标准 FNO 在面对翻转或转置的输入时误差剧增，而 Iso-FNO 在这些变换下的误差保持恒定，证明了其严格的等变性。
*   **缓解过拟合**：虽然 Iso-FNO 在训练集上的拟合速度略慢于 FNO，但其测试误差更低，说明物理约束起到了正则化作用。

### 7. 优点
*   **物理一致性**：通过架构设计直接保证了物理对称性，而非依赖数据增强。
*   **极高的效率**：在不增加推理延迟的前提下，大幅降低了显存占用和模型权重大小。
*   **通用性**：该方法可以轻松集成到 FNO 的其他变体（如 F-FNO, SFNO）中。

### 8. 不足与局限
*   **实验覆盖面窄**：仅验证了 2D 标量场问题，缺乏对 3D 问题和矢量场（如速度场、涡度场）的实证支持。
*   **灵活性限制**：对于某些非各向同性的物理系统（例如受重力方向影响的系统），这种硬编码的对称性可能会限制模型的表达能力。
*   **实现细节**：论文未详细讨论在非均匀网格或复杂边界条件下的表现。

（完）
