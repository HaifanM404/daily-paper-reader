---
title: "Di-BiLPS: Denoising induced Bidirectional Latent-PDE-Solver under Sparse Observations"
title_zh: Di-BiLPS：稀疏观测下基于去噪诱导的双向潜空间偏微分方程求解器
authors: "Zhonghao Li, Chaoyu Liu, Qian Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13790v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于正向和反向PDE问题的神经框架
tldr: "针对现实中偏微分方程（PDE）观测数据极度稀疏导致传统和神经求解器失效的问题，本文提出Di-BiLPS框架。该框架通过变分自编码器将高维数据压缩至潜空间，结合潜空间扩散模型建模不确定性，并利用对比学习对齐表征。Di-BiLPS在仅有3%观测数据的极端情况下，通过PDE感知去噪算法实现了高效的正向与反向问题求解，并支持零样本超分辨率重建。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13790v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of the architecture of Di-BiLPS : (a) Compression module via a pre-trained VAE (left), (b) Diffusion module with proposed PDE-Guided denoising algorithm(middle), and (c) Contrastive learning module (right). Proposed design leverages module (a) to extract informative and robust representations from extremely sparse inputs and performs high-speed denoising algorithm within the compressed latent space.\", \"page\": 2, \"index\": 1, \"width\": 917, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2605.13790v1/fig-002.webp\", \"caption\": \"Figure 3. Comparison between our model and DiffusionPDE on the Darcy Flow (top) and inhomogeneous Helmholtz equation (bottom) tasks.\", \"page\": 7, \"index\": 2, \"width\": 492, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.13790v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of the relative ℓ2 error of various methods on forward and inverse PDE tasks. The runtime performance (in seconds) of our model and DiffusionPDE is also reported. The best results in each task are highlighted in bold.\", \"page\": 8, \"index\": 3, \"width\": 1011, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.13790v1/fig-004.webp\", \"caption\": \"Figure 2. Illustration of contrastive learning module. Proposed GINO-ViT framework encodes both sparse and full observations into a unified latent space. By optimizing the match score within this space, framework is designed to capture informative and robust representations between sparse and full observations. Here, we emphasize that ViT modules with the same color share weights in this figure.\", \"page\": 5, \"index\": 4, \"width\": 510, \"height\": 269}]"
motivation: 现有神经PDE求解器在极度稀疏观测下推理效率受限且精度显著下降，难以处理复杂的时空建模任务。
method: 构建了一个在潜空间运行的统一框架，集成VAE压缩、扩散模型和对比学习，并引入基于方差保持扩散过程的PDE感知去噪算法。
result: "在多个PDE基准测试中，该方法在观测率低至3%时仍保持SOTA性能，同时大幅降低了计算开销。"
conclusion: Di-BiLPS为极端稀疏数据下的PDE求解提供了高效、准确且具备超分辨率能力的通用神经框架。
---

## 摘要
偏微分方程（PDEs）是模拟复杂自然和物理现象的基础。然而，在许多现实应用中，观测数据极其稀疏，这严重限制了经典数值求解器和现有神经方法的适用性。虽然神经方法在适度稀疏的观测下表现出良好的效果，但它们在高分辨率下的推理效率有限，且在极度稀疏的情况下准确度会大幅下降。在这项工作中，我们提出了 Di-BiLPS，这是一个统一的神经框架，能够有效处理极度稀疏观测下的 PDE 正向和反向问题。Di-BiLPS 结合了变分自编码器将高维输入压缩到紧凑的潜空间中，利用潜扩散模块建模不确定性，并采用对比学习来对齐表征。该框架完全在潜空间中运行，在保持灵活的输入输出映射的同时实现了高效推理。此外，我们引入了一种基于方差保持扩散过程的 PDE 启发式去噪算法，进一步提高了推理效率。在多个 PDE 基准测试上的广泛实验表明，Di-BiLPS 在极度稀疏的输入（低至 3%）下始终能达到当前最先进（SOTA）的性能，同时显著降低了计算成本。此外，由于 Di-BiLPS 允许在连续的时空域上进行预测，它还实现了零样本超分辨率。

## Abstract
Partial differential equations (PDEs) are fundamental for modeling complex natural and physical phenomena. In many real-world applications, however, observational data are extremely sparse, which severely limits the applicability of both classical numerical solvers and existing neural approaches. While neural methods have shown promising results under moderately sparse observations, their inference efficiency at high resolutions is limited, and their accuracy degrades substantially in the extremely sparse regime. In this work, we propose the Di-BiLPS, a unified neural framework that effectively handle both forward and inverse PDE problems under extremely sparse observations. Di-BiLPS combines a variational autoencoder to compress high-dimensional inputs into a compact latent space, a latent diffusion module to model uncertainty, and contrastive learning to align representations. Operating entirely in this latent space, the framework achieves efficient inference while retaining flexible input-output mapping. In addition, we introduce a PDE-informed denoising algorithm based on a variance-preserving diffusion process, which further improves inference efficiency. Extensive experiments on multiple PDE benchmarks demonstrate that Di-BiLPS consistently achieves SOTA performance under extremely sparse inputs (as low as 3%), while substantially reducing computational cost. Moreover, Di-BiLPS enables zero-shot super-resolution, as it allows predictions over continuous spatial-temporal domains.

---

## 论文详细总结（自动生成）

这是一份关于论文《Di-BiLPS: Denoising induced Bidirectional Latent-PDE-Solver under Sparse Observations》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实物理场景中，偏微分方程（PDE）的观测数据往往极度稀疏（如观测率低于3%），这导致传统数值求解器和现有的神经算子（Neural Operators）在精度和效率上大幅下降。
*   **研究背景**：现有的神经PDE方法通常依赖密集观测，且在高分辨率推理时计算开销巨大。此外，如何在一个统一框架内同时高效处理正向问题（从系数推解）和反向问题（从解推系数）仍是一个挑战。

### 2. 方法论：Di-BiLPS 框架
Di-BiLPS 提出了一个在压缩潜空间（Latent Space）中运行的统一生成框架，包含三个核心组件：
*   **VAE 压缩模块**：利用预训练的变分自编码器（VAE），结合 GINO（几何感知神经算子）的 Mesh 编码器和解码器，将不规则、高维的 PDE 数据压缩为紧凑的潜空间表示。这使得模型能处理连续空间域并实现零样本超分辨率。
*   **对比学习对齐（GiT）**：引入 GINO-ViT 架构，通过对比学习损失函数，将极度稀疏的观测点与完整的物理场表征在潜空间中进行对齐，从而提取出鲁棒的特征表示。
*   **潜空间扩散模型与 PDE 感知去噪**：
    *   在潜空间中执行扩散过程，建模物理系统的不确定性。
    *   **关键技术**：提出一种 **PDE 启发式去噪算法**。在反向去噪步中，不仅利用条件信息，还引入了物理约束（PDE 残差梯度）和观测约束（稀疏点误差梯度）作为引导，强制生成结果符合物理定律和已知观测。

### 3. 实验设计
*   **数据集**：涵盖了 5 个标准的 PDE 基准测试，包括静态和动态系统：
    *   Darcy Flow（达西流）、Inhomogeneous Helmholtz（非齐次亥姆霍兹方程）、Poisson（泊松方程）、Bounded/Non-Bounded Navier-Stokes（有界/无界纳维-斯托克斯方程）。
*   **基准方法（Baselines）**：
    *   **算子学习类**：PINO, DeepONet, PINNs, FNO。
    *   **不确定性预测/扩散类**：DiffusionPDE, GraphPDE。
*   **实验设置**：主要针对 **3% 观测率** 的极端稀疏场景，评估正向和反向问题的相对 $\ell_2$ 误差。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 Nvidia RTX 4090 GPU**（每张显卡 24GB 显存）。
*   **软件框架**：基于 Hugging Face 的 `diffusers` 开源框架实现。
*   **显存占用**：Di-BiLPS 在推理时的显存占用约为 8.6 GB（相比之下，DiffusionPDE 约为 4.5 GB，但 Di-BiLPS 在速度上有巨大优势）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个数据集上分别进行了正向和反向任务的测试（共 10 组核心对比）。
    *   每组实验运行 10 次以确保统计显著性。
    *   进行了详细的**消融实验**，验证了 PDE 引导、观测引导和条件引导各自的贡献。
    *   进行了**零样本超分辨率**实验和**不同稀疏度（1%, 3%, 10%）**的泛化性实验。
*   **评价**：实验设计较为充分，对比了从经典物理信息神经网络到最新的扩散模型，覆盖了多种物理场景，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：在极度稀疏（3%）的情况下，Di-BiLPS 在 10 项任务中的 8 项优于 SOTA 扩散模型（DiffusionPDE），且远超传统的神经算子方法。
*   **效率提升**：由于在压缩潜空间操作，其推理时间比 DiffusionPDE **减少了约 90%**。
*   **超分辨率能力**：得益于 GINO 的连续表征能力，模型可以在低分辨率训练后，直接在任意高分辨率网格上进行零样本推理。
*   **鲁棒性**：对比学习模块显著增强了模型从极少量数据中提取物理特征的能力。

### 7. 优点与亮点
*   **双向统一**：一个框架同时解决正向和反向问题，无需针对任务修改架构。
*   **潜空间加速**：巧妙地解决了扩散模型在处理高维 PDE 数据时推理缓慢的痛点。
*   **物理引导去噪**：将 PDE 残差作为梯度引导引入扩散采样过程，增强了生成结果的物理一致性。
*   **处理不规则域**：结合 GINO 使得模型能够处理复杂的几何形状和非结构化网格。

### 8. 不足与局限
*   **物理引导的增益有限**：消融实验显示，直接在潜空间应用 PDE 残差梯度（$N_{pde}$）带来的性能提升并不显著，说明潜空间与物理空间的梯度映射仍有优化空间。
*   **极端稀疏阈值**：当观测率进一步下降到 0.1% 时，模型性能会出现剧烈下降，存在失效阈值。
*   **显存开销**：虽然推理速度快，但由于集成了 VAE、ViT 和扩散模型，其显存占用高于部分轻量级基准模型。

（完）
