---
title: "Approximation of Maximally Monotone Operators : A Graph Convergence Perspective"
title_zh: 极大单调算子的逼近：一种图收敛视角
authors: "Takashi Furuya, Yury Korolev, Takaharu Yaguchi"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12301v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 偏微分方程解算子的算子学习
tldr: 本文针对传统算子学习在处理微分算子等不连续或多值算子时的局限性，提出了一种基于图收敛（Painlevé-Kuratowski收敛）的新型逼近范式。研究指出传统的一致逼近和Lp逼近在处理此类算子时存在根本缺陷。通过聚焦于极大单调算子，作者证明了利用连续编码器-解码器架构可在局部图收敛意义下实现有效逼近，并进一步设计了基于预解式的参数化方法，以构建能够保持算子极大单调性的结构化模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12301v1/fig-001.webp\", \"caption\": \"Figure 1: High-frequency input u(t) (left) and its derivative u′(t) (right). Here u(t) is generated as in (6) with K = 1, n = 6, aj = 1, bj = 0, and β = 0.5.\", \"page\": 26, \"index\": 1, \"width\": 826, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2605.12301v1/fig-002.webp\", \"caption\": \"Figure 2: High-frequency input u(x, y) (left) and the corresponding nonlinear p-Laplacian −div(|∇u|p−2∇u) with p = 1.2 (right). Here u(x, y) is generated as in (7) with K = 1, n = 9, aj = 1, bj = 1, and β = 0.\", \"page\": 26, \"index\": 2, \"width\": 829, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2605.12301v1/fig-003.webp\", \"caption\": \"Table 5: Comparison of test evaluations for A1(u) = u′.\", \"page\": 30, \"index\": 3, \"width\": 756, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.12301v1/fig-004.webp\", \"caption\": \"Table 6: Comparison of test evaluations for A2(u) = − div(|∇u|p−2∇u).\", \"page\": 30, \"index\": 4, \"width\": 772, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2605.12301v1/fig-005.webp\", \"caption\": \"Table 4: Hyperparameters for the experiment on A2(u) = − div(|∇u|p−2∇u).\", \"page\": 29, \"index\": 5, \"width\": 602, \"height\": 696}, {\"url\": \"assets/figures/arxiv/2605.12301v1/fig-006.webp\", \"caption\": \"Table 3: Hyperparameters for the experiment on A1(u) = u′.\", \"page\": 28, \"index\": 6, \"width\": 771, \"height\": 984}]"
motivation: 传统的算子学习框架无法有效处理微分算子等不连续或多值算子，且标准的一致逼近或Lp逼近度量在这些场景下不再适用。
method: 引入图收敛（Painlevé-Kuratowski收敛）理论作为逼近准则，并结合连续编码器-解码器架构与基于预解式的参数化技术进行建模。
result: 证明了任何极大单调算子都可以在局部图收敛意义下被连续架构逼近，并成功构造了能够保持极大单调性的结构保持逼近模型。
conclusion: 该研究为非连续和多值算子的学习提供了坚实的理论基础，通过图收敛视角拓展了算子逼近理论的适用边界。
---

## 摘要
算子学习在无限维空间之间的连续映射（如偏微分方程（PDE）解算子）方面取得了巨大成功。然而，许多备受关注的算子（包括微分算子）是不连续的或多值的，且超出了经典逼近框架的范畴。我们提出了一种范式转变，通过图收敛（Painlevé-Kuratowski 收敛）来构建逼近理论，这非常适用于闭算子。我们证明了在这种设定下，一致逼近和 $L^p$ 逼近在根本上是不充分的。针对极大单调算子，我们证明了任何此类算子都可以在局部图收敛的意义下由连续的编码器-解码器架构进行逼近，并进一步通过基于预解式（resolvent）的参数化构建了保持极大单调性的结构保持逼近。

## Abstract
Operator learning has been highly successful for continuous mappings between infinite-dimensional spaces, such as PDE solution operators. However, many operators of interest-including differential operators-are discontinuous or set-valued, and lie outside classical approximation frameworks. We propose a paradigm shift by formulating approximation via graph convergence (Painlevé-Kuratowski convergence), which is well-suited for closed operators. We show that uniform and $L^p$ approximation are fundamentally inadequate in this setting. Focusing on maximally monotone operators, we prove that any such operator can be approximated in the sense of local graph convergence by continuous encoder-decoder architectures, and further construct structure-preserving approximations that retain maximal monotonicity via resolvent-based parameterizations.

---

## 论文详细总结（自动生成）

这篇论文《Approximation of Maximally Monotone Operators: A Graph Convergence Perspective》探讨了算子学习在处理不连续、无界或多值算子（如微分算子）时的理论基础与实现方法。以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的算子学习框架（如 DeepONet、FNO）主要针对无限维空间之间的**连续映射**（如 PDE 的解算子）。然而，科学计算中核心的**微分算子**通常是不连续的、无界的，甚至是多值的（如次微分算子）。
*   **核心问题**：在处理这些“闭算子”（Closed Operators）时，传统的一致收敛（Uniform Convergence）或 $L^p$ 收敛准则在理论上是失效的。论文旨在回答：当目标算子不再连续时，应该如何定义“逼近”？
*   **整体含义**：论文提出了一种范式转变，主张使用**图收敛（Graph Convergence）**作为逼近闭算子的自然拓扑，并重点研究了**极大单调算子**这一具有重要物理意义的子类。

### 2. 论文提出的方法论
*   **核心思想**：不再要求算子值在点对点上接近，而是要求算子的**图（Graph）**在集合意义下趋于一致。
*   **关键技术细节**：
    *   **图收敛（Painlevé–Kuratowski 收敛）**：定义了算子序列 $A_m$ 收敛到 $A$ 的条件，即其图的内外极限均符合目标算子的图。
    *   **局部图距离（Local Graph Distance）**：引入了一种定量度量 $d_{gph, K}$，用于衡量两个算子图在紧集 $K$ 上的差异。
    *   **Yosida 正则化**：利用极大单调算子的性质，通过 Yosida 近似将不连续/多值算子转化为 Lipschitz 连续的算子，作为神经网络逼近的桥梁。
    *   **结构保持架构**：基于预解式（Resolvent）参数化模型 $\hat{A}(u) = \frac{u - \hat{S}(u)}{\lambda}$，其中 $\hat{S}$ 被约束为非扩张映射（1-Lipschitz）。这种构造确保了学习到的模型在数学上严格保持极大单调性。

### 3. 实验设计
*   **实验场景**：
    1.  **线性算子 $A_1$**：一阶导数算子 $u \mapsto u'$（典型无界算子）。
    2.  **非线性算子 $A_2$**：$p$-Laplacian 算子（非线性极大单调算子的代表）。
*   **数据集**：使用合成的傅里叶级数数据，特别设计了高频输入以模拟微分算子的不稳定性。
*   **对比方法（Baselines）**：
    *   基于 $\ell_2$ 损失训练的 FNO。
    *   基于 Soft $\ell_\infty$ 损失训练的 FNO。
    *   基于 **Soft Graph Loss**（论文提出）训练的 FNO。
    *   **Graph + Structure**：结合图损失与非扩张约束的结构化模型。
*   **评估指标**：测试集 MSE、相对 $L^2$ 误差、测试集图距离、单调性违规程度（Violation fraction/mean）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 Adam 优化器，训练轮数为 80-100 epoch，Batch size 为 32-64。

### 5. 实验数量与充分性
*   **实验规模**：针对两个核心算子进行了对比实验，每组实验均运行了 **4 个随机种子**并报告了均值和标准差。
*   **充分性评价**：
    *   **理论验证充分**：实验直接对应了文中的反例（如高频输入导致 $L^p$ 逼近失效），验证了图收敛的必要性。
    *   **消融/对比客观**：对比了不同损失函数和是否引入结构约束的效果。
    *   **局限性**：实验主要集中在合成数据和单值算子上，对于论文理论中提到的“多值算子（Set-valued）”尚未进行大规模数值实验验证。

### 6. 主要结论与发现
*   **传统准则的局限**：通过反例证明了闭算子无法在一致收敛或 $L^p$ 收敛下被连续算子逼近。
*   **图收敛的可行性**：证明了任何极大单调算子都可以在局部图收敛意义下由编码器-解码器架构逼近。
*   **结构化的优势**：引入非扩张约束的结构化模型在保持单调性方面显著优于标准模型，且在处理无界算子时表现出更好的稳定性。
*   **损失函数影响**：提出的 Soft Graph Loss 相比传统的 $\ell_2$ 损失，能更有效地捕捉算子的图结构特征。

### 7. 优点
*   **理论严谨性**：为算子学习提供了一个处理不连续算子的坚实数学框架，填补了闭算子逼近理论的空白。
*   **范式创新**：从“点值逼近”转向“图逼近”，更符合微分算子和变分问题的本质。
*   **结构保持**：提出的基于预解式的参数化方法具有很强的实用价值，保证了物理性质（单调性）的严格成立。

### 8. 不足与局限
*   **计算复杂度**：Soft Graph Loss 涉及样本间的成对比较，计算复杂度为 $O(N^2)$，在大规模数据集上可能存在扩展性问题。
*   **局部性限制**：目前的逼近定理是局部的（依赖于紧集 $K$），尚未实现全局图收敛的统一保证。
*   **实验覆盖面**：尚未在真实的实验观测数据或更复杂的耦合 PDE 系统上进行验证。
*   **参数敏感性**：结构化模型中的正则化参数 $\lambda$ 和非扩张约束权重 $\gamma$ 可能需要精细调优。

（完）
