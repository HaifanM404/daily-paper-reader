---
title: Isotropic Fourier Neural Operators
title_zh: 各向同性傅里叶神经算子
authors: Michael F. Staddon
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02597v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 用于学习和求解偏微分方程的傅里叶神经算子
tldr: 傅里叶神经算子（FNO）在求解偏微分方程方面表现优异，但其标准线性变换未能充分利用物理系统中的等向性对称性。本文提出了等向性傅里叶神经算子（IFNO），通过约束傅里叶层中的线性变换以遵循空间对称性。该方法在提升模型模拟精度的同时，显著降低了计算开销，在2D和3D任务中分别实现了16倍和96倍的参数量削减，为构建高效且符合物理规律的深度学习模型提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02597v1/fig-001.webp\", \"caption\": \"FIG. 1. Isotropic Fourier Neural Operator. (a) The full model architecture. Lift the input to a higher dimensional channel space by neural network P . Apply L Fourier layers. Project down to output channel space by neural network Q. (b) Fourier layer architecture. On top: Fourier transform the input, apply linear transform R on the lowest n modes, apply the inverse Fourier transform. On the bottom: apply linear transform W . Finally, add the top and bottom branches and apply non-linear activation function σ. (c) Isotropic Fourier Kernel. The Fourier kernel R is generated using symmetries of space from an upper right triangular tensor Riso. Starting from the left, the original parameters are used to generate the full parameters through reflections representing transposition, reflection in y, and reflection in x.\", \"page\": 2, \"index\": 1, \"width\": 501, \"height\": 284}]"
motivation: 传统的傅里叶神经算子在处理各向同性的物理系统时，其参数化方式未能尊重空间坐标系的旋转和平移对称性。
method: 通过修改傅里叶层中的线性变换，使其在处理傅里叶模态时强制满足空间对称性约束。
result: IFNO 在提高模型性能的同时，将 2D 和 3D 模型的参数量分别降低了 16 倍和 96 倍。
conclusion: 在神经算子中引入等向性对称性不仅能增强模型的物理一致性，还能极大地提升参数效率。
---

## 摘要
傅里叶神经算子（Fourier Neural Operators）是学习函数空间之间映射的深度学习模型，可用于学习和求解偏微分方程（PDEs），在某些情况下其速度显著快于传统的 PDE 求解器。模型内部包含傅里叶层，这些层直接对傅里叶模态应用线性变换，其参数取决于波数。然而，大多数物理系统是各向同性的，即结果与所选坐标系无关，但线性变换并不一定遵循这些对称性。我们提出了一种对线性变换的改进方法，以确保遵循空间对称性，称之为各向同性傅里叶神经算子。该方法不仅提高了模型性能，还将 2D 情况下的参数量减少了多达 16 倍，3D 情况下减少了多达 96 倍。

## Abstract
Fourier Neural Operators are deep learning models that learn mappings between function spaces and can be used to learn and solve partial differential equations (PDEs), in some cases significantly faster than traditional PDE solvers. Within the model are Fourier layers, which apply linear transformations directly to the Fourier modes, with parameters depending on the wave numbers. However, most physical systems are isotropic, with the results being independent of the coordinate system chosen, but the linear transformations do not necessarily respect these symmetries. We propose a modification to the linear transformations that ensures spatial symmetries are respected, called the Isotropic Fourier Neural Operator, which both improves model performance and reduces the number of parameters by up to a factor of 16 in 2D and 96 in 3D.

---

## 论文详细总结（自动生成）

以下是对论文《Isotropic Fourier Neural Operators》（各向同性傅里叶神经算子）的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：傅里叶神经算子（FNO）是求解偏微分方程（PDEs）的强大工具，通过在傅里叶空间进行线性变换来学习函数映射。
*   **核心问题**：大多数物理系统具有**各向同性（Isotropy）**，即物理规律不随坐标系的旋转或镜像而改变。然而，标准 FNO 的线性变换（傅里叶核）并不强制遵循这些空间对称性，导致模型参数冗余，且在处理对称变换后的输入时表现不一致。
*   **研究意义**：通过将空间对称性（如 $D_4$ 二面体群）引入 FNO 架构，旨在显著减少参数量，同时提升模型在未知数据上的泛化能力和物理一致性。

### 2. 方法论
*   **核心思想**：强制傅里叶层中的线性变换矩阵 $R$ 满足空间对称性约束，从而仅需存储一小部分独立参数即可生成完整的傅里叶核。
*   **关键技术细节**：
    *   **对称性约束**：在 2D 空间中，要求模型对 $x$ 轴反射、$y$ 轴反射以及 $x-y$ 坐标交换（转置）具有等变性。
    *   **参数缩减逻辑**：
        *   **反射对称**：要求 $R_{k,l} = R_{-k,l}$，参数量减半。
        *   **实值约束**：为了保证实值输出，要求 $R_{k,l} = R^*_{-k,l}$，这使得参数变为实数而非复数，参数量再次减半。
        *   **转置对称**：要求 $R_{k,l} = R_{l,k}$，进一步减少参数。
    *   **算法流程**：模型从一个较小的上三角张量 $R_{iso}$ 开始，通过一系列反射和转置操作生成完整的傅里叶核 $R$。在 2D 中理论参数减少 16 倍，在 3D 中（考虑 $z$ 轴及坐标排列）理论减少 96 倍。
    *   **等变性实现**：这种构造方式确保了模型在 $D_4$ 群下的等变性，无需像 G-FNO 那样使用昂贵的群卷积。

### 3. 实验设计
*   **数据集/场景**：**2D Darcy Flow（达西流）**，描述流体通过多孔介质的流动。
*   **Benchmark（基准）**：标准傅里叶神经算子（Standard FNO）。
*   **对比设置**：
    *   分辨率：$128 \times 128$。
    *   样本量：1000 个训练样本，200 个测试样本。
    *   超参数：16 个模态（modes），32 个通道（channels），3 个傅里叶层。
    *   评估指标：$L_2$ 相对误差和 $H_2$ 误差。
    *   对称性测试：对比模型在原始数据、转置数据、$x$ 轴翻转数据和 $y$ 轴翻转数据上的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **效率提及**：作者提到 Iso-FNO 的推理时间与标准 FNO 相同（因为最终计算张量大小一致），但内存占用显著降低。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 Darcy Flow 这一经典基准进行了深入实验。
*   **充分性评价**：
    *   **优点**：实验不仅对比了标准测试集误差，还专门设计了针对对称性的鲁棒性测试（翻转和转置训练数据），有力证明了 Iso-FNO 的等变性优势。
    *   **不足**：仅使用了一个数据集（Darcy Flow），未在更复杂的 Navier-Stokes 方程或 3D 场景下进行大规模验证。

### 6. 主要结论与发现
*   **参数大幅削减**：在 2D 实验中，Iso-FNO 的参数量从 420 万减少到 57 万（约 7.4 倍实际缩减，受限于 FFT 实现细节），理论缩减倍数更高。
*   **泛化性能提升**：尽管 Iso-FNO 在训练集上的误差略高于标准 FNO（因为约束限制了过拟合），但在**测试集上的 $L_2$ 误差降低了约 30%**。
*   **物理一致性**：Iso-FNO 在处理翻转或转置的输入时，其预测结果与原始预测的翻转/转置完全一致，而标准 FNO 则会出现显著偏差。

### 7. 优点
*   **极高的参数效率**：通过物理对称性先验，极大地降低了模型对显存的需求。
*   **内置等变性**：无需复杂的群卷积计算，通过简单的张量操作即可实现空间对称性。
*   **抗过拟合**：对称性约束起到了一种强正则化的作用，使模型在小样本或分布外数据上表现更稳健。

### 8. 不足与局限
*   **适用范围限制**：该方法假设系统是各向同性的。对于具有特定方向性（如重力方向、特定电磁场方向）的非对称物理系统，该模型可能不适用或需要修改。
*   **实验覆盖面较窄**：目前仅在标量场（Scalar Field）和单一 PDE 任务上验证，尚未探讨其在矢量场（如流体速度场）或时空耦合任务中的表现。
*   **3D 验证缺失**：虽然理论推导了 3D 下 96 倍的参数削减，但文中未给出实际的 3D 实验数据。

（完）
