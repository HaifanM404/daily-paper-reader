---
title: Online Safety Filter for Deformable Object Manipulation with Horizon Agnostic Neural Operators
title_zh: 基于时界无关神经算子的可变形物体操作在线安全过滤器
authors: "Jiaxing Li, Hanjiang Hu, Zhuoyuan Wang, Yorie Nakahira, Changliu Liu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01069v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 学习底层PDE动力学映射的神经算子
tldr: 针对流体、布料等可变形物体操作中的安全性难题，本文提出一种基于神经算子的在线安全过滤器。该方法结合了视界无关神经算子（HANO）来学习偏微分方程动力学，并利用边界控制障碍函数（BCBF）通过轻量级二次规划实时修正控制指令。实验表明，该方法在流体操作任务中显著提升了安全性，比传统的奖励塑造方法更可靠且高效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01069v1/fig-001.webp\", \"caption\": \"Fig. 3. Base and filtered target trajectories in the transporting task.\", \"page\": 5, \"index\": 1, \"width\": 1037, \"height\": 509}, {\"url\": \"assets/figures/arxiv/2605.01069v1/fig-002.webp\", \"caption\": \"Fig. 4. Comparison between the base policy and the filtered safe policy on the gathering task. The original and filtered control inputs, the evolution of the target distance, and the object trajectories in the XY plane are presented. The base policy induces turbulence near the end of the task, which pushes the objects farther apart and increases the distance around t ≈ 0.25s. The filtered policy mitigates this disturbance, maintains convergence toward the desired distance, and improves overall safety performance.\", \"page\": 6, \"index\": 2, \"width\": 1004, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2605.01069v1/fig-003.webp\", \"caption\": \"Fig. 1. Overall framework of the proposed boundary-safe neural operator control architecture comparing with the offline filtering baseline.\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 295}]"
motivation: 现有的可变形物体操作方法主要依靠奖励塑造来间接处理安全问题，缺乏在部署阶段的显式安全约束保证。
method: 提出一种结合视界无关神经算子与边界控制障碍函数的方法，通过实时求解轻量级二次规划来修正控制策略以满足安全约束。
result: "在FluidLab流体操作任务中，该方法将安全轨迹率提升了22%，并有效缩短了进入安全区域所需的步数。"
conclusion: 研究证明，约束驱动的安全过滤机制在处理具有复杂动力学的可变形物体时，比传统的奖励塑造方法更具可靠性和执行效率。
---

## 摘要
涉及流体、布料和软体物体等可变形介质的机器人操作任务的安全关键控制仍然具有挑战性，因为现有的基于学习的方法通过奖励塑造间接编码安全性，这在部署时无法保证约束的满足。我们提出了一种用于可变形物体操作的约束驱动在线安全过滤器，通过对任何标称控制策略进行最小程度的修改，实时强制执行显式的任务级安全约束。我们的方法结合了两个关键组件：一个是时界无关神经算子，它学习底层偏微分方程（PDE）动力学的边界输入输出映射，并且无需重新训练即可推广到不同的展开长度；另一个是边界控制障碍函数，它通过轻量级二次规划在任务相关的输出层级验证安全性。由此产生的安全约束在边界输入速率上是仿射的，从而实现了实时在线过滤。我们在 FluidLab 的流体操作任务上评估了所提方法，结果表明，与未过滤的基础策略相比，该过滤器将安全轨迹率提高了高达 22%，同时减少了到达安全集所需的步数，证明了约束驱动的安全执行比奖励塑造方法更可靠且更高效。

## Abstract
Safety critical control of robotic manipulation tasks involving deformable media such as fluids, cloth, and soft objects remains challenging because existing learning based approaches encode safety indirectly through reward shaping, which provides no guarantee of constraint satisfaction at deployment. We present a constraint driven online safety filter for deformable object manipulation that enforces explicit task level safety constraints in real time by minimally modifying any nominal control policy. Our approach combines two key components: a horizon agnostic neural operator that learns the boundary input output mapping of the underlying PDE dynamics and generalizes across variable rollout lengths without retraining, and a boundary control barrier function that certifies safety at the task relevant output level via a lightweight quadratic program. The resulting safety constraint is affine in the boundary input rate, enabling real time online filtering. We evaluate the proposed method on fluid manipulation tasks in FluidLab, where the filter improves safe trajectory rates by up to 22% over unfiltered base policies while also reducing the number of steps required to reach the safe set, demonstrating that constraint driven safety enforcement is both more reliable and more efficient than reward shaping approaches.

---

## 论文详细总结（自动生成）

以下是对论文《Online Safety Filter for Deformable Object Manipulation with Horizon Agnostic Neural Operators》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在涉及流体、布料等可变形物体的机器人操作中，实现**实时、显式的硬安全约束保证**。
*   **研究背景**：
    *   可变形物体动力学受偏微分方程（PDE）支配，具有高维、非线性且难以建模的特点。
    *   现有的深度强化学习（DRL）方法通常将安全性简化为“奖励惩罚”（Reward Shaping），这在部署时无法提供确定性的安全保证，且在面对扰动或分布偏移时容易产生危险行为。
    *   传统的控制障碍函数（CBF）难以直接应用于无限维的 PDE 系统。

### 2. 方法论
该论文提出了一种结合神经算子与边界控制障碍函数的在线过滤框架，核心组件包括：
*   **HANO (Horizon Agnostic Neural Operator，视界无关神经算子)**：
    *   **核心思想**：改进傅里叶神经算子（FNO），使其能够处理变长的输入序列。
    *   **技术细节**：在训练阶段采用“前缀采样”策略，随机采样不同长度的轨迹前缀进行学习。这使得模型在推理时无需重新训练即可支持任意长度的在线滚动预测（Rollout）。
*   **BCBF (Boundary Control Barrier Function，边界控制障碍函数)**：
    *   **核心思想**：在低维的边界输出层级（而非高维的全状态空间）定义安全集。
    *   **算法流程**：
        1.  **预测**：利用 HANO 根据历史控制序列预测当前的边界输出。
        2.  **构建约束**：将安全约束转化为关于控制输入变化率（$\dot{U}$）的仿射约束。
        3.  **在线过滤**：通过求解一个轻量级的**二次规划（QP）问题**，对标称策略（Nominal Policy）给出的指令进行最小修正，确保系统状态始终保持在安全集内。
        4.  **鲁棒性处理**：引入过滤阈值 $\beta$ 来应对神经算子与真实动力学之间的模型失配。

### 3. 实验设计
*   **实验平台**：使用 **FluidLab**（一个基于 Taichi 的微分流体-刚体交互模拟环境）。
*   **实验场景**：
    1.  **Transporting (运输任务)**：控制喷水装置推动刚体方块进入指定区域（区域约束）。
    2.  **Gathering (聚集任务)**：控制刮板在流体中移动，使两个漂浮物体靠近（距离约束）。
*   **对比基准 (Benchmarks)**：
    *   **Base Policy**：带有控制噪声的比例控制器（Noisy P-Controller）。
    *   **Offline Filter**：基于固定视界神经算子的离线过滤器。
*   **评估指标**：安全轨迹成功率、平均不安全步数、收敛至安全集的速度。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **模型参数细节**：提到了 HANO 使用了 64 个隐藏通道、4 层结构，截断频率模式为 16；BCBF 采用了一个四层 MLP（宽度为 [32, 128, 64, 32]）。控制频率设定为 500Hz。

### 5. 实验数量与充分性
*   **实验规模**：针对两个任务分别进行了大量随机初始化的 Rollouts 测试。
*   **充分性评价**：
    *   **较为充分**：实验涵盖了两种不同的安全几何形态（区域到达 vs. 点对距离），验证了方法的通用性。
    *   **对比客观**：通过引入控制噪声模拟了真实环境的不确定性，并对比了离线过滤器的失败案例，证明了在线过滤的必要性。
    *   **局限性**：实验完全基于仿真环境，缺乏在真实物理机器人上的验证。

### 6. 主要结论与发现
*   **安全性显著提升**：在运输任务中，安全轨迹率比基础策略提升了 **22.4%**；在聚集任务中提升了 **18.1%**。
*   **效率优化**：安全过滤器不仅保证了安全，还缩短了到达安全区域所需的步数（减少约 4-5%），证明约束驱动方法比奖励塑造更高效。
*   **在线适配的重要性**：实验发现离线过滤器表现甚至不如基准策略，强调了针对当前策略进行实时在线修正的关键作用。

### 7. 优点
*   **视界灵活性**：HANO 的设计解决了传统神经算子难以应用于变长在线任务的痛点。
*   **实时性强**：通过将 PDE 动力学简化为边界输出映射，并将安全验证转化为 QP 求解，实现了极高的计算效率。
*   **模块化**：该过滤器可以作为插件配合任何现有的标称控制策略（如 RL、PID）使用。

### 8. 不足与局限
*   **模型依赖风险**：安全保证高度依赖于神经算子对 PDE 动力学的拟合精度。如果环境发生剧烈变化（分布偏移），模型预测失效可能导致安全保证崩溃。
*   **场景复杂度有限**：目前仅在流体-刚体交互的简单场景下验证，尚未测试更复杂的布料折叠或多物体缠绕等高难度操作。
*   **超参数敏感性**：QP 问题中的松弛因子和安全边界参数可能需要针对不同任务进行手动调优。

（完）
