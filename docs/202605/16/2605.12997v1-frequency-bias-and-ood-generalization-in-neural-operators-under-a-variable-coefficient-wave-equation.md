---
title: Frequency Bias and OOD Generalization in Neural Operators under a Variable-Coefficient Wave Equation
title_zh: 变系数波动方程下神经算子的频率偏差与 OOD 泛化
authors: "Runlong Xie, An Luo"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.12997v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 在PDE背景下使用FNO和DeepONet进行算子学习
tldr: 本研究探讨了神经算子（如FNO和DeepONet）在变系数波动方程下的分布外（OOD）泛化能力。通过独立改变输入频率和系数平滑度，研究发现FNO在处理未见的高频输入时误差剧增，而DeepONet表现较稳健但整体误差较高。研究揭示了架构表示偏差对物理模拟可靠性的关键影响，为开发更具鲁棒性的神经算子提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12997v1/fig-001.webp\", \"caption\": \"Figure 4. Representative prediction examples comparing FNO and DeepONet under different OOD conditions. Top row shows predicted terminal waveforms versus ground truth for ID, OOD-frequency, and OOD-smoothness inputs. Bottom row shows the corresponding pointwise prediction errors. The figure illustrates that FNO suffers larger local distortions and phase errors under high-frequency OOD, whereas DeepONet maintains smoother waveform structure, highlighting differences in error patterns between the two architectures.\", \"page\": 7, \"index\": 1, \"width\": 1017, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2605.12997v1/fig-002.webp\", \"caption\": \"Figure 8. Additional OOD-smoothness prediction examples under shifted coefficient smoothness. Each panel follows the same convention as Figure 7, showing input fields, terminal prediction, and residual curves for an independently selected test sample.\", \"page\": 14, \"index\": 2, \"width\": 987, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.12997v1/fig-003.webp\", \"caption\": \"Table 1. Model and training hyperparameters used in the experiments.\", \"page\": 13, \"index\": 3, \"width\": 487, \"height\": 572}, {\"url\": \"assets/figures/arxiv/2605.12997v1/fig-004.webp\", \"caption\": \"Figure 7. Additional OOD-frequency prediction examples illustrating representative failure patterns under high-frequency initial conditions. Each sample includes the initial condition, spatial coefficient field, terminal prediction, and residual distribution.\", \"page\": 13, \"index\": 4, \"width\": 987, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.12997v1/fig-005.webp\", \"caption\": \"Figure 1. An overview of our experimental pipeline for operator learning under the variable-coefficient wave equation. Input functions consist of an initial displacement field u0(x) and a spatially varying wave-speed coefficient c(x). A finite-difference solver generates the terminal solution u(x, T ) at a fixed time horizon, forming supervised training data. FNO and DeepONet are then trained to approximate the mapping from (u0, c) to u(x, T ). Evaluation is performed on in-distribution samples and two structured OOD settings: a frequency shift in the initial condition and a smoothness shift in the coefficient field.\", \"page\": 5, \"index\": 5, \"width\": 763, \"height\": 440}]"
motivation: 旨在探究神经算子在面对结构化分布偏移（如频率和系数平滑度变化）时的泛化表现及内在机制。
method: 在一维变系数波动方程背景下，对比分析了傅里叶神经算子（FNO）和深度算子网络（DeepONet）在不同OOD设置下的性能。
result: FNO在平滑度偏移下表现优异但在高频偏移下误差激增，而DeepONet虽整体精度较低但在频率偏移下表现出更好的稳定性。
conclusion: 神经算子的架构表示偏差决定了其在分布外场景的可靠性，缩小分布内性能与分布外泛化之间的差距是未来研究的关键。
---

## 摘要
神经算子学习将初始条件映射到偏微分方程 (PDE) 的终态解，为完整的算子映射提供了一种代理模型。这使得在不同输入配置下进行快速预测成为可能。尽管近期的神经算子架构在多种 PDE 任务中表现出强大的性能，但它们在结构化分布偏移下的行为仍未得到充分理解。为了对此进行研究，我们利用两种代表性架构——傅里叶神经算子 (FNO) 和深度算子网络 (DeepONet)，研究了一维变系数波动方程控制下的波传播场景中的算子学习。为了检验它们在分布偏移下的泛化能力，我们考虑了独立改变输入频率和系数光滑度的结构化分布外 (OOD) 设置。结果表明，在光滑度偏移下，两种模型均保持了稳定的性能，其中 FNO 的误差更低。相比之下，在频率偏移下，FNO 在未见的高频输入下误差急剧增加，而 DeepONet 尽管整体误差较高，但性能下降较为缓和。我们的分析表明，这些差异源于每种架构表示和响应频率结构变化的方式。总之，这些发现强调了算子学习中强大的分布内性能与分布偏移下的泛化能力之间存在根本差距，并突显了架构表示偏差在开发更可靠的、适用于训练分布之外的基于物理的 PDE 模拟神经算子中的作用。

## Abstract
Neural operators learn to map initial conditions to the terminal solution of partial differential equations (PDEs), providing a surrogate for the full operator mapping. This enables rapid prediction across different input configurations. While recent neural operator architectures have demonstrated strong performance on diverse PDE tasks, their behavior under structured distribution shifts remains insufficiently understood. To investigate this, we study operator learning in a wave propagation setting governed by a one-dimensional variable-coefficient wave equation, using two representative architectures, the Fourier Neural Operator (FNO) and the Deep Operator Network (DeepONet). To examine their generalization under distribution shifts, we consider structured out-of-distribution (OOD) settings that independently vary input frequency and coefficient smoothness. The results show that under smoothness shifts, both models maintain stable performance, with FNO achieving lower error. In contrast, under frequency shifts, FNO exhibits a sharp increase in error under unseen high-frequency inputs, whereas DeepONet shows milder degradation despite higher overall error. Our analysis reveals that these differences arise from how each architecture represents and responds to variations in frequency structure. Together, these findings highlight a fundamental gap between strong in-distribution performance and generalization under distribution shifts in operator learning, underscoring the role of architectural representation bias in developing more reliable neural operators for physics-based PDE simulations beyond the training distribution.

---

## 论文详细总结（自动生成）

这篇论文对变系数波动方程背景下神经算子的频率偏差与分布外（OOD）泛化进行了深入研究。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：神经算子（如 FNO 和 DeepONet）在处理偏微分方程（PDE）时表现出强大的分布内（ID）预测能力，但在面对**结构化分布偏移**（如输入频率变化或介质不均匀性改变）时的泛化行为和内在机制尚不明确。
*   **研究动机**：在实际科学应用中，模型往往面临与训练数据物理特性不同的场景。研究旨在揭示不同架构的“表示偏差”（Architectural Bias）如何影响其在 OOD 场景下的可靠性，特别是针对具有复杂波动力学的变系数波动方程。

### 2. 方法论
*   **核心思想**：通过对比两种具有截然不同归纳偏置的算子架构——**傅里叶神经算子 (FNO)** 和 **深度算子网络 (DeepONet)**，分析它们在处理一维变系数波动方程时的表现。
*   **物理模型**：研究对象为一维保守型变系数波动方程：$u_{tt} = \partial_x(c(x)^2 u_x)$。输入为初始位移 $u_0(x)$ 和空间变化的波速系数 $c(x)$，输出为固定时间 $T$ 的终态解 $u(x, T)$。
*   **关键技术细节**：
    *   **FNO**：利用傅里叶变换在频域进行谱卷积，通过保留固定数量的低频模式来捕捉全局特征。
    *   **DeepONet**：采用分支-主干（Branch-Trunk）分解，分支网络编码输入函数，主干网络编码空间坐标，通过基函数展开近似算子映射。
    *   **数据生成**：使用二阶有限差分格式（Leapfrog 方案）求解 PDE，确保满足 CFL 稳定性条件，并采用调和平均处理空间变化的系数。

### 3. 实验设计
*   **场景与数据集**：
    *   **ID（分布内）**：训练和测试数据均采用低频初始条件和特定平滑度的系数场。
    *   **OOD-Frequency（频率偏移）**：测试集包含训练集中未见的高频初始位移分量。
    *   **OOD-Smoothness（平滑度偏移）**：改变系数场 $c(x)$ 的空间变异性（分为平滑、中等、粗糙三个等级）。
*   **Benchmark 与对比方法**：以高精度数值解为基准（Ground Truth），直接对比 FNO 和 DeepONet 在相同训练/测试协议下的表现。
*   **评估指标**：相对 $L_2$ 误差、能量诊断量（Energy-like diagnostic）以及谱误差分析（Spectral-error analysis）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了使用 Adam 优化器，学习率为 $1 \times 10^{-3}$，Batch Size 为 32，最大训练轮数为 100 Epochs，并使用了早停机制（Patience=10）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 ID、频率 OOD、平滑度 OOD 多种设置。
    *   进行了**消融实验**：针对 FNO 保留的傅里叶模式数量（8, 16, 32）进行了测试。
    *   提供了多组代表性样本的预测波形图、点误差图及谱误差曲线。
*   **充分性评价**：实验设计较为充分且客观。通过控制变量法独立改变频率和平滑度，能够有效隔离不同物理因素对模型的影响。谱误差分析为观察到的现象提供了深层的机理解释。

### 6. 主要结论与发现
*   **FNO 的表现**：在 ID 和平滑度偏移下精度极高，但在**频率偏移下表现出剧烈的性能下降**。即使增加保留的模式数量，也无法解决 OOD 频率下的相位偏移和振荡失真，这表明其谱表示在未见频率区间存在外推局限。
*   **DeepONet 的表现**：虽然整体 ID 误差高于 FNO，但在频率偏移下表现出**更平滑、更稳定的性能退化**。其误差在空间和频域分布更均匀，没有出现 FNO 那样的集中式谱失效。
*   **架构偏差的影响**：FNO 的全局频域耦合使其对已知频率敏感，而 DeepONet 的基函数展开机制在处理未见频率时更具鲁棒性。

### 7. 优点
*   **机理深入**：不仅对比了误差数值，还通过谱分析揭示了误差是如何在不同频率模态间分布的。
*   **控制变量严谨**：通过变系数波动方程这一具有挑战性的物理模型，成功分离了输入信号频率和介质异质性两个关键变量。
*   **架构透明性**：选择了最经典的 FNO 和 DeepONet，避免了复杂混合架构（如 Transformer-based）带来的干扰因素，使结论更具通用性。

### 8. 不足与局限
*   **维度限制**：实验仅限于一维空间，尚未验证在二维或三维复杂波场中的泛化规律。
*   **模型覆盖面**：虽然选择了代表性模型，但未包含最新的物理信息神经算子（PINO）或基于注意力机制的算子，这些模型可能具有不同的频率偏置特性。
*   **应用范围**：结论主要基于波动方程（双曲型 PDE），对于扩散方程（抛物型）或椭圆型方程的适用性有待进一步验证。

（完）
