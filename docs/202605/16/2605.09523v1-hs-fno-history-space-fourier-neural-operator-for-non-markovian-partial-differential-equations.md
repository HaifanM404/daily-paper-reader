---
title: "HS-FNO: History-Space Fourier Neural Operator for Non-Markovian Partial Differential Equations"
title_zh: HS-FNO：用于非马尔可夫偏微分方程的历史空间傅里叶神经算子
authors: Lennon J. Shikhman
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09523v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 针对非马尔可夫偏微分方程的神经算子
tldr: 针对非马尔可夫偏微分方程（如时滞方程和记忆系统），传统神经算子因忽略历史信息而表现不佳。本文提出历史空间傅里叶神经算子（HS-FNO），通过将状态提升至历史窗口空间，并结合学习未来预测与精确历史平移的分解机制，有效捕捉非马尔可夫动力学。在五类基准测试中，HS-FNO 在自回归预测精度和参数效率上均显著优于现有基准模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09523v1/fig-001.webp\", \"caption\": \"Figure 1: HS-FNO architecture. The predictor PΘ receives the current history field ut(θ, x) together with conditioning variables (µ, τ,∆t) and predicts the newly exposed future slice û(t + ∆t, ·). The known portion of the history is transported exactly, and ShiftAppend combines this with the predicted slice to form the updated history GΘ(ut).\", \"page\": 7, \"index\": 1, \"width\": 814, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.09523v1/fig-002.webp\", \"caption\": \"Table 1: Accuracy–efficiency comparison. Time is inference time per step in seconds; memory is peak memory in MB. Best value in each column is bolded\", \"page\": 18, \"index\": 2, \"width\": 574, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.09523v1/fig-003.webp\", \"caption\": \"Figure 4: Real-world traffic sanity check under the standard 12-input/12-output protocol. Bars show denormalized average MAE on METR-LA and PEMS-BAY; lower is better\", \"page\": 19, \"index\": 3, \"width\": 773, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2605.09523v1/fig-004.webp\", \"caption\": \"Figure 3: Mean rollout error over autoregressive prediction steps. HS-FNO has the lowest error at every rollout step among the main models.\", \"page\": 17, \"index\": 4, \"width\": 962, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2605.09523v1/fig-005.webp\", \"caption\": \"Figure 2: Aggregate relative errors across benchmark–regime cells. Bars show ten-seed means and error bars show 95% bootstrap confidence intervals. Lower is better.\", \"page\": 16, \"index\": 5, \"width\": 780, \"height\": 436}]"
motivation: 传统的神经算子通常假设系统具有马尔可夫性，无法有效处理依赖历史状态的非马尔可夫偏微分方程。
method: 提出 HS-FNO 架构，将状态定义在历史窗口上，并将更新过程分解为对未来切片的学习预测和对已知历史的精确平移操作。
result: "在延迟反应扩散、空间流行病学等五类任务中，HS-FNO 的自回归滚动误差相比现有基准模型降低了约 50%。"
conclusion: 强制执行历史状态演化的离散平移结构是构建非马尔可夫 PDE 代理模型的有效归纳偏置。
---

## 摘要
神经算子为随时间变化的偏微分方程提供了快速的代理模型，但其标准的自回归应用通常假设瞬时场 $u(t,\cdot)$ 是一个完整状态。这一假设对于时滞方程、分布式记忆系统和其他非马尔可夫动力学失效：两条轨迹可能在时间 $t$ 处一致，但由于其历史不同，仍具有不同的未来。我们引入了历史空间傅里叶神经算子（HS-FNO），这是一种针对时滞和记忆驱动的偏微分方程的神经算子，其公式建立在提升状态 $u_t(θ,x)=u(t+θ,x)$, $θ\in[-τ,0]$ 之上。关键的计算步骤是将一次历史状态更新分解为：针对新暴露的未来切片的学习预测器，以及针对从前一状态已知的历史窗口部分的精确移位-追加传输。这避免了学习确定性的历史坐标，减少了学习的输出维度，并强制执行了自然的离散历史更新。我们在五个基准系列上测试了 HS-FNO，涵盖了时滞反应-扩散、空间流行病学、非局部神经场动力学、时滞波和分布式记忆闭包。在十个随机种子中，HS-FNO 在主要基准模型中获得了最低的综合单步、历史空间和外推（rollout）误差。最大的提升出现在自回归预测中，综合外推误差从当前状态算子、滞后堆叠算子和无约束历史到历史算子的 $0.241$、$0.188$ 和 $0.185$ 分别下降到 $0.094$。该模型比无约束历史预测使用更少的参数。这些结果表明，强制执行历史状态演化的离散移位结构是非马尔可夫偏微分方程代理建模的一种有效归纳偏置。

## Abstract
Neural operators provide fast surrogate models for time-dependent partial differential equations, but their standard autoregressive use usually assumes that the instantaneous field $u(t,\cdot)$ is a complete state. This assumption fails for delay equations, distributed-memory systems, and other non-Markovian dynamics: two trajectories may agree at time $t$ and nevertheless have different futures because their histories differ. We introduce the History-Space Fourier Neural Operator (HS-FNO), a neural operator for delay and memory-driven PDEs formulated on the lifted state $u_t(θ,x)=u(t+θ,x)$, $θ\in[-τ,0]$. The key computational step is to decompose one history-state update into a learned predictor for the newly exposed future slice and an exact shift-append transport for the portion of the history window already known from the previous state. This avoids learning deterministic history coordinates, reduces the learned output dimension, and enforces the natural discrete history update. We test HS-FNO on five benchmark families covering delayed reaction--diffusion, spatial epidemiology, nonlocal neural-field dynamics, delayed waves, and distributed-memory closures. Across ten random seeds, HS-FNO attains the lowest aggregate one-step, history-space, and rollout errors among the principal baselines. The largest gain occurs in autoregressive prediction, where aggregate rollout error decreases from $0.241$, $0.188$, and $0.185$ for current-state, lag-stack, and unconstrained history-to-history operators, respectively, to $0.094$. The same model uses fewer parameters than unconstrained history prediction. These results indicate that enforcing the discrete shift structure of history-state evolution is an effective inductive bias for non-Markovian PDE surrogate modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《HS-FNO: History-Space Fourier Neural Operator for Non-Markovian Partial Differential Equations》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的神经算子（如 FNO）在处理随时间演化的 PDE 时，通常假设系统具有**马尔可夫性**，即当前时刻的状态 $u(t, \cdot)$ 足以决定未来。然而，在时滞微分方程（DDE/DPDE）或具有记忆效应的系统中，未来状态取决于一段**历史窗口**。
*   **研究动机**：如果仅使用瞬时场作为输入，模型会面临“非单值性”问题（即相同的当前状态可能对应不同的未来），导致自回归预测（Rollout）时误差迅速累积。论文旨在为非马尔可夫 PDE 构建一种能够显式处理历史状态的代理模型。

### 2. 方法论：HS-FNO
*   **核心思想**：将 PDE 的状态空间从瞬时场提升到**历史空间** $H_\tau = C([-\tau, 0]; X)$。模型不再仅仅预测下一个时刻的场，而是更新整个历史窗口。
*   **关键技术细节**：
    *   **算子分解**：将一步演化分解为“学习预测”和“确定性传输”两部分。
    *   **预测器（Predictor）**：使用 FNO 作为骨干网络，输入整个历史场 $u_t(\theta, x)$，仅预测新暴露出的**未来切片** $\hat{u}(t+\Delta t, \cdot)$。
    *   **移位-追加（Shift-Append）机制**：对于历史窗口中已知的部分，直接进行精确的平移操作，将预测的新切片追加到末尾。这避免了模型浪费参数去学习简单的“数据搬运”。
    *   **维度缩减**：相比于直接预测整个下一段历史，该方法将输出维度降低了约 $M$ 倍（$M$ 为历史采样点数）。
*   **公式流程**：$G_\Theta(u_t) = \text{ShiftAppend}(u_t, P_\Theta(u_t))$，其中 $P_\Theta$ 是学习到的未来切片映射。

### 3. 实验设计
*   **数据集/场景**：涵盖了五类具有代表性的非马尔可夫 PDE：
    1.  **时滞反应-扩散方程**：模拟生物种群的成熟延迟。
    2.  **空间流行病学**：模拟传染病的潜伏期或观测延迟。
    3.  **非局部神经场动力学**：具有空间依赖性时滞的积分微分方程。
    4.  **时滞波动方程**：测试相位准确性和长程稳定性。
    5.  **分布式记忆闭包**：模拟 Mori-Zwanzig 形式的非局部时间项。
*   **对比方法（Baselines）**：
    *   **Current-state**：仅输入当前时刻场。
    *   **Lag-stack**：将过去几个时刻的场堆叠为通道。
    *   **History2History (H2H)**：无约束地预测整个历史到历史的映射。
    *   **序列模型**：ConvLSTM、Temporal U-Net、Transformer。

### 4. 资源与算力
*   **硬件支持**：文中提到 Dell Technologies 提供了硬件支持（Dell Pro Precision 系列）。
*   **具体算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提供了推理时间（约 0.0018 秒/步）和内存占用（约 522 MB）等效率指标。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 5 个基准家族，每个家族包含多个参数制度（分布内、跨时滞、跨参数、跨分辨率）。
    *   所有主要实验均运行了 **10 个随机种子**，并提供了 95% 置信区间。
    *   包含消融实验（验证 Shift-Append 的必要性、骨干网络的选择等）。
*   **充分性评价**：实验设计非常**充分且客观**。通过自回归 Rollout 误差（而非仅看单步误差）来评估模型，这对于动力系统建模至关重要。同时，还加入了一个真实世界的交通流预测（METR-LA/PEMS-BAY）作为“常识性检查”。

### 6. 主要结论与发现
*   **精度提升**：HS-FNO 在所有指标上均优于基准。在最关键的自回归 Rollout 任务中，误差比当前状态模型降低了约 **61%**，比无约束历史模型降低了约 **49%**。
*   **稳定性**：显式的移位结构显著增强了长程预测的稳定性，防止了非物理误差在历史窗口中传播。
*   **效率**：HS-FNO 在保持高精度的同时，参数量远少于直接预测全历史的模型（H2H），且推理速度与简单模型相当。

### 7. 优点与亮点
*   **归纳偏置（Inductive Bias）**：成功地将时滞方程的数学结构（半流性质）转化为架构设计，即“只学未知的，平移已知的”。
*   **通用性**：不仅适用于简单的固定时滞，也能处理分布式记忆和非局部耦合。
*   **分辨率无关性**：继承了 FNO 的特性，支持在不同空间分辨率下进行推理。

### 8. 不足与局限
*   **分辨率转移挑战**：尽管在同分辨率下表现优异，但在跨分辨率测试中（尤其是反应-扩散方程），误差仍然较高，说明历史空间的超分辨率建模仍有难度。
*   **时滞依赖性**：模型性能仍受限于预设的历史窗口长度 $\tau$ 和采样频率，对于极长时滞或变时滞系统的适应性有待加强。
*   **数据依赖**：作为代理模型，它高度依赖于高质量数值解算器生成的训练数据。
*   **物理一致性**：虽然引入了半流一致性正则项，但在某些复杂制度下，该项反而可能干扰训练稳定性。

（完）
