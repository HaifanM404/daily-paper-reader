---
title: Adaptive Data Harvesting for Efficient Neural Network Learning with Universal Constraints
title_zh: 面向全称约束下高效神经网络学习的自适应数据采集
authors: "Siteng Kang, Xinhua Zhang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09707v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 物理信息神经网络 (PINNs) 的自适应数据采集
tldr: 针对在连续域上满足通用约束（如Lyapunov神经网络和PINN）的训练难题，本文提出一种自适应数据采集方法。该方法摒弃了传统的固定启发式采样，利用强化学习训练策略，根据模型学习状态动态调整样本。实验证明，该方法在提高约束满足度的同时显著提升了训练效率，具有广泛的适用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09707v1/fig-001.webp\", \"caption\": \"Figure 2: Ratio of safe samples on Lyapunov NN compared with baseline α\", \"page\": 6, \"index\": 1, \"width\": 849, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-002.webp\", \"caption\": \"Figure 3: Testing PINN error for PINN-Diffusion compared with baselines collocation selectors\", \"page\": 6, \"index\": 2, \"width\": 831, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-003.webp\", \"caption\": \"Figure 6: Testing PINN error on PINN-Wave compared with baselines collocation selectors\", \"page\": 8, \"index\": 3, \"width\": 820, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-004.webp\", \"caption\": \"Figure 7: Testing PINN error on PINN-Burgers compared with baselines collocation selectors\", \"page\": 8, \"index\": 4, \"width\": 820, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-005.webp\", \"caption\": \"Figure 1: Testing safe set fraction on Lyapunov-NN compared with baseline α\", \"page\": 5, \"index\": 5, \"width\": 902, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-006.webp\", \"caption\": \"Figure 4: Testing PINN error for PINN-Diffusion after 10,000 PINN iterations\", \"page\": 7, \"index\": 6, \"width\": 411, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.09707v1/fig-007.webp\", \"caption\": \"Figure 5: Training PDE residual and PINN error from different collocation selectors.\", \"page\": 7, \"index\": 7, \"width\": 411, \"height\": 678}]"
motivation: 传统的基于样本的约束强制方法依赖固定启发式规则，导致训练效率低下且解的质量不佳。
method: 提出一种基于强化学习的策略，通过学习如何根据模型性能动态、迭代地调整采样点。
result: 在Lyapunov神经网络和PINN上的实验表明，该方法显著提高了经验约束满足度和训练效率。
conclusion: 学习到的自适应采样策略优于手工设计的规则，为处理具有通用约束的神经网络训练提供了高效的新途径。
---

## 摘要
在连续域上训练满足全称约束的神经网络带来了独特的挑战。常见的例子包括 Lyapunov 神经网络 (Lyapunov NNs) 和物理信息神经网络 (PINNs)，在这些领域中，解析解通常不可获得或过于受限。因此，基于采样的方法被广泛用于强制执行这些约束，而样本的选择对收敛速度、稳定性和解的质量具有重大影响。大多数现有方法依赖于固定的启发式方法或手工规则，在实践中往往并非最优。在本文中，我们旨在通过从数据和经验中学习，根据模型不断演化的学习性能动态且迭代地调整样本，从而改进现有方法。通过强化学习训练，所学习的策略在提高测试问题经验约束满足度的同时，显著提升了效率。我们在 Lyapunov NNs 和 PINNs 上验证了该方法，并证明了其在需要自适应输入选择以实现有效训练的领域中具有更广泛的适用性。

## Abstract
Training neural networks to satisfy universal constraints over continuous domains poses unique challenges. Common examples include Lyapunov Neural Networks (Lyapunov NNs) and Physics-Informed Neural Networks (PINNs), where analytical solutions are generally either unavailable or overly restrictive. Sample-based methods are therefore commonly used to enforce these constraints, and the choice of samples has a substantial impact on convergence speed, stability, and solution quality. Most existing methods rely on fixed heuristics or handcrafted rules, and are suboptimal in practice. In this paper, we aim to improve upon them by learning, from data and experience, how to dynamically and iteratively adjust the samples in response to the model's evolving learning performance. Trained by reinforcement learning, the learned policy improves empirical constraint satisfaction on test problems while significantly improving efficiency. We validate the approach on both Lyapunov NNs and PINNs, and demonstrate its broader applicability to domains where adaptive input selection is essential for effective training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Data Harvesting for Efficient Neural Network Learning with Universal Constraints》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是在**连续域上具有全称约束（Universal Constraints）**的神经网络训练问题。这类问题的典型代表是 **Lyapunov 神经网络 (Lyapunov NNs)**（用于证明动力系统稳定性）和 **物理信息神经网络 (PINNs)**（用于求解偏微分方程）。
*   **核心挑战**：约束必须在整个连续输入空间内满足，但由于空间无限，无法穷举验证。
*   **研究动机**：目前的训练主要依赖于在特定点（配置点）进行采样。然而，现有的采样策略大多依赖于固定的启发式规则（如均匀分布、随机采样）或手工设计的自适应规则（如基于残差的采样）。这些方法往往无法根据模型在训练过程中的实时表现进行动态调整，导致训练效率低下、收敛缓慢或解的质量不佳。

### 2. 论文提出的方法论
论文提出了一种**基于强化学习（RL）的自适应数据采集框架**，将采样点的选择建模为一个马尔可夫决策过程（MDP）。

*   **核心思想**：引入一个“外部”RL 智能体来指导“内部”神经网络（Lyapunov NN 或 PINN）的训练。RL 智能体学习如何根据当前模型的训练状态（如残差分布、安全样本比例等）动态调整采样策略。
*   **关键技术细节**：
    *   **Lyapunov NNs 应用**：RL 智能体负责预测**外扩倍数 $\alpha$**。算法从当前安全区域的外扩区域 $V_\theta(\alpha c_k)$ 中采样，通过轨迹模拟标记正负样本，从而动态扩大认证的安全区域。
    *   **PINNs 应用**：RL 智能体充当“专家混合器”。它观察多个基础采样器（如随机、Sobol、Halton、RAD 等）产生的残差，然后输出一组**混合权重**，决定从各个采样器中抽取样本的比例。
*   **算法流程**：
    1.  初始化 RL 策略（如 TD3 或 SAC）。
    2.  在每个 RL 训练回合中，随机初始化内部模型（如不同参数的 PDE）。
    3.  在每个重采样步骤，RL 智能体观察当前状态，输出动作（$\alpha$ 或采样比例）。
    4.  根据动作采集新样本，更新内部模型参数。
    5.  计算奖励（如负误差或安全区域体积），并使用离线 RL 算法更新策略。

### 3. 实验设计
论文在四个基准环境上验证了该方法：
*   **Lyapunov NN**：倒立摆（Inverted Pendulum）基准测试。
*   **PINN-Diffusion**：一维扩散方程。
*   **PINN-Wave**：一维波动方程。
*   **PINN-Burgers**：Burgers 方程（无解析解，使用高精度数值解作为参考）。
*   **对比方法（Baselines）**：
    *   固定参数的启发式方法（如固定的 $\alpha$ 值）。
    *   随机采样（Random）、均匀采样（Uniform）。
    *   拟随机序列（Sobol, Halton）。
    *   先进的自适应方法：基于残差的自适应采样（RAD）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练规模**：文中提到 RL 智能体在 Lyapunov 任务中训练了 12,500 个 episodes，在 PINN 任务中训练了 150,000 个 episodes。PINN 的内部训练通常涉及 1,000 到 10,000 次迭代。虽然未给出硬件细节，但从 episode 数量来看，RL 策略的离线训练阶段具有一定的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：针对四种不同的物理/控制场景进行了实验，每项实验均使用 5 个随机种子重复运行并报告均值。
*   **充分性**：
    *   **多样性**：涵盖了控制理论（Lyapunov）和科学计算（PINN）两个不同领域，证明了框架的通用性。
    *   **消融与对比**：对比了多种经典的采样序列和最新的自适应方法（RAD），并展示了 RL 智能体在训练前后的性能提升。
    *   **客观性**：通过在训练过程中随机化环境参数（如摆杆长度、PDE 系数）来测试策略的泛化能力，而非仅在单一参数下过拟合。

### 6. 论文的主要结论与发现
*   **性能提升**：RL 引导的自适应采样在所有测试任务中均显著优于固定启发式方法和现有的自适应方法，表现为更快的收敛速度和更低的最终误差。
*   **动态适应性**：在 Lyapunov 任务中，RL 智能体学会了在早期采取激进的扩张策略（大 $\alpha$），在后期趋于保守，以维持稳定的边界探索。
*   **效率**：实验表明，经过训练的 RL 智能体仅需较少的采样点和迭代次数即可达到甚至超过基准方法的精度。

### 7. 优点
*   **模型无关性（Model-agnostic）**：该框架不依赖于特定的神经网络架构或 RL 算法（支持 TD3, SAC 等）。
*   **数据驱动的灵活性**：相比于人工设计的固定规则，RL 能够捕捉到训练动态中复杂的非线性模式，从而在不同阶段采取最优采样策略。
*   **通用框架**：成功将两个看似不同的领域（Lyapunov 和 PINN）统一在同一个自适应数据采集框架下。

### 8. 不足与局限
*   **离线训练开销**：RL 智能体本身需要大量的 episodes 进行预训练，这在处理极其复杂的 PDE 或高维系统时可能面临“冷启动”或计算成本过高的问题。
*   **奖励设计的敏感性**：RL 的效果高度依赖于奖励函数的设计（如 PINN 中需要参考解或高精度近似解来计算奖励），在完全没有参考解的实际场景中，如何定义有效的奖励仍是挑战。
*   **高维扩展性**：实验主要集中在一维 PDE 和简单的动力系统，该方法在高维复杂空间（如多维流体动力学）中的表现尚未得到验证。

（完）
