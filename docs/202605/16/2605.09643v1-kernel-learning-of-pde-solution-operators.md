---
title: Kernel Learning of PDE Solution Operators
title_zh: 偏微分方程解算子的核学习
authors: "Jianyu Hu, Juan-Pablo Ortega"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09643v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 学习通用偏微分方程解算子的核方法
tldr: 本研究提出了一种基于核方法的偏微分方程（PDE）解算子学习框架。该方法将PDE算子编码的物理先验融入核岭回归，构建出一种闭式估计器。与传统监督学习不同，该方法无需成对的输入输出训练数据，且具备系统性的外推能力。通过对达西流和亥姆霍兹方程的实验验证，证明了该方法在精度和计算效率上均优于现有算子学习方法，并提供了完整的收敛性误差分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09643v1/fig-001.webp\", \"caption\": \"Fig. 1. Input functions: f(x, y) = { 6, x ≥ 0.5, 4, x < 0.5, g(x, y) = { (x− 0.5)2 + y2, x ≥ 0.5, 2(x− 0.5)2 + y2, x < 0.5. .\", \"page\": 17, \"index\": 1, \"width\": 773, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2605.09643v1/fig-002.webp\", \"caption\": \"Fig. 2. Helmholtz equation. (a,c) True solutions with Kernel Operator (KO) and Green Operator (GO) approximations for ω = 20 and ω = 200, respectively; (b,d) corresponding absolute errors.\", \"page\": 19, \"index\": 2, \"width\": 773, \"height\": 581}]"
motivation: 旨在解决传统监督学习方法对成对训练数据的依赖，并提升PDE解算子在未观测区域的外推性能。
method: 将物理先验通过PDE算子引入核岭回归框架，构建出一种与输入函数无关的闭式算子估计器。
result: 在达西流和亥姆霍兹方程等实验中展现了高精度和高效率，且在近似质量和计算成本上具有竞争优势。
conclusion: 该核学习方法为PDE求解提供了一种无需配对数据、具备理论收敛保障且高效的算子化求解方案。
---

## 摘要
本文提出了一种基于核的方法，用于学习一般非齐次偏微分方程（PDEs）的解算子。该方法将通常通过 PDE 算子编码的物理先验融入核岭回归框架，并采用基于正则化的公式构建算子学习器。这产生了一个闭式估计量，该估计量独立于决定底层 PDE 的输入函数。从正则化理论的角度来看，所得估计量诱导了一个定义良好的算子，该算子连接了输入和输出空间，这两个空间分别包含定义 Dirichlet 问题及其解的函数。因此，它有效地从 PDE 求解器转变为基于算子的求解器。与标准的监督学习方法相比，它不依赖于成对的输入-输出训练数据，并能够实现超出观测范围的系统性外推。本文进行了完整的误差分析，提供了在适当选择正则化参数下基于算子的求解器的收敛率。广泛的数值实验（包括 Darcy 流和 Helmholtz 方程）表明，所提方法在各种问题设置中均实现了高精度和高效率，并且在逼近质量和计算成本方面均优于现有的算子学习方法。

## Abstract
A kernel-based approach for the learning of the solution operator of general nonhomogeneous partial differential equations (PDEs) is proposed. The method incorporates physical priors, typically encoded through the PDE operator, into a kernel ridge regression framework, and employs a regularization-based formulation to construct an operator learner. This yields a closed-form estimator that is independent of the input functions that determine the underlying PDE. From the perspective of regularization theory, the resulting estimator induces a well-defined operator that links input and output spaces, which contain the functions that define a Dirichlet problem and its solution, respectively. Consequently, it effectively shifts from a PDE solver to an operator-based solver. In contrast to standard supervised learning methods, it does not rely on paired input--output training data and enables systematic extrapolation beyond observed regimes.   A full error analysis is conducted, providing convergence rates for the operator-based solver under suitable choices of regularization parameters. Extensive numerical experiments, including Darcy flow and Helmholtz equations, demonstrate that the proposed method achieves high accuracy and efficiency across a range of problem settings, and compares favorably with operator learning approaches in both approximation quality and computational cost.

---

## 论文详细总结（自动生成）

### 论文总结：偏微分方程解算子的核学习 (Kernel Learning of PDE Solution Operators)

#### 1. 核心问题与整体含义（研究动机和背景）
本文旨在解决科学计算中偏微分方程（PDE）求解的效率与泛化问题。传统的数值方法（如有限元法）在处理需要重复调用的任务（如优化设计、不确定性量化）时计算成本过高。而现有的科学机器学习方法存在局限：
*   **算子学习（如 DeepONet, FNO）：** 属于“黑盒”模型，通常需要海量的成对输入-输出数据，且在未观测到的数据分布上外推能力较差。
*   **物理信息神经网络（PINNs）：** 虽然引入了物理先验，但针对每一个新的输入函数（如不同的源项或边界条件），都需要重新进行昂贵的网络训练。

**研究动机：** 开发一种既能利用物理先验（PDE 算子），又不需要成对训练数据，且能像算子一样实现“一次训练，多次快速推理”的核学习框架。

#### 2. 方法论：核心思想与关键技术
论文提出了一种基于**再生核希尔伯特空间（RKHS）**的物理信息核学习方法：
*   **核心思想：** 将 PDE 的解建模在 RKHS 中，通过最小化包含 PDE 残差和边界条件残差的正则化经验风险函数，直接推导出解算子的闭式表达式。
*   **关键技术细节：**
    *   **微分再生属性（Differential Reproducing Property）：** 利用核函数的导数依然在 RKHS 相关的空间内的特性，将微分算子 $L_s$ 作用于核函数 $K$。
    *   **算子化转换：** 证明了该问题的解可以表示为输入函数（源项 $f$ 和边界 $g$）与一组**核基（Kernel Basis）**的线性组合。这组基函数仅取决于采样点和 PDE 算子，而与具体的输入函数无关。
    *   **闭式估计器：** 最终得到的估计器 $\hat{u}_{\lambda,N}$ 具有类似格林函数（Green's Function）的形式，使得求解过程从“解方程”转变为“算子作用”。
    *   **在线低秩方案：** 针对大规模数据，提出了基于 Nyström 采样的在线低秩核回归方案，将计算复杂度从 $O(N^3)$ 降低到 $O(NL^2)$。

#### 3. 实验设计：场景、Benchmark 与对比
论文通过多个物理场景验证了方法的有效性：
*   **Darcy Flow（达西流）：** 测试了平滑渗透率、指数渗透率以及不连续（阶梯状）渗透率场。
*   **Helmholtz Equation（亥姆霍兹方程）：** 重点测试了高频波（$\omega=200$）情况下的表现。
*   **其他场景：** 包括 2D 薛定谔方程（Schrödinger）、1D 热传导方程（Heat Equation）以及 3D 泊松方程（Poisson）。
*   **Benchmark 与对比方法：** 主要对比了 **Green Operator Learning**（Stepaniants, 2023）。实验评估了相对 $L^2$ 误差、相对 $L^\infty$ 误差以及计算耗时。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的硬件型号（如 GPU/CPU 规格）。
*   **训练/推理耗时：** 论文给出了具体的时间数据。例如，在 Darcy 流实验中，构建算子仅需约 1.5 秒；在 Helmholtz 方程中，该方法仅需约 1.5 秒，而对比的格林算子学习方法需要约 0.7 小时（约 2500 秒）。这表明该方法在计算效率上具有量级优势。

#### 5. 实验数量与充分性
*   **实验组数：** 论文涵盖了椭圆型、抛物型等多种类型的线性 PDE，维度从 1D 跨越到 3D。
*   **充分性与公平性：** 
    *   实验设计较为充分，不仅测试了平滑函数，还专门针对**不连续输入**和**高频振荡**等数值计算中的难点进行了验证。
    *   对比实验中，对基准方法（Green Operator）进行了超参数优化以确保公平。
    *   提供了统计意义上的误差分析（如 20 次独立运行的平均误差和标准差），证明了结果的客观性。

#### 6. 主要结论与发现
*   **高精度与高效率：** 该方法在样本量较小的情况下即可达到 $10^{-3}$ 甚至更低的相对误差，且推理速度极快。
*   **无需配对数据：** 该方法仅需 PDE 算子本身和采样点信息，完全摆脱了对“输入-输出”样本对的依赖。
*   **强大的外推能力：** 在 Helmholtz 方程的高频测试中，当对比方法失效（误差接近 100%）时，本文方法依然保持了极高的精度，证明了物理先验在泛化中的核心作用。
*   **理论完备性：** 论文提供了严谨的收敛率证明，确定了误差界与样本量 $N$ 及正则化参数 $\lambda$ 的关系。

#### 7. 优点（亮点）
*   **数学优雅性：** 提供了闭式解，避免了神经网络训练中的非凸优化问题和随机性。
*   **算子化推理：** 一次计算核基后，处理新的源项只需进行简单的矩阵向量乘法，极大地加速了重复求解过程。
*   **理论支撑：** 不同于许多深度学习论文，本文提供了完整的 RKHS 框架下的误差分析和收敛性证明。

#### 8. 不足与局限
*   **线性限制：** 目前的方法主要针对**线性 PDE**。虽然论文提到了向非线性问题的扩展，但在本文中未给出具体的非线性算子学习实验。
*   **核矩阵规模：** 尽管有低秩方案，但对于极大规模的 3D 复杂几何区域，核矩阵的存储和计算压力依然存在。
*   **核函数选择：** 结果对核函数的超参数（如高斯核的带宽 $\eta$）具有一定的敏感性，如何自动选择最优核参数仍是一个实践中的挑战。

（完）
