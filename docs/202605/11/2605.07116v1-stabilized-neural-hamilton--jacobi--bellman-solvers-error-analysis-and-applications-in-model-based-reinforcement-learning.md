---
title: "Stabilized neural Hamilton--Jacobi--Bellman solvers: Error analysis and applications in model-based reinforcement learning"
title_zh: 稳定的神经 Hamilton-Jacobi-Bellman 求解器：误差分析及其在基于模型的强化学习中的应用
authors: "Minseok Kim, Yeongjong Kim, Namkyeong Cho, Yeoneung Kim"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07116v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 用于HJB方程的物理信息神经求解器
tldr: 本研究针对连续时间模型强化学习中的Hamilton-Jacobi-Bellman (HJB) 方程求解，提出了一种稳定的神经求解器。该方法结合了神经网络的函数表示能力与有限差分策略评估的稳定性，通过随机连续配置法最小化残差。研究建立了严谨的误差理论，证明了在学习动力学下的L2稳定性估计，并在高维LQR及多种复杂控制任务中展现了优于基准算法的性能，为高维连续控制提供了理论支撑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07116v1/fig-001.webp\", \"caption\": \"Table 4: Task specifications and evaluation protocol (d: state dim, m: action dim).\", \"page\": 21, \"index\": 1, \"width\": 827, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2605.07116v1/fig-002.webp\", \"caption\": \"Table 5: SDPI value-learning configurations. For SPINN, (r, g) denotes rank and group size.\", \"page\": 22, \"index\": 2, \"width\": 830, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2605.07116v1/fig-003.webp\", \"caption\": \"Figure 1: Proof map for the stability and convergence of the proposed semi-discrete PINN policy iteration. The analysis branch connects the dynamics identification error ηn to the one-step L2 value-error bound (Theorem 1) and propagates it through the greedy policy sensitivity to establish multi-step PI convergence (Corollary 1).\", \"page\": 3, \"index\": 3, \"width\": 672, \"height\": 298}]"
motivation: 旨在克服传统网格法在高维空间的维数灾难以及纯物理信息神经网络在求解HJB方程时的稳定性与收敛性问题。
method: 采用神经网络表示价值函数，利用有限差分移位算子进行策略评估，并结合随机采样配置点来最小化HJB残差。
result: 建立了包含残差、策略不匹配和模型识别误差的L2稳定性界限，并在高达64维的LQR及四旋翼等复杂基准测试中验证了理论预测。
conclusion: 该研究通过结合有限差分的结构稳定性与神经网络的表达能力，为基于HJB方程的神经求解器提供了理论保证和高效的计算框架。
---

## 摘要
物理信息神经求解器为连续时间下的基于模型的强化学习提供了一条极具前景的途径，其中最优反馈合成由 Hamilton-Jacobi-Bellman (HJB) 方程支配。实际实现通常处于一种既非经典网格方法也非连续偏微分方程（PDE）PINN 的范式中：价值函数由神经网络表示，有限差分 HJB 策略评估算子通过在偏移点处的网络查询进行评估，而残差则通过随机连续配置法进行最小化。该范式保留了稳定的有限差分策略评估结构，同时避免了基于网格的价值未知量。我们为这种混合范式开发了一套误差理论。通过将有限差分诠释为作用于神经网络的位移算子，我们证明了在学习到的动力学下，单步策略评估的总体 $L^2$ 稳定性估计。该界限分离了残差误差、初始和外部边界失配、策略失配以及模型辨识误差，并为学习到的动力学提供了一个显式的梯度放大因子，同时底层的线性评估稳定性避免了隐式的逆粘性爆炸。我们进一步给出了有限样本配置证明，以及通过贪婪策略改进实现的条件多步传播结果。在高达 64 维的紧凑控制 LQR、Allen-Cahn 控制、单摆、Hopper 和 3D 四旋翼基准测试上的实验，与具有代表性的基于模型和无模型的强化学习基准进行了对比，验证了预测的残差、策略失配和学习模型误差趋势。

## Abstract
Physics-informed neural solvers offer a promising route to model-based reinforcement learning in continuous time, where optimal feedback synthesis is governed by Hamilton--Jacobi--Bellman (HJB) equations. Practical implementations often occupy a regime that is neither a classical grid method nor a continuous-PDE PINN: the value function is represented by a neural network, finite-difference HJB policy-evaluation operators are evaluated by network queries at shifted points, and residuals are minimized by random continuous collocation. This regime preserves the stabilized finite-difference policy-evaluation structure while avoiding grid-based value unknowns. We develop an error theory for this hybrid regime. Interpreting finite differences as shift operators acting on neural networks, we prove a population $L^2$ stability estimate for one policy-evaluation step with learned dynamics. The bound separates residual error, initial and exterior-collar mismatch, policy mismatch, and model-identification error, with an explicit gradient amplification factor for learned dynamics, while the underlying linear evaluation stability remains free of hidden inverse-viscosity blow-up. We further give a finite-sample collocation certificate and a conditional multi-step propagation result through greedy policy improvement. Experiments on compact-control LQR upto 64 dimensions, Allen--Cahn control, pendulum, Hopper, and 3D quadrotor benchmarks compare against representative model-based and model-free RL baselines, demonstrating the predicted residual, policy-mismatch, and learned-model error trends.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilized Neural Hamilton–Jacobi–Bellman Solvers: Error Analysis and Applications in Model-Based Reinforcement Learning》的结构化深入分析总结：

### 1. 核心问题与研究动机
论文探讨了在**连续时间模型强化学习（MBRL）**中求解 **Hamilton-Jacobi-Bellman (HJB)** 方程的挑战。
*   **研究背景**：HJB 方程是连续时间最优控制的核心，但面临“维数灾难”。
*   **核心矛盾**：传统的**网格有限差分法**具有良好的数值稳定性（单调性），但无法扩展到高维空间；而现代的**物理信息神经网络（PINN）**虽然能处理高维问题，但在处理 HJB 方程的非线性项时往往不稳定，且自动微分（AD）难以保证粘性解所需的单调性。
*   **研究动机**：提出一种混合范式，既保留有限差分算子的稳定性，又利用神经网络的无网格扩展性，并为其建立严谨的误差理论。

### 2. 方法论：半离散神经策略迭代（SDPI）
核心思想是**将算子离散化与函数表示解耦**。
*   **核心技术细节**：
    *   **价值函数表示**：使用神经网络 $V_\theta$ 表示价值函数。
    *   **半离散算子**：不使用自动微分计算空间导数，而是通过在小位移 $h$ 处查询网络（如 $V_\theta(x \pm he_i)$）来构造有限差分梯度 $\nabla_h^0$ 和拉普拉斯算子 $\Delta_h$。
    *   **人工粘性**：引入 $\nu_h$ 项以确保数值稳定性。
    *   **策略迭代流程**：
        1.  **动力学学习**：从数据中拟合微分动力学模型 $\tilde{f}_\phi$。
        2.  **策略评估**：通过随机连续配置法（Random Collocation）最小化半离散 HJB 残差，更新 $V_\theta$。
        3.  **策略改进**：基于当前价值梯度，通过贪婪步更新策略 $\tilde{u}$。
*   **理论贡献**：证明了单步总体 $L^2$ 稳定性界限（Theorem 1），将误差分解为：残差误差、边界/初始值失配、策略失配以及模型辨识误差。

### 3. 实验设计
研究涵盖了从线性到高度非线性的多种控制任务：
*   **实验场景**：
    *   **高维 LQR**：4D 到 64D 的线性二次调节器（带控制约束）。
    *   **非线性系统**：Duffing 振子、航天器交会、倒立摆。
    *   **复杂动力学**：MuJoCo Hopper（12D 状态）、3D 四旋翼（9D 状态）。
    *   **偏微分方程控制**：10D 和 20D 的 Allen-Cahn 方程。
*   **基准方法（Benchmarks）**：
    *   **无模型 RL**：SAC (Soft Actor-Critic), PPO (Proximal Policy Optimization)。
    *   **基于模型 RL**：MBPO (Model-Based Policy Optimization)。
    *   **精确解对比**：在低维情况下对比有限差分（FDM）或解析解。

### 4. 资源与算力
论文明确列出了硬件配置：
*   **GPU**：5 张 NVIDIA RTX A5000 (每张 24GB 显存)。
*   **CPU**：2 颗 Intel Xeon Gold 6426Y (共 32 核心)。
*   **内存**：503 GiB RAM。
*   **软件**：基于 CUDA 12.2 和 NVIDIA 驱动 535.154.05。
*   **训练时长**：文中未详细列出每个任务的具体耗时，但提供了详细的超参数表（如 Batch Size 通常为 1024 到 16384）。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。涵盖了 10 种以上的不同环境，且 LQR 实验跨越了从低维到高维（64D）的梯度。
*   **客观性与公平性**：
    *   每个任务均进行了 3 次以上的独立运行并报告了均值与标准差。
    *   对比了当前最流行的强化学习算法（SAC/PPO/MBPO）。
    *   不仅关注最终成本，还定量分析了动力学模型的辨识误差（Table 3），验证了理论中模型误差对结果的影响。

### 6. 主要结论与发现
*   **稳定性**：SDPI 在高维 LQR（32D, 64D）中表现出极强的稳定性，而 SAC 和 PPO 在这些维度下性能大幅下降。
*   **样本效率**：在 Allen-Cahn 和 Hopper 等任务中，SDPI 展现了比传统 RL 基准更高的样本效率。
*   **理论验证**：实验结果验证了 Theorem 1 的预测，即策略质量受限于模型辨识精度。当动力学模型误差超过 10% 时，性能会出现明显拐点。
*   **数值特性**：位移查询法能有效捕捉非光滑价值函数的特征，避免了标准 PINN 常见的过度平滑问题。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了算法，还为“神经网络+有限差分”这一混合范式提供了首个严谨的总体误差分析。
*   **解决维数灾难**：成功将具有数值稳定性的 HJB 求解器扩展到了 64 维，这在传统网格方法中是不可能的。
*   **归纳偏置**：通过引入半离散算子，为神经网络训练提供了正确的物理/数值单调性引导。

### 8. 不足与局限
*   **模型依赖性**：作为 MBRL 方法，其性能高度依赖于动力学模型的准确性。在 Hopper 等具有复杂接触力（非光滑）的任务中，模型误差会显著放大。
*   **计算开销**：虽然避免了网格，但每个配置点需要多次网络查询（计算有限差分），这比标准 PINN 的单次前向传播计算量大。
*   **超参数敏感性**：位移尺度 $h$ 和人工粘性 $\nu_h$ 的选择对结果有影响，文中提到未来需要研究自适应调整策略。
*   **非光滑动力学**：对于存在尖锐力转换（如碰撞）的系统，数值平滑可能会模糊关键的物理特性。

（完）
