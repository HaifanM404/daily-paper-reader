---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: 紧缩PINN：偏微分方程与Landau-de Gennes模型的多解学习
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 集成DeepONets的Deflation-PINNs用于求解多种PDE解
tldr: 非线性偏微分方程在物理和工程中普遍存在，但传统物理信息神经网络一次只能找到一个解。针对此局限，本研究提出了紧缩PINNs新框架，通过将紧缩损失项与基于PINN和深度算子网络的架构相结合，系统性地迫使模型寻找并收敛于有限多个不同的解分支。理论分析和液晶Landau-de Gennes模型的数值实验验证了该方法的有效性，能成功识别复杂能量景观中的多个平衡态晶体结构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统PINNs难以发现PDE的多个不同解，而许多物理系统（如液晶）存在多个平衡态。
method: 提出Deflation-PINNs框架，通过在损失函数中引入紧缩项，并结合PINNs与DeepONets架构。
result: 数值实验表明，该方法能成功识别并表征液晶Landau-de Gennes模型中的多个不同晶体结构。
conclusion: 该框架为求解具有多解特性的复杂PDE系统提供了一种有效的神经网络方法。
---

## 摘要
非线性偏微分方程在数学物理与工程领域普遍存在。虽然物理信息神经网络已成为求解偏微分方程问题的有力工具，但由于其设计为每次仅寻找一个解，通常难以识别多个不同的解。为突破这一局限，我们提出了Deflation-PINNs——一种将紧缩损失项与基于PINNs及深度算子网络的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法能系统性地迫使Deflation-PINN探索并收敛于有限多个不同的解分支。我们提供了模型收敛的理论证明，并通过液晶Landau-de Gennes模型的数值实验展示了Deflation-PINNs的有效性——该体系以其复杂的能量景观和多重平衡态而著称。结果表明，Deflation-PINNs能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes》进行结构化、深入且客观的总结。

---

### **1. 核心问题与整体含义**
*   **研究动机**：许多非线性偏微分方程（PDE）存在多个解（如多稳态系统），这些解对应不同的物理状态。传统的物理信息神经网络（PINNs）在训练时通常收敛到单一解，难以系统地发现所有可能的解。现有方法（如多次随机初始化训练多个PINN）效率低下且无法保证解的多样性。
*   **研究目标**：提出一个能够**一次性、系统地学习PDE有限多个不同解**的神经网络框架，以解决PINNs在多解问题上的固有局限。

### **2. 方法论：Deflation-PINNs**
*   **核心思想**：结合PINNs的物理约束和DeepONets的算子学习架构，并引入一个“紧缩损失”项，迫使模型同时学习多个彼此“远离”的解。
*   **关键技术细节**：
    1.  **架构设计（简化DeepONet）**：
        *   借鉴DeepONet的“分支网络-主干网络”结构。
        *   将分支网络从处理输入函数的神经网络简化为一组可学习的特征向量 `β^k`（称为分支权重），每个向量对应一个待求的解 `u_k`。
        *   主干网络 `τ(x)` 负责将空间坐标映射到一组基函数。
        *   最终输出为：`G(k, x) = Σ_i τ_i(x) * β^k_i ≈ u_k(x)`。该架构专为学习有限个函数而设计，参数更高效。
    2.  **紧缩损失**：
        *   定义 `L_Def(G) = (2/(K(K-1))) Σ_{i<j} max(1 - (||G(i,·)-G(j,·)||_{L2} / d_min), 0)`。
        *   该损失惩罚模型输出的任意两个解在L2范数下过于接近（距离小于预设阈值 `d_min`），从而驱动它们分叉到不同的解支上。
    3.  **总损失函数**：
        *   `L_total(G) = α * Σ_i E_G(G(i,·)) + β * L_Def(G)`。
        *   其中 `E_G` 是标准的PINN物理残差损失（包括控制方程和边界条件残差），`α` 和 `β` 是超参数。
    4.  **硬边界约束处理**：
        *   采用构造性方法（径向边界距离函数和光滑权重函数ω）确保神经网络输出严格满足狄利克雷边界条件，从而简化了损失函数（无需单独的边界损失项），并提供了相应的逼近理论证明。

### **3. 实验设计**
*   **基准场景**：二维简化Landau-de Gennes模型。这是一个描述向列型液晶的变分问题，其Euler-Lagrange方程是一个具有狄利克雷边界条件的非线性PDE系统。已知在方形区域上存在6个稳定的平衡态解（2个对角态D1、D2和4个旋转态R1-R4）。
*   **Benchmark对比方法**：有限元法。论文将Deflation-PINNs找到的解与已有文献中使用FEM计算得到的精确解进行定性（矢量场图）对比。
*   **对比方法提及**：
    *   **HomPINNs**: 半监督方法，依赖观测数据。
    *   **随机初始化多PINN**: 并行训练多个随机初始化的传统PINN，但无法保证解的多样性和避免冗余。

### **4. 资源与算力**
文中明确说明：实验在一张**RTX 3060-Ti GPU (8GB VRAM)**上进行。训练进行了10,000个epoch。这表明该方法在消费级硬件上即可实现，算力需求相对亲民。

### **5. 实验数量与充分性**
*   **主要实验数量**：论文在一个核心PDE问题（Landau-de Gennes模型）上进行了验证。通过一次训练一个Deflation-PINN模型（设定K=6），成功复现了全部6个已知稳定解。
*   **充分性与客观性评估**：
    *   **优点**: 
        1.  选择了公认具有多解特性的经典物理模型作为测试案例，问题本身具有挑战性和明确的ground truth（FEM结果）。
        2.  提供了与FEM结果的直观可视化对比（图2、图4），定性展示了方法的有效性。
        3.  对关键超参数 `d_min`的选择过程进行了说明（通过经验测试调整）。
    *   **局限性/不足**: 
        1.  **实验广度不足**：仅在一个特定问题上验证了方法。缺乏在其他经典多解PDE问题上的测试，未能全面证明其普适性。
        2.  **缺乏定量对比与消融实验**：
            *   没有提供与“随机初始化多PINN”方法的定量比较报告成功率或效率提升多少倍）。
            *   未进行系统的消融研究来验证紧缩损失项的必要性以及架构中各个组件的作用贡献）。
            *   没有报告不同超参数α、β、d_min等对结果稳定性的影响分析。

### **6. 主要结论与发现**
1.  Deflation-PINNs框架能够成功解决传统PINNs难以处理的多解PDE问题。
2.  该方法可以一次性、系统地发现Landau-de Gennes模型的所有六个稳定平衡态解，其结果与高精度有限元法得到的结果在视觉上高度一致。
3.  理论分析表明，结合硬约束的简化DeepONet架构具有通用逼近性质。

### **7. 优点**
1.  **创新性融合**: 创造性地将紧缩算法思想引入基于神经网络的PDE求解中，为解决多解问题提供了一个新颖的思路框架）。
2.  **架构高效**: 通过简化DeepONet架构并用可学习的特征向量替代分支网络针对有限个解的设定进行了定制化优化降低了参数量提升了效率）。
3. 理论支撑:不仅提出了新方法还提供了关于硬约束模型逼近能力的理论证明增强了工作的严谨性）。
4.工程实现考虑周全:采用了硬边界约束技术简化了优化目标并开源了代码便于复现）。

###8.**不足与局限**
1.**普适性验证不足**:如前所述仅在单一特定问题上得到验证其对于其他类型的多解问题如分岔点附近无限多解的适用性未知）。
2.**对先验知识依赖**:需要预先知道或估计解的个数K以及合适的距离阈值d_min这在实际未知系统中可能难以确定）。
3.**收敛稳定性风险**:如同所有非凸优化问题同时优化物理损失和紧缩损失可能导致训练不稳定或陷入不良局部极小值文中未深入讨论此点）。
4.**计算成本权衡**:虽然比训练大量独立PNN可能更高效但单次训练的模型规模更大复杂度更高对于非常大量K>的解是否仍具优势有待考察）。

完

好的，我将从您提供的输出中断处继续补全。上一次输出在“###8.**不足与局限**”部分结束，但该部分内容似乎已完整列出。根据您的要求“从中断处继续补全，不要重复已输出内容”，我将假设需要补充的是在“不足与局限”之后可能存在的其他标准分析章节，例如**未来工作展望**或**总结性评述**。由于原文总结中未包含这些部分，我将基于对论文内容的分析进行补充。

---

### **9. 未来工作展望（基于论文内容与分析推断）**
尽管论文未明确设立独立的“未来工作”章节，但从其论述和局限性中可以合理推断出以下几个潜在的、有价值的研究方向：
1.  **扩展到更广泛的问题类别**：将Deflation-PINNs框架应用于其他具有多解特性的经典PDE系统（如非线性薛定谔方程、反应-扩散系统、弹性力学中的屈曲问题），以验证其普适性。
2.  **自适应机制研究**：开发无需预先指定解的数量`K`和距离阈值`d_min`的自适应算法。例如，可以探索在训练过程中动态增加分支权重向量或自动调整`d_min`的策略。
3.  **处理连续谱或无限多解**：当前方法针对离散的有限多个稳定解。如何修改框架以处理参数依赖系统中出现的连续解族或分岔点附近的无限多解，是一个更具挑战性的前沿问题。
4.  **与不确定性量化结合**：在多解背景下，可以研究不同解的稳定性或吸引域大小。将Deflation-PINNs与贝叶斯神经网络或集合方法结合，可能为每个找到的解提供某种置信度度量。
5.  **优化策略改进**：深入研究损失函数地形，设计更稳健的优化器或训练策略（如课程学习、损失权重自适应调度），以缓解同时优化物理损失和紧缩损失可能带来的不稳定性。

### **10. 总结性评述**
本文《Deflation-PINNs》是一项具有显著创新性和实用价值的研究。它敏锐地指出了传统PINNs在处理多解PDE时的核心缺陷，并提出了一个巧妙而直接的解决方案。通过将紧缩算法的思想、简化的算子学习架构以及硬边界约束技术三者融合，该方法实现了“一次训练，发现多解”的目标。

其实验在Landau-de Gennes这一经典且非平凡的问题上取得了令人信服的成功，证明了核心概念的有效性。提供的理论证明和代码开源也增强了工作的严谨性和可复现性。

然而，正如其局限性所指出的，该工作目前更像一个强有力的“概念验证”（Proof of Concept）。其在单一问题上的成功尚不能完全确立其作为通用工具的可靠性。未来的研究需要在更广泛的基准测试上进行验证，并解决对先验知识的依赖问题。

总体而言，这项工作为基于神经网络的PDE求解开辟了一条富有前景的新途径——从寻找“一个解”转向系统地发现“多个可能的物理状态”。它不仅是PINN领域的一个重要进展，也为计算物理学中多稳态问题的研究提供了新的工具思路。

（完）
