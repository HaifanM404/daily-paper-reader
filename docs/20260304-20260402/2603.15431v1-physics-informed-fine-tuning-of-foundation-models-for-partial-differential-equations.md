---
title: Physics-informed fine-tuning of foundation models for partial differential equations
title_zh: 基于物理信息的偏微分方程基础模型微调
authors: "Vlad Medvedev, Leon Armbruster, Christopher Straub, Georg Kruse, Andreas Rosskopf"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15431v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 偏微分方程基础模型的物理信息微调
tldr: 针对偏微分方程（PDE）基础模型在下游任务中因数据稀缺和分布偏移而难以适应的问题，本研究提出了一种物理信息微调框架。该框架通过在微调目标中直接融入物理约束（如PDE残差和边界条件），有效利用预训练模型并在数据匮乏时促进物理一致性。主要贡献是证明了该方法能在无需真实解的情况下达到高精度，并提出了混合策略以提升分布外泛化能力，为科学机器学习提供了一种可扩展且数据高效的模型适应新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15431v1/fig-001.webp\", \"caption\": \"Figure 5: Qualitative comparison of model predictions for the Helmholtz equation. Top three rows: interpolation performance for test samples from the training distributions (a(x, y) composed of Gaussians). Bottom three rows: extrapolation performance for out-of-distribution test samples (a(x, y) composed of wavy stripes). Number of labeled training samples M : 2048. Batch size: 40.\", \"page\": 11, \"index\": 1, \"width\": 812, \"height\": 749}, {\"url\": \"assets/figures/arxiv/2603.15431v1/fig-002.webp\", \"caption\": \"Figure 4: Extrapolation performance: qualitative comparison of model predictions on out-ofdistribution test samples. Number of labeled training samples M : 1. Batch size: 1.\", \"page\": 8, \"index\": 2, \"width\": 814, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2603.15431v1/fig-003.webp\", \"caption\": \"Figure 3: Interpolation performance: qualitative comparison of predicted solutions for test samples from the training distribution. Number of labeled training samples M : 2048. Batch size: 40.\", \"page\": 8, \"index\": 3, \"width\": 812, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.15431v1/fig-004.webp\", \"caption\": \"Figure 1: Physics-informed fine-tuning of pre-trained foundation model with scalable Operator Transformer architecture (Herde et al., 2024) for the Poisson downstream task (cf. Section 2.4).\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 268}]"
motivation: 尽管基于数据的PDE基础模型功能强大，但如何利用有限的任务特定数据使其适应新的下游任务仍具挑战性，而物理信息训练在此方面的潜力尚未被系统研究。
method: 该方法引入了一个物理信息微调框架，通过将物理约束（PDE残差和边界条件）直接纳入微调目标来适应预训练的PDE基础模型。
result: 评估表明，该方法在未见过的PDE类下游任务上达到了有竞争力的精度且无需训练用的PDE解；当仅有极少训练数据时，混合微调策略在分布外场景下表现出更优的泛化能力。
conclusion: 该研究确立了物理信息微调作为一种可扩展且数据高效的新范式，为科学机器学习中基础模型的适应提供了物理可解释的路径。
---

## 摘要
偏微分方程基础模型已成为在多样化物理系统上预训练的强大代理模型，但由于特定任务数据有限和分布偏移，使其适应新的下游任务仍具挑战性。尽管微调已在自然语言处理领域被证明具有变革性，但适应偏微分方程基础模型的最佳实践仍未得到充分探索。虽然物理信息训练已成功地在广泛的偏微分方程问题上训练出精确的求解器，但其用于微调基于数据的基础模型的潜力尚未得到系统研究。在本工作中，我们引入了一种物理信息微调框架，该框架通过将物理约束（偏微分方程残差和边界条件）直接纳入微调目标来适应预训练的偏微分方程基础模型。这使得在数据稀缺的情况下能够有效适应，同时促进物理一致性。我们在一个由未见过的偏微分方程类别组成的下游任务上评估我们的方法，并将其与数据驱动的微调方法进行比较。我们的结果表明，物理信息微调无需使用偏微分方程解进行训练即可达到有竞争力的精度。此外，当仅有极少量训练数据可用时，混合微调策略对分布外场景展现出更优的泛化能力。这些发现确立了物理信息微调作为一种可扩展且数据高效的范式，为科学机器学习中适应基础模型提供了一条具有物理解释性的途径。

## Abstract
Foundation models for partial differential equations (PDEs) have emerged as powerful surrogates pre-trained on diverse physical systems, but adapting them to new downstream tasks remains challenging due to limited task-specific data and distribution shifts. While fine-tuning has proven transformative in natural language processing, best practices for adapting PDE foundation models remain underexplored. Although physics-informed training has successfully trained accurate solvers across a wide range of PDE problems, its potential for fine-tuning data-based foundation models has not been systematically studied. In this work, we introduce a physics-informed fine-tuning framework that adapts pre-trained PDE foundation models by incorporating physical constraints (PDE residuals and boundary conditions) directly into the fine-tuning objective. This enables effective adaptation in data-scarce regimes while promoting physical consistency. We evaluate our method on a downstream task composed of an unseen PDE class and compare it with data-driven finetuning counterparts. Our results demonstrate that physics-informed fine-tuning achieves competitive accuracy without requiring PDE solutions for training. Furthermore, a hybrid fine-tuning strategy yields superior generalization to out-of-distribution scenarios when only minimal training data is available. These findings establish physics-informed fine-tuning as a scalable and data-efficient paradigm, providing a physically interpretable pathway for adapting foundation models in scientific machine learning.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Physics-informed fine-tuning of foundation models for partial differential equations》的论文进行结构化、深入且客观的总结。

---

## **论文总结：基于物理信息的偏微分方程基础模型微调**

### **1. 核心问题与整体含义**
*   **研究动机**：偏微分方程（PDE）基础模型（如 POSEIDON, WALRUS）通过大规模预训练学习通用物理表示，但在适应新的下游任务（如求解未见过的PDE类型）时，面临两大挑战：
    1.  **任务特定数据稀缺**：获取新PDE的高保真数值解成本高昂。
    2.  **分布偏移**：下游任务的输入函数分布（如源项、边界条件）可能与预训练数据不同，导致性能下降。
*   **研究空白**：尽管“物理信息机器学习”和“基础模型微调”各自在科学计算和自然语言处理领域取得了成功，但将两者结合——即利用物理定律作为监督信号来微调基于数据的PDE基础模型——尚未得到系统研究。
*   **整体含义**：本文旨在填补这一空白，探索一种**数据高效、物理可解释**的基础模型适应范式。其核心思想是：在微调阶段，不仅依赖有限的真实解数据，更通过最小化PDE残差来强制模型遵守已知的物理规律。

### **2. 方法论**
*   **核心思想**：提出一个**物理信息微调框架**。在微调预训练的PDE基础模型时，将物理约束直接纳入损失函数。
*   **关键技术细节**：
    1.  **损失函数构成**：
        *   `L_total = L_data + λ * L_physics`
        *   `L_data`：标准监督损失（预测解与真实解之间的误差）。
        *   `L_physics`：物理信息损失，由两部分组成：
            *   **PDE残差损失**：在计算域内采样点评估控制方程（如泊松方程 `-Δu - f = 0`）的残差。
            *   **边界条件损失**：在边界上评估边界条件的满足程度。本文采用了一种巧妙的实现方式——通过零填充输出直接硬编码狄利克雷边界条件，从而无需额外的软惩罚项。
    2.  **三种微调策略对比**：
        *   **(纯)数据驱动微调 (Data)**：仅使用 `L_data`。
        *   **(纯)物理信息微调 (Physics)**：仅使用 `L_physics`。这是一种“零样本”适应方式，完全不需要真实解标签。
        *   **混合微调 (Hybrid)**：联合优化 `L_data` 和 `L_physics`。关键创新在于引入了“动态生成”机制——在每个训练批次中，除了少量有标签的源-解对，还会动态生成无标签的源项（来自更广泛的分布），并仅用 `L_physics` 对其进行训练。这相当于用物理定律进行数据增强。
    3.  **模型参数初始化与更新策略**：
        *   沿用 POSEIDON 的设置。将参数分为三部分：(a)核心骨干网络参数、(b)嵌入与恢复层参数、(c)时间条件层归一化参数。
        *   对于新任务，(a)和(c)从预训练模型中迁移以保留通用知识，(b)则随机初始化以适应新的输入/输出通道数。
        *   所有参数均可训练（不冻结），但采用分层的学习率以平衡知识保留与任务适应。

### **3. 实验设计**
*   **基准任务与数据集**：
    *   **主要下游任务**: 二维稳态泊松方程 (`-Δu = f`)。
    *   **数据集来源**: 沿用 POSEIDON 框架中生成的基准数据集。源项 `f` 由随机数量、位置、振幅的高斯函数叠加而成。
    *   **评估场景一 (插值)**:
        *   测试集: 240个样本，其源项分布与训练集相同（高斯叠加）。
        *   核心实验: “数据缩放曲线”——在不同规模的标签数据量 (`M ∈ {1,2,4,...,4096}`)下评估性能。
    *   **评估场景二 (外推/分布外泛化)**:
        *   测试集: 50个样本，其源项为从未在训练中出现的“极端形状”（如正弦波、径向图案、棋盘格等），用于测试模型的泛化能力。
*   **对比方法 (Benchmark)**:
    1.  **[基线] FNO (从头训练)**:
        *   作为经典神经算子的代表。(a)纯数据驱动；(b)纯物理信息驱动。
    2.  **[目标] POSEIDON-T (预训练+微调)**:
        *   作为PDE基础模型的代表。(a)纯数据驱动微调；(b)纯物理信息微调；(c)**混合微调(本文核心贡献)**。

### **4. 资源与算力**
文中明确提到了实验硬件配置：
*   GPU: NVIDIA H200 GPU (141 GB VRAM)。
*   其他细节未详细说明具体数量或总训练时长。但提供了特定配置下的相对耗时对比（见表1）：对于4096个样本的训练集 (`M=4096`) ，POSEIDON-T的数据驱动、物理信息和混合三种模式的单次完整训练时间分别为37分钟和61分钟左右；FNO的训练时间未单独列出但应与前者相当或更长。推理延迟均为毫秒级。

### **5. 实验数量与充分性**
实验设计较为系统和充分：
1.  **(主实验组数)** ：针对泊松方程任务，系统比较了5种配置（POSEIDON三种 + FNO两种）在两个主要场景下的表现。每个配置都运行了完整的数据缩放曲线实验 (`M`从1到4096共13个点)，并在240个插值样本和50个外推样本上进行了测试。这构成了主要的定量分析基础。
2.  **(消融/扩展实验)** :
    - ✅ Helmholtz方程验证: （附录A）在另一个椭圆型PDE上重复了主要实验流程（包括插值和外推），证明了方法的普适性而非仅限于泊松方程特例。
    - ✅ “物理解一致性”分析: （附录B）额外计算了预测解的PDE残差误差图6)，从“是否满足控制方程”的角度提供了更深层次的物理解释性分析指标，补充了单纯的像素级误差比较(`L1`) 。
3.  **(公平性与客观性)** :
    - ✅ Benchmark一致: POSEIDON和FNO的参数规模被对齐至约2100万(21M)，确保了比较的公平性(参数量匹配)。
    - ✅ Baseline选择合理: FNO是当前最主流的神经算子之一；POSEIDON的数据驱动版本是原文的标准方法；对比覆盖了从头训练 vs.迁移学习、纯数据 vs.（纯/混合）物理信息等关键维度。

### **6.主要结论与发现**
1.	**(低数据区域优势明显)** ：在标签数据极少的区域(`M <16`) ，基于预训练的POSEIDON模型无论采用何种微调方式均显著优于从头训练的FNO基线；其中混合策略表现最佳甚至能从单个标签样本中有效学习；这证明了预训练的通用表征能极大降低下游任务的样本复杂度；
2.	**(物理解释性与泛化能力提升)：**
	-	物理解一致性分析表明即使在高精度下获得低像素误差的数据驱动方法也可能产生高残差的非物理解而引入正则化的方法则能强制输出符合控制方程的解决方案；
	-	在外推场景下当面对未见过的极端源形状时混合策略展现出最强的鲁棒性和最低误差因为它通过动态生成机制接触到了更广泛的输入分布；
3．**(零样本学习的可行性)：**
	-	纯粹的物理解释性调整可以在完全不需要任何真实解的情况下实现对新类型偏微分方程的适应性学习并且达到接近有监督方法的精度水平这对于难以获取或成本高昂的真实数据的科学领域具有重要价值；

###7．优点
-	创新性地将物理学原理融入到了大型语言模型的调整过程中开辟了一条结合先验知识与有限观测的新路径；
-	提出的动态生成机制是一种简单而有效的无标注增强手段能够在不增加标注成本的情况下扩大模型的输入经验范围从而提升泛化能力；
-	实验设计全面不仅关注预测精度还引入了物理学一致性这一关键指标进行评价使得论证更加立体和有说服力；

8．不足与局限
-	问题复杂性有限目前仅在相对简单的稳态椭圆型偏微分方程上进行验证对于具有强非线性多尺度特征或时间依赖性的复杂系统该方法的效果尚待检验作者也承认这是未来需要探索的方向之一；
-	可能存在潜在的偏差风险如果预先训练的基座本身存在偏差例如过度拟合某些特定的动力学模式那么即使加入物理学约束也可能无法完全纠正这些偏差甚至可能被放大因此如何构建更中立且全面的预先训练数据集是一个开放性问题；
-应用限制该方法假设控制方程式及其边界条件是已知且可导的实际应用中许多问题的物理学规律可能不完全明确或者难以用简洁的形式表达出来这会限制该框架的直接应用范围；

---

(完)

---
**【总结者注】** 
本总结严格依据您提供的PDF文本内容撰写力求客观还原论文的研究脉络与方法精髓同时指出了其在创新性与局限性方面的平衡观点供您参考

好的，我将从上一次总结的中断处继续补全。

---

### **8. 不足与局限**
*   **问题复杂性有限**：目前仅在相对简单的稳态椭圆型偏微分方程（泊松、Helmholtz）上进行验证。对于具有强非线性、多尺度特征或时间依赖性的复杂系统（如Navier-Stokes方程），该方法的效果尚待检验。作者也承认这是未来需要探索的方向之一。
*   **可能存在潜在的偏差风险**：如果预训练的基础模型本身存在偏差（例如过度拟合某些特定的动力学模式），那么即使加入物理约束，也可能无法完全纠正这些偏差，甚至可能被放大。因此，如何构建更中立且全面的预训练数据集是一个开放性问题。
*   **应用限制**：该方法假设控制方程及其边界条件是已知且可微的。实际应用中，许多问题的物理规律可能不完全明确或者难以用简洁的数学形式表达出来，这会限制该框架的直接应用范围。
*   **计算开销权衡**：虽然推理速度极快，但混合微调策略在每个训练批次中需要动态生成额外样本并计算其PDE残差损失，这增加了单次迭代的计算成本（如表1所示）。在数据极度稀缺时这是值得的，但在拥有大量标签数据时，其相对于纯数据驱动微调的额外收益是否足以抵消这部分开销，需要根据具体场景权衡。

### **9. 未来工作方向**
论文在结论和讨论部分指出了几个有前景的未来研究方向：
1.  **扩展到更复杂的PDE族**：将方法应用于瞬态问题、非线性系统和高维PDE，以验证其普适性。
2.  **探索更高效的物理约束整合方式**：研究如何将硬边界条件、守恒律等物理先验更自然地嵌入到模型架构或训练过程中。
3.  **基础模型预训练的改进**：研究如何设计预训练任务和数据，以学习更具泛化性、偏差更小的物理表示。
4.  **理论分析**：为物理信息微调提供收敛性、泛化误差界等理论保证。

### **10. 总结评价**
本文是一项扎实且富有洞察力的工作，成功地在“PDE基础模型”与“物理信息机器学习”这两个前沿领域之间架起了桥梁。其提出的**混合微调范式**核心思想清晰而有力：利用预训练获得的通用表征作为起点，再通过物理定律这一“免费”的监督信号来弥补下游任务中标签数据的不足。实验设计系统、对比充分，不仅证明了方法在数据效率和外推泛化上的优势，还通过“物理解一致性”分析提供了更深层次的机理解释。

尽管目前验证的问题类型相对基础，但论文为科学机器学习社区指明了一个极具潜力的研究方向——如何让大规模预训练的“通才”模型高效、可靠地适应到各种具体的“专家”科学任务中。这项工作为构建下一代兼具数据驱动灵活性与物理规律可靠性的智能科学计算工具迈出了重要一步。

（完）
