---
title: "PDE-SSM: A Spectral State Space Approach to Spatial Mixing in Diffusion Transformers"
title_zh: PDE-SSM：一种用于扩散Transformer中空间混合的谱状态空间方法
authors: "Eshed Gal, Moshe Eliasof, Siddharth Rout, Eldad Haber"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13663v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 基于PDE的扩散Transformer，用对流-扩散-反应方程取代注意力机制
tldr: 针对视觉Transformer中自注意力机制存在的二次计算复杂度和弱空间归纳偏差问题，本研究提出了PDE-SSM方法。该方法用一个可学习的、基于物理动力学的对流-扩散-反应偏微分方程算子来替代注意力机制，在傅里叶域求解以实现具有强空间先验的全局信息耦合，其计算复杂度接近线性（O(N log N)）。研究者将其集成到流匹配生成模型中，构建了PDE-SSM-DiT。实验证明该模型在性能上达到或超越了最先进的扩散Transformer，同时大幅减少了计算开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13663v1/fig-001.webp\", \"caption\": \"Figure 2. CIFAR-10 Images: (a) real images; (b) DiT; (c) PDE-SSM-DiT. Visual quality is comparable, in congruence with Table 1.\", \"page\": 6, \"index\": 1, \"width\": 1017, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-002.webp\", \"caption\": \"Figure 3. ImageNet64 training. (a) All methods converge at a similar rate and to an FID score that is similar. (b) The achieved FID score is consistent with the internal FID score.\", \"page\": 6, \"index\": 2, \"width\": 1001, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-003.webp\", \"caption\": \"Figure 7. Sample celebrity faces generated by PDE-SSM-DiT (left) and DiT (right), trained on CelebA-HQ 256.\", \"page\": 15, \"index\": 3, \"width\": 1017, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-004.webp\", \"caption\": \"Figure 4. LSUN-Churches generations: (a) real images; (b) DiT; (c) PDE-SSM-DiT.\", \"page\": 7, \"index\": 4, \"width\": 1017, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-005.webp\", \"caption\": \"Figure 6. SSM maps visualization: (a) real simple image; (b) simple image SSM map; (c) real church image; (d) church SSM map.\", \"page\": 13, \"index\": 5, \"width\": 1017, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-006.webp\", \"caption\": \"Table 6. Experimental settings and hyperparameters for different experiments.\", \"page\": 13, \"index\": 6, \"width\": 1010, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-007.webp\", \"caption\": \"Figure 5. Oxford-Flowers generations: (a) real images; (b) DiT; (c) PDE-SSM-DiT.\", \"page\": 8, \"index\": 7, \"width\": 1017, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2603.13663v1/fig-008.webp\", \"caption\": \"Table 5. DiT vs. PDE-SSM-DiT training step runtimes (in seconds). Parameters in millions (M).\", \"page\": 8, \"index\": 8, \"width\": 466, \"height\": 375}]"
motivation: 视觉Transformer的成功受限于自注意力的二次计算成本和弱空间归纳偏差，需要一种更具原则性和可扩展性的替代方案。
method: 该方法提出PDE-SSM模块，用可学习的对流-扩散-反应偏微分方程替代注意力机制，并在傅里叶域求解以实现全局耦合。
result: 实验表明，基于PDE的扩散Transformer（PDE-SSM-DiT）在性能上匹配或超越了最先进的扩散Transformer，同时显著降低了计算成本。
conclusion: 研究表明，类似于一维场景中状态空间模型取代注意力机制，多维偏微分方程算子为下一代视觉模型提供了一个高效且富含归纳偏好的基础。
---

## 摘要
视觉Transformer的成功——尤其是在生成建模方面——受到自注意力机制的二次方计算成本和弱空间归纳偏置的限制。我们提出了PDE-SSM，一种空间状态空间模块，它用一个可学习的对流-扩散-反应偏微分方程取代了注意力机制。该算子通过基于物理动力学的信息流建模而非全连接令牌交互，编码了一种强大的空间先验。在傅里叶域求解该PDE产生了具有近线性复杂度$O(N \log N)$的全局耦合，为注意力机制提供了一个原理性且可扩展的替代方案。我们将PDE-SSM集成到流匹配生成模型中，得到了基于PDE的扩散Transformer PDE-SSM-DiT。实验表明，PDE-SSM-DiT在性能上匹配或超越了最先进的扩散Transformer，同时显著降低了计算量。我们的结果表明，类似于在一维场景中状态空间模型取代注意力机制的情况，多维PDE算子为下一代视觉模型提供了一个高效且富含归纳偏置的基础。

## Abstract
The success of vision transformers-especially for generative modeling-is limited by the quadratic cost and weak spatial inductive bias of self-attention. We propose PDE-SSM, a spatial state-space block that replaces attention with a learnable convection-diffusion-reaction partial differential equation. This operator encodes a strong spatial prior by modeling information flow via physically grounded dynamics rather than all-to-all token interactions. Solving the PDE in the Fourier domain yields global coupling with near-linear complexity of $O(N \log N)$, delivering a principled and scalable alternative to attention. We integrate PDE-SSM into a flow-matching generative model to obtain the PDE-based Diffusion Transformer PDE-SSM-DiT. Empirically, PDE-SSM-DiT matches or exceeds the performance of state-of-the-art Diffusion Transformers while substantially reducing compute. Our results show that, analogous to 1D settings where SSMs supplant attention, multi-dimensional PDE operators provide an efficient, inductive-bias-rich foundation for next-generation vision models.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对论文《PDE-SSM: A Spectral State Space Approach to Spatial Mixing in Diffusion Transformers》进行结构化、深入且客观的总结。

---

### **1. 核心问题与整体含义**

*   **研究动机**：视觉Transformer（尤其是用于生成建模的扩散Transformer，DiT）的成功受限于其核心组件——自注意力机制的两个固有缺陷：
    1.  **计算瓶颈**：在2D图像上，自注意力的计算和内存复杂度随令牌（图像块）数量呈二次方增长（$O(N^2)$），限制了模型在高分辨率下的可扩展性。
    2.  **弱空间归纳偏置**：自注意力本质上是置换不变的，缺乏对图像固有的空间局部性和网格结构的直接编码。虽然可以通过位置编码弥补，但其空间先验不如卷积神经网络（CNN）那样直接和强。
*   **整体含义**：本文旨在为视觉模型提供一个兼具**高效性**和**强空间先验**的全局信息混合机制。受一维序列建模中状态空间模型（SSM）成功取代注意力的启发，本文提出将SSM的核心思想——连续时间系统动力学——从一维时间域推广到多维空间域。其核心洞见是：一维常微分方程（ODE）的自然多维推广是偏微分方程（PDE）。因此，本文提出用基于物理的可学习PDE算子来替代注意力机制。

### **2. 方法论**

*   **核心思想**：用一个可学习的、物理驱动的“对流-扩散-反应”偏微分方程来建模特征图在“虚拟时间”维度上的演化过程，以此作为全局信息混合的算子。
    *   **公式 (2)**：$\frac{\partial h}{\partial t} = \underbrace{\nabla \cdot (K\nabla h)}_{\text{扩散}} + \underbrace{b \cdot \nabla h}_{\text{对流}} + \underbrace{r h}_{\text{反应}}$
    *   `h(t, x)` 是隐藏状态；`K` (扩散张量)、`b` (对流向量场)、`r` (反应项) 是可学习参数；`τ` (积分时间)控制感受野大小。
*   **关键技术细节**：
    1.  **傅里叶域求解与高效实现**：
        *   将PDE转换到傅里叶域后，复杂的空间导数运算简化为乘法操作。每个频率分量 `k` 的解具有闭式表达 $\hat{h}(k, τ) = e^{τ λ(k)} \hat{h}(k, 0)$。
        *   其中 $λ(k) = -k^T K k + r + i(b·k)$。这使得整个演化过程可以通过快速傅里叶变换（FFT）高效实现为全局卷积操作。
        *   最终的计算复杂度为 $O(N log N)$ + $O(N·C_{hid}^2)$ （N为令牌数），远低于标准注意力的 $O(N^2)$。
    2.  **多通道耦合与算法流程 (Algorithm 1)**：
        *   输入嵌入 (`Bγ`)：通过一个点卷积和其空间梯度的加权组合生成初始状态。
        *   PDE演化 (`Gζ`)：在傅里叶域中构造并应用由参数 `ζ=(K, b, r, τ)` 定义的矩阵符号 $\hat{G}_ζ(k)$。
        *   通过将 `K`, `b`, `r` 从标量扩展为作用于通道维度的矩阵，实现了通道间的耦合交互。
    3.  **集成到DiT架构中**：
        *   将标准DiT块中的注意力层 (`ATT`) 直接替换为PDE-SSM模块，保持其余结构（如MLP、归一化层、位置编码等）不变。这种“即插即用”的设计便于公平比较。

### **3. 实验设计**

*   **任务与基准模型**：
    *   **任务**：无条件/条件图像生成（流匹配）。
    *   **基准方法/对比对象**：
        1.  DiT （原始基于注意力的扩散Transformer）。
        2.  传统U-Net （作为经典的扩散模型骨干网络）。
        3.  内部消融实验变体（用于分析PDE各分量的贡献）。
*   **数据集与场景覆盖度评估 (RQ4)**：
    1.  CIFAR-10 & ImageNet-64/256: “类别多样性”数据集。
    2.  CelebA-HQ & LSUN-Churches & Oxford-Flowers: “结构/纹理中心”数据集。

### **4. 资源与算力**

论文中明确提到所有实验均在一张NVIDIA RTX6000 Ada GPU上运行。但未详细说明每个具体实验的训练时长或总GPU小时数。

### **5. 实验数量与充分性**

实验设计较为系统和充分，旨在全面回答四个研究问题(RQ1-RQ4)：

*   **(RQ1/RQ2)性能对比实验**:在多个数据集上进行了广泛的定量比较（FID、MSE等），并提供了定性样本对比图。结果表明PDE-SSM-DiT性能匹配或超越DiT/U-Net基线。
*   **(RQ3)效率与可扩展性分析**:提供了详细的运行时对比表格(Table5)，分析了不同图像尺寸和块大小下DiT与PDE-SSM-DiT的参数量和单步训练时间变化趋势。验证了理论复杂度优势在实际中的体现。
*   **(RQ4)领域鲁棒性验证**:在不同类型的数据集上进行测试，证明了方法在不同数据分布下的有效性。
*   **(附录C)消融研究**:研究了扩散、对流、反应三个项各自的作用及其组合效果(Table7)。
*   **(附录D/E/F/G)补充分析**:包括SSM特征图可视化、更多计时结果、ImageNet-256结果以及额外生成样本展示。

总体而言，实验设计是客观且公平的：作者强调使用了官方的DiT代码库进行修改，“仅替换注意力块”，保持所有其他超参数、训练计划、采样器不变进行“即插即用”式比较。这有效隔离了架构变化带来的影响。

### **6.主要结论与发现**

1.  PDE-SSM可以作为一个有效的“即插即用”替代品替换DiT中的注意力层，在不重新调优超参数的情况下保持或提升生成质量(FID)。
2. PDE-SSM-DiT的计算优势在小块尺寸和高分辨率下尤为显著。当使用小块时(如patch size=2)，它能以更低的计算成本维持长程依赖建模能力；而标准DiT为了控制计算成本被迫使用大块(patch size=8)，牺牲了精细的空间交互能力(Table5)。
3.PDE提供的物理驱动归纳偏置(扩散平滑局部信息、对流引导信息流向、反应进行局部变换)对于需要全局一致性的结构化数据(如LSUN-Churches, Oxford-Flowers)尤其有益(PDE-SSM在这些数据集上FID提升更明显)。
4.PDE-SSM继承了其数学形式的稳定性保证(由于K半正定确保频谱实部非正)，这是标准注意力不具备的理论性质。

### **7.优点**

1.**原理创新性强**:首次系统性地将状态空间模型从一维序列推广到多维空间的连续动力学框架(PDE)，提供了一个优雅且数学基础坚实的视角来理解全局信息混合。
2.**效率优势显著**:通过傅里叶域的闭式解实现了近线性的全局耦合复杂度($O(N log N)$)，解决了自注意力的二次方瓶颈问题，尤其在高分辨率和小块尺寸场景下优势巨大。
3.**强归纳偏置**:将对流-扩散-反应这一经典物理过程作为算子内核，自然地编码了关于信息如何在空间中传播的先验知识(平滑性、方向性等)。
4.**易于集成**:模块化设计使其能无缝替换现有Transformer架构中的注意力层(“drop-in replacement”) ，便于迁移和应用。

### **8.不足与局限**

1.**理论深度有限**:虽然提出了基于PDE的框架并给出了稳定性和表达能力的讨论，但对于该特定形式的PDE为何最适合视觉任务缺乏更深层的理论解释或与其他可能PDE形式的比较论证。(例如为何选择线性二阶形式？)
2.**应用范围限制**:目前工作主要聚焦于图像生成的特定任务(Diffusion Transformer)。其在其他视觉任务(如分类、检测、分割)、模态(视频/3D体素)、以及更大规模预训练场景下的普适性和有效性尚未得到验证。(Impact Statement中也承认此点)
3.**通道耦合的计算开销**:尽管整体复杂度降低，但多通道耦合引入了$O(N·C_{hid}^2)$项。当隐藏通道数$C_{hid}$非常大时(如在极大模型中)，这部分开销可能成为新的瓶颈。(文中已提及)
4.**潜在的超参数敏感性**:虽然声称是“plug-and-play”，但新的模块引入了额外的超参数组(`K`, `b`, `r`, `τ`, Γ等)。这些参数的初始化策略和对最终性能的影响未做深入探讨。(例如积分时间τ如何设置？)
5.**缺乏与现代高效注意力变体的全面对比**:文中仅提到了线性注意力等方法表现不佳作为背景动机之一。但在实验中并未将提出的方法与最新的高效注意力变体(e.g., FlashAttention, Longformer for Vision等)进行直接比较以证明其在效率和质量上的双重优越性。

**(完)**

好的，我将从您上次输出的“不足与局限”部分继续，补充论文的其他关键方面，并最终完成总结。

---

### **9. 潜在影响与未来方向**

1.  **为视觉模型提供新范式**：PDE-SSM 展示了一种超越自注意力和传统卷积的、基于连续动力学的全局建模新路径。它可能启发更多将物理/数学启发的算子引入深度学习架构的研究。
2.  **推动高效高分辨率生成**：其近线性的复杂度特性为解决高分辨率图像/视频生成的计算瓶颈提供了切实可行的方案，可能推动相关应用的发展。
3.  **连接不同领域**：该工作架起了连续时间系统（SSM）、偏微分方程理论、谱方法与现代深度学习（尤其是扩散模型）之间的桥梁，促进了跨学科的交叉融合。
4.  **未来研究方向**：
    *   **理论深化**：探索更一般或非线性的PDE形式，研究其表达能力与稳定性的理论边界。
    *   **任务拓展**：将PDE-SSM模块应用于分类、分割、视频理解等其他视觉任务，验证其通用性。
    *   **架构创新**：设计完全基于PDE-SSM构建的新型骨干网络，而非仅仅作为注意力替代品。
    *   **效率优化**：进一步优化多通道耦合部分的计算，或探索在空域的高效实现以降低对FFT的依赖。

### **10. 总结评价**

本文《PDE-SSM: A Spectral State Space Approach to Spatial Mixing in Diffusion Transformers》是一项原理新颖、动机明确且实验扎实的工作。它精准地抓住了当前视觉Transformer在效率和归纳偏置上的核心痛点，并提出了一个优雅且有效的解决方案。

*   **核心贡献**在于将一维状态空间模型（SSM）的思想创造性地推广到多维空间，通过引入“对流-扩散-反应”偏微分方程作为全局混合算子。这一框架不仅在数学上具有美感（提供了闭式解和稳定性保证），而且在工程上实用（通过傅里叶变换实现高效计算）。
*   **实验验证充分且令人信服**。作者通过严格的“即插即用”式替换，在多个图像生成基准上证明了PDE-SSM-DiT能够匹配甚至超越标准注意力DiT的性能，同时显著降低了计算开销，尤其是在高分辨率和小块尺寸场景下。消融实验也清晰地揭示了PDE各组成部分的作用。
*   **局限性同样清晰**。该方法目前的应用范围较窄，理论解释尚可深入，并且在与最新高效注意力变体的对比上存在空白。这些不足也为后续研究指明了方向。

总体而言，这项工作不仅提出了一个具体的高效模块（PDE-SSM），更重要的是为重新思考如何建模视觉数据中的空间关系提供了一个富有潜力的新范式。它证明了将经典的物理数学模型与现代深度学习相结合可以产生强大的解决方案，具有重要的启发意义和进一步探索的价值。

（完）
