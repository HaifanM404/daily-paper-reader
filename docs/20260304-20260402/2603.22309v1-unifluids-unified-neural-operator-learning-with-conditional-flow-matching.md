---
title: "UniFluids: Unified Neural Operator Learning with Conditional Flow-matching"
title_zh: UniFluids：基于条件流匹配的统一神经算子学习
authors: "Haosen Li, Qi Meng, Jiahao Li, Rui Zhang, Ruihua Song, Liang Ma, Zhi-Ming Ma"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.22309v1"
tags: ["query:pde-llm"]
score: 9.0
evidence: 利用扩散 Transformer 统一学习多种 PDE 的解算子
tldr: 偏微分方程模拟在科研中意义重大，利用深度学习学习其解算子潜力巨大。本文提出UniFluids，一个条件流匹配框架，它利用扩散Transformer的可扩展性来统一学习不同维度与物理变量的多种PDE的解算子。与自回归PDE基础模型不同，它是首个实现并行序列生成的统一算子学习方法。其核心是引入了统一的四维时空表示进行联合训练与条件编码，并发现数据有效维度远低于分块维度，从而采用x-预测显著提升了精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22309v1/fig-001.webp\", \"caption\": \"Table 6. PDEBench subsets used in this work (variables and resolutions). N /T /Ns denote #trajectories, #timesteps, and spatial grid resolution in the PDEBench release; Target Ns is our unified resolution.\", \"page\": 13, \"index\": 1, \"width\": 735, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-002.webp\", \"caption\": \"Figure 4. Validation loss versus epoch on CFD fine-tuning (S model), comparing training from scratch (w/o pretrain) and fine-tuning from the foundation checkpoint (w/ pretrain) across 1D/2D/3D.\", \"page\": 8, \"index\": 2, \"width\": 1010, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-003.webp\", \"caption\": \"Figure 1. Overall architecture. (a) Unified 4D representation: heterogeneous 1D/2D/3D PDE trajectories with different variable sets are aligned to a canonical 4D grid (t, h, w, d) with zero-padding on degenerate axes and a channel mask for missing variables, then space–time patchified into tokens paired with explicit 4D coordinates, enabling 4D RoPE. (b) Unified condition encoder: a Transformer encodes the observed history into ctok (dense condition) and a aggregated cg (compact condition) via temporal/spatial aggregation. (c) Conditional flow-matching operator: starting from noisy future-window tokens, the operator stacks 4D-RoPE self-attention and cross-attention to ctok, with AdaLN modulated by (t, cg,dim type), to predict the clean solution.\", \"page\": 4, \"index\": 3, \"width\": 961, \"height\": 562}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-004.webp\", \"caption\": \"Table 1. Effective-dimension diagnostics for PDE patch vectors. V denotes the size of the patch vector.\", \"page\": 5, \"index\": 4, \"width\": 454, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-005.webp\", \"caption\": \"Figure 5. Prediction results on 1D PDEBench subsets. From left to right: 1D Advection, 1D Burgers, and 1D CFD (compressible Navier–Stokes). Each panel shows rollouts for time steps T+1 to T+6; the top row is ground truth and the bottom row is our prediction.\", \"page\": 16, \"index\": 5, \"width\": 1010, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-006.webp\", \"caption\": \"Figure 6. Prediction results on 2D PDEBench subsets. Top row: ground truth; bottom row: our prediction; shown for time steps T+1 to T+6. We visualize (top-left) 2D CFD (compressible Navier–Stokes), (top-right) reaction–diffusion, (bottom-left) shallow-water equation, and (bottom-right) 2D incompressible Navier–Stokes (with particle tracer).\", \"page\": 16, \"index\": 6, \"width\": 1017, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2603.22309v1/fig-007.webp\", \"caption\": \"Figure 7. Prediction results on the 3D CFD subset (compressible Navier–Stokes). We display rollouts for time steps T+1 to T+6; the top row shows ground truth and the bottom row shows our prediction.\", \"page\": 16, \"index\": 7, \"width\": 1017, \"height\": 346}]"
motivation: 为了统一学习不同维度、不同物理变量的多种偏微分方程的解算子，并实现并行序列生成以克服自回归模型的限制。
method: 该方法采用条件流匹配框架和四维时空统一表示，利用扩散Transformer的可扩展性，并引入x-预测来提升精度。
result: 在涵盖1D、2D和3D空间维度的多个PDE数据集上的大规模评估表明，UniFluids实现了高预测精度。
conclusion: UniFluids 展示了在统一框架下学习不同偏微分方程解算子的强大能力，具有良好的可扩展性和跨场景泛化性。
---

## 摘要
偏微分方程（PDE）模拟在科学研究中具有广泛的重要意义。目前，结合深度神经网络学习PDE的解算子已展现出巨大潜力。本文提出了UniFluids，一个条件流匹配框架，它利用扩散Transformer的可扩展性，统一学习不同维度和物理变量的多种PDE的解算子。与自回归的PDE基础模型不同，UniFluids采用流匹配来实现并行序列生成，使其成为首个用于统一算子学习的此类方法。具体而言，为异构PDE数据集引入统一的四维时空表示，实现了联合训练和条件编码。此外，我们发现PDE数据集的有效维度远低于其分块维度。因此，我们在流匹配算子学习中采用了$x$预测方法，经验证能显著提高预测精度。我们在涵盖1D、2D和3D空间维度的多个PDE数据集上对UniFluids进行了大规模评估。实验结果表明，UniFluids实现了强大的预测精度，并展现出良好的可扩展性和跨场景泛化能力。代码将于稍后发布。

## Abstract
Partial differential equation (PDE) simulation holds extensive significance in scientific research. Currently, the integration of deep neural networks to learn solution operators of PDEs has introduced great potential. In this paper, we present UniFluids, a conditional flow-matching framework that harnesses the scalability of diffusion Transformer to unify learning of solution operators across diverse PDEs with varying dimensionality and physical variables. Unlike the autoregressive PDE foundation models, UniFluids adopts flow-matching to achieve parallel sequence generation, making it the first such approach for unified operator learning. Specifically, the introduction of a unified four-dimensional spatiotemporal representation for the heterogeneous PDE datasets enables joint training and conditional encoding. Furthermore, we find the effective dimension of the PDE dataset is much lower than its patch dimension. We thus employ $x$-prediction in the flow-matching operator learning, which is verified to significantly improve prediction accuracy. We conduct a large-scale evaluation of UniFluids on several PDE datasets covering spatial dimensions 1D, 2D and 3D. Experimental results show that UniFluids achieves strong prediction accuracy and demonstrates good scalability and cross-scenario generalization capability. The code will be released later.

---

## 论文详细总结（自动生成）

# UniFluids 论文总结

## 1. 核心问题与整体含义
*   **研究动机**：偏微分方程（PDE）模拟是科学计算的核心，但传统数值方法计算成本高昂。近年来，神经算子（Neural Operators）通过学习从PDE参数到解的映射，展现出巨大潜力。然而，现有方法面临两大挑战：
    1.  **自回归误差累积**：大多数模型采用确定性、短步长的自回归预测，导致长期预测时误差不断放大。
    2.  **缺乏统一性**：现有模型通常针对特定数据集（维度、物理变量）设计架构或流程，难以用一个模型统一处理异构的PDE数据集（如1D/2D/3D、不同物理变量）。
*   **整体含义**：本文旨在提出一个**统一的、概率性的生成式算子学习框架**，能够用一个模型处理多种维度、多种物理变量的PDE未来状态预测问题，并通过并行生成避免自回归误差累积。

## 2. 方法论
*   **核心思想**：将条件流匹配（Conditional Flow-Matching）生成模型与Transformer架构结合，构建一个统一的概率生成算子 `UniFluids`。
*   **关键技术细节**：
    1.  **统一的四维时空表示**：
        *   将所有PDE轨迹（无论原始是1D、2D还是3D）对齐到一个规范的4D网格 `(t, h, w, d)`上。缺失的空间轴用零填充。
        *   使用一个共享的最大通道数 `C_max` 来容纳所有数据集的物理变量，缺失变量用零填充并用通道掩码标识。
        *   将数据划分为时空块（patch），每个块向量化为一个token，并附带其明确的4D整数坐标 `(t_i, h_i, w_i, d_i)`。
        *   这种表示使得不同维度的数据成为同一4D网格的子空间，支持使用单一的Transformer架构进行联合训练。
    2.  **条件编码器**：
        *   基于Transformer的编码器处理历史状态序列的token。
        *   输出两种条件信息：密集条件token `c_tok`（用于后续交叉注意力）和通过时空聚合层得到的紧凑全局条件向量 `c_g`。
    3.  **条件流匹配算子**：
        *   **目标**：学习从噪声到未来状态的条件概率流ODE。
        *   **关键创新 - x-预测 (x-prediction)**：
            *   分析发现PDE数据的有效维度远低于其分块后的原始维度（尤其是高维数据），即数据位于低维子空间。
            *   因此，网络直接预测干净的未来状态场 `x` (`U_fut`)，而非直接预测速度场 `v` 或噪声 `ϵ`。速度 `v` 通过公式 `v = (x - z_t) / (1 - t)` 由预测的 `x` 解析计算得到。
            *   训练时最小化标准流匹配的v-loss。这种方法被证明能显著提升训练的稳定性和预测精度。
        *   **架构**：基于Transformer的解码器，对带噪声的未来窗口token进行4D旋转位置编码的自注意力和对条件token `c_tok`的交叉注意力，并使用AdaLN注入时间步和全局条件信息。

## 3. 实验设计
*   **主要数据集**：在标准化基准套件 **PDEBench**上进行评估和预训练。涵盖的子集包括：
    *   **1D**: Advection方程、Burgers方程、可压缩Navier-Stokes方程(CFD)。
    *   **2D**: CFD（多个参数组合）、反应-扩散方程、浅水方程(SWE)、不可压缩Navier-Stokes方程(Incom)。
    *   **3D**: CFD（湍流和随机初始条件）。
*   **Baseline对比方法分为两类**：
    1.  **任务特定模型 (Task-specific)**：针对每个数据集从头训练的模型。包括PINNs、U-Net、FNO。
    2.  **统一预训练模型 (Unified pre-training)**：在多个物理系统上预训练的统一基础模型。包括MPP、DPOT、OmniArch。

## 4. 资源与算力
*   论文中未明确说明具体的GPU型号、数量及总训练时长等硬件细节和算力消耗。

## 5. 实验数量与充分性
*   **实验组别丰富且充分**：
    1.  **主实验 (Main Results)**：在涵盖1D/2D/3D的多个PDEBench子集上全面评估了四个不同规模的UniFluids模型(S/M/L/XL)，并与所有基线进行了对比。结果显示UniFluids在大多数任务上达到最优或极具竞争力的精度（相对L2误差降低最高达86.7%）。
    2.  **缩放实验 (Scaling Experiments)**：验证了模型性能随参数量增加而持续提升的良好缩放特性。
    3.  **泛化能力实验 (Generalization)**：
        *   ***域内适应***：展示了在CFD数据集上微调比从头训练收敛更快、效果更好。
        *   ***零样本外推***：将预训练好的UniFluids-XL直接应用于未见过的复杂2D流体场景（激波管、Kelvin-Helmholtz不稳定性等），取得了优于部分基线的性能，证明了其强大的跨场景泛化能力。
    4.  ***消融实验*** **(Ablation Study)** ：关键地验证了“x-预测”相对于“v-预测”和“ϵ-预测”的优越性。“ϵ-预测”甚至无法收敛，“v-预测”效果显著差于“x-预测”。
    5.  其他分析实验还包括多分辨率支持验证和推理时间分析。

## 6.主要结论与发现
*   成功提出了首个基于条件流匹配的统一神经算子框架UniFluids，能够用一个单一模型处理异构的多维多物理变量PDE数据集的概率性未来状态生成任务。
*   提出的统一四维时空表示是实现跨维度联合训练的关键技术支撑。
*   发现了高维PDE数据的有效维度远低于其网格维度的重要现象，并据此采用“x-预测”策略显著提升了模型的精度和稳定性。（这是本文的一个重要洞察）
*   大规模实验表明UniFluids在准确性上大幅超越现有基线方法，并展现出优异的可扩展性和跨场景泛化能力。

##7 .优点
*   ***创新性地统一了框架*** ：首次将流匹配生成范式系统地应用于统一的多维多物理变量PDE算子学习问题中实现了并行序列生成避免了自回归误差累积问题具有开创性意义.
*   ***技术设计精巧*** ：统一的四维表示非常巧妙地将异构数据处理为同构形式简化了建模复杂度；结合4DRoPE使得同一架构能灵活适应不同分辨率.
*   ***深刻的洞察与分析*** ：不仅提出了方法还深入分析了数据特性(有效维度)并据此优化了网络参数化方式(x-pred)使工作具有扎实的理论与实践基础.
*   ***实验全面且说服力强*** ：覆盖了从1 D到3 D的主流P DE类型进行了充分的对比消融以及泛化能力测试结果详实可信.

##8 .不足与局限
*   ***采样效率限制*** ：虽然避免了自回归但仍需迭代求解O DE(例如默认40步)相比一步推理的自回归或确定性模型推理速度较慢文中也提到探索更快的采样器是未来方向.
*   ***分辨率泛化的不完全自由*** ：尽管通过坐标编码支持不同分辨率但当测试分辨率与训练分布差异过大时特别是对于包含尖锐结构(如激波)或高雷诺数湍流的场景性能仍可能下降.
*   ***应用范围聚焦*** ：主要评估集中在CF D相关的基准数据集(P DEBench)上虽然涵盖了多种类型但对于更复杂的几何形状不规则网格或其他类型的偏微分方程系统的适用性有待进一步验证.
*   ***不确定性量化深度有限*** ：作为概率生成模型它能够产生分布但论文主要关注点在于点估计精度(如相对L2误差)对于所生成分布的质量校准以及在实际不确定性量化任务中的表现讨论较少.

（完）
