---
title: Stochastic Dimension-Free Zeroth-Order Estimator for High-Dimensional and High-Order PINNs
title_zh: 面向高维高阶 PINN 的随机无维度零阶估计器
authors: "Zhangyong Liang, Ji Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24002v1"
tags: ["query:pde-llm"]
score: 10.0
evidence: 高维PINN的零阶估计器
tldr: 针对高维高阶物理信息神经网络（PINNs）训练中存在的空间导数复杂度高和反向传播内存开销大的挑战，本研究提出了随机维度无关零阶估计器（SDZE）统一框架。该方法通过共同随机数同步技术锁定空间随机种子以抵消方差爆炸，并引入隐式矩阵自由子空间投影将参数探索方差从O(P)降至O(r)。该框架实现了空间和内存的维度无关复杂度，实验证明其能在单块A100 GPU上高效训练千万维PINNs。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24002v1/fig-001.webp\", \"caption\": \"Table 1: Speed comparison for the two-body Allen-Cahn equation. FO denotes first-order (backpropagation-based) methods, ZO denotes zeroth-order (backprop-free) methods.\", \"page\": 18, \"index\": 1, \"width\": 875, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.24002v1/fig-002.webp\", \"caption\": \"Table 2: Memory comparison for the two-body Allen-Cahn equation. FO denotes first-order, ZO denotes zeroth-order methods.\", \"page\": 18, \"index\": 2, \"width\": 846, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2603.24002v1/fig-003.webp\", \"caption\": \"Figure 1: Scalability bottlenecks of existing first-order high-dimensional PDE solvers (STDE, SDGD, HTE, FOBAD) as problem dimension d grows from 5× 102 to 106. (a) GPU peak memory (MB) grows rapidly with dimension, leading to catastrophic OOM failures at extreme scales. (b) Wall-clock time (s) likewise increases sharply, revealing severe computational overhead.\", \"page\": 2, \"index\": 3, \"width\": 678, \"height\": 319}]"
motivation: 高维高阶PINNs面临空间导数复杂度高和反向传播内存开销大的问题，现有方法存在方差爆炸或内存消耗过大的缺陷。
method: 提出SDZE框架，通过CRNS技术锁定随机种子以抵消方差爆炸，并引入隐式矩阵自由子空间投影降低参数探索方差。
result: 实验表明SDZE能在单块A100 GPU上训练千万维PINNs，在速度和内存效率上显著优于现有基线方法。
conclusion: SDZE框架成功解决了高维高阶PINNs训练中的计算瓶颈，为大规模科学计算问题提供了高效可行的解决方案。
---

## 摘要
针对高维高阶偏微分方程的物理信息神经网络（PINNs）主要受限于 $\mathcal{O}(d^k)$ 的空间导数计算复杂度以及反向传播（BP）的 $\mathcal{O}(P)$ 内存开销。虽然随机化空间估计器成功将空间复杂度降至 $\mathcal{O}(1)$，但其对一阶优化的依赖仍导致大规模训练时内存消耗过高。零阶（ZO）优化提供了一种无需 BP 的替代方案；然而，将随机化空间算子与 ZO 扰动简单结合会引发 $\mathcal{O}(1/\varepsilon^2)$ 的方差爆炸，导致数值发散。为应对这些挑战，我们提出了 \textbf{随机无维度零阶估计器}（\textbf{SDZE}），这是一个在空间和内存上均实现维度无关复杂度的统一框架。具体而言，SDZE 利用 \emph{公共随机数同步（CRNS）}，通过锁定跨扰动的空间随机种子，以代数方式消除 $\mathcal{O}(1/\varepsilon^2)$ 方差。此外，引入一种 \emph{隐式无矩阵子空间投影}方法，将参数探索方差从 $\mathcal{O}(P)$ 降至 $\mathcal{O}(r)$，同时保持 $\mathcal{O}(1)$ 的优化器内存占用。实验结果表明，SDZE 能够在单个 NVIDIA A100 GPU 上训练千万维度的 PINNs，在速度和内存效率上相比现有先进基线均有显著提升。

## Abstract
Physics-Informed Neural Networks (PINNs) for high-dimensional and high-order partial differential equations (PDEs) are primarily constrained by the $\mathcal{O}(d^k)$ spatial derivative complexity and the $\mathcal{O}(P)$ memory overhead of backpropagation (BP). While randomized spatial estimators successfully reduce the spatial complexity to $\mathcal{O}(1)$, their reliance on first-order optimization still leads to prohibitive memory consumption at scale. Zeroth-order (ZO) optimization offers a BP-free alternative; however, naively combining randomized spatial operators with ZO perturbations triggers a variance explosion of $\mathcal{O}(1/\varepsilon^2)$, leading to numerical divergence. To address these challenges, we propose the \textbf{S}tochastic \textbf{D}imension-free \textbf{Z}eroth-order \textbf{E}stimator (\textbf{SDZE}), a unified framework that achieves dimension-independent complexity in both space and memory. Specifically, SDZE leverages \emph{Common Random Numbers Synchronization (CRNS)} to algebraically cancel the $\mathcal{O}(1/\varepsilon^2)$ variance by locking spatial random seeds across perturbations. Furthermore, an \emph{implicit matrix-free subspace projection} is introduced to reduce parameter exploration variance from $\mathcal{O}(P)$ to $\mathcal{O}(r)$ while maintaining an $\mathcal{O}(1)$ optimizer memory footprint. Empirical results demonstrate that SDZE enables the training of 10-million-dimensional PINNs on a single NVIDIA A100 GPU, delivering significant improvements in speed and memory efficiency over state-of-the-art baselines.

---

## 论文详细总结（自动生成）

# 论文分析总结：面向高维高阶 PINN 的随机无维度零阶估计器 (SDZE)

## 1. 核心问题与整体含义
*   **研究动机**：物理信息神经网络（PINNs）在求解高维、高阶偏微分方程（PDEs）时面临两大“维度灾难”瓶颈：
    1.  **空间复杂度**：通过自动微分（AD）计算高阶导数时，计算图随维度 `d` 和阶数 `k` 呈指数级增长 (`O(d^k)`)。
    2.  **内存开销**：反向传播（BP）需要缓存大量中间变量，导致内存消耗与模型参数量 `P` 成正比 (`O(P)`)，限制了可扩展性。
*   **现有方法的局限**：
    *   随机化空间估计器（如 SDGD, STDE）成功将空间复杂度降至 `O(1)`，但仍依赖一阶优化器，无法摆脱 BP 的内存瓶颈。
    *   零阶优化器（ZO）无需 BP，但若与随机化空间算子简单结合，会因两次前向传播的空间采样噪声不同步，引发 `O(1/ε^2)` 的方差爆炸，导致训练发散。
*   **本文目标**：提出一个统一框架，同时实现**空间计算**和**参数优化**两个层面的维度无关性，彻底解决高维高阶 PINNs 的训练内存和计算瓶颈。

## 2. 提出的方法论
*   **核心思想**：SDZE (Stochastic Dimension-free Zeroth-order Estimator) 是一个完全无需反向传播的元优化器框架。它通过两项核心技术协同工作：
    1.  在空间层面使用随机化估计器来近似 PDE 算子。
    2.  在参数层面使用零阶优化进行更新。
    3.  关键创新在于解决了二者直接结合时产生的“双重随机方差死锁”。
*   **关键技术细节**：
    *   **公共随机数同步 (CRNS)**：
        *   **问题**：对参数进行对称扰动 (`θ ± εΔθ`) 时，若两次前向传播使用独立的空间随机种子 (`ω+`, `ω-`)，则空间截断噪声的差异会被微小扰动步长 `ε` 放大，导致梯度估计方差趋于无穷 (`O(1/ε^2)`)。
        *   **解决方案**：强制锁定并使用完全相同的空间随机种子 `ω` 来计算对称扰动点的损失。这样，在有限差分中，由随机采样引入的基础噪声项和二阶 Hessian噪声项会被精确地代数抵消，从而消除方差奇点。
    *   **隐式无矩阵子空间投影 (Implicit Matrix-Free Subspace-ZOO)**：
        *   **问题**：标准的全参数零阶扰动方差为 `O(P)`；现有基于随机投影的子空间方法虽能将方差降至 `O(r)` (r为子空间秩)，但需要存储巨大的投影矩阵 (`P × r`)，这本身就会耗尽内存优势。
        *   **解决方案**：
            *   对每一层网络参数矩阵 `W^(l)` ，动态生成低秩正交基 `U_t^(l)`, `V_t^(l)` (通过 QR分解高斯矩阵)，并采用“惰性更新”策略控制更新频率。
            *   构造低秩扰动 `ΔW_t^(l) = U_t^(l) Z_t^(l) (V_t^(l))^⊤`，其中核心扰动矩阵 `Z_t^(l)`很小 (`r_l × r_l`)。
            *   利用张量结合律重写前向传播公式。例如对于隐藏层激活 H(l-1)，计算顺序为:先计算小规模中间结果 `(H(l-1) U_t^(l)) ∈ R^{B×r_l}` ，再与 Z_t^(l)、 V_t^(l)^⊤相乘。整个过程避免了显式构造或存储任何规模为原始参数大小的稠密矩阵 (`m_l × n_l`) ，实现了真正的常数级(`O(1)`)额外内存开销。

## 3. 实验设计
*   **测试场景与基准方法**：
    *   **有效高维 PDEs**: Poisson、Allen-Cahn、Sine-Gordon方程以及半线性抛物型方程（热方程、Allen-Cahn、Sine-Gordon），维度从100到1000万。精确解采用“多体”形式构造以确保高维非分离性。
    *   **有效高阶 PDEs**: Korteweg-de Vries (KdV)、Kadomtsev-Petviashvili (KP)方程及其梯度增强版本(gPINN)。
*   **对比方法**: 
    *   **一阶方法**: Backward-mode SDGD, Parallelized SDGD, Forward-over-Backward SDGD, Forward Laplacian, Hutchinson Trace Estimation (HTE), Stochastic Taylor Derivative Estimator (STDE)。
    *   **(准)零阶方法**: RS-PINN, Mixed-mode SDGD (AD-Spatial + ZO-Param)。
*   **评估指标**: GPU峰值内存占用、每秒迭代次数、相对L2误差。

## 4. 资源与算力
论文未明确说明所有实验的总训练时长或使用的具体GPU数量。但关键性能数据（如速度 it/s、内存 MB）均基于单个 NVIDIA A100 GPU（40GB显存）测得。文中提到SDZE成功在单块A100上训练了千万维度的PINNs。

## 5.实验数量与充分性
实验较为充分和客观：
*   **主实验**:在多种PDE类型（椭圆型、抛物型）、不同维度（从10²到10⁷）、不同精确解结构上进行广泛测试，全面评估了SDZE的速度和内存效率优势。
*   **消融实验**: 
    1.  CRNS有效性验证:对比了有无CRNS时的梯度方差和训练稳定性（表3），证实了CRNS对消除方差爆炸的关键作用。
    2.子空间超参数研究:探究了子空间秩 r和更新频率 F对最终精度的影响（表4），验证了方法的鲁棒性并给出了调优指导。
*公平性:所有对比方法均在相同硬件环境和相同问题设置下进行评估。对于因内存不足(OOM)而失败的方法也明确标注。

##6.主要结论与发现
*SDZE首次实现了在高维高阶PINN训练中同时摆脱对输入维度 d和模型参数量 P的线性依赖。*
具体表现为:
1.**成功训练千万维PINN**:在单块A100 GPU上成功训练了参数量达千万级别的全连接密集PINN,而所有一阶基线方法均因 OOM而失败。
2.**双重优势**:相比最佳的一阶基线(STDE),SDZE在保持可比精度的同时,实现了显著的内存节省(例如在百万维问题上节省约22%显存)和速度提升(例如在十万维问题上快约13%)。
3.**理论保证**:严格证明了CRNS能消除 O(1/ε² )方差奇点,以及子空间投影能将参数探索方差从 O(P )压缩至 O(r)。收敛性定理表明其能以 O(√q /√T )的速率收敛到平稳点(q为子空间总容量)。

##7.优点
1.**开创性的框架设计**:首次系统性地解决了随机化空间估计与零阶优化结合时的“双重随机方差死锁”这一根本性问题,完成了无维度PDE求解器的最后一块拼图。
2.**巧妙的技术融合**:CRNS机制简单而有效;隐式子空间投影将理论上的降维思想与实际的无矩阵实现完美结合,真正兑现了常数级内存开销的承诺。
3.**卓越的可扩展性**:实验证明了其在极端维度下的可行性,为解决金融数学(HJB方程)、量子化学(多体薛定谔方程)等领域的高维PDE问题提供了新的可能工具链。

##8.不足与局限
1.**收敛速度可能较慢**:作为零阶方法,其收敛速率理论上可能慢于一阶方法。论文虽进行了收敛性分析并展示了实际可用的性能,但在某些问题上可能需要更多迭代步数或更精细的超参调整(如自适应步长)。
2.**超参数敏感性增加**:引入了新的超参数组:各层的子空间秩 {r_l} 、惰性更新频率 F 、扰动尺度 ε 。虽然消融实验表明了一定的鲁棒性,但在新问题上的调优成本可能增加。
3.**通用性的代价**:作为一种通用框架,S DZE放弃了针对特定微分算子(如拉普拉斯算子Forward Laplacian )进行专门优化的机会,因此在某些特定场景下可能不是最优选择.
4.**未探索更高级的方差缩减技术:**论文专注于解决核心的双重方差问题但未集成更复杂的ZO方差缩减技术未来可在此方向进一步改进.

**(完)**

## 9. 潜在影响与未来方向
*   **对PINN领域的意义**：SDZE为PINN在高维PDE求解中的应用扫清了关键的技术障碍，使其从理论上的“维度无关”真正走向了工程实践。它提供了一种在有限硬件资源下训练大规模、复杂PINN的可行路径。
*   **对更广泛机器学习优化的启示**：其核心思想——通过同步随机源来耦合不同层次的随机性以控制方差——为解决其他涉及多重随机估计的优化问题（如双层优化、元学习）提供了新颖的思路。
*   **未来工作展望**：
    1.  **自适应机制**：开发自适应的子空间秩调整策略和扰动步长调度器，以进一步提升收敛效率和减少超参数调优负担。
    2.  **高阶零阶方法集成**：探索将本文框架与更先进的零阶优化技术（如基于动量的ZO-SGD、ZO-Adam）结合，以期在保持内存优势的同时加速收敛。
    3.  **特定算子加速**：研究如何将针对特定微分算子（如拉普拉斯算子）的高效确定性算法与SDZE的随机框架进行有机融合，实现“通用性”与“特化性能”的平衡。
    4.  **扩展到更复杂场景**：验证SDZE在时变域、复杂边界条件、以及基于Transformer架构的现代PINN变体上的有效性。

## 10. 总结评价
本文是一项高质量、具有显著创新性的研究工作。它精准地识别并解决了高维高阶PINN训练中一个长期存在的核心矛盾：即空间随机化带来的计算效率提升与零阶优化所需的内存优势因方差爆炸而无法兼得。作者提出的SDZE框架，通过“公共随机数同步”和“隐式无矩阵子空间投影”两项关键技术，优雅地化解了这一矛盾，实现了理论严谨性与工程实用性的统一。

实验设计全面、对比公平，有力支撑了方法论的有效性和优越性。尽管作为一种零阶方法，其在收敛速度上可能存在固有局限，且引入了新的超参数，但这些代价换来了在极端高维场景下前所未有的可扩展性。这项工作不仅推动了PINN在科学计算中的应用边界，其方法论本身也对随机优化领域有独立的贡献。

总而言之，《面向高维高阶PINN的随机无维度零阶估计器》是一篇里程碑式的论文，它为求解超高维偏微分方程提供了一个强大而实用的新工具，并有望激发后续一系列在算法改进和应用拓展方面的研究。

（完）
